//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U16;
use carbon_core::postgres::primitives::U64;
use crate::instructions::SwapWithPriceImpact;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct SwapWithPriceImpactRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub amount_in: U64,
        pub active_id: Option<i32>,
        pub max_price_impact_bps: U16,
}

impl SwapWithPriceImpactRow {
    pub fn from_parts(source: SwapWithPriceImpact, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        amount_in: source.amount_in.into(),
                        active_id: source.active_id.map(|value| value.into()),
                        max_price_impact_bps: source.max_price_impact_bps.into(),
                    }
    }
}

impl TryFrom<SwapWithPriceImpactRow> for SwapWithPriceImpact {
    type Error = carbon_core::error::Error;
    fn try_from(source: SwapWithPriceImpactRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        amount_in: *source.amount_in,
                        active_id: source.active_id.map(|value| value.into()),
                        max_price_impact_bps: source.max_price_impact_bps.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
                    })
    }
}

impl carbon_core::postgres::operations::Table for SwapWithPriceImpact {
    fn table() -> &'static str {
        "swap_with_price_impact_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
                                    "amount_in",
                        "active_id",
                        "max_price_impact_bps",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SwapWithPriceImpactRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO swap_with_price_impact_instruction (
                            "amount_in",
                            "active_id",
                            "max_price_impact_bps",
                                        __signature, __instruction_index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6,                            $7                    )"#)
                .bind(self.amount_in.clone())
                .bind(self.active_id.clone())
                .bind(self.max_price_impact_bps.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SwapWithPriceImpactRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO swap_with_price_impact_instruction (
                        "amount_in",
                        "active_id",
                        "max_price_impact_bps",
                                    __signature, __instruction_index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6,                        $7                    ) ON CONFLICT (
                        __signature, __instruction_index
                    ) DO UPDATE SET
                        "amount_in" = EXCLUDED."amount_in",
                        "active_id" = EXCLUDED."active_id",
                        "max_price_impact_bps" = EXCLUDED."max_price_impact_bps",
                                    __instruction_index = EXCLUDED.__instruction_index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.amount_in.clone())
                .bind(self.active_id.clone())
                .bind(self.max_price_impact_bps.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for SwapWithPriceImpactRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM swap_with_price_impact_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for SwapWithPriceImpactRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM swap_with_price_impact_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct SwapWithPriceImpactMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for SwapWithPriceImpactMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS swap_with_price_impact_instruction (
                        -- Instruction data
                                    "amount_in" NUMERIC(20) NOT NULL,
                        "active_id" INT4 NOT NULL,
                        "max_price_impact_bps" INT4 NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __instruction_index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot NUMERIC(20),
            
                        PRIMARY KEY (__signature, __instruction_index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS swap_with_price_impact_instruction"#).execute(connection).await?;
        Ok(())
    }
}


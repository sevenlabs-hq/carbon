//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U64;
use crate::instructions::FundReward;
use crate::types::RemainingAccountsInfo;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct FundRewardRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub reward_index: U64,
        pub amount: U64,
        pub carry_forward: bool,
        pub remaining_accounts_info: sqlx::types::Json<RemainingAccountsInfo>,
}

impl FundRewardRow {
    pub fn from_parts(source: FundReward, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        reward_index: source.reward_index.into(),
                        amount: source.amount.into(),
                        carry_forward: source.carry_forward.into(),
                        remaining_accounts_info: sqlx::types::Json(source.remaining_accounts_info.into()),
                    }
    }
}

impl TryFrom<FundRewardRow> for FundReward {
    type Error = carbon_core::error::Error;
    fn try_from(source: FundRewardRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        reward_index: *source.reward_index,
                        amount: *source.amount,
                        carry_forward: source.carry_forward.into(),
                        remaining_accounts_info: source.remaining_accounts_info.0,
                    })
    }
}

impl carbon_core::postgres::operations::Table for FundReward {
    fn table() -> &'static str {
        "fund_reward_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
                                    "reward_index",
                        "amount",
                        "carry_forward",
                        "remaining_accounts_info",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for FundRewardRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO fund_reward_instruction (
                            "reward_index",
                            "amount",
                            "carry_forward",
                            "remaining_accounts_info",
                                        __signature, __instruction_index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6,                            $7,                            $8                    )"#)
                .bind(self.reward_index.clone())
                .bind(self.amount.clone())
                .bind(self.carry_forward.clone())
                .bind(self.remaining_accounts_info.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for FundRewardRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO fund_reward_instruction (
                        "reward_index",
                        "amount",
                        "carry_forward",
                        "remaining_accounts_info",
                                    __signature, __instruction_index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6,                        $7,                        $8                    ) ON CONFLICT (
                        __signature, __instruction_index
                    ) DO UPDATE SET
                        "reward_index" = EXCLUDED."reward_index",
                        "amount" = EXCLUDED."amount",
                        "carry_forward" = EXCLUDED."carry_forward",
                        "remaining_accounts_info" = EXCLUDED."remaining_accounts_info",
                                    __instruction_index = EXCLUDED.__instruction_index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.reward_index.clone())
                .bind(self.amount.clone())
                .bind(self.carry_forward.clone())
                .bind(self.remaining_accounts_info.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for FundRewardRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM fund_reward_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for FundRewardRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM fund_reward_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct FundRewardMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for FundRewardMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS fund_reward_instruction (
                        -- Instruction data
                                    "reward_index" NUMERIC(20) NOT NULL,
                        "amount" NUMERIC(20) NOT NULL,
                        "carry_forward" BOOLEAN NOT NULL,
                        "remaining_accounts_info" JSONB NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __instruction_index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot NUMERIC(20),
            
                        PRIMARY KEY (__signature, __instruction_index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS fund_reward_instruction"#).execute(connection).await?;
        Ok(())
    }
}


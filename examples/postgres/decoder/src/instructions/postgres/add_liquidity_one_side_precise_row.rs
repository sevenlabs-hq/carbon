//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U64;
use crate::instructions::AddLiquidityOneSidePrecise;
use crate::types::CompressedBinDepositAmount;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AddLiquidityOneSidePreciseRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub bins: sqlx::types::Json<Vec<CompressedBinDepositAmount>>,
        pub decompress_multiplier: U64,
}

impl AddLiquidityOneSidePreciseRow {
    pub fn from_parts(source: AddLiquidityOneSidePrecise, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        bins: sqlx::types::Json(source.bins.into_iter().map(|element| element.into()).collect()),
                        decompress_multiplier: source.decompress_multiplier.into(),
                    }
    }
}

impl TryFrom<AddLiquidityOneSidePreciseRow> for AddLiquidityOneSidePrecise {
    type Error = carbon_core::error::Error;
    fn try_from(source: AddLiquidityOneSidePreciseRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        bins: source.bins.0,
                        decompress_multiplier: *source.decompress_multiplier,
                    })
    }
}

impl carbon_core::postgres::operations::Table for AddLiquidityOneSidePrecise {
    fn table() -> &'static str {
        "add_liquidity_one_side_precise_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
                                    "bins",
                        "decompress_multiplier",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddLiquidityOneSidePreciseRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO add_liquidity_one_side_precise_instruction (
                            "bins",
                            "decompress_multiplier",
                                        __signature, __instruction_index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6                    )"#)
                .bind(self.bins.clone())
                .bind(self.decompress_multiplier.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddLiquidityOneSidePreciseRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO add_liquidity_one_side_precise_instruction (
                        "bins",
                        "decompress_multiplier",
                                    __signature, __instruction_index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6                    ) ON CONFLICT (
                        __signature, __instruction_index
                    ) DO UPDATE SET
                        "bins" = EXCLUDED."bins",
                        "decompress_multiplier" = EXCLUDED."decompress_multiplier",
                                    __instruction_index = EXCLUDED.__instruction_index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.bins.clone())
                .bind(self.decompress_multiplier.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AddLiquidityOneSidePreciseRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM add_liquidity_one_side_precise_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AddLiquidityOneSidePreciseRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM add_liquidity_one_side_precise_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct AddLiquidityOneSidePreciseMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AddLiquidityOneSidePreciseMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS add_liquidity_one_side_precise_instruction (
                        -- Instruction data
                                    "bins" JSONB NOT NULL,
                        "decompress_multiplier" NUMERIC(20) NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __instruction_index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot NUMERIC(20),
            
                        PRIMARY KEY (__signature, __instruction_index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS add_liquidity_one_side_precise_instruction"#).execute(connection).await?;
        Ok(())
    }
}


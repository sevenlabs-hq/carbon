//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U64;
use crate::instructions::AddLiquidityOneSide;
use crate::types::BinLiquidityDistributionByWeight;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AddLiquidityOneSideRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub amount: U64,
        pub active_id: i32,
        pub max_active_bin_slippage: i32,
        pub bin_liquidity_dist: sqlx::types::Json<Vec<BinLiquidityDistributionByWeight>>,
}

impl AddLiquidityOneSideRow {
    pub fn from_parts(source: AddLiquidityOneSide, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        amount: source.amount.into(),
                        active_id: source.active_id.into(),
                        max_active_bin_slippage: source.max_active_bin_slippage.into(),
                        bin_liquidity_dist: sqlx::types::Json(source.bin_liquidity_dist.into_iter().map(|element| element.into()).collect()),
                    }
    }
}

impl TryFrom<AddLiquidityOneSideRow> for AddLiquidityOneSide {
    type Error = carbon_core::error::Error;
    fn try_from(source: AddLiquidityOneSideRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        amount: *source.amount,
                        active_id: source.active_id.into(),
                        max_active_bin_slippage: source.max_active_bin_slippage.into(),
                        bin_liquidity_dist: source.bin_liquidity_dist.0,
                    })
    }
}

impl carbon_core::postgres::operations::Table for AddLiquidityOneSide {
    fn table() -> &'static str {
        "add_liquidity_one_side_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
                                    "amount",
                        "active_id",
                        "max_active_bin_slippage",
                        "bin_liquidity_dist",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddLiquidityOneSideRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO add_liquidity_one_side_instruction (
                            "amount",
                            "active_id",
                            "max_active_bin_slippage",
                            "bin_liquidity_dist",
                                        __signature, __instruction_index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6,                            $7,                            $8                    )"#)
                .bind(self.amount.clone())
                .bind(self.active_id.clone())
                .bind(self.max_active_bin_slippage.clone())
                .bind(self.bin_liquidity_dist.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddLiquidityOneSideRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO add_liquidity_one_side_instruction (
                        "amount",
                        "active_id",
                        "max_active_bin_slippage",
                        "bin_liquidity_dist",
                                    __signature, __instruction_index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6,                        $7,                        $8                    ) ON CONFLICT (
                        __signature, __instruction_index
                    ) DO UPDATE SET
                        "amount" = EXCLUDED."amount",
                        "active_id" = EXCLUDED."active_id",
                        "max_active_bin_slippage" = EXCLUDED."max_active_bin_slippage",
                        "bin_liquidity_dist" = EXCLUDED."bin_liquidity_dist",
                                    __instruction_index = EXCLUDED.__instruction_index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.amount.clone())
                .bind(self.active_id.clone())
                .bind(self.max_active_bin_slippage.clone())
                .bind(self.bin_liquidity_dist.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AddLiquidityOneSideRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM add_liquidity_one_side_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AddLiquidityOneSideRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM add_liquidity_one_side_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct AddLiquidityOneSideMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AddLiquidityOneSideMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS add_liquidity_one_side_instruction (
                        -- Instruction data
                                    "amount" NUMERIC(20) NOT NULL,
                        "active_id" INT4 NOT NULL,
                        "max_active_bin_slippage" INT4 NOT NULL,
                        "bin_liquidity_dist" JSONB NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __instruction_index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot NUMERIC(20),
            
                        PRIMARY KEY (__signature, __instruction_index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS add_liquidity_one_side_instruction"#).execute(connection).await?;
        Ok(())
    }
}


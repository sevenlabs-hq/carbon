//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use crate::instructions::InitializePosition;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct InitializePositionRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub lower_bin_id: i32,
        pub width: i32,
}

impl InitializePositionRow {
    pub fn from_parts(source: InitializePosition, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        lower_bin_id: source.lower_bin_id.into(),
                        width: source.width.into(),
                    }
    }
}

impl TryFrom<InitializePositionRow> for InitializePosition {
    type Error = carbon_core::error::Error;
    fn try_from(source: InitializePositionRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        lower_bin_id: source.lower_bin_id.into(),
                        width: source.width.into(),
                    })
    }
}

impl carbon_core::postgres::operations::Table for InitializePosition {
    fn table() -> &'static str {
        "initialize_position_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
                                    "lower_bin_id",
                        "width",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for InitializePositionRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO initialize_position_instruction (
                            "lower_bin_id",
                            "width",
                                        __signature, __instruction_index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6                    )"#)
                .bind(self.lower_bin_id.clone())
                .bind(self.width.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for InitializePositionRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO initialize_position_instruction (
                        "lower_bin_id",
                        "width",
                                    __signature, __instruction_index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6                    ) ON CONFLICT (
                        __signature, __instruction_index
                    ) DO UPDATE SET
                        "lower_bin_id" = EXCLUDED."lower_bin_id",
                        "width" = EXCLUDED."width",
                                    __instruction_index = EXCLUDED.__instruction_index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.lower_bin_id.clone())
                .bind(self.width.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.instruction_index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for InitializePositionRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM initialize_position_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for InitializePositionRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM initialize_position_instruction WHERE
                        __signature = $1 AND __instruction_index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct InitializePositionMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for InitializePositionMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS initialize_position_instruction (
                        -- Instruction data
                                    "lower_bin_id" INT4 NOT NULL,
                        "width" INT4 NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __instruction_index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot NUMERIC(20),
            
                        PRIMARY KEY (__signature, __instruction_index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS initialize_position_instruction"#).execute(connection).await?;
        Ok(())
    }
}


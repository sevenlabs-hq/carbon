//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U64;
use crate::instructions::CollectProtocolFee;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CollectProtocolFeeRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub amount0_requested: U64,
        pub amount1_requested: U64,
}

impl CollectProtocolFeeRow {
    pub fn from_parts(source: CollectProtocolFee, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        amount0_requested: source.amount0_requested.into(),
                        amount1_requested: source.amount1_requested.into(),
                    }
    }
}

impl TryFrom<CollectProtocolFeeRow> for CollectProtocolFee {
    type Error = carbon_core::error::Error;
    fn try_from(source: CollectProtocolFeeRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        amount0_requested: *source.amount0_requested,
                        amount1_requested: *source.amount1_requested,
                    })
    }
}

impl carbon_core::postgres::operations::Table for CollectProtocolFee {
    fn table() -> &'static str {
        "collect_protocol_fee_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__index",
            "__stack_height",
            "__slot",
                                    "amount0_requested",
                        "amount1_requested",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CollectProtocolFeeRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO collect_protocol_fee_instruction (
                            "amount0_requested",
                            "amount1_requested",
                                        __signature, __index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6                    )"#)
                .bind(self.amount0_requested.clone())
                .bind(self.amount1_requested.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CollectProtocolFeeRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO collect_protocol_fee_instruction (
                        "amount0_requested",
                        "amount1_requested",
                                    __signature, __index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6                    ) ON CONFLICT (
                        __signature, __index
                    ) DO UPDATE SET
                        "amount0_requested" = EXCLUDED."amount0_requested",
                        "amount1_requested" = EXCLUDED."amount1_requested",
                                    __index = EXCLUDED.__index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.amount0_requested.clone())
                .bind(self.amount1_requested.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CollectProtocolFeeRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM collect_protocol_fee_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CollectProtocolFeeRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM collect_protocol_fee_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct CollectProtocolFeeMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CollectProtocolFeeMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS collect_protocol_fee_instruction (
                        -- Instruction data
                                    "amount0_requested" NUMERIC(20) NOT NULL,
                        "amount1_requested" NUMERIC(20) NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot BIGINT,
            
                        PRIMARY KEY (__signature, __index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS collect_protocol_fee_instruction"#).execute(connection).await?;
        Ok(())
    }
}


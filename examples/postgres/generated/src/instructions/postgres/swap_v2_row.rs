//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U128;
use carbon_core::postgres::primitives::U64;
use crate::instructions::SwapV2;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct SwapV2Row {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub amount: U64,
        pub other_amount_threshold: U64,
        pub sqrt_price_limit_x64: U128,
        pub is_base_input: bool,
}

impl SwapV2Row {
    pub fn from_parts(source: SwapV2, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        amount: source.amount.into(),
                        other_amount_threshold: source.other_amount_threshold.into(),
                        sqrt_price_limit_x64: source.sqrt_price_limit_x64.into(),
                        is_base_input: source.is_base_input.into(),
                    }
    }
}

impl TryFrom<SwapV2Row> for SwapV2 {
    type Error = carbon_core::error::Error;
    fn try_from(source: SwapV2Row) -> Result<Self, Self::Error> {
        Ok(Self {
                        amount: *source.amount,
                        other_amount_threshold: *source.other_amount_threshold,
                        sqrt_price_limit_x64: *source.sqrt_price_limit_x64,
                        is_base_input: source.is_base_input.into(),
                    })
    }
}

impl carbon_core::postgres::operations::Table for SwapV2 {
    fn table() -> &'static str {
        "swap_v2_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__index",
            "__stack_height",
            "__slot",
                                    "amount",
                        "other_amount_threshold",
                        "sqrt_price_limit_x64",
                        "is_base_input",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SwapV2Row {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO swap_v2_instruction (
                            "amount",
                            "other_amount_threshold",
                            "sqrt_price_limit_x64",
                            "is_base_input",
                                        __signature, __index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5,                            $6,                            $7,                            $8                    )"#)
                .bind(self.amount.clone())
                .bind(self.other_amount_threshold.clone())
                .bind(self.sqrt_price_limit_x64.clone())
                .bind(self.is_base_input.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SwapV2Row {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO swap_v2_instruction (
                        "amount",
                        "other_amount_threshold",
                        "sqrt_price_limit_x64",
                        "is_base_input",
                                    __signature, __index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5,                        $6,                        $7,                        $8                    ) ON CONFLICT (
                        __signature, __index
                    ) DO UPDATE SET
                        "amount" = EXCLUDED."amount",
                        "other_amount_threshold" = EXCLUDED."other_amount_threshold",
                        "sqrt_price_limit_x64" = EXCLUDED."sqrt_price_limit_x64",
                        "is_base_input" = EXCLUDED."is_base_input",
                                    __index = EXCLUDED.__index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.amount.clone())
                .bind(self.other_amount_threshold.clone())
                .bind(self.sqrt_price_limit_x64.clone())
                .bind(self.is_base_input.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for SwapV2Row {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM swap_v2_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for SwapV2Row {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM swap_v2_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct SwapV2MigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for SwapV2MigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS swap_v2_instruction (
                        -- Instruction data
                                    "amount" NUMERIC(20) NOT NULL,
                        "other_amount_threshold" NUMERIC(20) NOT NULL,
                        "sqrt_price_limit_x64" NUMERIC(39) NOT NULL,
                        "is_base_input" BOOLEAN NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot BIGINT,
            
                        PRIMARY KEY (__signature, __index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS swap_v2_instruction"#).execute(connection).await?;
        Ok(())
    }
}


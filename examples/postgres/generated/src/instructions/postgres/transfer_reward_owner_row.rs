//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use crate::instructions::TransferRewardOwner;


#[derive(sqlx::FromRow, Debug, Clone)]
pub struct TransferRewardOwnerRow {
    #[sqlx(flatten)]
        pub metadata: InstructionRowMetadata,
            pub new_owner: Pubkey,
}

impl TransferRewardOwnerRow {
    pub fn from_parts(source: TransferRewardOwner, metadata: InstructionMetadata) -> Self {
        Self {
            metadata: metadata.into(),
                        new_owner: source.new_owner.into(),
                    }
    }
}

impl TryFrom<TransferRewardOwnerRow> for TransferRewardOwner {
    type Error = carbon_core::error::Error;
    fn try_from(source: TransferRewardOwnerRow) -> Result<Self, Self::Error> {
        Ok(Self {
                        new_owner: *source.new_owner,
                    })
    }
}

impl carbon_core::postgres::operations::Table for TransferRewardOwner {
    fn table() -> &'static str {
        "transfer_reward_owner_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
                        "__signature",
            "__index",
            "__stack_height",
            "__slot",
                                    "new_owner",
                    ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for TransferRewardOwnerRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO transfer_reward_owner_instruction (
                            "new_owner",
                                        __signature, __index, __stack_height, __slot
                        ) VALUES (
                                                                            $1,                            $2,                            $3,                            $4,                            $5                    )"#)
                .bind(self.new_owner.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for TransferRewardOwnerRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO transfer_reward_owner_instruction (
                        "new_owner",
                                    __signature, __index, __stack_height, __slot
                    ) VALUES (
                                                                        $1,                        $2,                        $3,                        $4,                        $5                    ) ON CONFLICT (
                        __signature, __index
                    ) DO UPDATE SET
                        "new_owner" = EXCLUDED."new_owner",
                                    __index = EXCLUDED.__index,
            __stack_height = EXCLUDED.__stack_height,
            __slot = EXCLUDED.__slot
                    "#)
                .bind(self.new_owner.clone())
                        .bind(self.metadata.signature.clone())
        .bind(self.metadata.index.clone())
        .bind(self.metadata.stack_height.clone())
        .bind(self.metadata.slot.clone())
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for TransferRewardOwnerRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM transfer_reward_owner_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for TransferRewardOwnerRow {
    type Key = (String, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM transfer_reward_owner_instruction WHERE
                        __signature = $1 AND __index = $2
                    "#)
                .bind(key.0)
        .bind(key.1)
                .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))
        ?;

        Ok(row)
    }
}

pub struct TransferRewardOwnerMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for TransferRewardOwnerMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS transfer_reward_owner_instruction (
                        -- Instruction data
                                    "new_owner" BYTEA NOT NULL,
            
                        -- Instruction metadata
            __signature TEXT NOT NULL,
            __index BIGINT NOT NULL,
            __stack_height BIGINT NOT NULL,
            __slot BIGINT,
            
                        PRIMARY KEY (__signature, __index)
                    )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS transfer_reward_owner_instruction"#).execute(connection).await?;
        Ok(())
    }
}


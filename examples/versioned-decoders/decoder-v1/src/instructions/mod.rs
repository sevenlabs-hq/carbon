//! This code was AUTOGENERATED using the Codama library.
use crate::{SimpleDexDecoder, PROGRAM_ID};

pub mod swap;

pub use self::swap::*;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum SimpleDexInstruction {
    Swap {
        program_id: solana_pubkey::Pubkey,
        data: Swap,
        accounts: SwapInstructionAccounts,
    },
}

impl carbon_core::instruction::InstructionDecoder<'_> for SimpleDexDecoder {
    type InstructionType = SimpleDexInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<Self::InstructionType> {
        if instruction.program_id != PROGRAM_ID {
            return None;
        }

        carbon_core::try_decode_instructions!(
            instruction,
            PROGRAM_ID,
            SimpleDexInstruction::Swap => Swap,
        )
    }
}

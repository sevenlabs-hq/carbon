# RPC variant (default)
RPC_URL=https://api.mainnet-beta.solana.com

# Helius gPA v2 variant — set this and swap variants::rpc(...) to
# variants::helius_gpa_v2(...) in src/main.rs.
# HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

RUST_LOG=info

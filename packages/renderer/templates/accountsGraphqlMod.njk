{% for account in accountsToExport %}
{% if account.data.kind == 'structTypeNode' and account.data.fields.length > 0 %}
pub mod {{ account.name | snakeCase }}_schema;
{% endif %}
{% endfor %}

{% for account in accountsToExport %}
{% if account.data.kind == 'structTypeNode' and account.data.fields.length > 0 %}
pub use {{ account.name | snakeCase }}_schema::*;
{% endif %}
{% endfor %}
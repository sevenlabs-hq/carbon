[package]
name = "carbon-macros"
version = "2.0.0"
rust-version = { workspace = true }
edition = { workspace = true }
description = "Macros for Carbon"
license = { workspace = true }
readme = "README.md"
repository = { workspace = true }
keywords = ["solana", "indexer", "macros"]
categories = ["encoding"]

[lib]
crate-type = ["rlib"]

[dependencies]

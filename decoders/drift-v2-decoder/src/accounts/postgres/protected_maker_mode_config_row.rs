//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{U32, U8},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ProtectedMakerModeConfigRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub max_users: U32,
    pub current_users: U32,
    pub reduce_only: U8,
    pub padding: Vec<u8>,
}

impl ProtectedMakerModeConfigRow {
    pub fn from_parts(
        source: crate::accounts::protected_maker_mode_config::ProtectedMakerModeConfig,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            max_users: source.max_users.into(),
            current_users: source.current_users.into(),
            reduce_only: source.reduce_only.into(),
            padding: source.padding.to_vec(),
        }
    }
}

impl TryFrom<ProtectedMakerModeConfigRow>
    for crate::accounts::protected_maker_mode_config::ProtectedMakerModeConfig
{
    type Error = carbon_core::error::Error;
    fn try_from(source: ProtectedMakerModeConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            max_users: source.max_users.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            current_users: source.current_users.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            reduce_only: source.reduce_only.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            padding: source.padding.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 31 bytes"
                        .to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::protected_maker_mode_config::ProtectedMakerModeConfig
{
    fn table() -> &'static str {
        "protected_maker_mode_config_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "max_users",
            "current_users",
            "reduce_only",
            "padding",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ProtectedMakerModeConfigRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO protected_maker_mode_config_account (
                "max_users",
                "current_users",
                "reduce_only",
                "padding",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            )"#,
        )
        .bind(self.max_users)
        .bind(self.current_users)
        .bind(self.reduce_only)
        .bind(&self.padding)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ProtectedMakerModeConfigRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO protected_maker_mode_config_account (
                "max_users",
                "current_users",
                "reduce_only",
                "padding",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "max_users" = EXCLUDED."max_users",
                "current_users" = EXCLUDED."current_users",
                "reduce_only" = EXCLUDED."reduce_only",
                "padding" = EXCLUDED."padding",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.max_users)
        .bind(self.current_users)
        .bind(self.reduce_only)
        .bind(&self.padding)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ProtectedMakerModeConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM protected_maker_mode_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ProtectedMakerModeConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM protected_maker_mode_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ProtectedMakerModeConfigMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for ProtectedMakerModeConfigMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS protected_maker_mode_config_account (
                -- Account data
                "max_users" INT8 NOT NULL,
                "current_users" INT8 NOT NULL,
                "reduce_only" INT2 NOT NULL,
                "padding" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS protected_maker_mode_config_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

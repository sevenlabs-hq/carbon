//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{metadata::AccountRowMetadata, primitives::Pubkey},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct SignedMsgWsDelegatesRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub delegates: Vec<Pubkey>,
}

impl SignedMsgWsDelegatesRow {
    pub fn from_parts(
        source: crate::accounts::signed_msg_ws_delegates::SignedMsgWsDelegates,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            delegates: source
                .delegates
                .into_iter()
                .map(|element| element.into())
                .collect(),
        }
    }
}

impl TryFrom<SignedMsgWsDelegatesRow>
    for crate::accounts::signed_msg_ws_delegates::SignedMsgWsDelegates
{
    type Error = carbon_core::error::Error;
    fn try_from(source: SignedMsgWsDelegatesRow) -> Result<Self, Self::Error> {
        Ok(Self {
            delegates: source
                .delegates
                .into_iter()
                .map(|element| *element)
                .collect(),
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::signed_msg_ws_delegates::SignedMsgWsDelegates
{
    fn table() -> &'static str {
        "signed_msg_ws_delegates_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "delegates"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SignedMsgWsDelegatesRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO signed_msg_ws_delegates_account (
                "delegates",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            )"#,
        )
        .bind(&self.delegates)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SignedMsgWsDelegatesRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO signed_msg_ws_delegates_account (
                "delegates",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "delegates" = EXCLUDED."delegates",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.delegates)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for SignedMsgWsDelegatesRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM signed_msg_ws_delegates_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for SignedMsgWsDelegatesRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM signed_msg_ws_delegates_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct SignedMsgWsDelegatesMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for SignedMsgWsDelegatesMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS signed_msg_ws_delegates_account (
                -- Account data
                "delegates" BYTEA[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS signed_msg_ws_delegates_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

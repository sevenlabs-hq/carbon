//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::AmmConstituentDatum,
    carbon_core::{
        account::AccountMetadata,
        postgres::{
            metadata::AccountRowMetadata,
            primitives::{Pubkey, U8},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AmmConstituentMappingRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub lp_pool: Pubkey,
    pub bump: U8,
    pub padding: Vec<u8>,
    pub weights: sqlx::types::Json<Vec<AmmConstituentDatum>>,
}

impl AmmConstituentMappingRow {
    pub fn from_parts(
        source: crate::accounts::amm_constituent_mapping::AmmConstituentMapping,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            lp_pool: source.lp_pool.into(),
            bump: source.bump.into(),
            padding: source.padding.to_vec(),
            weights: sqlx::types::Json(source.weights.to_vec()),
        }
    }
}

impl TryFrom<AmmConstituentMappingRow>
    for crate::accounts::amm_constituent_mapping::AmmConstituentMapping
{
    type Error = carbon_core::error::Error;
    fn try_from(source: AmmConstituentMappingRow) -> Result<Self, Self::Error> {
        Ok(Self {
            lp_pool: *source.lp_pool,
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            padding: source.padding.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 3 bytes"
                        .to_string(),
                )
            })?,
            weights: source.weights.0,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::amm_constituent_mapping::AmmConstituentMapping
{
    fn table() -> &'static str {
        "amm_constituent_mapping_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey", "__slot", "lp_pool", "bump", "padding", "weights",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AmmConstituentMappingRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO amm_constituent_mapping_account (
                "lp_pool",
                "bump",
                "padding",
                "weights",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            )"#,
        )
        .bind(self.lp_pool)
        .bind(self.bump)
        .bind(&self.padding)
        .bind(&self.weights)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AmmConstituentMappingRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO amm_constituent_mapping_account (
                "lp_pool",
                "bump",
                "padding",
                "weights",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "lp_pool" = EXCLUDED."lp_pool",
                "bump" = EXCLUDED."bump",
                "padding" = EXCLUDED."padding",
                "weights" = EXCLUDED."weights",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.lp_pool)
        .bind(self.bump)
        .bind(&self.padding)
        .bind(&self.weights)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AmmConstituentMappingRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM amm_constituent_mapping_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AmmConstituentMappingRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM amm_constituent_mapping_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct AmmConstituentMappingMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AmmConstituentMappingMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS amm_constituent_mapping_account (
                -- Account data
                "lp_pool" BYTEA NOT NULL,
                "bump" INT2 NOT NULL,
                "padding" BYTEA NOT NULL,
                "weights" JSONB NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS amm_constituent_mapping_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

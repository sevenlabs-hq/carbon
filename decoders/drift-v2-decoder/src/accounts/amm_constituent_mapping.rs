//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::AmmConstituentDatum, carbon_core::deserialize::CarbonDeserialize,
    solana_pubkey::Pubkey,
};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct AmmConstituentMapping {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub lp_pool: Pubkey,
    pub bump: u8,
    pub padding: [u8; 3],
    pub weights: Vec<AmmConstituentDatum>,
}

impl CarbonDeserialize for AmmConstituentMapping {
    const DISCRIMINATOR: &'static [u8] = &[254, 89, 5, 173, 66, 54, 214, 247];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

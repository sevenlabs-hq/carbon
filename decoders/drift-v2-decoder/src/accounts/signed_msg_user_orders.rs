//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::SignedMsgOrderId, carbon_core::deserialize::CarbonDeserialize,
    solana_pubkey::Pubkey,
};

/// * This struct is a duplicate of SignedMsgUserOrdersZeroCopy
/// * It is used to give anchor an struct to generate the idl for clients
/// * The struct SignedMsgUserOrdersZeroCopy is used to load the data in
///   efficiently
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SignedMsgUserOrders {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub authority_pubkey: Pubkey,
    pub padding: u32,
    pub signed_msg_order_data: Vec<SignedMsgOrderId>,
}

impl CarbonDeserialize for SignedMsgUserOrders {
    const DISCRIMINATOR: &'static [u8] = &[70, 6, 50, 248, 222, 1, 143, 49];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

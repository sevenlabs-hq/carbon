//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        OrderTriggerConditionGraphQL, PositionDirectionGraphQL, PostOnlyParamGraphQL,
    },
    carbon_core::graphql::primitives::{I64, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ModifyOrderParams")]
pub struct ModifyOrderParamsGraphQL {
    pub direction: Option<PositionDirectionGraphQL>,
    pub base_asset_amount: Option<U64>,
    pub price: Option<U64>,
    pub reduce_only: Option<bool>,
    pub post_only: Option<PostOnlyParamGraphQL>,
    pub bit_flags: Option<U8>,
    pub max_ts: Option<I64>,
    pub trigger_price: Option<U64>,
    pub trigger_condition: Option<OrderTriggerConditionGraphQL>,
    pub oracle_price_offset: Option<i32>,
    pub auction_duration: Option<U8>,
    pub auction_start_price: Option<I64>,
    pub auction_end_price: Option<I64>,
    pub policy: Option<U8>,
}

impl From<crate::types::ModifyOrderParams> for ModifyOrderParamsGraphQL {
    fn from(original: crate::types::ModifyOrderParams) -> Self {
        Self {
            direction: original.direction.map(|v| v.into()),
            base_asset_amount: original
                .base_asset_amount
                .map(carbon_core::graphql::primitives::U64),
            price: original.price.map(carbon_core::graphql::primitives::U64),
            reduce_only: original.reduce_only,
            post_only: original.post_only.map(|v| v.into()),
            bit_flags: original.bit_flags.map(carbon_core::graphql::primitives::U8),
            max_ts: original.max_ts.map(carbon_core::graphql::primitives::I64),
            trigger_price: original
                .trigger_price
                .map(carbon_core::graphql::primitives::U64),
            trigger_condition: original.trigger_condition.map(|v| v.into()),
            oracle_price_offset: original.oracle_price_offset,
            auction_duration: original
                .auction_duration
                .map(carbon_core::graphql::primitives::U8),
            auction_start_price: original
                .auction_start_price
                .map(carbon_core::graphql::primitives::I64),
            auction_end_price: original
                .auction_end_price
                .map(carbon_core::graphql::primitives::I64),
            policy: original.policy.map(carbon_core::graphql::primitives::U8),
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{I64, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ConstituentParams")]
pub struct ConstituentParamsGraphQL {
    pub max_weight_deviation: Option<I64>,
    pub swap_fee_min: Option<I64>,
    pub swap_fee_max: Option<I64>,
    pub max_borrow_token_amount: Option<U64>,
    pub oracle_staleness_threshold: Option<U64>,
    pub cost_to_trade_bps: Option<i32>,
    pub constituent_derivative_index: Option<i32>,
    pub derivative_weight: Option<U64>,
    pub volatility: Option<U64>,
    pub gamma_execution: Option<U8>,
    pub gamma_inventory: Option<U8>,
    pub xi: Option<U8>,
}

impl From<crate::types::ConstituentParams> for ConstituentParamsGraphQL {
    fn from(original: crate::types::ConstituentParams) -> Self {
        Self {
            max_weight_deviation: original
                .max_weight_deviation
                .map(carbon_core::graphql::primitives::I64),
            swap_fee_min: original
                .swap_fee_min
                .map(carbon_core::graphql::primitives::I64),
            swap_fee_max: original
                .swap_fee_max
                .map(carbon_core::graphql::primitives::I64),
            max_borrow_token_amount: original
                .max_borrow_token_amount
                .map(carbon_core::graphql::primitives::U64),
            oracle_staleness_threshold: original
                .oracle_staleness_threshold
                .map(carbon_core::graphql::primitives::U64),
            cost_to_trade_bps: original.cost_to_trade_bps,
            constituent_derivative_index: original.constituent_derivative_index.map(|v| v as i32),
            derivative_weight: original
                .derivative_weight
                .map(carbon_core::graphql::primitives::U64),
            volatility: original
                .volatility
                .map(carbon_core::graphql::primitives::U64),
            gamma_execution: original
                .gamma_execution
                .map(carbon_core::graphql::primitives::U8),
            gamma_inventory: original
                .gamma_inventory
                .map(carbon_core::graphql::primitives::U8),
            xi: original.xi.map(carbon_core::graphql::primitives::U8),
        }
    }
}

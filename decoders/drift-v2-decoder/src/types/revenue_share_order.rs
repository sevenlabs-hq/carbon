//! This code was AUTOGENERATED using the Codama library.
use crate::types::MarketType;
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RevenueShareOrder {
    /// fees accrued so far for this order slot. This is not exclusively fees
    /// from this order_id and may include fees from other orders in the
    /// same market. This may be swept to the builder's SpotPosition during
    /// settle_pnl.
    pub fees_accrued: u64,
    /// the order_id of the current active order in this slot. It's only
    /// relevant while bit_flag = Open
    pub order_id: u32,
    /// the builder fee on this order, in tenths of a bps, e.g. 100 = 0.01%
    pub fee_tenth_bps: u16,
    pub market_index: u16,
    /// the subaccount_id of the user who created this order. It's only relevant
    /// while bit_flag = Open
    pub sub_account_id: u16,
    /// the index of the RevenueShareEscrow.approved_builders list, that this
    /// order's fee will settle to. Ignored if bit_flag = Referral.
    pub builder_idx: u8,
    /// bitflags that describe the state of the order.
    /// [`RevenueShareOrderBitFlag::Init`]: this order slot is available for use.
    /// [`RevenueShareOrderBitFlag::Open`]: this order slot is occupied, `order_id` is the `sub_account_id`'s active order.
    /// [`RevenueShareOrderBitFlag::Completed`]: this order has been filled or canceled, and is waiting to be settled into.
    /// the builder's account order_id and sub_account_id are no longer
    /// relevant, it may be merged with other orders. [`RevenueShareOrderBitFlag::Referral`]: this order stores referral rewards waiting to be settled for this market.
    /// If it is set, no other bitflag should be set.
    pub bit_flags: u8,
    /// the index into the User's orders list when this RevenueShareOrder was
    /// created, make sure to verify that order_id matches.
    pub user_order_index: u8,
    pub market_type: MarketType,
    pub padding: [u8; 10],
}

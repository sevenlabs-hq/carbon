//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    instruction::InstructionMetadata,
    postgres::{metadata::InstructionRowMetadata, primitives::U128},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct MoveAmmPriceRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub base_asset_reserve: U128,
    pub quote_asset_reserve: U128,
    pub sqrt_k: U128,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl MoveAmmPriceRow {
    pub fn from_parts(
        source: crate::instructions::move_amm_price::MoveAmmPrice,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            base_asset_reserve: source.base_asset_reserve.into(),
            quote_asset_reserve: source.quote_asset_reserve.into(),
            sqrt_k: source.sqrt_k.into(),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<MoveAmmPriceRow> for crate::instructions::move_amm_price::MoveAmmPrice {
    type Error = carbon_core::error::Error;
    fn try_from(source: MoveAmmPriceRow) -> Result<Self, Self::Error> {
        Ok(Self {
            base_asset_reserve: *source.base_asset_reserve,
            quote_asset_reserve: *source.quote_asset_reserve,
            sqrt_k: *source.sqrt_k,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::move_amm_price::MoveAmmPrice
{
    fn table() -> &'static str {
        "move_amm_price_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "base_asset_reserve",
            "quote_asset_reserve",
            "sqrt_k",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MoveAmmPriceRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO move_amm_price_instruction (
                "base_asset_reserve",
                "quote_asset_reserve",
                "sqrt_k",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(&self.base_asset_reserve)
        .bind(&self.quote_asset_reserve)
        .bind(&self.sqrt_k)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MoveAmmPriceRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO move_amm_price_instruction (
                "base_asset_reserve",
                "quote_asset_reserve",
                "sqrt_k",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "base_asset_reserve" = EXCLUDED."base_asset_reserve",
                "quote_asset_reserve" = EXCLUDED."quote_asset_reserve",
                "sqrt_k" = EXCLUDED."sqrt_k",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(&self.base_asset_reserve)
        .bind(&self.quote_asset_reserve)
        .bind(&self.sqrt_k)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for MoveAmmPriceRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM move_amm_price_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for MoveAmmPriceRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM move_amm_price_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct MoveAmmPriceMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for MoveAmmPriceMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS move_amm_price_instruction (
                -- Instruction data
                "base_asset_reserve" NUMERIC(39) NOT NULL,
                "quote_asset_reserve" NUMERIC(39) NOT NULL,
                "sqrt_k" NUMERIC(39) NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS move_amm_price_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LpPoolRemoveLiquidity {
    pub in_market_index: u16,
    pub in_amount: u64,
    pub min_out_amount: u128,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LpPoolRemoveLiquidityInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub out_market_mint: solana_pubkey::Pubkey,
    pub out_constituent: solana_pubkey::Pubkey,
    pub user_out_token_account: solana_pubkey::Pubkey,
    pub constituent_out_token_account: solana_pubkey::Pubkey,
    pub user_lp_token_account: solana_pubkey::Pubkey,
    pub spot_market_token_account: solana_pubkey::Pubkey,
    pub lp_mint: solana_pubkey::Pubkey,
    pub constituent_target_base: solana_pubkey::Pubkey,
    pub lp_pool_token_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub amm_cache: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LpPoolRemoveLiquidity {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [164, 36, 193, 252, 196, 157, 138, 43] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LpPoolRemoveLiquidity {
    type ArrangedAccounts = LpPoolRemoveLiquidityInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let out_market_mint = next_account(&mut iter)?;
        let out_constituent = next_account(&mut iter)?;
        let user_out_token_account = next_account(&mut iter)?;
        let constituent_out_token_account = next_account(&mut iter)?;
        let user_lp_token_account = next_account(&mut iter)?;
        let spot_market_token_account = next_account(&mut iter)?;
        let lp_mint = next_account(&mut iter)?;
        let constituent_target_base = next_account(&mut iter)?;
        let lp_pool_token_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let amm_cache = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LpPoolRemoveLiquidityInstructionAccounts {
            state,
            drift_signer,
            lp_pool,
            authority,
            out_market_mint,
            out_constituent,
            user_out_token_account,
            constituent_out_token_account,
            user_lp_token_account,
            spot_market_token_account,
            lp_mint,
            constituent_target_base,
            lp_pool_token_vault,
            token_program,
            amm_cache,
            remaining: remaining.to_vec(),
        })
    }
}

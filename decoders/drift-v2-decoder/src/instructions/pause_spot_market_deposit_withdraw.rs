//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct PauseSpotMarketDepositWithdraw {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct PauseSpotMarketDepositWithdrawInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub keeper: solana_pubkey::Pubkey,
    pub spot_market: solana_pubkey::Pubkey,
    pub spot_market_vault: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for PauseSpotMarketDepositWithdraw {
    const DISCRIMINATOR: &'static [u8] = &[183, 119, 59, 170, 137, 35, 242, 86];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for PauseSpotMarketDepositWithdraw {
    type ArrangedAccounts = PauseSpotMarketDepositWithdrawInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let keeper = next_account(&mut iter)?;
        let spot_market = next_account(&mut iter)?;
        let spot_market_vault = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(PauseSpotMarketDepositWithdrawInstructionAccounts {
            state,
            keeper,
            spot_market,
            spot_market_vault,
            remaining: remaining.to_vec(),
        })
    }
}

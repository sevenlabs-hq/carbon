//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
    solana_pubkey::Pubkey,
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeLpPool {
    pub lp_pool_id: u8,
    pub min_mint_fee: i64,
    pub max_aum: u128,
    pub max_settle_quote_amount_per_market: u64,
    pub whitelist_mint: Pubkey,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeLpPoolInstructionAccounts {
    pub admin: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub lp_pool_token_vault: solana_pubkey::Pubkey,
    pub amm_constituent_mapping: solana_pubkey::Pubkey,
    pub constituent_target_base: solana_pubkey::Pubkey,
    pub constituent_correlations: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeLpPool {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [242, 64, 1, 222, 142, 46, 204, 227] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeLpPool {
    type ArrangedAccounts = InitializeLpPoolInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let lp_pool_token_vault = next_account(&mut iter)?;
        let amm_constituent_mapping = next_account(&mut iter)?;
        let constituent_target_base = next_account(&mut iter)?;
        let constituent_correlations = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeLpPoolInstructionAccounts {
            admin,
            lp_pool,
            mint,
            lp_pool_token_vault,
            amm_constituent_mapping,
            constituent_target_base,
            constituent_correlations,
            state,
            token_program,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

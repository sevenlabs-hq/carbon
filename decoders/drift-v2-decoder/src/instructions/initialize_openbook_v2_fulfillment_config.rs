//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeOpenbookV2FulfillmentConfig {
    pub market_index: u16,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeOpenbookV2FulfillmentConfigInstructionAccounts {
    pub base_spot_market: solana_pubkey::Pubkey,
    pub quote_spot_market: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub openbook_v2_program: solana_pubkey::Pubkey,
    pub openbook_v2_market: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub openbook_v2_fulfillment_config: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeOpenbookV2FulfillmentConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [7, 221, 103, 153, 107, 57, 27, 197] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeOpenbookV2FulfillmentConfig {
    type ArrangedAccounts = InitializeOpenbookV2FulfillmentConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let base_spot_market = next_account(&mut iter)?;
        let quote_spot_market = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let openbook_v2_program = next_account(&mut iter)?;
        let openbook_v2_market = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;
        let openbook_v2_fulfillment_config = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeOpenbookV2FulfillmentConfigInstructionAccounts {
            base_spot_market,
            quote_spot_market,
            state,
            openbook_v2_program,
            openbook_v2_market,
            drift_signer,
            openbook_v2_fulfillment_config,
            admin,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateConstituentCorrelationData {
    pub index1: u16,
    pub index2: u16,
    pub correlation: i64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateConstituentCorrelationDataInstructionAccounts {
    pub admin: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub constituent_correlations: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateConstituentCorrelationData {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [79, 14, 19, 73, 221, 106, 62, 109] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateConstituentCorrelationData {
    type ArrangedAccounts = UpdateConstituentCorrelationDataInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let constituent_correlations = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateConstituentCorrelationDataInstructionAccounts {
            admin,
            lp_pool,
            constituent_correlations,
            state,
            remaining: remaining.to_vec(),
        })
    }
}

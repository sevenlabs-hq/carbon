//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdatePythPullOracle {
    pub feed_id: [u8; 32],
    pub params: Vec<u8>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdatePythPullOracleInstructionAccounts {
    pub keeper: solana_pubkey::Pubkey,
    pub pyth_solana_receiver: solana_pubkey::Pubkey,
    pub encoded_vaa: solana_pubkey::Pubkey,
    pub price_feed: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdatePythPullOracle {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [230, 191, 189, 94, 108, 59, 74, 197] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdatePythPullOracle {
    type ArrangedAccounts = UpdatePythPullOracleInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let keeper = next_account(&mut iter)?;
        let pyth_solana_receiver = next_account(&mut iter)?;
        let encoded_vaa = next_account(&mut iter)?;
        let price_feed = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdatePythPullOracleInstructionAccounts {
            keeper,
            pyth_solana_receiver,
            encoded_vaa,
            price_feed,
            remaining: remaining.to_vec(),
        })
    }
}

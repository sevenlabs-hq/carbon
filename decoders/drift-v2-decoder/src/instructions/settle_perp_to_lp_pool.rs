//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SettlePerpToLpPool {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SettlePerpToLpPoolInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub keeper: solana_pubkey::Pubkey,
    pub amm_cache: solana_pubkey::Pubkey,
    pub quote_market: solana_pubkey::Pubkey,
    pub constituent: solana_pubkey::Pubkey,
    pub constituent_quote_token_account: solana_pubkey::Pubkey,
    pub quote_token_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl SettlePerpToLpPool {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [5, 98, 46, 188, 10, 59, 2, 249] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for SettlePerpToLpPool {
    type ArrangedAccounts = SettlePerpToLpPoolInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let keeper = next_account(&mut iter)?;
        let amm_cache = next_account(&mut iter)?;
        let quote_market = next_account(&mut iter)?;
        let constituent = next_account(&mut iter)?;
        let constituent_quote_token_account = next_account(&mut iter)?;
        let quote_token_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(SettlePerpToLpPoolInstructionAccounts {
            state,
            lp_pool,
            keeper,
            amm_cache,
            quote_market,
            constituent,
            constituent_quote_token_account,
            quote_token_vault,
            token_program,
            drift_signer,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeConstituent {
    pub spot_market_index: u16,
    pub decimals: u8,
    pub max_weight_deviation: i64,
    pub swap_fee_min: i64,
    pub swap_fee_max: i64,
    pub max_borrow_token_amount: u64,
    pub oracle_staleness_threshold: u64,
    pub cost_to_trade: i32,
    pub constituent_derivative_index: Option<i16>,
    pub constituent_derivative_depeg_threshold: u64,
    pub derivative_weight: u64,
    pub volatility: u64,
    pub gamma_execution: u8,
    pub gamma_inventory: u8,
    pub xi: u8,
    pub new_constituent_correlations: Vec<i64>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeConstituentInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub constituent_target_base: solana_pubkey::Pubkey,
    pub constituent_correlations: solana_pubkey::Pubkey,
    pub constituent: solana_pubkey::Pubkey,
    pub spot_market: solana_pubkey::Pubkey,
    pub spot_market_mint: solana_pubkey::Pubkey,
    pub constituent_vault: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeConstituent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [12, 196, 45, 218, 93, 89, 0, 33] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeConstituent {
    type ArrangedAccounts = InitializeConstituentInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let constituent_target_base = next_account(&mut iter)?;
        let constituent_correlations = next_account(&mut iter)?;
        let constituent = next_account(&mut iter)?;
        let spot_market = next_account(&mut iter)?;
        let spot_market_mint = next_account(&mut iter)?;
        let constituent_vault = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeConstituentInstructionAccounts {
            state,
            admin,
            lp_pool,
            constituent_target_base,
            constituent_correlations,
            constituent,
            spot_market,
            spot_market_mint,
            constituent_vault,
            rent,
            system_program,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

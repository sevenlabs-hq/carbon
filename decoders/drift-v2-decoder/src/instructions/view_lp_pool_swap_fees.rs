//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ViewLpPoolSwapFees {
    pub in_market_index: u16,
    pub out_market_index: u16,
    pub in_amount: u64,
    pub in_target_weight: i64,
    pub out_target_weight: i64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ViewLpPoolSwapFeesInstructionAccounts {
    pub drift_signer: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub lp_pool: solana_pubkey::Pubkey,
    pub constituent_target_base: solana_pubkey::Pubkey,
    pub constituent_correlations: solana_pubkey::Pubkey,
    pub constituent_in_token_account: solana_pubkey::Pubkey,
    pub constituent_out_token_account: solana_pubkey::Pubkey,
    pub in_constituent: solana_pubkey::Pubkey,
    pub out_constituent: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ViewLpPoolSwapFees {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [126, 189, 109, 189, 170, 156, 3, 46] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for ViewLpPoolSwapFees {
    type ArrangedAccounts = ViewLpPoolSwapFeesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let drift_signer = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let lp_pool = next_account(&mut iter)?;
        let constituent_target_base = next_account(&mut iter)?;
        let constituent_correlations = next_account(&mut iter)?;
        let constituent_in_token_account = next_account(&mut iter)?;
        let constituent_out_token_account = next_account(&mut iter)?;
        let in_constituent = next_account(&mut iter)?;
        let out_constituent = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ViewLpPoolSwapFeesInstructionAccounts {
            drift_signer,
            state,
            lp_pool,
            constituent_target_base,
            constituent_correlations,
            constituent_in_token_account,
            constituent_out_token_account,
            in_constituent,
            out_constituent,
            authority,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

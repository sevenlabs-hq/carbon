//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawFromProgramVault {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawFromProgramVaultInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub constituent: solana_pubkey::Pubkey,
    pub constituent_token_account: solana_pubkey::Pubkey,
    pub spot_market: solana_pubkey::Pubkey,
    pub spot_market_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub oracle: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawFromProgramVault {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [120, 40, 183, 149, 232, 18, 224, 151] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawFromProgramVault {
    type ArrangedAccounts = WithdrawFromProgramVaultInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;
        let constituent = next_account(&mut iter)?;
        let constituent_token_account = next_account(&mut iter)?;
        let spot_market = next_account(&mut iter)?;
        let spot_market_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let oracle = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawFromProgramVaultInstructionAccounts {
            state,
            admin,
            drift_signer,
            constituent,
            constituent_token_account,
            spot_market,
            spot_market_vault,
            token_program,
            mint,
            oracle,
            remaining: remaining.to_vec(),
        })
    }
}

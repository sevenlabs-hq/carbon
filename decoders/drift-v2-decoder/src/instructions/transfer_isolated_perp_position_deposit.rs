//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TransferIsolatedPerpPositionDeposit {
    pub spot_market_index: u16,
    pub perp_market_index: u16,
    pub amount: i64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct TransferIsolatedPerpPositionDepositInstructionAccounts {
    pub user: solana_pubkey::Pubkey,
    pub user_stats: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub spot_market_vault: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl TransferIsolatedPerpPositionDeposit {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [201, 131, 242, 228, 85, 226, 70, 237] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for TransferIsolatedPerpPositionDeposit {
    type ArrangedAccounts = TransferIsolatedPerpPositionDepositInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let user = next_account(&mut iter)?;
        let user_stats = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let spot_market_vault = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(TransferIsolatedPerpPositionDepositInstructionAccounts {
            user,
            user_stats,
            authority,
            state,
            spot_market_vault,
            remaining: remaining.to_vec(),
        })
    }
}

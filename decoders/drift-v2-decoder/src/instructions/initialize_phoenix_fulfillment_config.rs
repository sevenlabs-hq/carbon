//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializePhoenixFulfillmentConfig {
    pub market_index: u16,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializePhoenixFulfillmentConfigInstructionAccounts {
    pub base_spot_market: solana_pubkey::Pubkey,
    pub quote_spot_market: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub phoenix_program: solana_pubkey::Pubkey,
    pub phoenix_market: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub phoenix_fulfillment_config: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializePhoenixFulfillmentConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [135, 132, 110, 107, 185, 160, 169, 154] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializePhoenixFulfillmentConfig {
    type ArrangedAccounts = InitializePhoenixFulfillmentConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let base_spot_market = next_account(&mut iter)?;
        let quote_spot_market = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let phoenix_program = next_account(&mut iter)?;
        let phoenix_market = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;
        let phoenix_fulfillment_config = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializePhoenixFulfillmentConfigInstructionAccounts {
            base_spot_market,
            quote_spot_market,
            state,
            phoenix_program,
            phoenix_market,
            drift_signer,
            phoenix_fulfillment_config,
            admin,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

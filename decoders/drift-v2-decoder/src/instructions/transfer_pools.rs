//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TransferPools {
    pub deposit_from_market_index: u16,
    pub deposit_to_market_index: u16,
    pub borrow_from_market_index: u16,
    pub borrow_to_market_index: u16,
    pub deposit_amount: Option<u64>,
    pub borrow_amount: Option<u64>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct TransferPoolsInstructionAccounts {
    pub from_user: solana_pubkey::Pubkey,
    pub to_user: solana_pubkey::Pubkey,
    pub user_stats: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub deposit_from_spot_market_vault: solana_pubkey::Pubkey,
    pub deposit_to_spot_market_vault: solana_pubkey::Pubkey,
    pub borrow_from_spot_market_vault: solana_pubkey::Pubkey,
    pub borrow_to_spot_market_vault: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl TransferPools {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [197, 103, 154, 25, 107, 90, 60, 94] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for TransferPools {
    type ArrangedAccounts = TransferPoolsInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let from_user = next_account(&mut iter)?;
        let to_user = next_account(&mut iter)?;
        let user_stats = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let deposit_from_spot_market_vault = next_account(&mut iter)?;
        let deposit_to_spot_market_vault = next_account(&mut iter)?;
        let borrow_from_spot_market_vault = next_account(&mut iter)?;
        let borrow_to_spot_market_vault = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(TransferPoolsInstructionAccounts {
            from_user,
            to_user,
            user_stats,
            authority,
            state,
            deposit_from_spot_market_vault,
            deposit_to_spot_market_vault,
            borrow_from_spot_market_vault,
            borrow_to_spot_market_vault,
            drift_signer,
            remaining: remaining.to_vec(),
        })
    }
}

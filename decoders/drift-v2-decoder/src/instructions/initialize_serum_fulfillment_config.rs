//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeSerumFulfillmentConfig {
    pub market_index: u16,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeSerumFulfillmentConfigInstructionAccounts {
    pub base_spot_market: solana_pubkey::Pubkey,
    pub quote_spot_market: solana_pubkey::Pubkey,
    pub state: solana_pubkey::Pubkey,
    pub serum_program: solana_pubkey::Pubkey,
    pub serum_market: solana_pubkey::Pubkey,
    pub serum_open_orders: solana_pubkey::Pubkey,
    pub drift_signer: solana_pubkey::Pubkey,
    pub serum_fulfillment_config: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeSerumFulfillmentConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [193, 211, 132, 172, 70, 171, 7, 94] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeSerumFulfillmentConfig {
    type ArrangedAccounts = InitializeSerumFulfillmentConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let base_spot_market = next_account(&mut iter)?;
        let quote_spot_market = next_account(&mut iter)?;
        let state = next_account(&mut iter)?;
        let serum_program = next_account(&mut iter)?;
        let serum_market = next_account(&mut iter)?;
        let serum_open_orders = next_account(&mut iter)?;
        let drift_signer = next_account(&mut iter)?;
        let serum_fulfillment_config = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeSerumFulfillmentConfigInstructionAccounts {
            base_spot_market,
            quote_spot_market,
            state,
            serum_program,
            serum_market,
            serum_open_orders,
            drift_signer,
            serum_fulfillment_config,
            admin,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

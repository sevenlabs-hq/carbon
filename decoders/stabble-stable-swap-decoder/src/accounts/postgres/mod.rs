//! This code was AUTOGENERATED using the Codama library.
pub mod pool_row;
pub mod strategy_row;
pub mod vault_row;

pub use self::{pool_row::*, strategy_row::*, vault_row::*};
use super::StabbleStableSwapAccount;

pub struct StabbleStableSwapAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for StabbleStableSwapAccountsMigration {
    fn app(&self) -> &str {
        "stabble-stable-swap"
    }

    fn name(&self) -> &str {
        "stabble_stable_swap_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(PoolMigrationOperation),
            Box::new(StrategyMigrationOperation),
            Box::new(VaultMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct StabbleStableSwapAccountWithMetadata(
    pub StabbleStableSwapAccount,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        StabbleStableSwapAccount,
        carbon_core::account::AccountMetadata,
    )> for StabbleStableSwapAccountWithMetadata
{
    fn from(
        value: (
            StabbleStableSwapAccount,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        StabbleStableSwapAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for StabbleStableSwapAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleStableSwapAccountWithMetadata(account, metadata) = self;

        match account {
            StabbleStableSwapAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapAccount::Strategy(account) => {
                let row = strategy_row::StrategyRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapAccount::Vault(account) => {
                let row = vault_row::VaultRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for StabbleStableSwapAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleStableSwapAccountWithMetadata(account, metadata) = self;
        match account {
            StabbleStableSwapAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapAccount::Strategy(account) => {
                let row = strategy_row::StrategyRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapAccount::Vault(account) => {
                let row = vault_row::VaultRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

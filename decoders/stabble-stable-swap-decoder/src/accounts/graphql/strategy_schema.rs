//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U32},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Strategy")]
pub struct StrategyGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub pool: Pubkey,
    pub is_active: bool,
    pub amp_min_factor: i32,
    pub amp_max_factor: i32,
    pub ramp_min_step: i32,
    pub ramp_max_step: i32,
    pub ramp_min_duration: U32,
    pub ramp_max_duration: U32,
}

impl TryFrom<crate::accounts::postgres::StrategyRow> for StrategyGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::StrategyRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            pool: carbon_core::graphql::primitives::Pubkey(row.pool.0),
            is_active: row.is_active,
            amp_min_factor: *row.amp_min_factor,
            amp_max_factor: *row.amp_max_factor,
            ramp_min_step: *row.ramp_min_step,
            ramp_max_step: *row.ramp_max_step,
            ramp_min_duration: carbon_core::graphql::primitives::U32(
                (*row.ramp_min_duration) as u32,
            ),
            ramp_max_duration: carbon_core::graphql::primitives::U32(
                (*row.ramp_max_duration) as u32,
            ),
        })
    }
}

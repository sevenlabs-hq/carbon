//! This code was AUTOGENERATED using the Codama library.
pub mod accept_owner_row;
pub mod approve_strategy_row;
pub mod change_amp_factor_row;
pub mod change_max_supply_row;
pub mod change_swap_fee_privileged_row;
pub mod change_swap_fee_row;
pub mod close_strategy_row;
pub mod cpi_event_row;
pub mod create_strategy_row;
pub mod deposit_row;
pub mod exec_strategy_row;
pub mod initialize_row;
pub mod pause_row;
pub mod reject_owner_row;
pub mod shutdown_row;
pub mod swap_row;
pub mod swap_v2_row;
pub mod transfer_owner_row;
pub mod unpause_row;
pub mod withdraw_row;

pub use self::{
    accept_owner_row::*, approve_strategy_row::*, change_amp_factor_row::*,
    change_max_supply_row::*, change_swap_fee_privileged_row::*, change_swap_fee_row::*,
    close_strategy_row::*, cpi_event_row::*, create_strategy_row::*, deposit_row::*,
    exec_strategy_row::*, initialize_row::*, pause_row::*, reject_owner_row::*, shutdown_row::*,
    swap_row::*, swap_v2_row::*, transfer_owner_row::*, unpause_row::*, withdraw_row::*,
};
use super::StabbleStableSwapInstruction;

pub struct StabbleStableSwapInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for StabbleStableSwapInstructionsMigration {
    fn app(&self) -> &str {
        "stabble-stable-swap"
    }

    fn name(&self) -> &str {
        "stabble_stable_swap_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptOwnerMigrationOperation),
            Box::new(ApproveStrategyMigrationOperation),
            Box::new(ChangeAmpFactorMigrationOperation),
            Box::new(ChangeMaxSupplyMigrationOperation),
            Box::new(ChangeSwapFeeMigrationOperation),
            Box::new(ChangeSwapFeePrivilegedMigrationOperation),
            Box::new(CloseStrategyMigrationOperation),
            Box::new(CreateStrategyMigrationOperation),
            Box::new(DepositMigrationOperation),
            Box::new(ExecStrategyMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(PauseMigrationOperation),
            Box::new(RejectOwnerMigrationOperation),
            Box::new(ShutdownMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(SwapV2MigrationOperation),
            Box::new(TransferOwnerMigrationOperation),
            Box::new(UnpauseMigrationOperation),
            Box::new(WithdrawMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct StabbleStableSwapInstructionWithMetadata(
    pub StabbleStableSwapInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        StabbleStableSwapInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for StabbleStableSwapInstructionWithMetadata
{
    fn from(
        value: (
            StabbleStableSwapInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        StabbleStableSwapInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for StabbleStableSwapInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleStableSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            StabbleStableSwapInstruction::AcceptOwner { data, .. } => {
                let row = accept_owner_row::AcceptOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ApproveStrategy { data, .. } => {
                let row = approve_strategy_row::ApproveStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeAmpFactor { data, .. } => {
                let row = change_amp_factor_row::ChangeAmpFactorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeMaxSupply { data, .. } => {
                let row = change_max_supply_row::ChangeMaxSupplyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeSwapFee { data, .. } => {
                let row = change_swap_fee_row::ChangeSwapFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeSwapFeePrivileged { data, .. } => {
                let row = change_swap_fee_privileged_row::ChangeSwapFeePrivilegedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CloseStrategy { data, .. } => {
                let row = close_strategy_row::CloseStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CreateStrategy { data, .. } => {
                let row = create_strategy_row::CreateStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ExecStrategy { data, .. } => {
                let row = exec_strategy_row::ExecStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::RejectOwner { data, .. } => {
                let row = reject_owner_row::RejectOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Shutdown { data, .. } => {
                let row = shutdown_row::ShutdownRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::SwapV2 { data, .. } => {
                let row = swap_v2_row::SwapV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::TransferOwner { data, .. } => {
                let row = transfer_owner_row::TransferOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for StabbleStableSwapInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleStableSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            StabbleStableSwapInstruction::AcceptOwner { data, .. } => {
                let row = accept_owner_row::AcceptOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ApproveStrategy { data, .. } => {
                let row = approve_strategy_row::ApproveStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeAmpFactor { data, .. } => {
                let row = change_amp_factor_row::ChangeAmpFactorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeMaxSupply { data, .. } => {
                let row = change_max_supply_row::ChangeMaxSupplyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeSwapFee { data, .. } => {
                let row = change_swap_fee_row::ChangeSwapFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ChangeSwapFeePrivileged { data, .. } => {
                let row = change_swap_fee_privileged_row::ChangeSwapFeePrivilegedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CloseStrategy { data, .. } => {
                let row = close_strategy_row::CloseStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CreateStrategy { data, .. } => {
                let row = create_strategy_row::CreateStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::ExecStrategy { data, .. } => {
                let row = exec_strategy_row::ExecStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::RejectOwner { data, .. } => {
                let row = reject_owner_row::RejectOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Shutdown { data, .. } => {
                let row = shutdown_row::ShutdownRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::SwapV2 { data, .. } => {
                let row = swap_v2_row::SwapV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::TransferOwner { data, .. } => {
                let row = transfer_owner_row::TransferOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleStableSwapInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

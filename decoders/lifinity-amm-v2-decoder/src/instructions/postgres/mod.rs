//! This code was AUTOGENERATED using the Codama library.
pub mod deposit_all_token_types_row;
pub mod swap_row;
pub mod withdraw_all_token_types_row;

pub use self::{deposit_all_token_types_row::*, swap_row::*, withdraw_all_token_types_row::*};
use super::LifinityAmmV2Instruction;

pub struct LifinityAmmV2InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for LifinityAmmV2InstructionsMigration {
    fn app(&self) -> &str {
        "lifinity-amm-v2"
    }

    fn name(&self) -> &str {
        "lifinity_amm_v2_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(DepositAllTokenTypesMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(WithdrawAllTokenTypesMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct LifinityAmmV2InstructionWithMetadata(
    pub LifinityAmmV2Instruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        LifinityAmmV2Instruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for LifinityAmmV2InstructionWithMetadata
{
    fn from(
        value: (
            LifinityAmmV2Instruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        LifinityAmmV2InstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for LifinityAmmV2InstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let LifinityAmmV2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            LifinityAmmV2Instruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            LifinityAmmV2Instruction::DepositAllTokenTypes { data, .. } => {
                let row = deposit_all_token_types_row::DepositAllTokenTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            LifinityAmmV2Instruction::WithdrawAllTokenTypes { data, .. } => {
                let row = withdraw_all_token_types_row::WithdrawAllTokenTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for LifinityAmmV2InstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let LifinityAmmV2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            LifinityAmmV2Instruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            LifinityAmmV2Instruction::DepositAllTokenTypes { data, .. } => {
                let row = deposit_all_token_types_row::DepositAllTokenTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            LifinityAmmV2Instruction::WithdrawAllTokenTypes { data, .. } => {
                let row = withdraw_all_token_types_row::WithdrawAllTokenTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

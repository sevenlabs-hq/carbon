//! This code was AUTOGENERATED using the Codama library.
pub mod deposit_row;
pub mod initialize_row;
pub mod pre_initialize_row;
pub mod swap_base_in_row;
pub mod swap_base_out_row;
pub mod withdraw_row;

pub use self::{
    deposit_row::*, initialize_row::*, pre_initialize_row::*, swap_base_in_row::*,
    swap_base_out_row::*, withdraw_row::*,
};
use super::RaydiumStableSwapInstruction;

pub struct RaydiumStableSwapInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaydiumStableSwapInstructionsMigration {
    fn app(&self) -> &str {
        "raydium-stable-swap"
    }

    fn name(&self) -> &str {
        "raydium_stable_swap_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(DepositMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(PreInitializeMigrationOperation),
            Box::new(SwapBaseInMigrationOperation),
            Box::new(SwapBaseOutMigrationOperation),
            Box::new(WithdrawMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaydiumStableSwapInstructionWithMetadata(
    pub RaydiumStableSwapInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        RaydiumStableSwapInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for RaydiumStableSwapInstructionWithMetadata
{
    fn from(
        value: (
            RaydiumStableSwapInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        RaydiumStableSwapInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaydiumStableSwapInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumStableSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            RaydiumStableSwapInstruction::PreInitialize { data, .. } => {
                let row = pre_initialize_row::PreInitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::SwapBaseIn { data, .. } => {
                let row = swap_base_in_row::SwapBaseInRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::SwapBaseOut { data, .. } => {
                let row = swap_base_out_row::SwapBaseOutRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaydiumStableSwapInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumStableSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            RaydiumStableSwapInstruction::PreInitialize { data, .. } => {
                let row = pre_initialize_row::PreInitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::SwapBaseIn { data, .. } => {
                let row = swap_base_in_row::SwapBaseInRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumStableSwapInstruction::SwapBaseOut { data, .. } => {
                let row = swap_base_out_row::SwapBaseOutRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

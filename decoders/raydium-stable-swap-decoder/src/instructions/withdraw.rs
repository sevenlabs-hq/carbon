//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Withdraw liquidity from the stable AMM pool
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Withdraw {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawInstructionAccounts {
    pub amm: solana_pubkey::Pubkey,
    pub amm_authority: solana_pubkey::Pubkey,
    pub amm_open_orders: solana_pubkey::Pubkey,
    pub amm_target_orders: solana_pubkey::Pubkey,
    pub amm_lp_mint: solana_pubkey::Pubkey,
    pub amm_coin_vault: solana_pubkey::Pubkey,
    pub amm_pc_vault: solana_pubkey::Pubkey,
    pub model_data_account: solana_pubkey::Pubkey,
    pub serum_program: solana_pubkey::Pubkey,
    pub serum_market: solana_pubkey::Pubkey,
    pub serum_coin_vault: solana_pubkey::Pubkey,
    pub serum_pc_vault: solana_pubkey::Pubkey,
    pub serum_vault_signer: solana_pubkey::Pubkey,
    pub user_source_lp_token: solana_pubkey::Pubkey,
    pub user_dest_coin_token: solana_pubkey::Pubkey,
    pub user_dest_pc_token: solana_pubkey::Pubkey,
    pub user_owner: solana_pubkey::Pubkey,
    pub spl_token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Withdraw {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [183, 18, 70, 156, 148, 109, 161, 34] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Withdraw {
    type ArrangedAccounts = WithdrawInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let amm = next_account(&mut iter)?;
        let amm_authority = next_account(&mut iter)?;
        let amm_open_orders = next_account(&mut iter)?;
        let amm_target_orders = next_account(&mut iter)?;
        let amm_lp_mint = next_account(&mut iter)?;
        let amm_coin_vault = next_account(&mut iter)?;
        let amm_pc_vault = next_account(&mut iter)?;
        let model_data_account = next_account(&mut iter)?;
        let serum_program = next_account(&mut iter)?;
        let serum_market = next_account(&mut iter)?;
        let serum_coin_vault = next_account(&mut iter)?;
        let serum_pc_vault = next_account(&mut iter)?;
        let serum_vault_signer = next_account(&mut iter)?;
        let user_source_lp_token = next_account(&mut iter)?;
        let user_dest_coin_token = next_account(&mut iter)?;
        let user_dest_pc_token = next_account(&mut iter)?;
        let user_owner = next_account(&mut iter)?;
        let spl_token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawInstructionAccounts {
            amm,
            amm_authority,
            amm_open_orders,
            amm_target_orders,
            amm_lp_mint,
            amm_coin_vault,
            amm_pc_vault,
            model_data_account,
            serum_program,
            serum_market,
            serum_coin_vault,
            serum_pc_vault,
            serum_vault_signer,
            user_source_lp_token,
            user_dest_coin_token,
            user_dest_pc_token,
            user_owner,
            spl_token_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use crate::{SolayerRestakingProgramDecoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod restaking_pool;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum SolayerRestakingProgramAccount {
    RestakingPool(Box<restaking_pool::RestakingPool>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for SolayerRestakingProgramDecoder {
    type AccountType = SolayerRestakingProgramAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = restaking_pool::RestakingPool::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: SolayerRestakingProgramAccount::RestakingPool(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod deposit_all_token_types_row;
pub mod deposit_single_token_type_exact_amount_in_row;
pub mod initialize_row;
pub mod swap_row;
pub mod withdraw_all_token_types_row;
pub mod withdraw_single_token_type_exact_amount_out_row;

pub use self::{
    deposit_all_token_types_row::*, deposit_single_token_type_exact_amount_in_row::*,
    initialize_row::*, swap_row::*, withdraw_all_token_types_row::*,
    withdraw_single_token_type_exact_amount_out_row::*,
};
use super::FluxbeamInstruction;

pub struct FluxbeamInstructionsMigration;

impl carbon_core::clickhouse::Migration for FluxbeamInstructionsMigration {
    fn app(&self) -> &str {
        "fluxbeam"
    }

    fn name(&self) -> &str {
        "fluxbeam_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(DepositAllTokenTypesRowMigrationOperation),
            Box::new(DepositSingleTokenTypeExactAmountInRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(SwapRowMigrationOperation),
            Box::new(WithdrawAllTokenTypesRowMigrationOperation),
            Box::new(WithdrawSingleTokenTypeExactAmountOutRowMigrationOperation),
        ]
    }
}

pub enum FluxbeamInstructionRow {
    DepositAllTokenTypes(DepositAllTokenTypesRow),
    DepositSingleTokenTypeExactAmountIn(DepositSingleTokenTypeExactAmountInRow),
    Initialize(InitializeRow),
    Swap(SwapRow),
    WithdrawAllTokenTypes(WithdrawAllTokenTypesRow),
    WithdrawSingleTokenTypeExactAmountOut(WithdrawSingleTokenTypeExactAmountOutRow),
}

pub struct FluxbeamInstructionMetadata(
    pub carbon_core::instruction::InstructionMetadata,
    pub FluxbeamInstruction,
);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for FluxbeamInstructionMetadata {
    type Row = FluxbeamInstructionRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, instruction) = self;

        match instruction {
            FluxbeamInstruction::DepositAllTokenTypes { data, accounts, .. } => {
                rows.push(FluxbeamInstructionRow::DepositAllTokenTypes(
                    DepositAllTokenTypesRow::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?,
                ));
            }
            FluxbeamInstruction::DepositSingleTokenTypeExactAmountIn { data, accounts, .. } => {
                rows.push(FluxbeamInstructionRow::DepositSingleTokenTypeExactAmountIn(
                    DepositSingleTokenTypeExactAmountInRow::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?,
                ));
            }
            FluxbeamInstruction::Initialize { data, accounts, .. } => {
                rows.push(FluxbeamInstructionRow::Initialize(InitializeRow::try_from(
                    (data.clone(), metadata.clone(), accounts.clone()),
                )?));
            }
            FluxbeamInstruction::Swap { data, accounts, .. } => {
                rows.push(FluxbeamInstructionRow::Swap(SwapRow::try_from((
                    data.clone(),
                    metadata.clone(),
                    accounts.clone(),
                ))?));
            }
            FluxbeamInstruction::WithdrawAllTokenTypes { data, accounts, .. } => {
                rows.push(FluxbeamInstructionRow::WithdrawAllTokenTypes(
                    WithdrawAllTokenTypesRow::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?,
                ));
            }
            FluxbeamInstruction::WithdrawSingleTokenTypeExactAmountOut {
                data, accounts, ..
            } => {
                rows.push(
                    FluxbeamInstructionRow::WithdrawSingleTokenTypeExactAmountOut(
                        WithdrawSingleTokenTypeExactAmountOutRow::try_from((
                            data.clone(),
                            metadata.clone(),
                            accounts.clone(),
                        ))?,
                    ),
                );
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for FluxbeamInstructionRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(DepositAllTokenTypes, DepositAllTokenTypesRow);
        commit_branch!(
            DepositSingleTokenTypeExactAmountIn,
            DepositSingleTokenTypeExactAmountInRow
        );
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(Swap, SwapRow);
        commit_branch!(WithdrawAllTokenTypes, WithdrawAllTokenTypesRow);
        commit_branch!(
            WithdrawSingleTokenTypeExactAmountOut,
            WithdrawSingleTokenTypeExactAmountOutRow
        );
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Removes an offered but not taken loan
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RescindLoan {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RescindLoanInstructionAccounts {
    pub loan: solana_pubkey::Pubkey,
    pub lender_value_token_account: Option<solana_pubkey::Pubkey>,
    pub lender: solana_pubkey::Pubkey,
    pub value_mint: solana_pubkey::Pubkey,
    pub escrow: solana_pubkey::Pubkey,
    pub escrow_token_account: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RescindLoan {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [64, 64, 160, 211, 51, 36, 177, 158] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for RescindLoan {
    type ArrangedAccounts = RescindLoanInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let loan = next_account(&mut iter)?;
        let lender_value_token_account = next_account(&mut iter);
        let lender = next_account(&mut iter)?;
        let value_mint = next_account(&mut iter)?;
        let escrow = next_account(&mut iter)?;
        let escrow_token_account = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RescindLoanInstructionAccounts {
            loan,
            lender_value_token_account,
            lender,
            value_mint,
            escrow,
            escrow_token_account,
            system_program,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

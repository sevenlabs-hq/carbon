//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TakeTokenLoan {
    pub usdc_amount: u64,
    pub expected_loan: String,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct TakeTokenLoanInstructionAccounts {
    pub orderbook: solana_pubkey::Pubkey,
    pub loan: solana_pubkey::Pubkey,
    pub pool: solana_pubkey::Pubkey,
    pub usdc_vault: solana_pubkey::Pubkey,
    pub collateral_vault: solana_pubkey::Pubkey,
    pub borrower: solana_pubkey::Pubkey,
    pub borrower_usdc: solana_pubkey::Pubkey,
    pub borrower_collateral: solana_pubkey::Pubkey,
    pub fee_authority: solana_pubkey::Pubkey,
    pub burn_wallet: solana_pubkey::Pubkey,
    pub price_update: solana_pubkey::Pubkey,
    pub loan_mint: solana_pubkey::Pubkey,
    pub collateral_mint: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl TakeTokenLoan {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [221, 216, 127, 10, 107, 189, 152, 111] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for TakeTokenLoan {
    type ArrangedAccounts = TakeTokenLoanInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let orderbook = next_account(&mut iter)?;
        let loan = next_account(&mut iter)?;
        let pool = next_account(&mut iter)?;
        let usdc_vault = next_account(&mut iter)?;
        let collateral_vault = next_account(&mut iter)?;
        let borrower = next_account(&mut iter)?;
        let borrower_usdc = next_account(&mut iter)?;
        let borrower_collateral = next_account(&mut iter)?;
        let fee_authority = next_account(&mut iter)?;
        let burn_wallet = next_account(&mut iter)?;
        let price_update = next_account(&mut iter)?;
        let loan_mint = next_account(&mut iter)?;
        let collateral_mint = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(TakeTokenLoanInstructionAccounts {
            orderbook,
            loan,
            pool,
            usdc_vault,
            collateral_vault,
            borrower,
            borrower_usdc,
            borrower_collateral,
            fee_authority,
            burn_wallet,
            price_update,
            loan_mint,
            collateral_mint,
            token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

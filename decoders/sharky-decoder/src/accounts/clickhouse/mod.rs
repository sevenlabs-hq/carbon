//! This code was AUTOGENERATED using the Codama library.
pub mod escrow_pda_row;
pub mod loan_row;
pub mod nft_list_row;
pub mod order_book_row;
pub mod price_update_v2_row;
pub mod program_version_row;
pub mod token_lending_loan_row;
pub mod token_lending_order_book_row;
pub mod token_lending_pool_row;

pub use self::{
    escrow_pda_row::*, loan_row::*, nft_list_row::*, order_book_row::*, price_update_v2_row::*,
    program_version_row::*, token_lending_loan_row::*, token_lending_order_book_row::*,
    token_lending_pool_row::*,
};
use super::SharkyAccount;

pub struct SharkyAccountsMigration;

impl carbon_core::clickhouse::Migration for SharkyAccountsMigration {
    fn app(&self) -> &str {
        "sharky"
    }

    fn name(&self) -> &str {
        "sharky_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(EscrowPDARowMigrationOperation),
            Box::new(LoanRowMigrationOperation),
            Box::new(NFTListRowMigrationOperation),
            Box::new(OrderBookRowMigrationOperation),
            Box::new(PriceUpdateV2RowMigrationOperation),
            Box::new(ProgramVersionRowMigrationOperation),
            Box::new(TokenLendingLoanRowMigrationOperation),
            Box::new(TokenLendingOrderBookRowMigrationOperation),
            Box::new(TokenLendingPoolRowMigrationOperation),
        ]
    }
}

pub enum SharkyAccountRow {
    EscrowPDA(EscrowPDARow),
    Loan(LoanRow),
    NFTList(NFTListRow),
    OrderBook(OrderBookRow),
    PriceUpdateV2(PriceUpdateV2Row),
    ProgramVersion(ProgramVersionRow),
    TokenLendingLoan(TokenLendingLoanRow),
    TokenLendingOrderBook(TokenLendingOrderBookRow),
    TokenLendingPool(TokenLendingPoolRow),
}

pub struct SharkyAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a SharkyAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for SharkyAccountMetadata<'a> {
    type Row = SharkyAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let SharkyAccount::$variant(account) = account {
                    rows.push(SharkyAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let SharkyAccount::$variant(account) = account {
                    rows.push(SharkyAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(EscrowPDA, EscrowPDARow, boxed);
        insert_branch!(Loan, LoanRow, boxed);
        insert_branch!(NFTList, NFTListRow, boxed);
        insert_branch!(OrderBook, OrderBookRow, boxed);
        insert_branch!(PriceUpdateV2, PriceUpdateV2Row, boxed);
        insert_branch!(ProgramVersion, ProgramVersionRow, boxed);
        insert_branch!(TokenLendingLoan, TokenLendingLoanRow, boxed);
        insert_branch!(TokenLendingOrderBook, TokenLendingOrderBookRow, boxed);
        insert_branch!(TokenLendingPool, TokenLendingPoolRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for SharkyAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(EscrowPDA, EscrowPDARow);
        commit_branch!(Loan, LoanRow);
        commit_branch!(NFTList, NFTListRow);
        commit_branch!(OrderBook, OrderBookRow);
        commit_branch!(PriceUpdateV2, PriceUpdateV2Row);
        commit_branch!(ProgramVersion, ProgramVersionRow);
        commit_branch!(TokenLendingLoan, TokenLendingLoanRow);
        commit_branch!(TokenLendingOrderBook, TokenLendingOrderBookRow);
        commit_branch!(TokenLendingPool, TokenLendingPoolRow);

        Ok(())
    }
}

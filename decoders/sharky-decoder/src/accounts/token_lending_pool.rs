//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TokenLendingPool {
    pub orderbook: Pubkey,
    pub lender: Pubkey,
    pub usdc_vault: Pubkey,
    pub collateral_vault: Pubkey,
    pub nonce: u64,
    pub total_usdc_liquidity: u64,
    pub available_usdc_liquidity: u64,
    pub collateral_required_amount: u64,
    pub total_usdc_interest: u64,
    pub total_loan_count: u64,
    pub active_loan_count: u64,
    pub total_defaulted_loan_count: u64,
    pub total_defaulted_usdc_amount: u64,
    pub total_defaulted_collateral_amount: u64,
    pub version: u8,
    pub bump: u8,
    pub padding: [u8; 6],
}

impl TokenLendingPool {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [33, 59, 13, 135, 153, 105, 127, 146] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

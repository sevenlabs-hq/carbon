//! This code was AUTOGENERATED using the Codama library.
pub mod token_ledger_row;

pub use self::token_ledger_row::*;
use super::JupiterSwapAccount;

pub struct JupiterSwapAccountsMigration;

impl carbon_core::clickhouse::Migration for JupiterSwapAccountsMigration {
    fn app(&self) -> &str {
        "jupiter-swap"
    }

    fn name(&self) -> &str {
        "jupiter_swap_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![Box::new(TokenLedgerRowMigrationOperation)]
    }
}

pub enum JupiterSwapAccountRow {
    TokenLedger(TokenLedgerRow),
}

pub struct JupiterSwapAccountMetadata(
    pub carbon_core::account::AccountMetadata,
    pub JupiterSwapAccount,
);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for JupiterSwapAccountMetadata {
    type Row = JupiterSwapAccountRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, account) = self;

        match account {
            JupiterSwapAccount::TokenLedger(account) => {
                rows.push(JupiterSwapAccountRow::TokenLedger(
                    TokenLedgerRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for JupiterSwapAccountRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(TokenLedger, TokenLedgerRow);
        Ok(())
    }
}

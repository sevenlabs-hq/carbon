//! This code was AUTOGENERATED using the Codama library.
pub mod add_liquidity2_row;
pub mod add_liquidity_by_strategy2_row;
pub mod add_liquidity_by_strategy_one_side_row;
pub mod add_liquidity_by_strategy_row;
pub mod add_liquidity_by_weight_row;
pub mod add_liquidity_one_side_precise2_row;
pub mod add_liquidity_one_side_precise_row;
pub mod add_liquidity_one_side_row;
pub mod add_liquidity_row;
pub mod claim_fee2_row;
pub mod claim_fee_row;
pub mod claim_reward2_row;
pub mod claim_reward_row;
pub mod close_claim_fee_operator_account_row;
pub mod close_operator_account_row;
pub mod close_position2_row;
pub mod close_position_if_empty_row;
pub mod close_position_row;
pub mod close_preset_parameter2_row;
pub mod close_preset_parameter_row;
pub mod close_token_badge_row;
pub mod cpi_event_row;
pub mod create_operator_account_row;
pub mod decrease_position_length_row;
pub mod for_idl_type_generation_do_not_call_row;
pub mod fund_reward_row;
pub mod go_to_a_bin_row;
pub mod increase_oracle_length_row;
pub mod increase_position_length2_row;
pub mod increase_position_length_row;
pub mod initialize_bin_array_bitmap_extension_row;
pub mod initialize_bin_array_row;
pub mod initialize_customizable_permissionless_lb_pair2_row;
pub mod initialize_customizable_permissionless_lb_pair_row;
pub mod initialize_lb_pair2_row;
pub mod initialize_lb_pair_row;
pub mod initialize_permission_lb_pair_row;
pub mod initialize_position2_row;
pub mod initialize_position_by_operator_row;
pub mod initialize_position_pda_row;
pub mod initialize_position_row;
pub mod initialize_preset_parameter_row;
pub mod initialize_reward_row;
pub mod initialize_token_badge_row;
pub mod migrate_position_row;
pub mod rebalance_liquidity_row;
pub mod remove_all_liquidity_row;
pub mod remove_liquidity2_row;
pub mod remove_liquidity_by_range2_row;
pub mod remove_liquidity_by_range_row;
pub mod remove_liquidity_row;
pub mod reset_bin_array_tombstone_fields_row;
pub mod reset_pool_tombstone_fields_row;
pub mod reset_position_tombstone_fields_row;
pub mod set_activation_point_row;
pub mod set_pair_status_permissionless_row;
pub mod set_pair_status_row;
pub mod set_pre_activation_duration_row;
pub mod set_pre_activation_swap_address_row;
pub mod swap2_row;
pub mod swap_exact_out2_row;
pub mod swap_exact_out_row;
pub mod swap_row;
pub mod swap_with_price_impact2_row;
pub mod swap_with_price_impact_row;
pub mod update_base_fee_parameters_row;
pub mod update_dynamic_fee_parameters_row;
pub mod update_fees_and_reward2_row;
pub mod update_fees_and_rewards_row;
pub mod update_position_operator_row;
pub mod update_reward_duration_row;
pub mod update_reward_funder_row;
pub mod withdraw_ineligible_reward_row;
pub mod withdraw_protocol_fee_row;
pub mod zap_protocol_fee_row;

pub use self::{
    add_liquidity2_row::*, add_liquidity_by_strategy2_row::*,
    add_liquidity_by_strategy_one_side_row::*, add_liquidity_by_strategy_row::*,
    add_liquidity_by_weight_row::*, add_liquidity_one_side_precise2_row::*,
    add_liquidity_one_side_precise_row::*, add_liquidity_one_side_row::*, add_liquidity_row::*,
    claim_fee2_row::*, claim_fee_row::*, claim_reward2_row::*, claim_reward_row::*,
    close_claim_fee_operator_account_row::*, close_operator_account_row::*, close_position2_row::*,
    close_position_if_empty_row::*, close_position_row::*, close_preset_parameter2_row::*,
    close_preset_parameter_row::*, close_token_badge_row::*, cpi_event_row::*,
    create_operator_account_row::*, decrease_position_length_row::*,
    for_idl_type_generation_do_not_call_row::*, fund_reward_row::*, go_to_a_bin_row::*,
    increase_oracle_length_row::*, increase_position_length2_row::*,
    increase_position_length_row::*, initialize_bin_array_bitmap_extension_row::*,
    initialize_bin_array_row::*, initialize_customizable_permissionless_lb_pair2_row::*,
    initialize_customizable_permissionless_lb_pair_row::*, initialize_lb_pair2_row::*,
    initialize_lb_pair_row::*, initialize_permission_lb_pair_row::*, initialize_position2_row::*,
    initialize_position_by_operator_row::*, initialize_position_pda_row::*,
    initialize_position_row::*, initialize_preset_parameter_row::*, initialize_reward_row::*,
    initialize_token_badge_row::*, migrate_position_row::*, rebalance_liquidity_row::*,
    remove_all_liquidity_row::*, remove_liquidity2_row::*, remove_liquidity_by_range2_row::*,
    remove_liquidity_by_range_row::*, remove_liquidity_row::*,
    reset_bin_array_tombstone_fields_row::*, reset_pool_tombstone_fields_row::*,
    reset_position_tombstone_fields_row::*, set_activation_point_row::*,
    set_pair_status_permissionless_row::*, set_pair_status_row::*,
    set_pre_activation_duration_row::*, set_pre_activation_swap_address_row::*, swap2_row::*,
    swap_exact_out2_row::*, swap_exact_out_row::*, swap_row::*, swap_with_price_impact2_row::*,
    swap_with_price_impact_row::*, update_base_fee_parameters_row::*,
    update_dynamic_fee_parameters_row::*, update_fees_and_reward2_row::*,
    update_fees_and_rewards_row::*, update_position_operator_row::*, update_reward_duration_row::*,
    update_reward_funder_row::*, withdraw_ineligible_reward_row::*, withdraw_protocol_fee_row::*,
    zap_protocol_fee_row::*,
};
use super::MeteoraDlmmInstruction;

pub struct MeteoraDlmmInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for MeteoraDlmmInstructionsMigration {
    fn app(&self) -> &str {
        "meteora-dlmm"
    }

    fn name(&self) -> &str {
        "meteora_dlmm_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AddLiquidityMigrationOperation),
            Box::new(AddLiquidity2MigrationOperation),
            Box::new(AddLiquidityByStrategyMigrationOperation),
            Box::new(AddLiquidityByStrategy2MigrationOperation),
            Box::new(AddLiquidityByStrategyOneSideMigrationOperation),
            Box::new(AddLiquidityByWeightMigrationOperation),
            Box::new(AddLiquidityOneSideMigrationOperation),
            Box::new(AddLiquidityOneSidePreciseMigrationOperation),
            Box::new(AddLiquidityOneSidePrecise2MigrationOperation),
            Box::new(ClaimFeeMigrationOperation),
            Box::new(ClaimFee2MigrationOperation),
            Box::new(ClaimRewardMigrationOperation),
            Box::new(ClaimReward2MigrationOperation),
            Box::new(CloseClaimFeeOperatorAccountMigrationOperation),
            Box::new(CloseOperatorAccountMigrationOperation),
            Box::new(ClosePositionMigrationOperation),
            Box::new(ClosePosition2MigrationOperation),
            Box::new(ClosePositionIfEmptyMigrationOperation),
            Box::new(ClosePresetParameterMigrationOperation),
            Box::new(ClosePresetParameter2MigrationOperation),
            Box::new(CloseTokenBadgeMigrationOperation),
            Box::new(CreateOperatorAccountMigrationOperation),
            Box::new(DecreasePositionLengthMigrationOperation),
            Box::new(ForIdlTypeGenerationDoNotCallMigrationOperation),
            Box::new(FundRewardMigrationOperation),
            Box::new(GoToABinMigrationOperation),
            Box::new(IncreaseOracleLengthMigrationOperation),
            Box::new(IncreasePositionLengthMigrationOperation),
            Box::new(IncreasePositionLength2MigrationOperation),
            Box::new(InitializeBinArrayMigrationOperation),
            Box::new(InitializeBinArrayBitmapExtensionMigrationOperation),
            Box::new(InitializeCustomizablePermissionlessLbPairMigrationOperation),
            Box::new(InitializeCustomizablePermissionlessLbPair2MigrationOperation),
            Box::new(InitializeLbPairMigrationOperation),
            Box::new(InitializeLbPair2MigrationOperation),
            Box::new(InitializePermissionLbPairMigrationOperation),
            Box::new(InitializePositionMigrationOperation),
            Box::new(InitializePosition2MigrationOperation),
            Box::new(InitializePositionByOperatorMigrationOperation),
            Box::new(InitializePositionPdaMigrationOperation),
            Box::new(InitializePresetParameterMigrationOperation),
            Box::new(InitializeRewardMigrationOperation),
            Box::new(InitializeTokenBadgeMigrationOperation),
            Box::new(MigratePositionMigrationOperation),
            Box::new(RebalanceLiquidityMigrationOperation),
            Box::new(RemoveAllLiquidityMigrationOperation),
            Box::new(RemoveLiquidityMigrationOperation),
            Box::new(RemoveLiquidity2MigrationOperation),
            Box::new(RemoveLiquidityByRangeMigrationOperation),
            Box::new(RemoveLiquidityByRange2MigrationOperation),
            Box::new(ResetBinArrayTombstoneFieldsMigrationOperation),
            Box::new(ResetPoolTombstoneFieldsMigrationOperation),
            Box::new(ResetPositionTombstoneFieldsMigrationOperation),
            Box::new(SetActivationPointMigrationOperation),
            Box::new(SetPairStatusMigrationOperation),
            Box::new(SetPairStatusPermissionlessMigrationOperation),
            Box::new(SetPreActivationDurationMigrationOperation),
            Box::new(SetPreActivationSwapAddressMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(Swap2MigrationOperation),
            Box::new(SwapExactOutMigrationOperation),
            Box::new(SwapExactOut2MigrationOperation),
            Box::new(SwapWithPriceImpactMigrationOperation),
            Box::new(SwapWithPriceImpact2MigrationOperation),
            Box::new(UpdateBaseFeeParametersMigrationOperation),
            Box::new(UpdateDynamicFeeParametersMigrationOperation),
            Box::new(UpdateFeesAndReward2MigrationOperation),
            Box::new(UpdateFeesAndRewardsMigrationOperation),
            Box::new(UpdatePositionOperatorMigrationOperation),
            Box::new(UpdateRewardDurationMigrationOperation),
            Box::new(UpdateRewardFunderMigrationOperation),
            Box::new(WithdrawIneligibleRewardMigrationOperation),
            Box::new(WithdrawProtocolFeeMigrationOperation),
            Box::new(ZapProtocolFeeMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct MeteoraDlmmInstructionWithMetadata(
    pub MeteoraDlmmInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        MeteoraDlmmInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for MeteoraDlmmInstructionWithMetadata
{
    fn from(
        value: (
            MeteoraDlmmInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        MeteoraDlmmInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MeteoraDlmmInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraDlmmInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            MeteoraDlmmInstruction::AddLiquidity { data, .. } => {
                let row = add_liquidity_row::AddLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidity2 { data, .. } => {
                let row = add_liquidity2_row::AddLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategy { data, .. } => {
                let row = add_liquidity_by_strategy_row::AddLiquidityByStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategy2 { data, .. } => {
                let row = add_liquidity_by_strategy2_row::AddLiquidityByStrategy2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategyOneSide { data, .. } => {
                let row = add_liquidity_by_strategy_one_side_row::AddLiquidityByStrategyOneSideRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByWeight { data, .. } => {
                let row = add_liquidity_by_weight_row::AddLiquidityByWeightRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSide { data, .. } => {
                let row = add_liquidity_one_side_row::AddLiquidityOneSideRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSidePrecise { data, .. } => {
                let row =
                    add_liquidity_one_side_precise_row::AddLiquidityOneSidePreciseRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSidePrecise2 { data, .. } => {
                let row =
                    add_liquidity_one_side_precise2_row::AddLiquidityOneSidePrecise2Row::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimFee { data, .. } => {
                let row = claim_fee_row::ClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimFee2 { data, .. } => {
                let row = claim_fee2_row::ClaimFee2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimReward { data, .. } => {
                let row = claim_reward_row::ClaimRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimReward2 { data, .. } => {
                let row = claim_reward2_row::ClaimReward2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseClaimFeeOperatorAccount { data, .. } => {
                let row = close_claim_fee_operator_account_row::CloseClaimFeeOperatorAccountRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseOperatorAccount { data, .. } => {
                let row = close_operator_account_row::CloseOperatorAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePosition { data, .. } => {
                let row = close_position_row::ClosePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePosition2 { data, .. } => {
                let row = close_position2_row::ClosePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePositionIfEmpty { data, .. } => {
                let row = close_position_if_empty_row::ClosePositionIfEmptyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePresetParameter { data, .. } => {
                let row = close_preset_parameter_row::ClosePresetParameterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePresetParameter2 { data, .. } => {
                let row = close_preset_parameter2_row::ClosePresetParameter2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseTokenBadge { data, .. } => {
                let row = close_token_badge_row::CloseTokenBadgeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CreateOperatorAccount { data, .. } => {
                let row = create_operator_account_row::CreateOperatorAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::DecreasePositionLength { data, .. } => {
                let row = decrease_position_length_row::DecreasePositionLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ForIdlTypeGenerationDoNotCall { data, .. } => {
                let row = for_idl_type_generation_do_not_call_row::ForIdlTypeGenerationDoNotCallRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::FundReward { data, .. } => {
                let row = fund_reward_row::FundRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::GoToABin { data, .. } => {
                let row = go_to_a_bin_row::GoToABinRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreaseOracleLength { data, .. } => {
                let row = increase_oracle_length_row::IncreaseOracleLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreasePositionLength { data, .. } => {
                let row = increase_position_length_row::IncreasePositionLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreasePositionLength2 { data, .. } => {
                let row = increase_position_length2_row::IncreasePositionLength2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeBinArray { data, .. } => {
                let row = initialize_bin_array_row::InitializeBinArrayRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeBinArrayBitmapExtension { data, .. } => {
                let row = initialize_bin_array_bitmap_extension_row::InitializeBinArrayBitmapExtensionRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeCustomizablePermissionlessLbPair { data, .. } => {
                let row = initialize_customizable_permissionless_lb_pair_row::InitializeCustomizablePermissionlessLbPairRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeCustomizablePermissionlessLbPair2 {
                data, ..
            } => {
                let row = initialize_customizable_permissionless_lb_pair2_row::InitializeCustomizablePermissionlessLbPair2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeLbPair { data, .. } => {
                let row = initialize_lb_pair_row::InitializeLbPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeLbPair2 { data, .. } => {
                let row = initialize_lb_pair2_row::InitializeLbPair2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePermissionLbPair { data, .. } => {
                let row =
                    initialize_permission_lb_pair_row::InitializePermissionLbPairRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePosition { data, .. } => {
                let row = initialize_position_row::InitializePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePosition2 { data, .. } => {
                let row = initialize_position2_row::InitializePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePositionByOperator { data, .. } => {
                let row = initialize_position_by_operator_row::InitializePositionByOperatorRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePositionPda { data, .. } => {
                let row = initialize_position_pda_row::InitializePositionPdaRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePresetParameter { data, .. } => {
                let row = initialize_preset_parameter_row::InitializePresetParameterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeReward { data, .. } => {
                let row = initialize_reward_row::InitializeRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeTokenBadge { data, .. } => {
                let row = initialize_token_badge_row::InitializeTokenBadgeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::MigratePosition { data, .. } => {
                let row = migrate_position_row::MigratePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RebalanceLiquidity { data, .. } => {
                let row = rebalance_liquidity_row::RebalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveAllLiquidity { data, .. } => {
                let row = remove_all_liquidity_row::RemoveAllLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidity { data, .. } => {
                let row = remove_liquidity_row::RemoveLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidity2 { data, .. } => {
                let row = remove_liquidity2_row::RemoveLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidityByRange { data, .. } => {
                let row = remove_liquidity_by_range_row::RemoveLiquidityByRangeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidityByRange2 { data, .. } => {
                let row = remove_liquidity_by_range2_row::RemoveLiquidityByRange2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetBinArrayTombstoneFields { data, .. } => {
                let row = reset_bin_array_tombstone_fields_row::ResetBinArrayTombstoneFieldsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetPoolTombstoneFields { data, .. } => {
                let row = reset_pool_tombstone_fields_row::ResetPoolTombstoneFieldsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetPositionTombstoneFields { data, .. } => {
                let row = reset_position_tombstone_fields_row::ResetPositionTombstoneFieldsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetActivationPoint { data, .. } => {
                let row = set_activation_point_row::SetActivationPointRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPairStatus { data, .. } => {
                let row = set_pair_status_row::SetPairStatusRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPairStatusPermissionless { data, .. } => {
                let row =
                    set_pair_status_permissionless_row::SetPairStatusPermissionlessRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPreActivationDuration { data, .. } => {
                let row = set_pre_activation_duration_row::SetPreActivationDurationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPreActivationSwapAddress { data, .. } => {
                let row =
                    set_pre_activation_swap_address_row::SetPreActivationSwapAddressRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::Swap2 { data, .. } => {
                let row = swap2_row::Swap2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapExactOut { data, .. } => {
                let row = swap_exact_out_row::SwapExactOutRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapExactOut2 { data, .. } => {
                let row = swap_exact_out2_row::SwapExactOut2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapWithPriceImpact { data, .. } => {
                let row = swap_with_price_impact_row::SwapWithPriceImpactRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapWithPriceImpact2 { data, .. } => {
                let row = swap_with_price_impact2_row::SwapWithPriceImpact2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateBaseFeeParameters { data, .. } => {
                let row = update_base_fee_parameters_row::UpdateBaseFeeParametersRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateDynamicFeeParameters { data, .. } => {
                let row =
                    update_dynamic_fee_parameters_row::UpdateDynamicFeeParametersRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateFeesAndReward2 { data, .. } => {
                let row = update_fees_and_reward2_row::UpdateFeesAndReward2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateFeesAndRewards { data, .. } => {
                let row = update_fees_and_rewards_row::UpdateFeesAndRewardsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdatePositionOperator { data, .. } => {
                let row = update_position_operator_row::UpdatePositionOperatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateRewardDuration { data, .. } => {
                let row = update_reward_duration_row::UpdateRewardDurationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateRewardFunder { data, .. } => {
                let row = update_reward_funder_row::UpdateRewardFunderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::WithdrawIneligibleReward { data, .. } => {
                let row = withdraw_ineligible_reward_row::WithdrawIneligibleRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::WithdrawProtocolFee { data, .. } => {
                let row = withdraw_protocol_fee_row::WithdrawProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ZapProtocolFee { data, .. } => {
                let row = zap_protocol_fee_row::ZapProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MeteoraDlmmInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraDlmmInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            MeteoraDlmmInstruction::AddLiquidity { data, .. } => {
                let row = add_liquidity_row::AddLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidity2 { data, .. } => {
                let row = add_liquidity2_row::AddLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategy { data, .. } => {
                let row = add_liquidity_by_strategy_row::AddLiquidityByStrategyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategy2 { data, .. } => {
                let row = add_liquidity_by_strategy2_row::AddLiquidityByStrategy2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByStrategyOneSide { data, .. } => {
                let row = add_liquidity_by_strategy_one_side_row::AddLiquidityByStrategyOneSideRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityByWeight { data, .. } => {
                let row = add_liquidity_by_weight_row::AddLiquidityByWeightRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSide { data, .. } => {
                let row = add_liquidity_one_side_row::AddLiquidityOneSideRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSidePrecise { data, .. } => {
                let row =
                    add_liquidity_one_side_precise_row::AddLiquidityOneSidePreciseRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::AddLiquidityOneSidePrecise2 { data, .. } => {
                let row =
                    add_liquidity_one_side_precise2_row::AddLiquidityOneSidePrecise2Row::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimFee { data, .. } => {
                let row = claim_fee_row::ClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimFee2 { data, .. } => {
                let row = claim_fee2_row::ClaimFee2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimReward { data, .. } => {
                let row = claim_reward_row::ClaimRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClaimReward2 { data, .. } => {
                let row = claim_reward2_row::ClaimReward2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseClaimFeeOperatorAccount { data, .. } => {
                let row = close_claim_fee_operator_account_row::CloseClaimFeeOperatorAccountRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseOperatorAccount { data, .. } => {
                let row = close_operator_account_row::CloseOperatorAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePosition { data, .. } => {
                let row = close_position_row::ClosePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePosition2 { data, .. } => {
                let row = close_position2_row::ClosePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePositionIfEmpty { data, .. } => {
                let row = close_position_if_empty_row::ClosePositionIfEmptyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePresetParameter { data, .. } => {
                let row = close_preset_parameter_row::ClosePresetParameterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ClosePresetParameter2 { data, .. } => {
                let row = close_preset_parameter2_row::ClosePresetParameter2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CloseTokenBadge { data, .. } => {
                let row = close_token_badge_row::CloseTokenBadgeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CreateOperatorAccount { data, .. } => {
                let row = create_operator_account_row::CreateOperatorAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::DecreasePositionLength { data, .. } => {
                let row = decrease_position_length_row::DecreasePositionLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ForIdlTypeGenerationDoNotCall { data, .. } => {
                let row = for_idl_type_generation_do_not_call_row::ForIdlTypeGenerationDoNotCallRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::FundReward { data, .. } => {
                let row = fund_reward_row::FundRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::GoToABin { data, .. } => {
                let row = go_to_a_bin_row::GoToABinRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreaseOracleLength { data, .. } => {
                let row = increase_oracle_length_row::IncreaseOracleLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreasePositionLength { data, .. } => {
                let row = increase_position_length_row::IncreasePositionLengthRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::IncreasePositionLength2 { data, .. } => {
                let row = increase_position_length2_row::IncreasePositionLength2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeBinArray { data, .. } => {
                let row = initialize_bin_array_row::InitializeBinArrayRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeBinArrayBitmapExtension { data, .. } => {
                let row = initialize_bin_array_bitmap_extension_row::InitializeBinArrayBitmapExtensionRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeCustomizablePermissionlessLbPair { data, .. } => {
                let row = initialize_customizable_permissionless_lb_pair_row::InitializeCustomizablePermissionlessLbPairRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeCustomizablePermissionlessLbPair2 {
                data, ..
            } => {
                let row = initialize_customizable_permissionless_lb_pair2_row::InitializeCustomizablePermissionlessLbPair2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeLbPair { data, .. } => {
                let row = initialize_lb_pair_row::InitializeLbPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeLbPair2 { data, .. } => {
                let row = initialize_lb_pair2_row::InitializeLbPair2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePermissionLbPair { data, .. } => {
                let row =
                    initialize_permission_lb_pair_row::InitializePermissionLbPairRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePosition { data, .. } => {
                let row = initialize_position_row::InitializePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePosition2 { data, .. } => {
                let row = initialize_position2_row::InitializePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePositionByOperator { data, .. } => {
                let row = initialize_position_by_operator_row::InitializePositionByOperatorRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePositionPda { data, .. } => {
                let row = initialize_position_pda_row::InitializePositionPdaRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializePresetParameter { data, .. } => {
                let row = initialize_preset_parameter_row::InitializePresetParameterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeReward { data, .. } => {
                let row = initialize_reward_row::InitializeRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::InitializeTokenBadge { data, .. } => {
                let row = initialize_token_badge_row::InitializeTokenBadgeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::MigratePosition { data, .. } => {
                let row = migrate_position_row::MigratePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RebalanceLiquidity { data, .. } => {
                let row = rebalance_liquidity_row::RebalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveAllLiquidity { data, .. } => {
                let row = remove_all_liquidity_row::RemoveAllLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidity { data, .. } => {
                let row = remove_liquidity_row::RemoveLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidity2 { data, .. } => {
                let row = remove_liquidity2_row::RemoveLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidityByRange { data, .. } => {
                let row = remove_liquidity_by_range_row::RemoveLiquidityByRangeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::RemoveLiquidityByRange2 { data, .. } => {
                let row = remove_liquidity_by_range2_row::RemoveLiquidityByRange2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetBinArrayTombstoneFields { data, .. } => {
                let row = reset_bin_array_tombstone_fields_row::ResetBinArrayTombstoneFieldsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetPoolTombstoneFields { data, .. } => {
                let row = reset_pool_tombstone_fields_row::ResetPoolTombstoneFieldsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ResetPositionTombstoneFields { data, .. } => {
                let row = reset_position_tombstone_fields_row::ResetPositionTombstoneFieldsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetActivationPoint { data, .. } => {
                let row = set_activation_point_row::SetActivationPointRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPairStatus { data, .. } => {
                let row = set_pair_status_row::SetPairStatusRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPairStatusPermissionless { data, .. } => {
                let row =
                    set_pair_status_permissionless_row::SetPairStatusPermissionlessRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPreActivationDuration { data, .. } => {
                let row = set_pre_activation_duration_row::SetPreActivationDurationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SetPreActivationSwapAddress { data, .. } => {
                let row =
                    set_pre_activation_swap_address_row::SetPreActivationSwapAddressRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::Swap2 { data, .. } => {
                let row = swap2_row::Swap2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapExactOut { data, .. } => {
                let row = swap_exact_out_row::SwapExactOutRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapExactOut2 { data, .. } => {
                let row = swap_exact_out2_row::SwapExactOut2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapWithPriceImpact { data, .. } => {
                let row = swap_with_price_impact_row::SwapWithPriceImpactRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::SwapWithPriceImpact2 { data, .. } => {
                let row = swap_with_price_impact2_row::SwapWithPriceImpact2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateBaseFeeParameters { data, .. } => {
                let row = update_base_fee_parameters_row::UpdateBaseFeeParametersRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateDynamicFeeParameters { data, .. } => {
                let row =
                    update_dynamic_fee_parameters_row::UpdateDynamicFeeParametersRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateFeesAndReward2 { data, .. } => {
                let row = update_fees_and_reward2_row::UpdateFeesAndReward2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateFeesAndRewards { data, .. } => {
                let row = update_fees_and_rewards_row::UpdateFeesAndRewardsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdatePositionOperator { data, .. } => {
                let row = update_position_operator_row::UpdatePositionOperatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateRewardDuration { data, .. } => {
                let row = update_reward_duration_row::UpdateRewardDurationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::UpdateRewardFunder { data, .. } => {
                let row = update_reward_funder_row::UpdateRewardFunderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::WithdrawIneligibleReward { data, .. } => {
                let row = withdraw_ineligible_reward_row::WithdrawIneligibleRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::WithdrawProtocolFee { data, .. } => {
                let row = withdraw_protocol_fee_row::WithdrawProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::ZapProtocolFee { data, .. } => {
                let row = zap_protocol_fee_row::ZapProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

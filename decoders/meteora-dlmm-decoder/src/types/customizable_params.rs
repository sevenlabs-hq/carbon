//! This code was AUTOGENERATED using the Codama library.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CustomizableParams {
    /// Pool price
    pub active_id: i32,
    /// Bin step
    pub bin_step: u16,
    /// Base factor
    pub base_factor: u16,
    /// Activation type. 0 = Slot, 1 = Time. Check ActivationType enum
    pub activation_type: u8,
    /// Whether the pool has an alpha vault
    pub has_alpha_vault: bool,
    /// Decide when does the pool start trade. None = Now
    pub activation_point: Option<u64>,
    /// Pool creator have permission to enable/disable pool with restricted
    /// program validation. Only applicable for customizable permissionless
    /// pool.
    pub creator_pool_on_off_control: bool,
    /// Base fee power factor
    pub base_fee_power_factor: u8,
    /// function type
    pub function_type: u8,
    /// Padding, for future use
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding: [u8; 61],
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{AddLiquidityParamsGraphQL, RemoveLiquidityParamsGraphQL},
    carbon_core::graphql::primitives::{U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "RebalanceLiquidityParams")]
pub struct RebalanceLiquidityParamsGraphQL {
    pub active_id: i32,
    pub max_active_bin_slippage: i32,
    pub should_claim_fee: bool,
    pub should_claim_reward: bool,
    pub min_withdraw_x_amount: U64,
    pub max_deposit_x_amount: U64,
    pub min_withdraw_y_amount: U64,
    pub max_deposit_y_amount: U64,
    pub shrink_mode: U8,
    pub padding: Vec<U8>,
    pub removes: Vec<RemoveLiquidityParamsGraphQL>,
    pub adds: Vec<AddLiquidityParamsGraphQL>,
}

impl From<crate::types::RebalanceLiquidityParams> for RebalanceLiquidityParamsGraphQL {
    fn from(original: crate::types::RebalanceLiquidityParams) -> Self {
        Self {
            active_id: original.active_id,
            max_active_bin_slippage: original.max_active_bin_slippage as i32,
            should_claim_fee: original.should_claim_fee,
            should_claim_reward: original.should_claim_reward,
            min_withdraw_x_amount: carbon_core::graphql::primitives::U64(
                original.min_withdraw_x_amount,
            ),
            max_deposit_x_amount: carbon_core::graphql::primitives::U64(
                original.max_deposit_x_amount,
            ),
            min_withdraw_y_amount: carbon_core::graphql::primitives::U64(
                original.min_withdraw_y_amount,
            ),
            max_deposit_y_amount: carbon_core::graphql::primitives::U64(
                original.max_deposit_y_amount,
            ),
            shrink_mode: carbon_core::graphql::primitives::U8(original.shrink_mode),
            padding: original
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            removes: original
                .removes
                .into_iter()
                .map(|item| item.into())
                .collect(),
            adds: original.adds.into_iter().map(|item| item.into()).collect(),
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{BinLimitOrderAmountGraphQL, RelativeBinGraphQL},
    carbon_core::graphql::primitives::U8,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "PlaceLimitOrderParams")]
pub struct PlaceLimitOrderParamsGraphQL {
    pub is_ask_side: bool,
    pub padding: Vec<U8>,
    pub relative_bin: Option<RelativeBinGraphQL>,
    pub bins: Vec<BinLimitOrderAmountGraphQL>,
}

impl From<crate::types::PlaceLimitOrderParams> for PlaceLimitOrderParamsGraphQL {
    fn from(original: crate::types::PlaceLimitOrderParams) -> Self {
        Self {
            is_ask_side: original.is_ask_side,
            padding: original
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            relative_bin: original.relative_bin.map(|v| v.into()),
            bins: original.bins.into_iter().map(|item| item.into()).collect(),
        }
    }
}

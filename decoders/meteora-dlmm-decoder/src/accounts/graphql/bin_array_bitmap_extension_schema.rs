//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U64},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "BinArrayBitmapExtension")]
pub struct BinArrayBitmapExtensionGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub lb_pair: Pubkey,
    pub positive_bin_array_bitmap: Vec<Vec<U64>>,
    pub negative_bin_array_bitmap: Vec<Vec<U64>>,
}

impl TryFrom<crate::accounts::postgres::BinArrayBitmapExtensionRow>
    for BinArrayBitmapExtensionGraphQL
{
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::accounts::postgres::BinArrayBitmapExtensionRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            lb_pair: carbon_core::graphql::primitives::Pubkey(row.lb_pair.0),
            positive_bin_array_bitmap: row
                .positive_bin_array_bitmap
                .0
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(|item| carbon_core::graphql::primitives::U64(*item))
                        .collect()
                })
                .collect(),
            negative_bin_array_bitmap: row
                .negative_bin_array_bitmap
                .0
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(|item| carbon_core::graphql::primitives::U64(*item))
                        .collect()
                })
                .collect(),
        })
    }
}

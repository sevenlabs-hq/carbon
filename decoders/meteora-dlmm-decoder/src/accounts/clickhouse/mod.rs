//! This code was AUTOGENERATED using the Codama library.
pub mod bin_array_bitmap_extension_row;
pub mod bin_array_row;
pub mod claim_fee_operator_row;
pub mod dummy_zc_account_row;
pub mod lb_pair_row;
pub mod limit_order_row;
pub mod operator_row;
pub mod oracle_row;
pub mod position_v2_row;
pub mod preset_parameter2_row;
pub mod preset_parameter_row;
pub mod token_badge_row;

pub use self::{
    bin_array_bitmap_extension_row::*, bin_array_row::*, claim_fee_operator_row::*,
    dummy_zc_account_row::*, lb_pair_row::*, limit_order_row::*, operator_row::*, oracle_row::*,
    position_v2_row::*, preset_parameter2_row::*, preset_parameter_row::*, token_badge_row::*,
};
use super::MeteoraDlmmAccount;

pub struct MeteoraDlmmAccountsMigration;

impl carbon_core::clickhouse::Migration for MeteoraDlmmAccountsMigration {
    fn app(&self) -> &str {
        "meteora-dlmm"
    }

    fn name(&self) -> &str {
        "meteora_dlmm_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BinArrayRowMigrationOperation),
            Box::new(BinArrayBitmapExtensionRowMigrationOperation),
            Box::new(ClaimFeeOperatorRowMigrationOperation),
            Box::new(DummyZcAccountRowMigrationOperation),
            Box::new(LbPairRowMigrationOperation),
            Box::new(LimitOrderRowMigrationOperation),
            Box::new(OperatorRowMigrationOperation),
            Box::new(OracleRowMigrationOperation),
            Box::new(PositionV2RowMigrationOperation),
            Box::new(PresetParameterRowMigrationOperation),
            Box::new(PresetParameter2RowMigrationOperation),
            Box::new(TokenBadgeRowMigrationOperation),
        ]
    }
}

pub enum MeteoraDlmmAccountRow {
    BinArray(BinArrayRow),
    BinArrayBitmapExtension(BinArrayBitmapExtensionRow),
    ClaimFeeOperator(ClaimFeeOperatorRow),
    DummyZcAccount(DummyZcAccountRow),
    LbPair(LbPairRow),
    LimitOrder(LimitOrderRow),
    Operator(OperatorRow),
    Oracle(OracleRow),
    PositionV2(PositionV2Row),
    PresetParameter(PresetParameterRow),
    PresetParameter2(PresetParameter2Row),
    TokenBadge(TokenBadgeRow),
}

pub struct MeteoraDlmmAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a MeteoraDlmmAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for MeteoraDlmmAccountMetadata<'a> {
    type Row = MeteoraDlmmAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let MeteoraDlmmAccount::$variant(account) = account {
                    rows.push(MeteoraDlmmAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let MeteoraDlmmAccount::$variant(account) = account {
                    rows.push(MeteoraDlmmAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(BinArray, BinArrayRow, boxed);
        insert_branch!(BinArrayBitmapExtension, BinArrayBitmapExtensionRow, boxed);
        insert_branch!(ClaimFeeOperator, ClaimFeeOperatorRow, boxed);
        insert_branch!(DummyZcAccount, DummyZcAccountRow, boxed);
        insert_branch!(LbPair, LbPairRow, boxed);
        insert_branch!(LimitOrder, LimitOrderRow, boxed);
        insert_branch!(Operator, OperatorRow, boxed);
        insert_branch!(Oracle, OracleRow, boxed);
        insert_branch!(PositionV2, PositionV2Row, boxed);
        insert_branch!(PresetParameter, PresetParameterRow, boxed);
        insert_branch!(PresetParameter2, PresetParameter2Row, boxed);
        insert_branch!(TokenBadge, TokenBadgeRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MeteoraDlmmAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(BinArray, BinArrayRow);
        commit_branch!(BinArrayBitmapExtension, BinArrayBitmapExtensionRow);
        commit_branch!(ClaimFeeOperator, ClaimFeeOperatorRow);
        commit_branch!(DummyZcAccount, DummyZcAccountRow);
        commit_branch!(LbPair, LbPairRow);
        commit_branch!(LimitOrder, LimitOrderRow);
        commit_branch!(Operator, OperatorRow);
        commit_branch!(Oracle, OracleRow);
        commit_branch!(PositionV2, PositionV2Row);
        commit_branch!(PresetParameter, PresetParameterRow);
        commit_branch!(PresetParameter2, PresetParameter2Row);
        commit_branch!(TokenBadge, TokenBadgeRow);

        Ok(())
    }
}

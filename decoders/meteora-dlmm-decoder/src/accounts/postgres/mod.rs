//! This code was AUTOGENERATED using the Codama library.
pub mod bin_array_bitmap_extension_row;
pub mod bin_array_row;
pub mod claim_fee_operator_row;
pub mod dummy_zc_account_row;
pub mod lb_pair_row;
pub mod operator_row;
pub mod oracle_row;
pub mod position_row;
pub mod position_v2_row;
pub mod preset_parameter2_row;
pub mod preset_parameter_row;
pub mod token_badge_row;

pub use self::{
    bin_array_bitmap_extension_row::*, bin_array_row::*, claim_fee_operator_row::*,
    dummy_zc_account_row::*, lb_pair_row::*, operator_row::*, oracle_row::*, position_row::*,
    position_v2_row::*, preset_parameter2_row::*, preset_parameter_row::*, token_badge_row::*,
};
use super::MeteoraDlmmAccount;

pub struct MeteoraDlmmAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for MeteoraDlmmAccountsMigration {
    fn app(&self) -> &str {
        "meteora-dlmm"
    }

    fn name(&self) -> &str {
        "meteora_dlmm_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(BinArrayMigrationOperation),
            Box::new(BinArrayBitmapExtensionMigrationOperation),
            Box::new(ClaimFeeOperatorMigrationOperation),
            Box::new(DummyZcAccountMigrationOperation),
            Box::new(LbPairMigrationOperation),
            Box::new(OperatorMigrationOperation),
            Box::new(OracleMigrationOperation),
            Box::new(PositionMigrationOperation),
            Box::new(PositionV2MigrationOperation),
            Box::new(PresetParameterMigrationOperation),
            Box::new(PresetParameter2MigrationOperation),
            Box::new(TokenBadgeMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct MeteoraDlmmAccountWithMetadata(
    pub MeteoraDlmmAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(MeteoraDlmmAccount, carbon_core::account::AccountMetadata)>
    for MeteoraDlmmAccountWithMetadata
{
    fn from(value: (MeteoraDlmmAccount, carbon_core::account::AccountMetadata)) -> Self {
        MeteoraDlmmAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MeteoraDlmmAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraDlmmAccountWithMetadata(account, metadata) = self;

        match account {
            MeteoraDlmmAccount::BinArray(account) => {
                let row =
                    bin_array_row::BinArrayRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::BinArrayBitmapExtension(account) => {
                let row = bin_array_bitmap_extension_row::BinArrayBitmapExtensionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::ClaimFeeOperator(account) => {
                let row = claim_fee_operator_row::ClaimFeeOperatorRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::DummyZcAccount(account) => {
                let row = dummy_zc_account_row::DummyZcAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::LbPair(account) => {
                let row = lb_pair_row::LbPairRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Operator(account) => {
                let row = operator_row::OperatorRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Oracle(account) => {
                let row = oracle_row::OracleRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Position(account) => {
                let row = position_row::PositionRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PositionV2(account) => {
                let row =
                    position_v2_row::PositionV2Row::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PresetParameter(account) => {
                let row = preset_parameter_row::PresetParameterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PresetParameter2(account) => {
                let row = preset_parameter2_row::PresetParameter2Row::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::TokenBadge(account) => {
                let row =
                    token_badge_row::TokenBadgeRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MeteoraDlmmAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraDlmmAccountWithMetadata(account, metadata) = self;
        match account {
            MeteoraDlmmAccount::BinArray(account) => {
                let row =
                    bin_array_row::BinArrayRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::BinArrayBitmapExtension(account) => {
                let row = bin_array_bitmap_extension_row::BinArrayBitmapExtensionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::ClaimFeeOperator(account) => {
                let row = claim_fee_operator_row::ClaimFeeOperatorRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::DummyZcAccount(account) => {
                let row = dummy_zc_account_row::DummyZcAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::LbPair(account) => {
                let row = lb_pair_row::LbPairRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Operator(account) => {
                let row = operator_row::OperatorRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Oracle(account) => {
                let row = oracle_row::OracleRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::Position(account) => {
                let row = position_row::PositionRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PositionV2(account) => {
                let row =
                    position_v2_row::PositionV2Row::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PresetParameter(account) => {
                let row = preset_parameter_row::PresetParameterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::PresetParameter2(account) => {
                let row = preset_parameter2_row::PresetParameter2Row::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraDlmmAccount::TokenBadge(account) => {
                let row =
                    token_badge_row::TokenBadgeRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{FeeValueChangeGraphQL, U64ValueChangeGraphQL},
    carbon_core::graphql::primitives::Pubkey,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ConfigLpEvent")]
pub struct ConfigLpEventGraphQL {
    pub state: Pubkey,
    pub min_fee_change: Option<FeeValueChangeGraphQL>,
    pub max_fee_change: Option<FeeValueChangeGraphQL>,
    pub liquidity_target_change: Option<U64ValueChangeGraphQL>,
    pub treasury_cut_change: Option<FeeValueChangeGraphQL>,
}

impl From<crate::types::ConfigLpEvent> for ConfigLpEventGraphQL {
    fn from(original: crate::types::ConfigLpEvent) -> Self {
        Self {
            state: carbon_core::graphql::primitives::Pubkey(original.state),
            min_fee_change: original.min_fee_change.map(|v| v.into()),
            max_fee_change: original.max_fee_change.map(|v| v.into()),
            liquidity_target_change: original.liquidity_target_change.map(|v| v.into()),
            treasury_cut_change: original.treasury_cut_change.map(|v| v.into()),
        }
    }
}

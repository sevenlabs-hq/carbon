//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        BoolValueChangeGraphQL, FeeCentsValueChangeGraphQL, FeeValueChangeGraphQL,
        U64ValueChangeGraphQL,
    },
    carbon_core::graphql::primitives::Pubkey,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ConfigMarinadeEvent")]
pub struct ConfigMarinadeEventGraphQL {
    pub state: Pubkey,
    pub rewards_fee_change: Option<FeeValueChangeGraphQL>,
    pub slots_for_stake_delta_change: Option<U64ValueChangeGraphQL>,
    pub min_stake_change: Option<U64ValueChangeGraphQL>,
    pub min_deposit_change: Option<U64ValueChangeGraphQL>,
    pub min_withdraw_change: Option<U64ValueChangeGraphQL>,
    pub staking_sol_cap_change: Option<U64ValueChangeGraphQL>,
    pub liquidity_sol_cap_change: Option<U64ValueChangeGraphQL>,
    pub withdraw_stake_account_enabled_change: Option<BoolValueChangeGraphQL>,
    pub delayed_unstake_fee_change: Option<FeeCentsValueChangeGraphQL>,
    pub withdraw_stake_account_fee_change: Option<FeeCentsValueChangeGraphQL>,
    pub max_stake_moved_per_epoch_change: Option<FeeValueChangeGraphQL>,
}

impl From<crate::types::ConfigMarinadeEvent> for ConfigMarinadeEventGraphQL {
    fn from(original: crate::types::ConfigMarinadeEvent) -> Self {
        Self {
            state: carbon_core::graphql::primitives::Pubkey(original.state),
            rewards_fee_change: original.rewards_fee_change.map(|v| v.into()),
            slots_for_stake_delta_change: original.slots_for_stake_delta_change.map(|v| v.into()),
            min_stake_change: original.min_stake_change.map(|v| v.into()),
            min_deposit_change: original.min_deposit_change.map(|v| v.into()),
            min_withdraw_change: original.min_withdraw_change.map(|v| v.into()),
            staking_sol_cap_change: original.staking_sol_cap_change.map(|v| v.into()),
            liquidity_sol_cap_change: original.liquidity_sol_cap_change.map(|v| v.into()),
            withdraw_stake_account_enabled_change: original
                .withdraw_stake_account_enabled_change
                .map(|v| v.into()),
            delayed_unstake_fee_change: original.delayed_unstake_fee_change.map(|v| v.into()),
            withdraw_stake_account_fee_change: original
                .withdraw_stake_account_fee_change
                .map(|v| v.into()),
            max_stake_moved_per_epoch_change: original
                .max_stake_moved_per_epoch_change
                .map(|v| v.into()),
        }
    }
}

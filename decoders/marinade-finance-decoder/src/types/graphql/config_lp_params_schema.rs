//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::FeeGraphQL, carbon_core::graphql::primitives::U64,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ConfigLpParams")]
pub struct ConfigLpParamsGraphQL {
    pub min_fee: Option<FeeGraphQL>,
    pub max_fee: Option<FeeGraphQL>,
    pub liquidity_target: Option<U64>,
    pub treasury_cut: Option<FeeGraphQL>,
}

impl From<crate::types::ConfigLpParams> for ConfigLpParamsGraphQL {
    fn from(original: crate::types::ConfigLpParams) -> Self {
        Self {
            min_fee: original.min_fee.map(|v| v.into()),
            max_fee: original.max_fee.map(|v| v.into()),
            liquidity_target: original
                .liquidity_target
                .map(carbon_core::graphql::primitives::U64),
            treasury_cut: original.treasury_cut.map(|v| v.into()),
        }
    }
}

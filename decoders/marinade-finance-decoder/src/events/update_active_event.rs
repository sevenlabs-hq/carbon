//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{Fee, U64ValueChange},
    carbon_core::deserialize::CarbonDeserialize,
    solana_pubkey::Pubkey,
};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateActiveEventEvent {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub state: Pubkey,
    pub epoch: u64,
    pub stake_index: u32,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub stake_account: Pubkey,
    pub validator_index: u32,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub validator_vote: Pubkey,
    pub delegation_change: U64ValueChange,
    pub delegation_growth_msol_fees: Option<u64>,
    pub extra_lamports: u64,
    pub extra_msol_fees: Option<u64>,
    pub validator_active_balance: u64,
    pub total_active_balance: u64,
    pub msol_price_change: U64ValueChange,
    pub reward_fee_used: Fee,
    pub total_virtual_staked_lamports: u64,
    pub msol_supply: u64,
}

impl CarbonDeserialize for UpdateActiveEventEvent {
    const DISCRIMINATOR: &'static [u8] = &[
        228, 69, 165, 46, 81, 203, 154, 29, 251, 18, 128, 75, 208, 80, 174, 140,
    ];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 16 {
            return None;
        }
        let discriminator = &data[0..16];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[16..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

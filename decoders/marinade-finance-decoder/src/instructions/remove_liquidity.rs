//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RemoveLiquidity {
    pub tokens: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RemoveLiquidityInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub lp_mint: solana_pubkey::Pubkey,
    pub burn_from: solana_pubkey::Pubkey,
    pub burn_from_authority: solana_pubkey::Pubkey,
    pub transfer_sol_to: solana_pubkey::Pubkey,
    pub transfer_msol_to: solana_pubkey::Pubkey,
    pub liq_pool_sol_leg_pda: solana_pubkey::Pubkey,
    pub liq_pool_msol_leg: solana_pubkey::Pubkey,
    pub liq_pool_msol_leg_authority: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RemoveLiquidity {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [80, 85, 209, 72, 24, 206, 177, 108] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for RemoveLiquidity {
    type ArrangedAccounts = RemoveLiquidityInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let lp_mint = next_account(&mut iter)?;
        let burn_from = next_account(&mut iter)?;
        let burn_from_authority = next_account(&mut iter)?;
        let transfer_sol_to = next_account(&mut iter)?;
        let transfer_msol_to = next_account(&mut iter)?;
        let liq_pool_sol_leg_pda = next_account(&mut iter)?;
        let liq_pool_msol_leg = next_account(&mut iter)?;
        let liq_pool_msol_leg_authority = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RemoveLiquidityInstructionAccounts {
            state,
            lp_mint,
            burn_from,
            burn_from_authority,
            transfer_sol_to,
            transfer_msol_to,
            liq_pool_sol_leg_pda,
            liq_pool_msol_leg,
            liq_pool_msol_leg_authority,
            system_program,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

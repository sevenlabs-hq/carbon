//! This code was AUTOGENERATED using the Codama library.
pub mod add_liquidity_row;
pub mod add_validator_row;
pub mod change_authority_row;
pub mod claim_row;
pub mod config_lp_row;
pub mod config_marinade_row;
pub mod config_validator_system_row;
pub mod cpi_event_row;
pub mod deactivate_stake_row;
pub mod deposit_row;
pub mod deposit_stake_account_row;
pub mod emergency_unstake_row;
pub mod initialize_row;
pub mod liquid_unstake_row;
pub mod merge_stakes_row;
pub mod order_unstake_row;
pub mod partial_unstake_row;
pub mod pause_row;
pub mod realloc_stake_list_row;
pub mod realloc_validator_list_row;
pub mod redelegate_row;
pub mod remove_liquidity_row;
pub mod remove_validator_row;
pub mod resume_row;
pub mod set_validator_score_row;
pub mod stake_reserve_row;
pub mod update_active_row;
pub mod update_deactivated_row;
pub mod withdraw_stake_account_row;

pub use self::{
    add_liquidity_row::*, add_validator_row::*, change_authority_row::*, claim_row::*,
    config_lp_row::*, config_marinade_row::*, config_validator_system_row::*, cpi_event_row::*,
    deactivate_stake_row::*, deposit_row::*, deposit_stake_account_row::*,
    emergency_unstake_row::*, initialize_row::*, liquid_unstake_row::*, merge_stakes_row::*,
    order_unstake_row::*, partial_unstake_row::*, pause_row::*, realloc_stake_list_row::*,
    realloc_validator_list_row::*, redelegate_row::*, remove_liquidity_row::*,
    remove_validator_row::*, resume_row::*, set_validator_score_row::*, stake_reserve_row::*,
    update_active_row::*, update_deactivated_row::*, withdraw_stake_account_row::*,
};
use super::MarinadeFinanceInstruction;

pub struct MarinadeFinanceInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for MarinadeFinanceInstructionsMigration {
    fn app(&self) -> &str {
        "marinade-finance"
    }

    fn name(&self) -> &str {
        "marinade_finance_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AddLiquidityMigrationOperation),
            Box::new(AddValidatorMigrationOperation),
            Box::new(ChangeAuthorityMigrationOperation),
            Box::new(ClaimMigrationOperation),
            Box::new(ConfigLpMigrationOperation),
            Box::new(ConfigMarinadeMigrationOperation),
            Box::new(ConfigValidatorSystemMigrationOperation),
            Box::new(DeactivateStakeMigrationOperation),
            Box::new(DepositMigrationOperation),
            Box::new(DepositStakeAccountMigrationOperation),
            Box::new(EmergencyUnstakeMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(LiquidUnstakeMigrationOperation),
            Box::new(MergeStakesMigrationOperation),
            Box::new(OrderUnstakeMigrationOperation),
            Box::new(PartialUnstakeMigrationOperation),
            Box::new(PauseMigrationOperation),
            Box::new(ReallocStakeListMigrationOperation),
            Box::new(ReallocValidatorListMigrationOperation),
            Box::new(RedelegateMigrationOperation),
            Box::new(RemoveLiquidityMigrationOperation),
            Box::new(RemoveValidatorMigrationOperation),
            Box::new(ResumeMigrationOperation),
            Box::new(SetValidatorScoreMigrationOperation),
            Box::new(StakeReserveMigrationOperation),
            Box::new(UpdateActiveMigrationOperation),
            Box::new(UpdateDeactivatedMigrationOperation),
            Box::new(WithdrawStakeAccountMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct MarinadeFinanceInstructionWithMetadata(
    pub MarinadeFinanceInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        MarinadeFinanceInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for MarinadeFinanceInstructionWithMetadata
{
    fn from(
        value: (
            MarinadeFinanceInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        MarinadeFinanceInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MarinadeFinanceInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MarinadeFinanceInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            MarinadeFinanceInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ChangeAuthority { data, .. } => {
                let row = change_authority_row::ChangeAuthorityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::AddValidator { data, .. } => {
                let row = add_validator_row::AddValidatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::RemoveValidator { data, .. } => {
                let row = remove_validator_row::RemoveValidatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::SetValidatorScore { data, .. } => {
                let row = set_validator_score_row::SetValidatorScoreRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigValidatorSystem { data, .. } => {
                let row = config_validator_system_row::ConfigValidatorSystemRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::DepositStakeAccount { data, .. } => {
                let row = deposit_stake_account_row::DepositStakeAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::LiquidUnstake { data, .. } => {
                let row = liquid_unstake_row::LiquidUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::AddLiquidity { data, .. } => {
                let row = add_liquidity_row::AddLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::RemoveLiquidity { data, .. } => {
                let row = remove_liquidity_row::RemoveLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigLp { data, .. } => {
                let row = config_lp_row::ConfigLpRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigMarinade { data, .. } => {
                let row = config_marinade_row::ConfigMarinadeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::OrderUnstake { data, .. } => {
                let row = order_unstake_row::OrderUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Claim { data, .. } => {
                let row = claim_row::ClaimRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::StakeReserve { data, .. } => {
                let row = stake_reserve_row::StakeReserveRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::UpdateActive { data, .. } => {
                let row = update_active_row::UpdateActiveRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::UpdateDeactivated { data, .. } => {
                let row = update_deactivated_row::UpdateDeactivatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::DeactivateStake { data, .. } => {
                let row = deactivate_stake_row::DeactivateStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::EmergencyUnstake { data, .. } => {
                let row = emergency_unstake_row::EmergencyUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::PartialUnstake { data, .. } => {
                let row = partial_unstake_row::PartialUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::MergeStakes { data, .. } => {
                let row = merge_stakes_row::MergeStakesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Redelegate { data, .. } => {
                let row = redelegate_row::RedelegateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Resume { data, .. } => {
                let row = resume_row::ResumeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::WithdrawStakeAccount { data, .. } => {
                let row = withdraw_stake_account_row::WithdrawStakeAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ReallocValidatorList { data, .. } => {
                let row = realloc_validator_list_row::ReallocValidatorListRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ReallocStakeList { data, .. } => {
                let row = realloc_stake_list_row::ReallocStakeListRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MarinadeFinanceInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MarinadeFinanceInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            MarinadeFinanceInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ChangeAuthority { data, .. } => {
                let row = change_authority_row::ChangeAuthorityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::AddValidator { data, .. } => {
                let row = add_validator_row::AddValidatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::RemoveValidator { data, .. } => {
                let row = remove_validator_row::RemoveValidatorRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::SetValidatorScore { data, .. } => {
                let row = set_validator_score_row::SetValidatorScoreRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigValidatorSystem { data, .. } => {
                let row = config_validator_system_row::ConfigValidatorSystemRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::DepositStakeAccount { data, .. } => {
                let row = deposit_stake_account_row::DepositStakeAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::LiquidUnstake { data, .. } => {
                let row = liquid_unstake_row::LiquidUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::AddLiquidity { data, .. } => {
                let row = add_liquidity_row::AddLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::RemoveLiquidity { data, .. } => {
                let row = remove_liquidity_row::RemoveLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigLp { data, .. } => {
                let row = config_lp_row::ConfigLpRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ConfigMarinade { data, .. } => {
                let row = config_marinade_row::ConfigMarinadeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::OrderUnstake { data, .. } => {
                let row = order_unstake_row::OrderUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Claim { data, .. } => {
                let row = claim_row::ClaimRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::StakeReserve { data, .. } => {
                let row = stake_reserve_row::StakeReserveRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::UpdateActive { data, .. } => {
                let row = update_active_row::UpdateActiveRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::UpdateDeactivated { data, .. } => {
                let row = update_deactivated_row::UpdateDeactivatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::DeactivateStake { data, .. } => {
                let row = deactivate_stake_row::DeactivateStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::EmergencyUnstake { data, .. } => {
                let row = emergency_unstake_row::EmergencyUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::PartialUnstake { data, .. } => {
                let row = partial_unstake_row::PartialUnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::MergeStakes { data, .. } => {
                let row = merge_stakes_row::MergeStakesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Redelegate { data, .. } => {
                let row = redelegate_row::RedelegateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::Resume { data, .. } => {
                let row = resume_row::ResumeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::WithdrawStakeAccount { data, .. } => {
                let row = withdraw_stake_account_row::WithdrawStakeAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ReallocValidatorList { data, .. } => {
                let row = realloc_validator_list_row::ReallocValidatorListRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::ReallocStakeList { data, .. } => {
                let row = realloc_stake_list_row::ReallocStakeListRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MarinadeFinanceInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

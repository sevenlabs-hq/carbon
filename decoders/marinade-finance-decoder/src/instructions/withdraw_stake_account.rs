//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
    solana_pubkey::Pubkey,
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawStakeAccount {
    pub stake_index: u32,
    pub validator_index: u32,
    pub msol_amount: u64,
    pub beneficiary: Pubkey,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawStakeAccountInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub msol_mint: solana_pubkey::Pubkey,
    pub burn_msol_from: solana_pubkey::Pubkey,
    pub burn_msol_authority: solana_pubkey::Pubkey,
    pub treasury_msol_account: solana_pubkey::Pubkey,
    pub validator_list: solana_pubkey::Pubkey,
    pub stake_list: solana_pubkey::Pubkey,
    pub stake_withdraw_authority: solana_pubkey::Pubkey,
    pub stake_deposit_authority: solana_pubkey::Pubkey,
    pub stake_account: solana_pubkey::Pubkey,
    pub split_stake_account: solana_pubkey::Pubkey,
    pub split_stake_rent_payer: solana_pubkey::Pubkey,
    pub clock: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub stake_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawStakeAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [211, 85, 184, 65, 183, 177, 233, 217] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawStakeAccount {
    type ArrangedAccounts = WithdrawStakeAccountInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let msol_mint = next_account(&mut iter)?;
        let burn_msol_from = next_account(&mut iter)?;
        let burn_msol_authority = next_account(&mut iter)?;
        let treasury_msol_account = next_account(&mut iter)?;
        let validator_list = next_account(&mut iter)?;
        let stake_list = next_account(&mut iter)?;
        let stake_withdraw_authority = next_account(&mut iter)?;
        let stake_deposit_authority = next_account(&mut iter)?;
        let stake_account = next_account(&mut iter)?;
        let split_stake_account = next_account(&mut iter)?;
        let split_stake_rent_payer = next_account(&mut iter)?;
        let clock = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let stake_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawStakeAccountInstructionAccounts {
            state,
            msol_mint,
            burn_msol_from,
            burn_msol_authority,
            treasury_msol_account,
            validator_list,
            stake_list,
            stake_withdraw_authority,
            stake_deposit_authority,
            stake_account,
            split_stake_account,
            split_stake_rent_payer,
            clock,
            system_program,
            token_program,
            stake_program,
            remaining: remaining.to_vec(),
        })
    }
}

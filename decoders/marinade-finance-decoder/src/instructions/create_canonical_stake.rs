//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CreateCanonicalStake {
    pub source_stake_index: u32,
    pub validator_index: u32,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateCanonicalStakeInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub stake_list: solana_pubkey::Pubkey,
    pub validator_list: solana_pubkey::Pubkey,
    pub canonical_stake: solana_pubkey::Pubkey,
    pub source_stake: solana_pubkey::Pubkey,
    pub stake_deposit_authority: solana_pubkey::Pubkey,
    pub stake_withdraw_authority: solana_pubkey::Pubkey,
    pub operational_sol_account: solana_pubkey::Pubkey,
    pub clock: solana_pubkey::Pubkey,
    pub stake_history: solana_pubkey::Pubkey,
    pub stake_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for CreateCanonicalStake {
    const DISCRIMINATOR: &'static [u8] = &[109, 2, 136, 224, 147, 51, 182, 232];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CreateCanonicalStake {
    type ArrangedAccounts = CreateCanonicalStakeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let stake_list = next_account(&mut iter)?;
        let validator_list = next_account(&mut iter)?;
        let canonical_stake = next_account(&mut iter)?;
        let source_stake = next_account(&mut iter)?;
        let stake_deposit_authority = next_account(&mut iter)?;
        let stake_withdraw_authority = next_account(&mut iter)?;
        let operational_sol_account = next_account(&mut iter)?;
        let clock = next_account(&mut iter)?;
        let stake_history = next_account(&mut iter)?;
        let stake_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateCanonicalStakeInstructionAccounts {
            state,
            stake_list,
            validator_list,
            canonical_stake,
            source_stake,
            stake_deposit_authority,
            stake_withdraw_authority,
            operational_sol_account,
            clock,
            stake_history,
            stake_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

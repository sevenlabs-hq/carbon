//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::types::FixedPoint;
use carbon_core::account_utils::next_account;
use carbon_core::borsh::{self, BorshDeserialize};
use carbon_core::deserialize::ArrangeAccounts;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(
    Debug,
    Clone,
    carbon_core::borsh::BorshSerialize,
    carbon_core::borsh::BorshDeserialize,
    PartialEq,
)]
pub struct UpdateFees {
    pub new_buyback_fee: FixedPoint,
    pub new_project_fee: FixedPoint,
    pub new_provider_fee: FixedPoint,
    pub new_mercanti_fee: FixedPoint,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateFeesInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub pool: solana_pubkey::Pubkey,
    pub token_x: solana_pubkey::Pubkey,
    pub token_y: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub program_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateFees {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [225, 27, 13, 6, 69, 84, 172, 191] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        if let Ok(decoded) = Self::deserialize(&mut data_slice) {
            return Some(decoded);
        }

        None
    }
}

impl ArrangeAccounts for UpdateFees {
    type ArrangedAccounts = UpdateFeesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let pool = next_account(&mut iter)?;
        let token_x = next_account(&mut iter)?;
        let token_y = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let program_authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateFeesInstructionAccounts {
            state,
            pool,
            token_x,
            token_y,
            admin,
            program_authority,
            remaining: remaining.to_vec(),
        })
    }
}

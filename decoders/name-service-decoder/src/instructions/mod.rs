//! This code was AUTOGENERATED using the Codama library.
use crate::{NameServiceDecoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod create;
pub mod delete;
pub mod realloc;
pub mod transfer;
pub mod update;

pub use self::{create::*, delete::*, realloc::*, transfer::*, update::*};

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum NameServiceInstruction {
    Create {
        program_id: solana_pubkey::Pubkey,
        data: Create,
        accounts: CreateInstructionAccounts,
    },
    Delete {
        program_id: solana_pubkey::Pubkey,
        data: Delete,
        accounts: DeleteInstructionAccounts,
    },
    Realloc {
        program_id: solana_pubkey::Pubkey,
        data: Realloc,
        accounts: ReallocInstructionAccounts,
    },
    Transfer {
        program_id: solana_pubkey::Pubkey,
        data: Transfer,
        accounts: TransferInstructionAccounts,
    },
    Update {
        program_id: solana_pubkey::Pubkey,
        data: Update,
        accounts: UpdateInstructionAccounts,
    },
}

impl carbon_core::instruction::InstructionDecoder<'_> for NameServiceDecoder {
    type InstructionType = NameServiceInstruction;

    fn decode_instruction(
        &self,
        instruction: &solana_instruction::Instruction,
    ) -> Option<Self::InstructionType> {
        if instruction.program_id != PROGRAM_ID {
            return None;
        }

        carbon_core::try_decode_instructions!(
            instruction,
            PROGRAM_ID,
            NameServiceInstruction::Create => Create,
            NameServiceInstruction::Delete => Delete,
            NameServiceInstruction::Realloc => Realloc,
            NameServiceInstruction::Transfer => Transfer,
            NameServiceInstruction::Update => Update,
        )
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod create_row;
pub mod delete_row;
pub mod realloc_row;
pub mod transfer_row;
pub mod update_row;

pub use self::{create_row::*, delete_row::*, realloc_row::*, transfer_row::*, update_row::*};
use super::NameServiceInstruction;

pub struct NameServiceInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for NameServiceInstructionsMigration {
    fn app(&self) -> &str {
        "name-service"
    }

    fn name(&self) -> &str {
        "name_service_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CreateMigrationOperation),
            Box::new(DeleteMigrationOperation),
            Box::new(ReallocMigrationOperation),
            Box::new(TransferMigrationOperation),
            Box::new(UpdateMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct NameServiceInstructionWithMetadata(
    pub NameServiceInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        NameServiceInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for NameServiceInstructionWithMetadata
{
    fn from(
        value: (
            NameServiceInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        NameServiceInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for NameServiceInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let NameServiceInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            NameServiceInstruction::Create { data, .. } => {
                let row = create_row::CreateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Update { data, .. } => {
                let row = update_row::UpdateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Transfer { data, .. } => {
                let row = transfer_row::TransferRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Delete { data, .. } => {
                let row = delete_row::DeleteRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Realloc { data, .. } => {
                let row = realloc_row::ReallocRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for NameServiceInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let NameServiceInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            NameServiceInstruction::Create { data, .. } => {
                let row = create_row::CreateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Update { data, .. } => {
                let row = update_row::UpdateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Transfer { data, .. } => {
                let row = transfer_row::TransferRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Delete { data, .. } => {
                let row = delete_row::DeleteRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            NameServiceInstruction::Realloc { data, .. } => {
                let row = realloc_row::ReallocRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod create_row;
pub mod delete_row;
pub mod realloc_row;
pub mod transfer_row;
pub mod update_row;

pub use self::{create_row::*, delete_row::*, realloc_row::*, transfer_row::*, update_row::*};
use super::NameServiceInstruction;

pub struct NameServiceInstructionsMigration;

impl carbon_core::clickhouse::Migration for NameServiceInstructionsMigration {
    fn app(&self) -> &str {
        "name-service"
    }

    fn name(&self) -> &str {
        "name_service_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(CreateRowMigrationOperation),
            Box::new(DeleteRowMigrationOperation),
            Box::new(ReallocRowMigrationOperation),
            Box::new(TransferRowMigrationOperation),
            Box::new(UpdateRowMigrationOperation),
        ]
    }
}

pub enum NameServiceInstructionRow {
    Create(CreateRow),
    Delete(DeleteRow),
    Realloc(ReallocRow),
    Transfer(TransferRow),
    Update(UpdateRow),
}

pub struct NameServiceInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a NameServiceInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for NameServiceInstructionMetadata<'a> {
    type Row = NameServiceInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let NameServiceInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(NameServiceInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Create, CreateRow);
        insert_branch!(Delete, DeleteRow);
        insert_branch!(Realloc, ReallocRow);
        insert_branch!(Transfer, TransferRow);
        insert_branch!(Update, UpdateRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for NameServiceInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Create, CreateRow);
        commit_branch!(Delete, DeleteRow);
        commit_branch!(Realloc, ReallocRow);
        commit_branch!(Transfer, TransferRow);
        commit_branch!(Update, UpdateRow);

        Ok(())
    }
}

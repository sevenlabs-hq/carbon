//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Create {
    pub hashed_name: Vec<u8>,
    pub lamports: u64,
    pub space: u32,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateInstructionAccounts {
    pub system_program: solana_pubkey::Pubkey,
    pub funding_account: solana_pubkey::Pubkey,
    pub name_record: solana_pubkey::Pubkey,
    pub account_class: solana_pubkey::Pubkey,
    pub parent_name_record: solana_pubkey::Pubkey,
    pub parent_name_record_class: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Create {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [0] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Create {
    type ArrangedAccounts = CreateInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let system_program = next_account(&mut iter)?;
        let funding_account = next_account(&mut iter)?;
        let name_record = next_account(&mut iter)?;
        let account_class = next_account(&mut iter)?;
        let parent_name_record = next_account(&mut iter)?;
        let parent_name_record_class = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateInstructionAccounts {
            system_program,
            funding_account,
            name_record,
            account_class,
            parent_name_record,
            parent_name_record_class,
            remaining: remaining.to_vec(),
        })
    }
}

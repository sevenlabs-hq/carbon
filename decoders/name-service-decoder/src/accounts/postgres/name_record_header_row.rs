//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{metadata::AccountRowMetadata, primitives::Pubkey},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct NameRecordHeaderRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub parent_name: Pubkey,
    pub owner: Pubkey,
    pub class: Pubkey,
}

impl NameRecordHeaderRow {
    pub fn from_parts(
        source: crate::accounts::name_record_header::NameRecordHeader,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            parent_name: source.parent_name.into(),
            owner: source.owner.into(),
            class: source.class.into(),
        }
    }
}

impl TryFrom<NameRecordHeaderRow> for crate::accounts::name_record_header::NameRecordHeader {
    type Error = carbon_core::error::Error;
    fn try_from(source: NameRecordHeaderRow) -> Result<Self, Self::Error> {
        Ok(Self {
            parent_name: *source.parent_name,
            owner: *source.owner,
            class: *source.class,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::name_record_header::NameRecordHeader
{
    fn table() -> &'static str {
        "name_record_header_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "parent_name", "owner", "class"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for NameRecordHeaderRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO name_record_header_account (
                "parent_name",
                "owner",
                "class",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5
            )"#,
        )
        .bind(self.parent_name)
        .bind(self.owner)
        .bind(self.class)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for NameRecordHeaderRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO name_record_header_account (
                "parent_name",
                "owner",
                "class",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "parent_name" = EXCLUDED."parent_name",
                "owner" = EXCLUDED."owner",
                "class" = EXCLUDED."class",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.parent_name)
        .bind(self.owner)
        .bind(self.class)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for NameRecordHeaderRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM name_record_header_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for NameRecordHeaderRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM name_record_header_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct NameRecordHeaderMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for NameRecordHeaderMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS name_record_header_account (
                -- Account data
                "parent_name" BYTEA NOT NULL,
                "owner" BYTEA NOT NULL,
                "class" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS name_record_header_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

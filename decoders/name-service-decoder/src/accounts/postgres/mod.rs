//! This code was AUTOGENERATED using the Codama library.
pub mod name_record_header_row;

pub use self::name_record_header_row::*;
use super::NameServiceAccount;

pub struct NameServiceAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for NameServiceAccountsMigration {
    fn app(&self) -> &str {
        "name-service"
    }

    fn name(&self) -> &str {
        "name_service_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![Box::new(NameRecordHeaderMigrationOperation)]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct NameServiceAccountWithMetadata(
    pub NameServiceAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(NameServiceAccount, carbon_core::account::AccountMetadata)>
    for NameServiceAccountWithMetadata
{
    fn from(value: (NameServiceAccount, carbon_core::account::AccountMetadata)) -> Self {
        NameServiceAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for NameServiceAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let NameServiceAccountWithMetadata(account, metadata) = self;

        match account {
            NameServiceAccount::NameRecordHeader(account) => {
                let row = name_record_header_row::NameRecordHeaderRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for NameServiceAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let NameServiceAccountWithMetadata(account, metadata) = self;
        match account {
            NameServiceAccount::NameRecordHeader(account) => {
                let row = name_record_header_row::NameRecordHeaderRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

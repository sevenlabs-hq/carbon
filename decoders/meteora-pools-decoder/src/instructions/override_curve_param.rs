//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::CurveType,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
/// Update swap curve parameters. This function do not allow update of curve
/// type. For example: stable swap curve to constant product curve. Only
/// supported by pool with stable swap curve. Only amp is allowed to be
/// override. The other attributes of stable swap curve will be ignored.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct OverrideCurveParam {
    pub curve_type: CurveType,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct OverrideCurveParamInstructionAccounts {
    pub pool: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl OverrideCurveParam {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [98, 86, 204, 51, 94, 71, 69, 187] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for OverrideCurveParam {
    type ArrangedAccounts = OverrideCurveParamInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let pool = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(OverrideCurveParamInstructionAccounts {
            pool,
            admin,
            remaining: remaining.to_vec(),
        })
    }
}

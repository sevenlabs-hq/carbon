//! This code was AUTOGENERATED using the Codama library.
pub mod add_balance_liquidity_row;
pub mod add_imbalance_liquidity_row;
pub mod bootstrap_liquidity_row;
pub mod claim_fee_row;
pub mod close_config_row;
pub mod cpi_event_row;
pub mod create_config_row;
pub mod create_lock_escrow_row;
pub mod create_mint_metadata_row;
pub mod enable_or_disable_pool_row;
pub mod get_pool_info_row;
pub mod initialize_customizable_permissionless_constant_product_pool_row;
pub mod initialize_permissioned_pool_row;
pub mod initialize_permissionless_constant_product_pool_with_config2_row;
pub mod initialize_permissionless_constant_product_pool_with_config_row;
pub mod initialize_permissionless_pool_row;
pub mod initialize_permissionless_pool_with_fee_tier_row;
pub mod lock_row;
pub mod override_curve_param_row;
pub mod partner_claim_fee_row;
pub mod remove_balance_liquidity_row;
pub mod remove_liquidity_single_side_row;
pub mod set_pool_fees_row;
pub mod set_whitelisted_vault_row;
pub mod swap_row;
pub mod update_activation_point_row;
pub mod withdraw_protocol_fees_row;

pub use self::{
    add_balance_liquidity_row::*, add_imbalance_liquidity_row::*, bootstrap_liquidity_row::*,
    claim_fee_row::*, close_config_row::*, cpi_event_row::*, create_config_row::*,
    create_lock_escrow_row::*, create_mint_metadata_row::*, enable_or_disable_pool_row::*,
    get_pool_info_row::*, initialize_customizable_permissionless_constant_product_pool_row::*,
    initialize_permissioned_pool_row::*,
    initialize_permissionless_constant_product_pool_with_config2_row::*,
    initialize_permissionless_constant_product_pool_with_config_row::*,
    initialize_permissionless_pool_row::*, initialize_permissionless_pool_with_fee_tier_row::*,
    lock_row::*, override_curve_param_row::*, partner_claim_fee_row::*,
    remove_balance_liquidity_row::*, remove_liquidity_single_side_row::*, set_pool_fees_row::*,
    set_whitelisted_vault_row::*, swap_row::*, update_activation_point_row::*,
    withdraw_protocol_fees_row::*,
};
use super::MeteoraPoolsInstruction;

pub struct MeteoraPoolsInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for MeteoraPoolsInstructionsMigration {
    fn app(&self) -> &str {
        "meteora-pools"
    }

    fn name(&self) -> &str {
        "meteora_pools_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AddBalanceLiquidityMigrationOperation),
            Box::new(AddImbalanceLiquidityMigrationOperation),
            Box::new(BootstrapLiquidityMigrationOperation),
            Box::new(ClaimFeeMigrationOperation),
            Box::new(CloseConfigMigrationOperation),
            Box::new(CreateConfigMigrationOperation),
            Box::new(CreateLockEscrowMigrationOperation),
            Box::new(CreateMintMetadataMigrationOperation),
            Box::new(EnableOrDisablePoolMigrationOperation),
            Box::new(GetPoolInfoMigrationOperation),
            Box::new(InitializeCustomizablePermissionlessConstantProductPoolMigrationOperation),
            Box::new(InitializePermissionedPoolMigrationOperation),
            Box::new(InitializePermissionlessConstantProductPoolWithConfigMigrationOperation),
            Box::new(InitializePermissionlessConstantProductPoolWithConfig2MigrationOperation),
            Box::new(InitializePermissionlessPoolMigrationOperation),
            Box::new(InitializePermissionlessPoolWithFeeTierMigrationOperation),
            Box::new(LockMigrationOperation),
            Box::new(OverrideCurveParamMigrationOperation),
            Box::new(PartnerClaimFeeMigrationOperation),
            Box::new(RemoveBalanceLiquidityMigrationOperation),
            Box::new(RemoveLiquiditySingleSideMigrationOperation),
            Box::new(SetPoolFeesMigrationOperation),
            Box::new(SetWhitelistedVaultMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(UpdateActivationPointMigrationOperation),
            Box::new(WithdrawProtocolFeesMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct MeteoraPoolsInstructionWithMetadata(
    pub MeteoraPoolsInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        MeteoraPoolsInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for MeteoraPoolsInstructionWithMetadata
{
    fn from(
        value: (
            MeteoraPoolsInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        MeteoraPoolsInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MeteoraPoolsInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraPoolsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            MeteoraPoolsInstruction::InitializePermissionedPool { data, .. } => {
                let row =
                    initialize_permissioned_pool_row::InitializePermissionedPoolRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessPool { data, .. } => {
                let row =
                    initialize_permissionless_pool_row::InitializePermissionlessPoolRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessPoolWithFeeTier { data, .. } => {
                let row = initialize_permissionless_pool_with_fee_tier_row::InitializePermissionlessPoolWithFeeTierRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::EnableOrDisablePool { data, .. } => {
                let row = enable_or_disable_pool_row::EnableOrDisablePoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::RemoveLiquiditySingleSide { data, .. } => {
                let row =
                    remove_liquidity_single_side_row::RemoveLiquiditySingleSideRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::AddImbalanceLiquidity { data, .. } => {
                let row = add_imbalance_liquidity_row::AddImbalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::RemoveBalanceLiquidity { data, .. } => {
                let row = remove_balance_liquidity_row::RemoveBalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::AddBalanceLiquidity { data, .. } => {
                let row = add_balance_liquidity_row::AddBalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::SetPoolFees { data, .. } => {
                let row = set_pool_fees_row::SetPoolFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::OverrideCurveParam { data, .. } => {
                let row = override_curve_param_row::OverrideCurveParamRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::GetPoolInfo { data, .. } => {
                let row = get_pool_info_row::GetPoolInfoRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::BootstrapLiquidity { data, .. } => {
                let row = bootstrap_liquidity_row::BootstrapLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateMintMetadata { data, .. } => {
                let row = create_mint_metadata_row::CreateMintMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateLockEscrow { data, .. } => {
                let row = create_lock_escrow_row::CreateLockEscrowRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::Lock { data, .. } => {
                let row = lock_row::LockRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::ClaimFee { data, .. } => {
                let row = claim_fee_row::ClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateConfig { data, .. } => {
                let row = create_config_row::CreateConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CloseConfig { data, .. } => {
                let row = close_config_row::CloseConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessConstantProductPoolWithConfig {
                data,
                ..
            } => {
                let row = initialize_permissionless_constant_product_pool_with_config_row::InitializePermissionlessConstantProductPoolWithConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessConstantProductPoolWithConfig2 {
                data,
                ..
            } => {
                let row = initialize_permissionless_constant_product_pool_with_config2_row::InitializePermissionlessConstantProductPoolWithConfig2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializeCustomizablePermissionlessConstantProductPool {
                data,
                ..
            } => {
                let row = initialize_customizable_permissionless_constant_product_pool_row::InitializeCustomizablePermissionlessConstantProductPoolRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::UpdateActivationPoint { data, .. } => {
                let row = update_activation_point_row::UpdateActivationPointRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::WithdrawProtocolFees { data, .. } => {
                let row = withdraw_protocol_fees_row::WithdrawProtocolFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::SetWhitelistedVault { data, .. } => {
                let row = set_whitelisted_vault_row::SetWhitelistedVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::PartnerClaimFee { data, .. } => {
                let row = partner_claim_fee_row::PartnerClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MeteoraPoolsInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let MeteoraPoolsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            MeteoraPoolsInstruction::InitializePermissionedPool { data, .. } => {
                let row =
                    initialize_permissioned_pool_row::InitializePermissionedPoolRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessPool { data, .. } => {
                let row =
                    initialize_permissionless_pool_row::InitializePermissionlessPoolRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessPoolWithFeeTier { data, .. } => {
                let row = initialize_permissionless_pool_with_fee_tier_row::InitializePermissionlessPoolWithFeeTierRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::EnableOrDisablePool { data, .. } => {
                let row = enable_or_disable_pool_row::EnableOrDisablePoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::RemoveLiquiditySingleSide { data, .. } => {
                let row =
                    remove_liquidity_single_side_row::RemoveLiquiditySingleSideRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::AddImbalanceLiquidity { data, .. } => {
                let row = add_imbalance_liquidity_row::AddImbalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::RemoveBalanceLiquidity { data, .. } => {
                let row = remove_balance_liquidity_row::RemoveBalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::AddBalanceLiquidity { data, .. } => {
                let row = add_balance_liquidity_row::AddBalanceLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::SetPoolFees { data, .. } => {
                let row = set_pool_fees_row::SetPoolFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::OverrideCurveParam { data, .. } => {
                let row = override_curve_param_row::OverrideCurveParamRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::GetPoolInfo { data, .. } => {
                let row = get_pool_info_row::GetPoolInfoRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::BootstrapLiquidity { data, .. } => {
                let row = bootstrap_liquidity_row::BootstrapLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateMintMetadata { data, .. } => {
                let row = create_mint_metadata_row::CreateMintMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateLockEscrow { data, .. } => {
                let row = create_lock_escrow_row::CreateLockEscrowRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::Lock { data, .. } => {
                let row = lock_row::LockRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::ClaimFee { data, .. } => {
                let row = claim_fee_row::ClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CreateConfig { data, .. } => {
                let row = create_config_row::CreateConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CloseConfig { data, .. } => {
                let row = close_config_row::CloseConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessConstantProductPoolWithConfig {
                data,
                ..
            } => {
                let row = initialize_permissionless_constant_product_pool_with_config_row::InitializePermissionlessConstantProductPoolWithConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializePermissionlessConstantProductPoolWithConfig2 {
                data,
                ..
            } => {
                let row = initialize_permissionless_constant_product_pool_with_config2_row::InitializePermissionlessConstantProductPoolWithConfig2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::InitializeCustomizablePermissionlessConstantProductPool {
                data,
                ..
            } => {
                let row = initialize_customizable_permissionless_constant_product_pool_row::InitializeCustomizablePermissionlessConstantProductPoolRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::UpdateActivationPoint { data, .. } => {
                let row = update_activation_point_row::UpdateActivationPointRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::WithdrawProtocolFees { data, .. } => {
                let row = withdraw_protocol_fees_row::WithdrawProtocolFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::SetWhitelistedVault { data, .. } => {
                let row = set_whitelisted_vault_row::SetWhitelistedVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::PartnerClaimFee { data, .. } => {
                let row = partner_claim_fee_row::PartnerClaimFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            MeteoraPoolsInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Padding")]
pub struct PaddingGraphQL {
    pub padding0: Vec<U8>,
    pub padding1: Vec<U64>,
    pub padding2: Vec<U64>,
}

impl From<crate::types::Padding> for PaddingGraphQL {
    fn from(original: crate::types::Padding) -> Self {
        Self {
            padding0: original
                .padding0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding1: original
                .padding1
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
            padding2: original
                .padding2
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
        }
    }
}

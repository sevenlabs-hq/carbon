//! This code was AUTOGENERATED using the Codama library.
use crate::types::{Depeg, TokenMultiplier};
/// Type of the swap curve
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub enum CurveType {
    ConstantProduct,
    Stable {
        /// Amplification coefficient
        amp: u64,
        /// Multiplier for the pool token. Used to normalized token with
        /// different decimal into the same precision.
        token_multiplier: TokenMultiplier,
        /// Depeg pool information. Contains functions to allow token amount to
        /// be repeg using stake / interest bearing token virtual price
        depeg: Depeg,
        /// The last amp updated timestamp. Used to prevent update_curve_info
        /// called infinitely many times within a short period
        last_amp_updated_timestamp: u64,
    },
}

//! This code was AUTOGENERATED using the Codama library.
pub mod collection_authority_record_row;
pub mod edition_marker_row;
pub mod edition_marker_v2_row;
pub mod edition_row;
pub mod holder_delegate_record_row;
pub mod master_edition_v1_row;
pub mod master_edition_v2_row;
pub mod metadata_delegate_record_row;
pub mod metadata_row;
pub mod reservation_list_v1_row;
pub mod reservation_list_v2_row;
pub mod token_owned_escrow_row;
pub mod token_record_row;
pub mod use_authority_record_row;

pub use self::{
    collection_authority_record_row::*, edition_marker_row::*, edition_marker_v2_row::*,
    edition_row::*, holder_delegate_record_row::*, master_edition_v1_row::*,
    master_edition_v2_row::*, metadata_delegate_record_row::*, metadata_row::*,
    reservation_list_v1_row::*, reservation_list_v2_row::*, token_owned_escrow_row::*,
    token_record_row::*, use_authority_record_row::*,
};
use super::MplTokenMetadataAccount;

pub struct MplTokenMetadataAccountsMigration;

impl carbon_core::clickhouse::Migration for MplTokenMetadataAccountsMigration {
    fn app(&self) -> &str {
        "mpl-token-metadata"
    }

    fn name(&self) -> &str {
        "mpl_token_metadata_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(CollectionAuthorityRecordRowMigrationOperation),
            Box::new(EditionRowMigrationOperation),
            Box::new(EditionMarkerRowMigrationOperation),
            Box::new(EditionMarkerV2RowMigrationOperation),
            Box::new(HolderDelegateRecordRowMigrationOperation),
            Box::new(MasterEditionV1RowMigrationOperation),
            Box::new(MasterEditionV2RowMigrationOperation),
            Box::new(MetadataRowMigrationOperation),
            Box::new(MetadataDelegateRecordRowMigrationOperation),
            Box::new(ReservationListV1RowMigrationOperation),
            Box::new(ReservationListV2RowMigrationOperation),
            Box::new(TokenOwnedEscrowRowMigrationOperation),
            Box::new(TokenRecordRowMigrationOperation),
            Box::new(UseAuthorityRecordRowMigrationOperation),
        ]
    }
}

pub enum MplTokenMetadataAccountRow {
    CollectionAuthorityRecord(CollectionAuthorityRecordRow),
    Edition(EditionRow),
    EditionMarker(EditionMarkerRow),
    EditionMarkerV2(EditionMarkerV2Row),
    HolderDelegateRecord(HolderDelegateRecordRow),
    MasterEditionV1(MasterEditionV1Row),
    MasterEditionV2(MasterEditionV2Row),
    Metadata(MetadataRow),
    MetadataDelegateRecord(MetadataDelegateRecordRow),
    ReservationListV1(ReservationListV1Row),
    ReservationListV2(ReservationListV2Row),
    TokenOwnedEscrow(TokenOwnedEscrowRow),
    TokenRecord(TokenRecordRow),
    UseAuthorityRecord(UseAuthorityRecordRow),
}

pub struct MplTokenMetadataAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a MplTokenMetadataAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for MplTokenMetadataAccountMetadata<'a> {
    type Row = MplTokenMetadataAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let MplTokenMetadataAccount::$variant(account) = account {
                    rows.push(MplTokenMetadataAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let MplTokenMetadataAccount::$variant(account) = account {
                    rows.push(MplTokenMetadataAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(
            CollectionAuthorityRecord,
            CollectionAuthorityRecordRow,
            boxed
        );
        insert_branch!(Edition, EditionRow, boxed);
        insert_branch!(EditionMarker, EditionMarkerRow, boxed);
        insert_branch!(EditionMarkerV2, EditionMarkerV2Row, boxed);
        insert_branch!(HolderDelegateRecord, HolderDelegateRecordRow, boxed);
        insert_branch!(MasterEditionV1, MasterEditionV1Row, boxed);
        insert_branch!(MasterEditionV2, MasterEditionV2Row, boxed);
        insert_branch!(Metadata, MetadataRow, boxed);
        insert_branch!(MetadataDelegateRecord, MetadataDelegateRecordRow, boxed);
        insert_branch!(ReservationListV1, ReservationListV1Row, boxed);
        insert_branch!(ReservationListV2, ReservationListV2Row, boxed);
        insert_branch!(TokenOwnedEscrow, TokenOwnedEscrowRow, boxed);
        insert_branch!(TokenRecord, TokenRecordRow, boxed);
        insert_branch!(UseAuthorityRecord, UseAuthorityRecordRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MplTokenMetadataAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(CollectionAuthorityRecord, CollectionAuthorityRecordRow);
        commit_branch!(Edition, EditionRow);
        commit_branch!(EditionMarker, EditionMarkerRow);
        commit_branch!(EditionMarkerV2, EditionMarkerV2Row);
        commit_branch!(HolderDelegateRecord, HolderDelegateRecordRow);
        commit_branch!(MasterEditionV1, MasterEditionV1Row);
        commit_branch!(MasterEditionV2, MasterEditionV2Row);
        commit_branch!(Metadata, MetadataRow);
        commit_branch!(MetadataDelegateRecord, MetadataDelegateRecordRow);
        commit_branch!(ReservationListV1, ReservationListV1Row);
        commit_branch!(ReservationListV2, ReservationListV2Row);
        commit_branch!(TokenOwnedEscrow, TokenOwnedEscrowRow);
        commit_branch!(TokenRecord, TokenRecordRow);
        commit_branch!(UseAuthorityRecord, UseAuthorityRecordRow);

        Ok(())
    }
}

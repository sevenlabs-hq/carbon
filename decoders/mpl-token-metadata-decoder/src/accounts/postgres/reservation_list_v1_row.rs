//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{Key, ReservationV1},
    carbon_core::{
        account::AccountMetadata,
        postgres::{
            metadata::AccountRowMetadata,
            primitives::{Pubkey, U64},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ReservationListV1Row {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub key: sqlx::types::Json<Key>,
    pub master_edition: Pubkey,
    pub supply_snapshot: Option<U64>,
    pub reservations: sqlx::types::Json<Vec<ReservationV1>>,
}

impl ReservationListV1Row {
    pub fn from_parts(
        source: crate::accounts::reservation_list_v1::ReservationListV1,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            key: sqlx::types::Json(source.key),
            master_edition: source.master_edition.into(),
            supply_snapshot: source.supply_snapshot.map(|value| value.into()),
            reservations: sqlx::types::Json(source.reservations.to_vec()),
        }
    }
}

impl TryFrom<ReservationListV1Row> for crate::accounts::reservation_list_v1::ReservationListV1 {
    type Error = carbon_core::error::Error;
    fn try_from(source: ReservationListV1Row) -> Result<Self, Self::Error> {
        Ok(Self {
            key: source.key.0,
            master_edition: *source.master_edition,
            supply_snapshot: source.supply_snapshot.map(|value| *value),
            reservations: source.reservations.0,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::reservation_list_v1::ReservationListV1
{
    fn table() -> &'static str {
        "reservation_list_v1_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "key",
            "master_edition",
            "supply_snapshot",
            "reservations",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ReservationListV1Row {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO reservation_list_v1_account (
                "key",
                "master_edition",
                "supply_snapshot",
                "reservations",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            )"#,
        )
        .bind(&self.key)
        .bind(self.master_edition)
        .bind(&self.supply_snapshot)
        .bind(&self.reservations)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ReservationListV1Row {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO reservation_list_v1_account (
                "key",
                "master_edition",
                "supply_snapshot",
                "reservations",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "key" = EXCLUDED."key",
                "master_edition" = EXCLUDED."master_edition",
                "supply_snapshot" = EXCLUDED."supply_snapshot",
                "reservations" = EXCLUDED."reservations",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.key)
        .bind(self.master_edition)
        .bind(&self.supply_snapshot)
        .bind(&self.reservations)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ReservationListV1Row {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM reservation_list_v1_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ReservationListV1Row {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM reservation_list_v1_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ReservationListV1MigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for ReservationListV1MigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS reservation_list_v1_account (
                -- Account data
                "key" JSONB NOT NULL,
                "master_edition" BYTEA NOT NULL,
                "supply_snapshot" NUMERIC(20),
                "reservations" JSONB NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS reservation_list_v1_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

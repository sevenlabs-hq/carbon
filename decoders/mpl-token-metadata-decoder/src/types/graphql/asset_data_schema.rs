//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        CollectionDetailsGraphQL, CollectionGraphQL, CreatorGraphQL, TokenStandardGraphQL,
        UsesGraphQL,
    },
    carbon_core::graphql::primitives::Pubkey,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AssetData")]
pub struct AssetDataGraphQL {
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub seller_fee_basis_points: i32,
    pub creators: Option<Vec<CreatorGraphQL>>,
    pub primary_sale_happened: bool,
    pub is_mutable: bool,
    pub token_standard: TokenStandardGraphQL,
    pub collection: Option<CollectionGraphQL>,
    pub uses: Option<UsesGraphQL>,
    pub collection_details: Option<CollectionDetailsGraphQL>,
    pub rule_set: Option<Pubkey>,
}

impl From<crate::types::AssetData> for AssetDataGraphQL {
    fn from(original: crate::types::AssetData) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            seller_fee_basis_points: original.seller_fee_basis_points as i32,
            creators: original
                .creators
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
            primary_sale_happened: original.primary_sale_happened,
            is_mutable: original.is_mutable,
            token_standard: original.token_standard.into(),
            collection: original.collection.map(|v| v.into()),
            uses: original.uses.map(|v| v.into()),
            collection_details: original.collection_details.map(|v| v.into()),
            rule_set: original
                .rule_set
                .map(carbon_core::graphql::primitives::Pubkey),
        }
    }
}

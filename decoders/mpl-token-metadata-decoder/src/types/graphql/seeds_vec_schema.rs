//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::graphql::primitives::U8, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "SeedsVec")]
pub struct SeedsVecGraphQL {
    pub seeds: Vec<Vec<U8>>,
}

impl From<crate::types::SeedsVec> for SeedsVecGraphQL {
    fn from(original: crate::types::SeedsVec) -> Self {
        Self {
            seeds: original
                .seeds
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(carbon_core::graphql::primitives::U8)
                        .collect()
                })
                .collect(),
        }
    }
}

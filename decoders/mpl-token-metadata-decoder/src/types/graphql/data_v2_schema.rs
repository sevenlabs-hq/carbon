//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{CollectionGraphQL, CreatorGraphQL, UsesGraphQL},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "DataV2")]
pub struct DataV2GraphQL {
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub seller_fee_basis_points: i32,
    pub creators: Option<Vec<CreatorGraphQL>>,
    pub collection: Option<CollectionGraphQL>,
    pub uses: Option<UsesGraphQL>,
}

impl From<crate::types::DataV2> for DataV2GraphQL {
    fn from(original: crate::types::DataV2) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            seller_fee_basis_points: original.seller_fee_basis_points as i32,
            creators: original
                .creators
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
            collection: original.collection.map(|v| v.into()),
            uses: original.uses.map(|v| v.into()),
        }
    }
}

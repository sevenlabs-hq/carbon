//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::graphql::primitives::U8, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ProofInfo")]
pub struct ProofInfoGraphQL {
    pub proof: Vec<Vec<U8>>,
}

impl From<crate::types::ProofInfo> for ProofInfoGraphQL {
    fn from(original: crate::types::ProofInfo) -> Self {
        Self {
            proof: original
                .proof
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(carbon_core::graphql::primitives::U8)
                        .collect()
                })
                .collect(),
        }
    }
}

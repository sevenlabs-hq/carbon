//! This code was AUTOGENERATED using the Codama library.
pub mod asset_v1_row;
pub mod collection_v1_row;
pub mod group_v1_row;
pub mod hashed_asset_v1_row;
pub mod plugin_header_v1_row;
pub mod plugin_registry_v1_row;

pub use self::{
    asset_v1_row::*, collection_v1_row::*, group_v1_row::*, hashed_asset_v1_row::*,
    plugin_header_v1_row::*, plugin_registry_v1_row::*,
};
use super::MplCoreAccount;

pub struct MplCoreAccountsMigration;

impl carbon_core::clickhouse::Migration for MplCoreAccountsMigration {
    fn app(&self) -> &str {
        "mpl-core"
    }

    fn name(&self) -> &str {
        "mpl_core_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AssetV1RowMigrationOperation),
            Box::new(CollectionV1RowMigrationOperation),
            Box::new(GroupV1RowMigrationOperation),
            Box::new(HashedAssetV1RowMigrationOperation),
            Box::new(PluginHeaderV1RowMigrationOperation),
            Box::new(PluginRegistryV1RowMigrationOperation),
        ]
    }
}

pub enum MplCoreAccountRow {
    AssetV1(AssetV1Row),
    CollectionV1(CollectionV1Row),
    GroupV1(GroupV1Row),
    HashedAssetV1(HashedAssetV1Row),
    PluginHeaderV1(PluginHeaderV1Row),
    PluginRegistryV1(PluginRegistryV1Row),
}

pub struct MplCoreAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a MplCoreAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for MplCoreAccountMetadata<'a> {
    type Row = MplCoreAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let MplCoreAccount::$variant(account) = account {
                    rows.push(MplCoreAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let MplCoreAccount::$variant(account) = account {
                    rows.push(MplCoreAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(AssetV1, AssetV1Row, boxed);
        insert_branch!(CollectionV1, CollectionV1Row, boxed);
        insert_branch!(GroupV1, GroupV1Row, boxed);
        insert_branch!(HashedAssetV1, HashedAssetV1Row, boxed);
        insert_branch!(PluginHeaderV1, PluginHeaderV1Row, boxed);
        insert_branch!(PluginRegistryV1, PluginRegistryV1Row, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MplCoreAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(AssetV1, AssetV1Row);
        commit_branch!(CollectionV1, CollectionV1Row);
        commit_branch!(GroupV1, GroupV1Row);
        commit_branch!(HashedAssetV1, HashedAssetV1Row);
        commit_branch!(PluginHeaderV1, PluginHeaderV1Row);
        commit_branch!(PluginRegistryV1, PluginRegistryV1Row);

        Ok(())
    }
}

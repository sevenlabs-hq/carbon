//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{Key, UpdateAuthority},
    carbon_core::{
        account::AccountMetadata,
        postgres::{
            metadata::AccountRowMetadata,
            primitives::{Pubkey, U64},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AssetV1Row {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub key: sqlx::types::Json<Key>,
    pub owner: Pubkey,
    pub update_authority: sqlx::types::Json<UpdateAuthority>,
    pub name: String,
    pub uri: String,
    pub seq: Option<U64>,
}

impl AssetV1Row {
    pub fn from_parts(
        source: crate::accounts::asset_v1::AssetV1,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            key: sqlx::types::Json(source.key),
            owner: source.owner.into(),
            update_authority: sqlx::types::Json(source.update_authority),
            name: source.name,
            uri: source.uri,
            seq: source.seq.map(|value| value.into()),
        }
    }
}

impl TryFrom<AssetV1Row> for crate::accounts::asset_v1::AssetV1 {
    type Error = carbon_core::error::Error;
    fn try_from(source: AssetV1Row) -> Result<Self, Self::Error> {
        Ok(Self {
            key: source.key.0,
            owner: *source.owner,
            update_authority: source.update_authority.0,
            name: source.name,
            uri: source.uri,
            seq: source.seq.map(|value| *value),
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::asset_v1::AssetV1 {
    fn table() -> &'static str {
        "asset_v1_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "key",
            "owner",
            "update_authority",
            "name",
            "uri",
            "seq",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AssetV1Row {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO asset_v1_account (
                "key",
                "owner",
                "update_authority",
                "name",
                "uri",
                "seq",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(&self.key)
        .bind(self.owner)
        .bind(&self.update_authority)
        .bind(&self.name)
        .bind(&self.uri)
        .bind(&self.seq)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AssetV1Row {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO asset_v1_account (
                "key",
                "owner",
                "update_authority",
                "name",
                "uri",
                "seq",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "key" = EXCLUDED."key",
                "owner" = EXCLUDED."owner",
                "update_authority" = EXCLUDED."update_authority",
                "name" = EXCLUDED."name",
                "uri" = EXCLUDED."uri",
                "seq" = EXCLUDED."seq",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.key)
        .bind(self.owner)
        .bind(&self.update_authority)
        .bind(&self.name)
        .bind(&self.uri)
        .bind(&self.seq)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AssetV1Row {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM asset_v1_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AssetV1Row {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM asset_v1_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct AssetV1MigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AssetV1MigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS asset_v1_account (
                -- Account data
                "key" JSONB NOT NULL,
                "owner" BYTEA NOT NULL,
                "update_authority" JSONB NOT NULL,
                "name" TEXT NOT NULL,
                "uri" TEXT NOT NULL,
                "seq" NUMERIC(20),
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS asset_v1_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

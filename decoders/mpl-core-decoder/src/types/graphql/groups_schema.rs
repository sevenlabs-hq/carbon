//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::graphql::primitives::Pubkey, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Groups")]
pub struct GroupsGraphQL {
    pub groups: Vec<Pubkey>,
}

impl From<crate::types::Groups> for GroupsGraphQL {
    fn from(original: crate::types::Groups) -> Self {
        Self {
            groups: original
                .groups
                .into_iter()
                .map(carbon_core::graphql::primitives::Pubkey)
                .collect(),
        }
    }
}

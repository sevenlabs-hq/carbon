//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::RelationshipEntryGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "CreateGroupV1Args")]
pub struct CreateGroupV1ArgsGraphQL {
    pub name: String,
    pub uri: String,
    pub relationships: Vec<RelationshipEntryGraphQL>,
}

impl From<crate::types::CreateGroupV1Args> for CreateGroupV1ArgsGraphQL {
    fn from(original: crate::types::CreateGroupV1Args) -> Self {
        Self {
            name: original.name,
            uri: original.uri,
            relationships: original
                .relationships
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

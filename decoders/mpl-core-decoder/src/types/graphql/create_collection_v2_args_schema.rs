//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{ExternalPluginAdapterInitInfoGraphQL, PluginAuthorityPairGraphQL},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "CreateCollectionV2Args")]
pub struct CreateCollectionV2ArgsGraphQL {
    pub name: String,
    pub uri: String,
    pub plugins: Option<Vec<PluginAuthorityPairGraphQL>>,
    pub external_plugin_adapters: Option<Vec<ExternalPluginAdapterInitInfoGraphQL>>,
}

impl From<crate::types::CreateCollectionV2Args> for CreateCollectionV2ArgsGraphQL {
    fn from(original: crate::types::CreateCollectionV2Args) -> Self {
        Self {
            name: original.name,
            uri: original.uri,
            plugins: original
                .plugins
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
            external_plugin_adapters: original
                .external_plugin_adapters
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::AutographSignatureGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Autograph")]
pub struct AutographGraphQL {
    pub signatures: Vec<AutographSignatureGraphQL>,
}

impl From<crate::types::Autograph> for AutographGraphQL {
    fn from(original: crate::types::Autograph) -> Self {
        Self {
            signatures: original
                .signatures
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

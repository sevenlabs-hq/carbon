//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        DataStateGraphQL, ExternalPluginAdapterInitInfoGraphQL, PluginAuthorityPairGraphQL,
    },
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "CreateV2Args")]
pub struct CreateV2ArgsGraphQL {
    pub data_state: DataStateGraphQL,
    pub name: String,
    pub uri: String,
    pub plugins: Option<Vec<PluginAuthorityPairGraphQL>>,
    pub external_plugin_adapters: Option<Vec<ExternalPluginAdapterInitInfoGraphQL>>,
}

impl From<crate::types::CreateV2Args> for CreateV2ArgsGraphQL {
    fn from(original: crate::types::CreateV2Args) -> Self {
        Self {
            data_state: original.data_state.into(),
            name: original.name,
            uri: original.uri,
            plugins: original
                .plugins
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
            external_plugin_adapters: original
                .external_plugin_adapters
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
        }
    }
}

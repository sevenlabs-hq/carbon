//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::VerifiedCreatorsSignatureGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "VerifiedCreators")]
pub struct VerifiedCreatorsGraphQL {
    pub signatures: Vec<VerifiedCreatorsSignatureGraphQL>,
}

impl From<crate::types::VerifiedCreators> for VerifiedCreatorsGraphQL {
    fn from(original: crate::types::VerifiedCreators) -> Self {
        Self {
            signatures: original
                .signatures
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

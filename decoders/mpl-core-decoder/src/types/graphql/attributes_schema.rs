//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::AttributeGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Attributes")]
pub struct AttributesGraphQL {
    pub attribute_list: Vec<AttributeGraphQL>,
}

impl From<crate::types::Attributes> for AttributesGraphQL {
    fn from(original: crate::types::Attributes) -> Self {
        Self {
            attribute_list: original
                .attribute_list
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

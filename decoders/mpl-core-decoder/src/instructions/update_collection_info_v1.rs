//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::UpdateCollectionInfoV1Args,
    carbon_core::{
        account_utils::next_account,
        deserialize::{ArrangeAccounts, CarbonDeserialize},
    },
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateCollectionInfoV1 {
    pub update_collection_info_v1_args: UpdateCollectionInfoV1Args,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateCollectionInfoV1InstructionAccounts {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub collection: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub bubblegum_signer: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for UpdateCollectionInfoV1 {
    const DISCRIMINATOR: &'static [u8] = &[32];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateCollectionInfoV1 {
    type ArrangedAccounts = UpdateCollectionInfoV1InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let collection = next_account(&mut iter)?;
        let bubblegum_signer = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateCollectionInfoV1InstructionAccounts {
            collection,
            bubblegum_signer,
            remaining: remaining.to_vec(),
        })
    }
}

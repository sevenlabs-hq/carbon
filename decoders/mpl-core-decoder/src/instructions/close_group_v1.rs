//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::CloseGroupV1Args,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CloseGroupV1 {
    pub close_group_v1_args: CloseGroupV1Args,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CloseGroupV1InstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub authority: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CloseGroupV1 {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [40] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CloseGroupV1 {
    type ArrangedAccounts = CloseGroupV1InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let authority = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(CloseGroupV1InstructionAccounts {
            group,
            payer,
            authority,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::AddCollectionsToGroupV1Args,
    carbon_core::{
        account_utils::next_account,
        deserialize::{ArrangeAccounts, CarbonDeserialize},
    },
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct AddCollectionsToGroupV1 {
    pub add_collections_to_group_v1_args: AddCollectionsToGroupV1Args,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AddCollectionsToGroupV1InstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub authority: Option<solana_pubkey::Pubkey>,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for AddCollectionsToGroupV1 {
    const DISCRIMINATOR: &'static [u8] = &[33];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for AddCollectionsToGroupV1 {
    type ArrangedAccounts = AddCollectionsToGroupV1InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let authority = next_account(&mut iter);
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(AddCollectionsToGroupV1InstructionAccounts {
            group,
            payer,
            authority,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

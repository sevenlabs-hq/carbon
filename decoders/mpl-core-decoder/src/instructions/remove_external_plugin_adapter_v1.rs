//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::RemoveExternalPluginAdapterV1Args,
    carbon_core::{
        account_utils::next_account,
        deserialize::{ArrangeAccounts, CarbonDeserialize},
    },
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RemoveExternalPluginAdapterV1 {
    pub remove_external_plugin_adapter_v1_args: RemoveExternalPluginAdapterV1Args,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RemoveExternalPluginAdapterV1InstructionAccounts {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub asset: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize_option")
    )]
    pub collection: Option<solana_pubkey::Pubkey>,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub payer: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize_option")
    )]
    pub authority: Option<solana_pubkey::Pubkey>,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub system_program: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize_option")
    )]
    pub log_wrapper: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for RemoveExternalPluginAdapterV1 {
    const DISCRIMINATOR: &'static [u8] = &[24];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for RemoveExternalPluginAdapterV1 {
    type ArrangedAccounts = RemoveExternalPluginAdapterV1InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let asset = next_account(&mut iter)?;
        let collection = next_account(&mut iter);
        let payer = next_account(&mut iter)?;
        let authority = next_account(&mut iter);
        let system_program = next_account(&mut iter)?;
        let log_wrapper = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(RemoveExternalPluginAdapterV1InstructionAccounts {
            asset,
            collection,
            payer,
            authority,
            system_program,
            log_wrapper,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod deposit_row;
pub mod give_up_pending_fees_row;
pub mod init_vault_row;
pub mod initialize_shares_metadata_row;
pub mod invest_row;
pub mod update_admin_row;
pub mod update_reserve_allocation_row;
pub mod update_shares_metadata_row;
pub mod update_vault_config_row;
pub mod withdraw_from_available_row;
pub mod withdraw_pending_fees_row;
pub mod withdraw_row;

pub use self::{
    deposit_row::*, give_up_pending_fees_row::*, init_vault_row::*,
    initialize_shares_metadata_row::*, invest_row::*, update_admin_row::*,
    update_reserve_allocation_row::*, update_shares_metadata_row::*, update_vault_config_row::*,
    withdraw_from_available_row::*, withdraw_pending_fees_row::*, withdraw_row::*,
};
use super::KaminoVaultInstruction;

pub struct KaminoVaultInstructionsMigration;

impl carbon_core::clickhouse::Migration for KaminoVaultInstructionsMigration {
    fn app(&self) -> &str {
        "kamino-vault"
    }

    fn name(&self) -> &str {
        "kamino_vault_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(DepositRowMigrationOperation),
            Box::new(GiveUpPendingFeesRowMigrationOperation),
            Box::new(InitializeSharesMetadataRowMigrationOperation),
            Box::new(InitVaultRowMigrationOperation),
            Box::new(InvestRowMigrationOperation),
            Box::new(UpdateAdminRowMigrationOperation),
            Box::new(UpdateReserveAllocationRowMigrationOperation),
            Box::new(UpdateSharesMetadataRowMigrationOperation),
            Box::new(UpdateVaultConfigRowMigrationOperation),
            Box::new(WithdrawRowMigrationOperation),
            Box::new(WithdrawFromAvailableRowMigrationOperation),
            Box::new(WithdrawPendingFeesRowMigrationOperation),
        ]
    }
}

pub enum KaminoVaultInstructionRow {
    Deposit(DepositRow),
    GiveUpPendingFees(GiveUpPendingFeesRow),
    InitializeSharesMetadata(InitializeSharesMetadataRow),
    InitVault(InitVaultRow),
    Invest(InvestRow),
    UpdateAdmin(UpdateAdminRow),
    UpdateReserveAllocation(UpdateReserveAllocationRow),
    UpdateSharesMetadata(UpdateSharesMetadataRow),
    UpdateVaultConfig(UpdateVaultConfigRow),
    Withdraw(WithdrawRow),
    WithdrawFromAvailable(WithdrawFromAvailableRow),
    WithdrawPendingFees(WithdrawPendingFeesRow),
}

pub struct KaminoVaultInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a KaminoVaultInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for KaminoVaultInstructionMetadata<'a> {
    type Row = KaminoVaultInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let KaminoVaultInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(KaminoVaultInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Deposit, DepositRow);
        insert_branch!(GiveUpPendingFees, GiveUpPendingFeesRow);
        insert_branch!(InitializeSharesMetadata, InitializeSharesMetadataRow);
        insert_branch!(InitVault, InitVaultRow);
        insert_branch!(Invest, InvestRow);
        insert_branch!(UpdateAdmin, UpdateAdminRow);
        insert_branch!(UpdateReserveAllocation, UpdateReserveAllocationRow);
        insert_branch!(UpdateSharesMetadata, UpdateSharesMetadataRow);
        insert_branch!(UpdateVaultConfig, UpdateVaultConfigRow);
        insert_branch!(Withdraw, WithdrawRow);
        insert_branch!(WithdrawFromAvailable, WithdrawFromAvailableRow);
        insert_branch!(WithdrawPendingFees, WithdrawPendingFeesRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for KaminoVaultInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Deposit, DepositRow);
        commit_branch!(GiveUpPendingFees, GiveUpPendingFeesRow);
        commit_branch!(InitializeSharesMetadata, InitializeSharesMetadataRow);
        commit_branch!(InitVault, InitVaultRow);
        commit_branch!(Invest, InvestRow);
        commit_branch!(UpdateAdmin, UpdateAdminRow);
        commit_branch!(UpdateReserveAllocation, UpdateReserveAllocationRow);
        commit_branch!(UpdateSharesMetadata, UpdateSharesMetadataRow);
        commit_branch!(UpdateVaultConfig, UpdateVaultConfigRow);
        commit_branch!(Withdraw, WithdrawRow);
        commit_branch!(WithdrawFromAvailable, WithdrawFromAvailableRow);
        commit_branch!(WithdrawPendingFees, WithdrawPendingFeesRow);

        Ok(())
    }
}

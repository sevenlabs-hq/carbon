//! This code was AUTOGENERATED using the Codama library.
pub mod deposit_row;
pub mod give_up_pending_fees_row;
pub mod init_vault_row;
pub mod initialize_shares_metadata_row;
pub mod invest_row;
pub mod update_admin_row;
pub mod update_reserve_allocation_row;
pub mod update_shares_metadata_row;
pub mod update_vault_config_row;
pub mod withdraw_from_available_row;
pub mod withdraw_pending_fees_row;
pub mod withdraw_row;

pub use self::{
    deposit_row::*, give_up_pending_fees_row::*, init_vault_row::*,
    initialize_shares_metadata_row::*, invest_row::*, update_admin_row::*,
    update_reserve_allocation_row::*, update_shares_metadata_row::*, update_vault_config_row::*,
    withdraw_from_available_row::*, withdraw_pending_fees_row::*, withdraw_row::*,
};
use super::KaminoVaultInstruction;

pub struct KaminoVaultInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for KaminoVaultInstructionsMigration {
    fn app(&self) -> &str {
        "kamino-vault"
    }

    fn name(&self) -> &str {
        "kamino_vault_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(DepositMigrationOperation),
            Box::new(GiveUpPendingFeesMigrationOperation),
            Box::new(InitializeSharesMetadataMigrationOperation),
            Box::new(InitVaultMigrationOperation),
            Box::new(InvestMigrationOperation),
            Box::new(UpdateAdminMigrationOperation),
            Box::new(UpdateReserveAllocationMigrationOperation),
            Box::new(UpdateSharesMetadataMigrationOperation),
            Box::new(UpdateVaultConfigMigrationOperation),
            Box::new(WithdrawMigrationOperation),
            Box::new(WithdrawFromAvailableMigrationOperation),
            Box::new(WithdrawPendingFeesMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct KaminoVaultInstructionWithMetadata(
    pub KaminoVaultInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        KaminoVaultInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for KaminoVaultInstructionWithMetadata
{
    fn from(
        value: (
            KaminoVaultInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        KaminoVaultInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for KaminoVaultInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoVaultInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            KaminoVaultInstruction::InitVault { data, .. } => {
                let row = init_vault_row::InitVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateReserveAllocation { data, .. } => {
                let row = update_reserve_allocation_row::UpdateReserveAllocationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Invest { data, .. } => {
                let row = invest_row::InvestRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateVaultConfig { data, .. } => {
                let row = update_vault_config_row::UpdateVaultConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::WithdrawPendingFees { data, .. } => {
                let row = withdraw_pending_fees_row::WithdrawPendingFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateAdmin { data, .. } => {
                let row = update_admin_row::UpdateAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::GiveUpPendingFees { data, .. } => {
                let row = give_up_pending_fees_row::GiveUpPendingFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::InitializeSharesMetadata { data, .. } => {
                let row = initialize_shares_metadata_row::InitializeSharesMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateSharesMetadata { data, .. } => {
                let row = update_shares_metadata_row::UpdateSharesMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::WithdrawFromAvailable { data, .. } => {
                let row = withdraw_from_available_row::WithdrawFromAvailableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for KaminoVaultInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoVaultInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            KaminoVaultInstruction::InitVault { data, .. } => {
                let row = init_vault_row::InitVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateReserveAllocation { data, .. } => {
                let row = update_reserve_allocation_row::UpdateReserveAllocationRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::Invest { data, .. } => {
                let row = invest_row::InvestRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateVaultConfig { data, .. } => {
                let row = update_vault_config_row::UpdateVaultConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::WithdrawPendingFees { data, .. } => {
                let row = withdraw_pending_fees_row::WithdrawPendingFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateAdmin { data, .. } => {
                let row = update_admin_row::UpdateAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::GiveUpPendingFees { data, .. } => {
                let row = give_up_pending_fees_row::GiveUpPendingFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::InitializeSharesMetadata { data, .. } => {
                let row = initialize_shares_metadata_row::InitializeSharesMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::UpdateSharesMetadata { data, .. } => {
                let row = update_shares_metadata_row::UpdateSharesMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoVaultInstruction::WithdrawFromAvailable { data, .. } => {
                let row = withdraw_from_available_row::WithdrawFromAvailableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

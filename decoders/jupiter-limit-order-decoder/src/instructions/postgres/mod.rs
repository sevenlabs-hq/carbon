//! This code was AUTOGENERATED using the Codama library.
pub mod cancel_expired_order_row;
pub mod cancel_order_row;
pub mod cpi_event_row;
pub mod fill_order_row;
pub mod flash_fill_order_row;
pub mod init_fee_row;
pub mod initialize_order_row;
pub mod pre_flash_fill_order_row;
pub mod update_fee_row;
pub mod withdraw_fee_row;

pub use self::{
    cancel_expired_order_row::*, cancel_order_row::*, cpi_event_row::*, fill_order_row::*,
    flash_fill_order_row::*, init_fee_row::*, initialize_order_row::*, pre_flash_fill_order_row::*,
    update_fee_row::*, withdraw_fee_row::*,
};
use super::JupiterLimitOrderInstruction;

pub struct JupiterLimitOrderInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterLimitOrderInstructionsMigration {
    fn app(&self) -> &str {
        "jupiter-limit-order"
    }

    fn name(&self) -> &str {
        "jupiter_limit_order_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CancelExpiredOrderMigrationOperation),
            Box::new(CancelOrderMigrationOperation),
            Box::new(FillOrderMigrationOperation),
            Box::new(FlashFillOrderMigrationOperation),
            Box::new(InitFeeMigrationOperation),
            Box::new(InitializeOrderMigrationOperation),
            Box::new(PreFlashFillOrderMigrationOperation),
            Box::new(UpdateFeeMigrationOperation),
            Box::new(WithdrawFeeMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterLimitOrderInstructionWithMetadata(
    pub JupiterLimitOrderInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        JupiterLimitOrderInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for JupiterLimitOrderInstructionWithMetadata
{
    fn from(
        value: (
            JupiterLimitOrderInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        JupiterLimitOrderInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterLimitOrderInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrderInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterLimitOrderInstruction::InitializeOrder { data, .. } => {
                let row = initialize_order_row::InitializeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::FillOrder { data, .. } => {
                let row = fill_order_row::FillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::PreFlashFillOrder { data, .. } => {
                let row = pre_flash_fill_order_row::PreFlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::FlashFillOrder { data, .. } => {
                let row = flash_fill_order_row::FlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CancelOrder { data, .. } => {
                let row = cancel_order_row::CancelOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CancelExpiredOrder { data, .. } => {
                let row = cancel_expired_order_row::CancelExpiredOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::WithdrawFee { data, .. } => {
                let row = withdraw_fee_row::WithdrawFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::InitFee { data, .. } => {
                let row = init_fee_row::InitFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::UpdateFee { data, .. } => {
                let row = update_fee_row::UpdateFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterLimitOrderInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrderInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterLimitOrderInstruction::InitializeOrder { data, .. } => {
                let row = initialize_order_row::InitializeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::FillOrder { data, .. } => {
                let row = fill_order_row::FillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::PreFlashFillOrder { data, .. } => {
                let row = pre_flash_fill_order_row::PreFlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::FlashFillOrder { data, .. } => {
                let row = flash_fill_order_row::FlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CancelOrder { data, .. } => {
                let row = cancel_order_row::CancelOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CancelExpiredOrder { data, .. } => {
                let row = cancel_expired_order_row::CancelExpiredOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::WithdrawFee { data, .. } => {
                let row = withdraw_fee_row::WithdrawFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::InitFee { data, .. } => {
                let row = init_fee_row::InitFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::UpdateFee { data, .. } => {
                let row = update_fee_row::UpdateFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

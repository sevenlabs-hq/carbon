//! This code was AUTOGENERATED using the Codama library.
pub mod fee_row;
pub mod order_row;

pub use self::{fee_row::*, order_row::*};
use super::JupiterLimitOrderAccount;

pub struct JupiterLimitOrderAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterLimitOrderAccountsMigration {
    fn app(&self) -> &str {
        "jupiter-limit-order"
    }

    fn name(&self) -> &str {
        "jupiter_limit_order_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(FeeMigrationOperation),
            Box::new(OrderMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterLimitOrderAccountWithMetadata(
    pub JupiterLimitOrderAccount,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        JupiterLimitOrderAccount,
        carbon_core::account::AccountMetadata,
    )> for JupiterLimitOrderAccountWithMetadata
{
    fn from(
        value: (
            JupiterLimitOrderAccount,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        JupiterLimitOrderAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterLimitOrderAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrderAccountWithMetadata(account, metadata) = self;

        match account {
            JupiterLimitOrderAccount::Fee(account) => {
                let row = fee_row::FeeRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderAccount::Order(account) => {
                let row = order_row::OrderRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterLimitOrderAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrderAccountWithMetadata(account, metadata) = self;
        match account {
            JupiterLimitOrderAccount::Fee(account) => {
                let row = fee_row::FeeRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrderAccount::Order(account) => {
                let row = order_row::OrderRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!
use crate::types::DecryptableBalance;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

/// Transfer tokens confidentially with fee.
///
/// In order for this instruction to be successfully processed, it must be
/// accompanied by the following list of `zk_elgamal_proof` program
/// instructions:
/// - `VerifyCiphertextCommitmentEquality`
/// - `VerifyBatchedGroupedCiphertext3HandlesValidity` (transfer amount
///   ciphertext)
/// - `VerifyPercentageWithFee`
/// - `VerifyBatchedGroupedCiphertext2HandlesValidity` (fee ciphertext)
/// - `VerifyBatchedRangeProofU256`
///
/// These instructions can be accompanied in the same transaction or can be
/// pre-verified into a context state account, in which case, only their
/// context state account addresses need to be provided.
///
/// The same restrictions for the `Transfer` applies to
/// `TransferWithFee`. Namely, the instruction fails if the
/// associated mint is extended as `NonTransferable`.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct ConfidentialTransferWithFee {
    pub confidential_transfer_discriminator: u8,
    /// The new source decryptable balance if the transfer succeeds.
    pub new_source_decryptable_available_balance: DecryptableBalance,
    /// Relative location of the
    /// `ProofInstruction::VerifyCiphertextCommitmentEquality` instruction
    /// to the `TransferWithFee` instruction in the transaction. If the offset
    /// is `0`, then use a context state account for the proof.
    pub equality_proof_instruction_offset: i8,
    /// Relative location of the
    /// `ProofInstruction::VerifyBatchedGroupedCiphertext3HandlesValidity`
    /// instruction to the `TransferWithFee` instruction in the transaction.
    /// If the offset is `0`, then use a context state account for the
    /// proof.
    pub transfer_amount_ciphertext_validity_proof_instruction_offset: i8,
    /// Relative location of the `ProofInstruction::VerifyPercentageWithFee`
    /// instruction to the `TransferWithFee` instruction in the transaction.
    /// If the offset is `0`, then use a context state account for the
    /// proof.
    pub fee_sigma_proof_instruction_offset: i8,
    /// Relative location of the
    /// `ProofInstruction::VerifyBatchedGroupedCiphertext2HandlesValidity`
    /// instruction to the `TransferWithFee` instruction in the transaction.
    /// If the offset is `0`, then use a context state account for the
    /// proof.
    pub fee_ciphertext_validity_proof_instruction_offset: i8,
    /// Relative location of the `ProofInstruction::BatchedRangeProofU256Data`
    /// instruction to the `TransferWithFee` instruction in the transaction.
    /// If the offset is `0`, then use a context state account for the
    /// proof.
    pub range_proof_instruction_offset: i8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ConfidentialTransferWithFeeInstructionAccounts {
    pub source_token: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub destination_token: solana_pubkey::Pubkey,
    pub instructions_sysvar: Option<solana_pubkey::Pubkey>,
    pub equality_record: Option<solana_pubkey::Pubkey>,
    pub transfer_amount_ciphertext_validity_record: Option<solana_pubkey::Pubkey>,
    pub fee_sigma_record: Option<solana_pubkey::Pubkey>,
    pub fee_ciphertext_validity_record: Option<solana_pubkey::Pubkey>,
    pub range_record: Option<solana_pubkey::Pubkey>,
    pub authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ConfidentialTransferWithFee {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 2 {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [27] {
            return None;
        }
        let confidential_transfer_discriminator = data[1];
        if confidential_transfer_discriminator != 13 {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[1..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for ConfidentialTransferWithFee {
    type ArrangedAccounts = ConfidentialTransferWithFeeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let source_token = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let destination_token = next_account(&mut iter)?;
        let instructions_sysvar = next_account(&mut iter);
        let equality_record = next_account(&mut iter);
        let transfer_amount_ciphertext_validity_record = next_account(&mut iter);
        let fee_sigma_record = next_account(&mut iter);
        let fee_ciphertext_validity_record = next_account(&mut iter);
        let range_record = next_account(&mut iter);
        let authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ConfidentialTransferWithFeeInstructionAccounts {
            source_token,
            mint,
            destination_token,
            instructions_sysvar,
            equality_record,
            transfer_amount_ciphertext_validity_record,
            fee_sigma_record,
            fee_ciphertext_validity_record,
            range_record,
            authority,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Initialize a new `Member` of a `Group`
/// Assumes the `Group` has already been initialized,
/// as well as the mint for the member.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeTokenGroupMember {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeTokenGroupMemberInstructionAccounts {
    pub member: solana_pubkey::Pubkey,
    pub member_mint: solana_pubkey::Pubkey,
    pub member_mint_authority: solana_pubkey::Pubkey,
    pub group: solana_pubkey::Pubkey,
    pub group_update_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeTokenGroupMember {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [152, 32, 222, 176, 223, 237, 116, 134] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeTokenGroupMember {
    type ArrangedAccounts = InitializeTokenGroupMemberInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let member = next_account(&mut iter)?;
        let member_mint = next_account(&mut iter)?;
        let member_mint_authority = next_account(&mut iter)?;
        let group = next_account(&mut iter)?;
        let group_update_authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeTokenGroupMemberInstructionAccounts {
            member,
            member_mint,
            member_mint_authority,
            group,
            group_update_authority,
            remaining: remaining.to_vec(),
        })
    }
}

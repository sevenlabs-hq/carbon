//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
    solana_pubkey::Pubkey,
};
/// Initialize a new mint with a transfer hook program.
/// Fails if the mint has already been initialized, so must be called before
/// `InitializeMint`. The mint must have exactly enough space allocated for the
/// base mint (82 bytes), plus 83 bytes of padding, 1 byte reserved for the
/// account type, then space required for this extension, plus any others.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeTransferHook {
    /// The public key for the account that can update the program id
    pub authority: Option<Pubkey>,
    /// The program id that performs logic during transfers
    pub program_id: Option<Pubkey>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeTransferHookInstructionAccounts {
    pub mint: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeTransferHook {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 2 {
            return None;
        }
        let discriminator = data[0];
        if discriminator != 36 {
            return None;
        }
        let transfer_hook_discriminator = data[1];
        if transfer_hook_discriminator != 0 {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeTransferHook {
    type ArrangedAccounts = InitializeTransferHookInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let mint = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeTransferHookInstructionAccounts {
            mint,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::DecryptableBalance,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
/// Transfer all withheld confidential tokens in the mint to an account.
/// Signed by the mint's withdraw withheld tokens authority.
/// The withheld confidential tokens are aggregated directly into the
/// destination available balance. Must be accompanied by the
/// VerifyCiphertextCiphertextEquality instruction of the zk_elgamal_proof
/// program in the same transaction or the address of a context state account
/// for the proof must be provided.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawWithheldTokensFromMintForConfidentialTransferFee {
    /// Proof instruction offset
    pub proof_instruction_offset: i8,
    /// The new decryptable balance in the destination token account
    pub new_decryptable_available_balance: DecryptableBalance,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawWithheldTokensFromMintForConfidentialTransferFeeInstructionAccounts {
    pub mint: solana_pubkey::Pubkey,
    pub destination: solana_pubkey::Pubkey,
    pub instructions_sysvar_or_context_state: solana_pubkey::Pubkey,
    pub record: Option<solana_pubkey::Pubkey>,
    pub authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawWithheldTokensFromMintForConfidentialTransferFee {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 2 {
            return None;
        }
        let discriminator = data[0];
        if discriminator != 37 {
            return None;
        }
        let confidential_transfer_fee_discriminator = data[1];
        if confidential_transfer_fee_discriminator != 1 {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawWithheldTokensFromMintForConfidentialTransferFee {
    type ArrangedAccounts =
        WithdrawWithheldTokensFromMintForConfidentialTransferFeeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let mint = next_account(&mut iter)?;
        let destination = next_account(&mut iter)?;
        let instructions_sysvar_or_context_state = next_account(&mut iter)?;
        let record = next_account(&mut iter);
        let authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(
            WithdrawWithheldTokensFromMintForConfidentialTransferFeeInstructionAccounts {
                mint,
                destination,
                instructions_sysvar_or_context_state,
                record,
                authority,
                remaining: remaining.to_vec(),
            },
        )
    }
}

//! This code was AUTOGENERATED using the Codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun Codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

/// Update the multiplier. Only supported for mints that include the
/// `ScaledUiAmountConfig` extension.
/// You can set a specific timestamp for the multiplier to take effect.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct UpdateMultiplierScaledUiMint {
    pub scaled_ui_amount_mint_discriminator: u8,
    /// The new multiplier for the scaled UI extension
    pub multiplier: f64,
    /// The timestamp at which the new multiplier will take effect
    pub effective_timestamp: i64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateMultiplierScaledUiMintInstructionAccounts {
    pub mint: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateMultiplierScaledUiMint {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 2 {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [43] {
            return None;
        }
        let scaled_ui_amount_mint_discriminator = data[1];
        if scaled_ui_amount_mint_discriminator != 1 {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[1..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for UpdateMultiplierScaledUiMint {
    type ArrangedAccounts = UpdateMultiplierScaledUiMintInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let mint = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateMultiplierScaledUiMintInstructionAccounts {
            mint,
            authority,
            remaining: remaining.to_vec(),
        })
    }
}

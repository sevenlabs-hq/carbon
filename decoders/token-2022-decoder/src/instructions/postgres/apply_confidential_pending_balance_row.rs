//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::DecryptableBalance,
    carbon_core::{
        instruction::InstructionMetadata,
        postgres::{metadata::InstructionRowMetadata, primitives::U64},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ApplyConfidentialPendingBalanceRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub expected_pending_balance_credit_counter: U64,
    pub new_decryptable_available_balance: sqlx::types::Json<DecryptableBalance>,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl ApplyConfidentialPendingBalanceRow {
    pub fn from_parts(
        source: crate::instructions::apply_confidential_pending_balance::ApplyConfidentialPendingBalance,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            expected_pending_balance_credit_counter: source
                .expected_pending_balance_credit_counter
                .into(),
            new_decryptable_available_balance: sqlx::types::Json(
                source.new_decryptable_available_balance,
            ),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<ApplyConfidentialPendingBalanceRow>
    for crate::instructions::apply_confidential_pending_balance::ApplyConfidentialPendingBalance
{
    type Error = carbon_core::error::Error;
    fn try_from(source: ApplyConfidentialPendingBalanceRow) -> Result<Self, Self::Error> {
        Ok(Self {
            expected_pending_balance_credit_counter: *source
                .expected_pending_balance_credit_counter,
            new_decryptable_available_balance: source.new_decryptable_available_balance.0,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::apply_confidential_pending_balance::ApplyConfidentialPendingBalance
{
    fn table() -> &'static str {
        "apply_confidential_pending_balance_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "expected_pending_balance_credit_counter",
            "new_decryptable_available_balance",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ApplyConfidentialPendingBalanceRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO apply_confidential_pending_balance_instruction (
                "expected_pending_balance_credit_counter",
                "new_decryptable_available_balance",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(&self.expected_pending_balance_credit_counter)
        .bind(&self.new_decryptable_available_balance)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ApplyConfidentialPendingBalanceRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO apply_confidential_pending_balance_instruction (
                "expected_pending_balance_credit_counter",
                "new_decryptable_available_balance",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "expected_pending_balance_credit_counter" = EXCLUDED."expected_pending_balance_credit_counter",
                "new_decryptable_available_balance" = EXCLUDED."new_decryptable_available_balance",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#)
        .bind(&self.expected_pending_balance_credit_counter)
        .bind(&self.new_decryptable_available_balance)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ApplyConfidentialPendingBalanceRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM apply_confidential_pending_balance_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ApplyConfidentialPendingBalanceRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM apply_confidential_pending_balance_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ApplyConfidentialPendingBalanceMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres>
    for ApplyConfidentialPendingBalanceMigrationOperation
{
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS apply_confidential_pending_balance_instruction (
                -- Instruction data
                "expected_pending_balance_credit_counter" NUMERIC(20) NOT NULL,
                "new_decryptable_available_balance" JSONB NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS apply_confidential_pending_balance_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::TrimFeeInfo, carbon_core::deserialize::CarbonDeserialize, solana_pubkey::Pubkey,
};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SwapWithFeeCpiEventV3Event {
    pub order_id: u64,
    pub source_mint: Pubkey,
    pub destination_mint: Pubkey,
    pub source_token_account_owner: Pubkey,
    pub destination_token_account_owner: Pubkey,
    pub amount_in: u64,
    pub source_token_change: u64,
    pub destination_token_change: u64,
    pub commission_direction: bool,
    pub commission_paid_in_sol: bool,
    pub total_commission_rate: u32,
    pub total_commission_amount: u64,
    pub num_commission_levels: u8,
    pub commission_rates: Vec<u32>,
    pub commission_amounts: Vec<u64>,
    pub commission_accounts: Vec<Pubkey>,
    pub trim_info: Option<TrimFeeInfo>,
}

impl CarbonDeserialize for SwapWithFeeCpiEventV3Event {
    const DISCRIMINATOR: &'static [u8] = &[45, 53, 167, 19, 180, 196, 96, 150];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

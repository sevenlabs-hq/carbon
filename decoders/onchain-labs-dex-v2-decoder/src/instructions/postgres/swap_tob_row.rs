//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::SwapArgs,
    carbon_core::{
        instruction::InstructionMetadata,
        postgres::{
            metadata::InstructionRowMetadata,
            primitives::{U16, U32, U8},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct SwapTobRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub args: sqlx::types::Json<SwapArgs>,
    pub commission_info: U32,
    pub platform_fee_rate: U16,
    pub trim_rate: U8,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl SwapTobRow {
    pub fn from_parts(
        source: crate::instructions::swap_tob::SwapTob,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            args: sqlx::types::Json(source.args),
            commission_info: source.commission_info.into(),
            platform_fee_rate: source.platform_fee_rate.into(),
            trim_rate: source.trim_rate.into(),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<SwapTobRow> for crate::instructions::swap_tob::SwapTob {
    type Error = carbon_core::error::Error;
    fn try_from(source: SwapTobRow) -> Result<Self, Self::Error> {
        Ok(Self {
            args: source.args.0,
            commission_info: source.commission_info.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            platform_fee_rate: source.platform_fee_rate.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            trim_rate: source.trim_rate.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::instructions::swap_tob::SwapTob {
    fn table() -> &'static str {
        "swap_tob_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "args",
            "commission_info",
            "platform_fee_rate",
            "trim_rate",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for SwapTobRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO swap_tob_instruction (
                "args",
                "commission_info",
                "platform_fee_rate",
                "trim_rate",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(&self.args)
        .bind(self.commission_info)
        .bind(self.platform_fee_rate)
        .bind(self.trim_rate)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for SwapTobRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO swap_tob_instruction (
                "args",
                "commission_info",
                "platform_fee_rate",
                "trim_rate",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "args" = EXCLUDED."args",
                "commission_info" = EXCLUDED."commission_info",
                "platform_fee_rate" = EXCLUDED."platform_fee_rate",
                "trim_rate" = EXCLUDED."trim_rate",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(&self.args)
        .bind(self.commission_info)
        .bind(self.platform_fee_rate)
        .bind(self.trim_rate)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for SwapTobRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM swap_tob_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for SwapTobRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM swap_tob_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct SwapTobMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for SwapTobMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS swap_tob_instruction (
                -- Instruction data
                "args" JSONB NOT NULL,
                "commission_info" INT8 NOT NULL,
                "platform_fee_rate" INT4 NOT NULL,
                "trim_rate" INT2 NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS swap_tob_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

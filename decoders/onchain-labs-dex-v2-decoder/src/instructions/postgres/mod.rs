//! This code was AUTOGENERATED using the Codama library.
pub mod claim_cashback_pumpfun_row;
pub mod claim_cashback_pumpswap_row;
pub mod claim_row;
pub mod cpi_event_row;
pub mod create_token_account_row;
pub mod create_token_account_with_seed_row;
pub mod init_token_ledger_row;
pub mod proxy_swap_row;
pub mod set_token_ledger_row;
pub mod swap_row;
pub mod swap_tob_enhanced_row;
pub mod swap_tob_row;
pub mod swap_tob_v2_row;
pub mod swap_tob_with_receiver_row;
pub mod swap_tob_with_receiver_token_ledger_row;
pub mod swap_tob_with_token_ledger_row;
pub mod swap_toc_row;
pub mod swap_toc_v2_row;
pub mod wrap_unwrap_row;
pub mod wrap_unwrap_with_receiver_row;

pub use self::{
    claim_cashback_pumpfun_row::*, claim_cashback_pumpswap_row::*, claim_row::*, cpi_event_row::*,
    create_token_account_row::*, create_token_account_with_seed_row::*, init_token_ledger_row::*,
    proxy_swap_row::*, set_token_ledger_row::*, swap_row::*, swap_tob_enhanced_row::*,
    swap_tob_row::*, swap_tob_v2_row::*, swap_tob_with_receiver_row::*,
    swap_tob_with_receiver_token_ledger_row::*, swap_tob_with_token_ledger_row::*, swap_toc_row::*,
    swap_toc_v2_row::*, wrap_unwrap_row::*, wrap_unwrap_with_receiver_row::*,
};
use super::OnchainLabsDexV2Instruction;

pub struct OnchainLabsDexV2InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for OnchainLabsDexV2InstructionsMigration {
    fn app(&self) -> &str {
        "onchain-labs-dex-v2"
    }

    fn name(&self) -> &str {
        "onchain_labs_dex_v2_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(ClaimMigrationOperation),
            Box::new(ClaimCashbackPumpfunMigrationOperation),
            Box::new(ClaimCashbackPumpswapMigrationOperation),
            Box::new(CreateTokenAccountMigrationOperation),
            Box::new(CreateTokenAccountWithSeedMigrationOperation),
            Box::new(InitTokenLedgerMigrationOperation),
            Box::new(ProxySwapMigrationOperation),
            Box::new(SetTokenLedgerMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(SwapTobMigrationOperation),
            Box::new(SwapTobEnhancedMigrationOperation),
            Box::new(SwapTobV2MigrationOperation),
            Box::new(SwapTobWithReceiverMigrationOperation),
            Box::new(SwapTobWithReceiverTokenLedgerMigrationOperation),
            Box::new(SwapTobWithTokenLedgerMigrationOperation),
            Box::new(SwapTocMigrationOperation),
            Box::new(SwapTocV2MigrationOperation),
            Box::new(WrapUnwrapMigrationOperation),
            Box::new(WrapUnwrapWithReceiverMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct OnchainLabsDexV2InstructionWithMetadata(
    pub OnchainLabsDexV2Instruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        OnchainLabsDexV2Instruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for OnchainLabsDexV2InstructionWithMetadata
{
    fn from(
        value: (
            OnchainLabsDexV2Instruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        OnchainLabsDexV2InstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for OnchainLabsDexV2InstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let OnchainLabsDexV2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            OnchainLabsDexV2Instruction::Claim { data, .. } => {
                let row = claim_row::ClaimRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ClaimCashbackPumpfun { data, .. } => {
                let row = claim_cashback_pumpfun_row::ClaimCashbackPumpfunRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ClaimCashbackPumpswap { data, .. } => {
                let row = claim_cashback_pumpswap_row::ClaimCashbackPumpswapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CreateTokenAccount { data, .. } => {
                let row = create_token_account_row::CreateTokenAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CreateTokenAccountWithSeed { data, .. } => {
                let row =
                    create_token_account_with_seed_row::CreateTokenAccountWithSeedRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::InitTokenLedger { data, .. } => {
                let row = init_token_ledger_row::InitTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ProxySwap { data, .. } => {
                let row = proxy_swap_row::ProxySwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SetTokenLedger { data, .. } => {
                let row = set_token_ledger_row::SetTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTob { data, .. } => {
                let row = swap_tob_row::SwapTobRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobEnhanced { data, .. } => {
                let row = swap_tob_enhanced_row::SwapTobEnhancedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobV2 { data, .. } => {
                let row = swap_tob_v2_row::SwapTobV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithReceiver { data, .. } => {
                let row = swap_tob_with_receiver_row::SwapTobWithReceiverRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithReceiverTokenLedger { data, .. } => {
                let row = swap_tob_with_receiver_token_ledger_row::SwapTobWithReceiverTokenLedgerRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithTokenLedger { data, .. } => {
                let row = swap_tob_with_token_ledger_row::SwapTobWithTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapToc { data, .. } => {
                let row = swap_toc_row::SwapTocRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTocV2 { data, .. } => {
                let row = swap_toc_v2_row::SwapTocV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::WrapUnwrap { data, .. } => {
                let row = wrap_unwrap_row::WrapUnwrapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::WrapUnwrapWithReceiver { data, .. } => {
                let row = wrap_unwrap_with_receiver_row::WrapUnwrapWithReceiverRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for OnchainLabsDexV2InstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let OnchainLabsDexV2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            OnchainLabsDexV2Instruction::Claim { data, .. } => {
                let row = claim_row::ClaimRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ClaimCashbackPumpfun { data, .. } => {
                let row = claim_cashback_pumpfun_row::ClaimCashbackPumpfunRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ClaimCashbackPumpswap { data, .. } => {
                let row = claim_cashback_pumpswap_row::ClaimCashbackPumpswapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CreateTokenAccount { data, .. } => {
                let row = create_token_account_row::CreateTokenAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CreateTokenAccountWithSeed { data, .. } => {
                let row =
                    create_token_account_with_seed_row::CreateTokenAccountWithSeedRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::InitTokenLedger { data, .. } => {
                let row = init_token_ledger_row::InitTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::ProxySwap { data, .. } => {
                let row = proxy_swap_row::ProxySwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SetTokenLedger { data, .. } => {
                let row = set_token_ledger_row::SetTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTob { data, .. } => {
                let row = swap_tob_row::SwapTobRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobEnhanced { data, .. } => {
                let row = swap_tob_enhanced_row::SwapTobEnhancedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobV2 { data, .. } => {
                let row = swap_tob_v2_row::SwapTobV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithReceiver { data, .. } => {
                let row = swap_tob_with_receiver_row::SwapTobWithReceiverRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithReceiverTokenLedger { data, .. } => {
                let row = swap_tob_with_receiver_token_ledger_row::SwapTobWithReceiverTokenLedgerRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTobWithTokenLedger { data, .. } => {
                let row = swap_tob_with_token_ledger_row::SwapTobWithTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapToc { data, .. } => {
                let row = swap_toc_row::SwapTocRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::SwapTocV2 { data, .. } => {
                let row = swap_toc_v2_row::SwapTocV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::WrapUnwrap { data, .. } => {
                let row = wrap_unwrap_row::WrapUnwrapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::WrapUnwrapWithReceiver { data, .. } => {
                let row = wrap_unwrap_with_receiver_row::WrapUnwrapWithReceiverRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            OnchainLabsDexV2Instruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

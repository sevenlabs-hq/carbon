//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "TrimConfig")]
pub struct TrimConfigGraphQL {
    pub trim_rate: i32,
    pub charge_rate: i32,
}

impl From<crate::types::TrimConfig> for TrimConfigGraphQL {
    fn from(original: crate::types::TrimConfig) -> Self {
        Self {
            trim_rate: original.trim_rate as i32,
            charge_rate: original.charge_rate as i32,
        }
    }
}

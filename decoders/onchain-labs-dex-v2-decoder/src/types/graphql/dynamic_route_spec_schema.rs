//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{DynamicCandidateGraphQL, SelectionModeGraphQL},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "DynamicRouteSpec")]
pub struct DynamicRouteSpecGraphQL {
    pub candidates: Vec<DynamicCandidateGraphQL>,
    pub mode: SelectionModeGraphQL,
}

impl From<crate::types::DynamicRouteSpec> for DynamicRouteSpecGraphQL {
    fn from(original: crate::types::DynamicRouteSpec) -> Self {
        Self {
            candidates: original
                .candidates
                .into_iter()
                .map(|item| item.into())
                .collect(),
            mode: original.mode.into(),
        }
    }
}

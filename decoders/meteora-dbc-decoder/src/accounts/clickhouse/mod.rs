//! This code was AUTOGENERATED using the Codama library.
pub mod claim_fee_operator_row;
pub mod config_row;
pub mod lock_escrow_row;
pub mod meteora_damm_migration_metadata_row;
pub mod operator_row;
pub mod partner_metadata_row;
pub mod pool_config_row;
pub mod virtual_pool_metadata_row;
pub mod virtual_pool_row;

pub use self::{
    claim_fee_operator_row::*, config_row::*, lock_escrow_row::*,
    meteora_damm_migration_metadata_row::*, operator_row::*, partner_metadata_row::*,
    pool_config_row::*, virtual_pool_metadata_row::*, virtual_pool_row::*,
};
use super::MeteoraDbcAccount;

pub struct MeteoraDbcAccountsMigration;

impl carbon_core::clickhouse::Migration for MeteoraDbcAccountsMigration {
    fn app(&self) -> &str {
        "meteora-dbc"
    }

    fn name(&self) -> &str {
        "meteora_dbc_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(ClaimFeeOperatorRowMigrationOperation),
            Box::new(ConfigRowMigrationOperation),
            Box::new(LockEscrowRowMigrationOperation),
            Box::new(MeteoraDammMigrationMetadataRowMigrationOperation),
            Box::new(OperatorRowMigrationOperation),
            Box::new(PartnerMetadataRowMigrationOperation),
            Box::new(PoolConfigRowMigrationOperation),
            Box::new(VirtualPoolRowMigrationOperation),
            Box::new(VirtualPoolMetadataRowMigrationOperation),
        ]
    }
}

pub enum MeteoraDbcAccountRow {
    ClaimFeeOperator(ClaimFeeOperatorRow),
    Config(ConfigRow),
    LockEscrow(LockEscrowRow),
    MeteoraDammMigrationMetadata(MeteoraDammMigrationMetadataRow),
    Operator(OperatorRow),
    PartnerMetadata(PartnerMetadataRow),
    PoolConfig(PoolConfigRow),
    VirtualPool(VirtualPoolRow),
    VirtualPoolMetadata(VirtualPoolMetadataRow),
}

pub struct MeteoraDbcAccountMetadata(
    pub carbon_core::account::AccountMetadata,
    pub MeteoraDbcAccount,
);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for MeteoraDbcAccountMetadata {
    type Row = MeteoraDbcAccountRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, account) = self;

        match account {
            MeteoraDbcAccount::ClaimFeeOperator(account) => {
                rows.push(MeteoraDbcAccountRow::ClaimFeeOperator(
                    ClaimFeeOperatorRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MeteoraDbcAccount::Config(account) => {
                rows.push(MeteoraDbcAccountRow::Config(ConfigRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MeteoraDbcAccount::LockEscrow(account) => {
                rows.push(MeteoraDbcAccountRow::LockEscrow(LockEscrowRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            MeteoraDbcAccount::MeteoraDammMigrationMetadata(account) => {
                rows.push(MeteoraDbcAccountRow::MeteoraDammMigrationMetadata(
                    MeteoraDammMigrationMetadataRow::try_from((
                        *account.clone(),
                        metadata.clone(),
                    ))?,
                ));
            }
            MeteoraDbcAccount::Operator(account) => {
                rows.push(MeteoraDbcAccountRow::Operator(OperatorRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MeteoraDbcAccount::PartnerMetadata(account) => {
                rows.push(MeteoraDbcAccountRow::PartnerMetadata(
                    PartnerMetadataRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MeteoraDbcAccount::PoolConfig(account) => {
                rows.push(MeteoraDbcAccountRow::PoolConfig(PoolConfigRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            MeteoraDbcAccount::VirtualPool(account) => {
                rows.push(MeteoraDbcAccountRow::VirtualPool(VirtualPoolRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            MeteoraDbcAccount::VirtualPoolMetadata(account) => {
                rows.push(MeteoraDbcAccountRow::VirtualPoolMetadata(
                    VirtualPoolMetadataRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MeteoraDbcAccountRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(ClaimFeeOperator, ClaimFeeOperatorRow);
        commit_branch!(Config, ConfigRow);
        commit_branch!(LockEscrow, LockEscrowRow);
        commit_branch!(
            MeteoraDammMigrationMetadata,
            MeteoraDammMigrationMetadataRow
        );
        commit_branch!(Operator, OperatorRow);
        commit_branch!(PartnerMetadata, PartnerMetadataRow);
        commit_branch!(PoolConfig, PoolConfigRow);
        commit_branch!(VirtualPool, VirtualPoolRow);
        commit_branch!(VirtualPoolMetadata, VirtualPoolMetadataRow);
        Ok(())
    }
}

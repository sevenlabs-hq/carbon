//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U128},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct VirtualPoolMetadataRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub virtual_pool: Pubkey,
    pub padding: Vec<U128>,
    pub name: String,
    pub website: String,
    pub logo: String,
}

impl VirtualPoolMetadataRow {
    pub fn from_parts(
        source: crate::accounts::virtual_pool_metadata::VirtualPoolMetadata,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            virtual_pool: source.virtual_pool.into(),
            padding: source
                .padding
                .into_iter()
                .map(|element| element.into())
                .collect(),
            name: source.name,
            website: source.website,
            logo: source.logo,
        }
    }
}

impl TryFrom<VirtualPoolMetadataRow>
    for crate::accounts::virtual_pool_metadata::VirtualPoolMetadata
{
    type Error = carbon_core::error::Error;
    fn try_from(source: VirtualPoolMetadataRow) -> Result<Self, Self::Error> {
        Ok(Self {
            virtual_pool: *source.virtual_pool,
            padding: source
                .padding
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            name: source.name,
            website: source.website,
            logo: source.logo,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::virtual_pool_metadata::VirtualPoolMetadata
{
    fn table() -> &'static str {
        "virtual_pool_metadata_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "virtual_pool",
            "padding",
            "name",
            "website",
            "logo",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for VirtualPoolMetadataRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO virtual_pool_metadata_account (
                "virtual_pool",
                "padding",
                "name",
                "website",
                "logo",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(self.virtual_pool)
        .bind(&self.padding)
        .bind(&self.name)
        .bind(&self.website)
        .bind(&self.logo)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for VirtualPoolMetadataRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO virtual_pool_metadata_account (
                "virtual_pool",
                "padding",
                "name",
                "website",
                "logo",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "virtual_pool" = EXCLUDED."virtual_pool",
                "padding" = EXCLUDED."padding",
                "name" = EXCLUDED."name",
                "website" = EXCLUDED."website",
                "logo" = EXCLUDED."logo",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.virtual_pool)
        .bind(&self.padding)
        .bind(&self.name)
        .bind(&self.website)
        .bind(&self.logo)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for VirtualPoolMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM virtual_pool_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for VirtualPoolMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM virtual_pool_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct VirtualPoolMetadataMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for VirtualPoolMetadataMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS virtual_pool_metadata_account (
                -- Account data
                "virtual_pool" BYTEA NOT NULL,
                "padding" NUMERIC(39)[] NOT NULL,
                "name" TEXT NOT NULL,
                "website" TEXT NOT NULL,
                "logo" TEXT NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS virtual_pool_metadata_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U128},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct PartnerMetadataRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub fee_claimer: Pubkey,
    pub padding: Vec<U128>,
    pub name: String,
    pub website: String,
    pub logo: String,
}

impl PartnerMetadataRow {
    pub fn from_parts(
        source: crate::accounts::partner_metadata::PartnerMetadata,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            fee_claimer: source.fee_claimer.into(),
            padding: source
                .padding
                .into_iter()
                .map(|element| element.into())
                .collect(),
            name: source.name,
            website: source.website,
            logo: source.logo,
        }
    }
}

impl TryFrom<PartnerMetadataRow> for crate::accounts::partner_metadata::PartnerMetadata {
    type Error = carbon_core::error::Error;
    fn try_from(source: PartnerMetadataRow) -> Result<Self, Self::Error> {
        Ok(Self {
            fee_claimer: *source.fee_claimer,
            padding: source
                .padding
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            name: source.name,
            website: source.website,
            logo: source.logo,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::partner_metadata::PartnerMetadata
{
    fn table() -> &'static str {
        "partner_metadata_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "fee_claimer",
            "padding",
            "name",
            "website",
            "logo",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PartnerMetadataRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO partner_metadata_account (
                "fee_claimer",
                "padding",
                "name",
                "website",
                "logo",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(self.fee_claimer)
        .bind(&self.padding)
        .bind(&self.name)
        .bind(&self.website)
        .bind(&self.logo)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PartnerMetadataRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO partner_metadata_account (
                "fee_claimer",
                "padding",
                "name",
                "website",
                "logo",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "fee_claimer" = EXCLUDED."fee_claimer",
                "padding" = EXCLUDED."padding",
                "name" = EXCLUDED."name",
                "website" = EXCLUDED."website",
                "logo" = EXCLUDED."logo",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.fee_claimer)
        .bind(&self.padding)
        .bind(&self.name)
        .bind(&self.website)
        .bind(&self.logo)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for PartnerMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM partner_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for PartnerMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM partner_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct PartnerMetadataMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for PartnerMetadataMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS partner_metadata_account (
                -- Account data
                "fee_claimer" BYTEA NOT NULL,
                "padding" NUMERIC(39)[] NOT NULL,
                "name" TEXT NOT NULL,
                "website" TEXT NOT NULL,
                "logo" TEXT NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS partner_metadata_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

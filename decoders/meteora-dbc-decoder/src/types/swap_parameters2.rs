//! This code was AUTOGENERATED using the Codama library.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SwapParameters2 {
    /// When it's exact in, partial fill, this will be amount_in. When it's
    /// exact out, this will be amount_out
    pub amount0: u64,
    /// When it's exact in, partial fill, this will be minimum_amount_out. When
    /// it's exact out, this will be maximum_amount_in
    pub amount1: u64,
    /// Swap mode, refer [SwapMode]
    pub swap_mode: u8,
}

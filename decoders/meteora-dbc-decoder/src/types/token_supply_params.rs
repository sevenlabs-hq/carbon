//! This code was AUTOGENERATED using the Codama library.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TokenSupplyParams {
    /// pre migration token supply
    pub pre_migration_token_supply: u64,
    /// post migration token supply
    /// because DBC allow user to swap over the migration quote threshold, so in
    /// extreme case user may swap more than allowed buffer on curve
    /// that result the total supply in post migration may be increased a bit
    /// (between pre_migration_token_supply and post_migration_token_supply)
    pub post_migration_token_supply: u64,
}

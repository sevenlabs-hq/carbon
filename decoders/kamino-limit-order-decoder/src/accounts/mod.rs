//! This code was AUTOGENERATED using the Codama library.
use crate::{KaminoLimitOrderDecoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod global_config;
pub mod order;
pub mod user_swap_balances_state;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum KaminoLimitOrderAccount {
    GlobalConfig(Box<global_config::GlobalConfig>),
    Order(Box<order::Order>),
    UserSwapBalancesState(Box<user_swap_balances_state::UserSwapBalancesState>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for KaminoLimitOrderDecoder {
    type AccountType = KaminoLimitOrderAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = order::Order::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: KaminoLimitOrderAccount::Order(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = user_swap_balances_state::UserSwapBalancesState::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: KaminoLimitOrderAccount::UserSwapBalancesState(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = global_config::GlobalConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: KaminoLimitOrderAccount::GlobalConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

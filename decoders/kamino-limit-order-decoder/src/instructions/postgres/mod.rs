//! This code was AUTOGENERATED using the Codama library.
pub mod assert_user_swap_balances_end_row;
pub mod assert_user_swap_balances_start_row;
pub mod close_order_and_claim_tip_row;
pub mod cpi_event_row;
pub mod create_order_row;
pub mod flash_take_order_end_row;
pub mod flash_take_order_start_row;
pub mod initialize_global_config_row;
pub mod initialize_vault_row;
pub mod log_user_swap_balances_end_row;
pub mod log_user_swap_balances_start_row;
pub mod take_order_row;
pub mod update_global_config_admin_row;
pub mod update_global_config_row;
pub mod update_order_row;
pub mod withdraw_host_tip_row;

pub use self::{
    assert_user_swap_balances_end_row::*, assert_user_swap_balances_start_row::*,
    close_order_and_claim_tip_row::*, cpi_event_row::*, create_order_row::*,
    flash_take_order_end_row::*, flash_take_order_start_row::*, initialize_global_config_row::*,
    initialize_vault_row::*, log_user_swap_balances_end_row::*,
    log_user_swap_balances_start_row::*, take_order_row::*, update_global_config_admin_row::*,
    update_global_config_row::*, update_order_row::*, withdraw_host_tip_row::*,
};
use super::KaminoLimitOrderInstruction;

pub struct KaminoLimitOrderInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for KaminoLimitOrderInstructionsMigration {
    fn app(&self) -> &str {
        "kamino-limit-order"
    }

    fn name(&self) -> &str {
        "kamino_limit_order_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AssertUserSwapBalancesEndMigrationOperation),
            Box::new(AssertUserSwapBalancesStartMigrationOperation),
            Box::new(CloseOrderAndClaimTipMigrationOperation),
            Box::new(CreateOrderMigrationOperation),
            Box::new(FlashTakeOrderEndMigrationOperation),
            Box::new(FlashTakeOrderStartMigrationOperation),
            Box::new(InitializeGlobalConfigMigrationOperation),
            Box::new(InitializeVaultMigrationOperation),
            Box::new(LogUserSwapBalancesEndMigrationOperation),
            Box::new(LogUserSwapBalancesStartMigrationOperation),
            Box::new(TakeOrderMigrationOperation),
            Box::new(UpdateGlobalConfigMigrationOperation),
            Box::new(UpdateGlobalConfigAdminMigrationOperation),
            Box::new(UpdateOrderMigrationOperation),
            Box::new(WithdrawHostTipMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct KaminoLimitOrderInstructionWithMetadata(
    pub KaminoLimitOrderInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        KaminoLimitOrderInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for KaminoLimitOrderInstructionWithMetadata
{
    fn from(
        value: (
            KaminoLimitOrderInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        KaminoLimitOrderInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for KaminoLimitOrderInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoLimitOrderInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            KaminoLimitOrderInstruction::InitializeGlobalConfig { data, .. } => {
                let row = initialize_global_config_row::InitializeGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::InitializeVault { data, .. } => {
                let row = initialize_vault_row::InitializeVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CreateOrder { data, .. } => {
                let row = create_order_row::CreateOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateOrder { data, .. } => {
                let row = update_order_row::UpdateOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CloseOrderAndClaimTip { data, .. } => {
                let row = close_order_and_claim_tip_row::CloseOrderAndClaimTipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::TakeOrder { data, .. } => {
                let row = take_order_row::TakeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::FlashTakeOrderStart { data, .. } => {
                let row = flash_take_order_start_row::FlashTakeOrderStartRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::FlashTakeOrderEnd { data, .. } => {
                let row = flash_take_order_end_row::FlashTakeOrderEndRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::WithdrawHostTip { data, .. } => {
                let row = withdraw_host_tip_row::WithdrawHostTipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::LogUserSwapBalancesStart { data, .. } => {
                let row = log_user_swap_balances_start_row::LogUserSwapBalancesStartRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::LogUserSwapBalancesEnd { data, .. } => {
                let row = log_user_swap_balances_end_row::LogUserSwapBalancesEndRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::AssertUserSwapBalancesStart { data, .. } => {
                let row =
                    assert_user_swap_balances_start_row::AssertUserSwapBalancesStartRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::AssertUserSwapBalancesEnd { data, .. } => {
                let row =
                    assert_user_swap_balances_end_row::AssertUserSwapBalancesEndRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for KaminoLimitOrderInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoLimitOrderInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            KaminoLimitOrderInstruction::InitializeGlobalConfig { data, .. } => {
                let row = initialize_global_config_row::InitializeGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::InitializeVault { data, .. } => {
                let row = initialize_vault_row::InitializeVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CreateOrder { data, .. } => {
                let row = create_order_row::CreateOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateOrder { data, .. } => {
                let row = update_order_row::UpdateOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CloseOrderAndClaimTip { data, .. } => {
                let row = close_order_and_claim_tip_row::CloseOrderAndClaimTipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::TakeOrder { data, .. } => {
                let row = take_order_row::TakeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::FlashTakeOrderStart { data, .. } => {
                let row = flash_take_order_start_row::FlashTakeOrderStartRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::FlashTakeOrderEnd { data, .. } => {
                let row = flash_take_order_end_row::FlashTakeOrderEndRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::WithdrawHostTip { data, .. } => {
                let row = withdraw_host_tip_row::WithdrawHostTipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::LogUserSwapBalancesStart { data, .. } => {
                let row = log_user_swap_balances_start_row::LogUserSwapBalancesStartRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::LogUserSwapBalancesEnd { data, .. } => {
                let row = log_user_swap_balances_end_row::LogUserSwapBalancesEndRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::AssertUserSwapBalancesStart { data, .. } => {
                let row =
                    assert_user_swap_balances_start_row::AssertUserSwapBalancesStartRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::AssertUserSwapBalancesEnd { data, .. } => {
                let row =
                    assert_user_swap_balances_end_row::AssertUserSwapBalancesEndRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLimitOrderInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

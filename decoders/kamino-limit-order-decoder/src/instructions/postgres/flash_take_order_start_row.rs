//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    instruction::InstructionMetadata,
    postgres::{metadata::InstructionRowMetadata, primitives::U64},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct FlashTakeOrderStartRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub input_amount: U64,
    pub min_output_amount: U64,
    pub tip_amount_permissionless_taking: U64,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl FlashTakeOrderStartRow {
    pub fn from_parts(
        source: crate::instructions::flash_take_order_start::FlashTakeOrderStart,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            input_amount: source.input_amount.into(),
            min_output_amount: source.min_output_amount.into(),
            tip_amount_permissionless_taking: source.tip_amount_permissionless_taking.into(),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<FlashTakeOrderStartRow>
    for crate::instructions::flash_take_order_start::FlashTakeOrderStart
{
    type Error = carbon_core::error::Error;
    fn try_from(source: FlashTakeOrderStartRow) -> Result<Self, Self::Error> {
        Ok(Self {
            input_amount: *source.input_amount,
            min_output_amount: *source.min_output_amount,
            tip_amount_permissionless_taking: *source.tip_amount_permissionless_taking,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::flash_take_order_start::FlashTakeOrderStart
{
    fn table() -> &'static str {
        "flash_take_order_start_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "input_amount",
            "min_output_amount",
            "tip_amount_permissionless_taking",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for FlashTakeOrderStartRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO flash_take_order_start_instruction (
                "input_amount",
                "min_output_amount",
                "tip_amount_permissionless_taking",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(&self.input_amount)
        .bind(&self.min_output_amount)
        .bind(&self.tip_amount_permissionless_taking)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for FlashTakeOrderStartRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO flash_take_order_start_instruction (
                "input_amount",
                "min_output_amount",
                "tip_amount_permissionless_taking",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "input_amount" = EXCLUDED."input_amount",
                "min_output_amount" = EXCLUDED."min_output_amount",
                "tip_amount_permissionless_taking" = EXCLUDED."tip_amount_permissionless_taking",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(&self.input_amount)
        .bind(&self.min_output_amount)
        .bind(&self.tip_amount_permissionless_taking)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for FlashTakeOrderStartRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM flash_take_order_start_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for FlashTakeOrderStartRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM flash_take_order_start_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct FlashTakeOrderStartMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for FlashTakeOrderStartMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS flash_take_order_start_instruction (
                -- Instruction data
                "input_amount" NUMERIC(20) NOT NULL,
                "min_output_amount" NUMERIC(20) NOT NULL,
                "tip_amount_permissionless_taking" NUMERIC(20) NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS flash_take_order_start_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use crate::{ZapDecoder, PROGRAM_ID};

pub mod protocol_fee;
pub mod user_ledger;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum ZapAccount {
    ProtocolFee(Box<protocol_fee::ProtocolFee>),
    UserLedger(Box<user_ledger::UserLedger>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for ZapDecoder {
    type AccountType = ZapAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = protocol_fee::ProtocolFee::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ZapAccount::ProtocolFee(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = user_ledger::UserLedger::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: ZapAccount::UserLedger(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

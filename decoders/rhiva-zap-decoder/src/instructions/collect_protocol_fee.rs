//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::CollectProtocolFeeParameters,
    carbon_core::{account_utils::next_account, borsh, deserialize::ArrangeAccounts},
};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CollectProtocolFee {
    pub params: CollectProtocolFeeParameters,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CollectProtocolFeeInstructionAccounts {
    pub mint: solana_pubkey::Pubkey,
    pub protocol_fee: solana_pubkey::Pubkey,
    pub protocol_fee_vault: solana_pubkey::Pubkey,
    pub owner_token_account: solana_pubkey::Pubkey,
    pub owner: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CollectProtocolFee {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[136, 136, 252, 221, 194, 66, 126, 89] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CollectProtocolFee {
    type ArrangedAccounts = CollectProtocolFeeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let mint = next_account(&mut iter)?;
        let protocol_fee = next_account(&mut iter)?;
        let protocol_fee_vault = next_account(&mut iter)?;
        let owner_token_account = next_account(&mut iter)?;
        let owner = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CollectProtocolFeeInstructionAccounts {
            mint,
            protocol_fee,
            protocol_fee_vault,
            owner_token_account,
            owner,
            system_program,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

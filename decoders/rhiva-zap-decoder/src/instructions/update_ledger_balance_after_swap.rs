//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, borsh, deserialize::ArrangeAccounts};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateLedgerBalanceAfterSwap {
    pub pre_source_token_balance: u64,
    pub max_transfer_amount: u64,
    pub is_token_a: bool,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateLedgerBalanceAfterSwapInstructionAccounts {
    pub ledger: solana_pubkey::Pubkey,
    pub token_account: solana_pubkey::Pubkey,
    pub owner: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateLedgerBalanceAfterSwap {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[59, 206, 173, 232, 94, 57, 174, 202] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateLedgerBalanceAfterSwap {
    type ArrangedAccounts = UpdateLedgerBalanceAfterSwapInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let ledger = next_account(&mut iter)?;
        let token_account = next_account(&mut iter)?;
        let owner = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateLedgerBalanceAfterSwapInstructionAccounts {
            ledger,
            token_account,
            owner,
            remaining: remaining.to_vec(),
        })
    }
}

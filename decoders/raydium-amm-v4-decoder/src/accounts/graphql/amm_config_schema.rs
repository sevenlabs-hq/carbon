//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U64},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AmmConfig")]
pub struct AmmConfigGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub pnl_owner: Pubkey,
    pub cancel_owner: Pubkey,
    pub pending1: Vec<Vec<U64>>,
    pub pending2: Vec<Vec<U64>>,
    pub create_pool_fee: U64,
}

impl TryFrom<crate::accounts::postgres::AmmConfigRow> for AmmConfigGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::AmmConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            pnl_owner: carbon_core::graphql::primitives::Pubkey(row.pnl_owner.0),
            cancel_owner: carbon_core::graphql::primitives::Pubkey(row.cancel_owner.0),
            pending1: row
                .pending1
                .0
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(carbon_core::graphql::primitives::U64)
                        .collect()
                })
                .collect(),
            pending2: row
                .pending2
                .0
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(carbon_core::graphql::primitives::U64)
                        .collect()
                })
                .collect(),
            create_pool_fee: carbon_core::graphql::primitives::U64(*row.create_pool_fee),
        })
    }
}

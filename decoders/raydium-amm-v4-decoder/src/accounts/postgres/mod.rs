//! This code was AUTOGENERATED using the Codama library.
pub mod amm_info_row;
pub mod target_orders_row;

pub use self::{amm_info_row::*, target_orders_row::*};
use super::RaydiumAmmV4Account;

pub struct RaydiumAmmV4AccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaydiumAmmV4AccountsMigration {
    fn app(&self) -> &str {
        "raydium-amm-v4"
    }

    fn name(&self) -> &str {
        "raydium_amm_v4_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AmmInfoMigrationOperation),
            Box::new(TargetOrdersMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaydiumAmmV4AccountWithMetadata(
    pub RaydiumAmmV4Account,
    pub carbon_core::account::AccountMetadata,
);

impl From<(RaydiumAmmV4Account, carbon_core::account::AccountMetadata)>
    for RaydiumAmmV4AccountWithMetadata
{
    fn from(value: (RaydiumAmmV4Account, carbon_core::account::AccountMetadata)) -> Self {
        RaydiumAmmV4AccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaydiumAmmV4AccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumAmmV4AccountWithMetadata(account, metadata) = self;

        match account {
            RaydiumAmmV4Account::TargetOrders(account) => {
                let row = target_orders_row::TargetOrdersRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumAmmV4Account::AmmInfo(account) => {
                let row = amm_info_row::AmmInfoRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaydiumAmmV4AccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumAmmV4AccountWithMetadata(account, metadata) = self;
        match account {
            RaydiumAmmV4Account::TargetOrders(account) => {
                let row = target_orders_row::TargetOrdersRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumAmmV4Account::AmmInfo(account) => {
                let row = amm_info_row::AmmInfoRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::ConfigParam,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
    solana_pubkey::Pubkey,
};
/// Admin: update the AmmConfig. Payload after `param` depends on the variant:
/// - pnlOwner / cancelOrderOwner: 32-byte Pubkey
/// - createPoolFee: u64
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateConfigAccount {
    pub param: ConfigParam,
    pub owner: Option<Pubkey>,
    pub create_pool_fee: Option<u64>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateConfigAccountInstructionAccounts {
    pub admin: solana_pubkey::Pubkey,
    pub amm_config: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateConfigAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [15] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateConfigAccount {
    type ArrangedAccounts = UpdateConfigAccountInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin = next_account(&mut iter)?;
        let amm_config = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateConfigAccountInstructionAccounts {
            admin,
            amm_config,
            remaining: remaining.to_vec(),
        })
    }
}

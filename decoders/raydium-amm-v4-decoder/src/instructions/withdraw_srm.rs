//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Admin: withdraw (M)SRM held for fee discounts.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawSrm {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawSrmInstructionAccounts {
    pub token_program: solana_pubkey::Pubkey,
    pub amm: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub amm_authority: solana_pubkey::Pubkey,
    pub srm_from: solana_pubkey::Pubkey,
    pub srm_to: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawSrm {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [8] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawSrm {
    type ArrangedAccounts = WithdrawSrmInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let token_program = next_account(&mut iter)?;
        let amm = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let amm_authority = next_account(&mut iter)?;
        let srm_from = next_account(&mut iter)?;
        let srm_to = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawSrmInstructionAccounts {
            token_program,
            amm,
            admin,
            amm_authority,
            srm_from,
            srm_to,
            remaining: remaining.to_vec(),
        })
    }
}

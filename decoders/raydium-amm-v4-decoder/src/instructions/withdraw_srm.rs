//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawSrm {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawSrmInstructionAccounts {
    pub token_program: solana_pubkey::Pubkey,
    pub amm: solana_pubkey::Pubkey,
    pub amm_owner_account: solana_pubkey::Pubkey,
    pub amm_authority: solana_pubkey::Pubkey,
    pub srm_token: solana_pubkey::Pubkey,
    pub dest_srm_token: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawSrm {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [193, 101, 58, 65, 120, 78, 99, 31] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawSrm {
    type ArrangedAccounts = WithdrawSrmInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let token_program = next_account(&mut iter)?;
        let amm = next_account(&mut iter)?;
        let amm_owner_account = next_account(&mut iter)?;
        let amm_authority = next_account(&mut iter)?;
        let srm_token = next_account(&mut iter)?;
        let dest_srm_token = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawSrmInstructionAccounts {
            token_program,
            amm,
            amm_owner_account,
            amm_authority,
            srm_token,
            dest_srm_token,
            remaining: remaining.to_vec(),
        })
    }
}

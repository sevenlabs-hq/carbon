//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{SimulateParams, SwapInstructionBaseIn, SwapInstructionBaseOut},
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
/// Simulation-only instruction (used via simulateTransaction).
/// Payload after `param` depends on the variant:
/// - poolInfo / runCrankInfo: no extra payload
/// - swapBaseInInfo: amountIn(u64) + minimumAmountOut(u64)
/// - swapBaseOutInfo: maxAmountIn(u64) + amountOut(u64)
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SimulateInfo {
    pub param: SimulateParams,
    pub swap_base_in_value: Option<SwapInstructionBaseIn>,
    pub swap_base_out_value: Option<SwapInstructionBaseOut>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SimulateInfoInstructionAccounts {
    pub amm: solana_pubkey::Pubkey,
    pub amm_authority: solana_pubkey::Pubkey,
    pub amm_open_orders: solana_pubkey::Pubkey,
    pub amm_coin_vault: solana_pubkey::Pubkey,
    pub amm_pc_vault: solana_pubkey::Pubkey,
    pub amm_lp_mint: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub market_event_queue: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl SimulateInfo {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [12] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for SimulateInfo {
    type ArrangedAccounts = SimulateInfoInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let amm = next_account(&mut iter)?;
        let amm_authority = next_account(&mut iter)?;
        let amm_open_orders = next_account(&mut iter)?;
        let amm_coin_vault = next_account(&mut iter)?;
        let amm_pc_vault = next_account(&mut iter)?;
        let amm_lp_mint = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let market_event_queue = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(SimulateInfoInstructionAccounts {
            amm,
            amm_authority,
            amm_open_orders,
            amm_coin_vault,
            amm_pc_vault,
            amm_lp_mint,
            market,
            market_event_queue,
            remaining: remaining.to_vec(),
        })
    }
}

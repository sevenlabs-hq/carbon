//! This code was AUTOGENERATED using the Codama library.
pub mod admin_cancel_orders_row;
pub mod create_config_account_row;
pub mod deposit_row;
pub mod initialize2_row;
pub mod initialize_row;
pub mod migrate_to_open_book_row;
pub mod monitor_step_row;
pub mod pre_initialize_row;
pub mod set_params_row;
pub mod simulate_info_row;
pub mod swap_base_in_row;
pub mod swap_base_in_v2_row;
pub mod swap_base_out_row;
pub mod swap_base_out_v2_row;
pub mod update_config_account_row;
pub mod withdraw_pnl_row;
pub mod withdraw_row;
pub mod withdraw_srm_row;

pub use self::{
    admin_cancel_orders_row::*, create_config_account_row::*, deposit_row::*, initialize2_row::*,
    initialize_row::*, migrate_to_open_book_row::*, monitor_step_row::*, pre_initialize_row::*,
    set_params_row::*, simulate_info_row::*, swap_base_in_row::*, swap_base_in_v2_row::*,
    swap_base_out_row::*, swap_base_out_v2_row::*, update_config_account_row::*,
    withdraw_pnl_row::*, withdraw_row::*, withdraw_srm_row::*,
};
use super::RaydiumAmmV4Instruction;

pub struct RaydiumAmmV4InstructionsMigration;

impl carbon_core::clickhouse::Migration for RaydiumAmmV4InstructionsMigration {
    fn app(&self) -> &str {
        "raydium-amm-v4"
    }

    fn name(&self) -> &str {
        "raydium_amm_v4_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AdminCancelOrdersRowMigrationOperation),
            Box::new(CreateConfigAccountRowMigrationOperation),
            Box::new(DepositRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(Initialize2RowMigrationOperation),
            Box::new(MigrateToOpenBookRowMigrationOperation),
            Box::new(MonitorStepRowMigrationOperation),
            Box::new(PreInitializeRowMigrationOperation),
            Box::new(SetParamsRowMigrationOperation),
            Box::new(SimulateInfoRowMigrationOperation),
            Box::new(SwapBaseInRowMigrationOperation),
            Box::new(SwapBaseInV2RowMigrationOperation),
            Box::new(SwapBaseOutRowMigrationOperation),
            Box::new(SwapBaseOutV2RowMigrationOperation),
            Box::new(UpdateConfigAccountRowMigrationOperation),
            Box::new(WithdrawRowMigrationOperation),
            Box::new(WithdrawPnlRowMigrationOperation),
            Box::new(WithdrawSrmRowMigrationOperation),
        ]
    }
}

pub enum RaydiumAmmV4InstructionRow {
    AdminCancelOrders(AdminCancelOrdersRow),
    CreateConfigAccount(CreateConfigAccountRow),
    Deposit(DepositRow),
    Initialize(InitializeRow),
    Initialize2(Initialize2Row),
    MigrateToOpenBook(MigrateToOpenBookRow),
    MonitorStep(MonitorStepRow),
    PreInitialize(PreInitializeRow),
    SetParams(SetParamsRow),
    SimulateInfo(SimulateInfoRow),
    SwapBaseIn(SwapBaseInRow),
    SwapBaseInV2(SwapBaseInV2Row),
    SwapBaseOut(SwapBaseOutRow),
    SwapBaseOutV2(SwapBaseOutV2Row),
    UpdateConfigAccount(UpdateConfigAccountRow),
    Withdraw(WithdrawRow),
    WithdrawPnl(WithdrawPnlRow),
    WithdrawSrm(WithdrawSrmRow),
}

pub struct RaydiumAmmV4InstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a RaydiumAmmV4Instruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for RaydiumAmmV4InstructionMetadata<'a> {
    type Row = RaydiumAmmV4InstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let RaydiumAmmV4Instruction::$variant { data, accounts, .. } = instruction {
                    rows.push(RaydiumAmmV4InstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(AdminCancelOrders, AdminCancelOrdersRow);
        insert_branch!(CreateConfigAccount, CreateConfigAccountRow);
        insert_branch!(Deposit, DepositRow);
        insert_branch!(Initialize, InitializeRow);
        insert_branch!(Initialize2, Initialize2Row);
        insert_branch!(MigrateToOpenBook, MigrateToOpenBookRow);
        insert_branch!(MonitorStep, MonitorStepRow);
        insert_branch!(PreInitialize, PreInitializeRow);
        insert_branch!(SetParams, SetParamsRow);
        insert_branch!(SimulateInfo, SimulateInfoRow);
        insert_branch!(SwapBaseIn, SwapBaseInRow);
        insert_branch!(SwapBaseInV2, SwapBaseInV2Row);
        insert_branch!(SwapBaseOut, SwapBaseOutRow);
        insert_branch!(SwapBaseOutV2, SwapBaseOutV2Row);
        insert_branch!(UpdateConfigAccount, UpdateConfigAccountRow);
        insert_branch!(Withdraw, WithdrawRow);
        insert_branch!(WithdrawPnl, WithdrawPnlRow);
        insert_branch!(WithdrawSrm, WithdrawSrmRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for RaydiumAmmV4InstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(AdminCancelOrders, AdminCancelOrdersRow);
        commit_branch!(CreateConfigAccount, CreateConfigAccountRow);
        commit_branch!(Deposit, DepositRow);
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(Initialize2, Initialize2Row);
        commit_branch!(MigrateToOpenBook, MigrateToOpenBookRow);
        commit_branch!(MonitorStep, MonitorStepRow);
        commit_branch!(PreInitialize, PreInitializeRow);
        commit_branch!(SetParams, SetParamsRow);
        commit_branch!(SimulateInfo, SimulateInfoRow);
        commit_branch!(SwapBaseIn, SwapBaseInRow);
        commit_branch!(SwapBaseInV2, SwapBaseInV2Row);
        commit_branch!(SwapBaseOut, SwapBaseOutRow);
        commit_branch!(SwapBaseOutV2, SwapBaseOutV2Row);
        commit_branch!(UpdateConfigAccount, UpdateConfigAccountRow);
        commit_branch!(Withdraw, WithdrawRow);
        commit_branch!(WithdrawPnl, WithdrawPnlRow);
        commit_branch!(WithdrawSrm, WithdrawSrmRow);

        Ok(())
    }
}

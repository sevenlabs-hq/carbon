//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U64},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AmmConfig")]
pub struct AmmConfigGraphQL {
    pub pnl_owner: Pubkey,
    pub cancel_owner: Pubkey,
    pub pending1: Vec<U64>,
    pub pending2: Vec<U64>,
}

impl From<crate::types::AmmConfig> for AmmConfigGraphQL {
    fn from(original: crate::types::AmmConfig) -> Self {
        Self {
            pnl_owner: carbon_core::graphql::primitives::Pubkey(original.pnl_owner),
            cancel_owner: carbon_core::graphql::primitives::Pubkey(original.cancel_owner),
            pending1: original
                .pending1
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
            pending2: original
                .pending2
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
        }
    }
}

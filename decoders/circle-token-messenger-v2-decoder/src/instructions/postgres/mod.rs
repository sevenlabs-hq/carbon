//! This code was AUTOGENERATED using the Codama library.
pub mod accept_ownership_row;
pub mod add_local_token_row;
pub mod add_remote_token_messenger_row;
pub mod burn_token_custody_row;
pub mod cpi_event_row;
pub mod denylist_account_row;
pub mod deposit_for_burn_row;
pub mod deposit_for_burn_with_hook_row;
pub mod handle_receive_finalized_message_row;
pub mod handle_receive_unfinalized_message_row;
pub mod initialize_row;
pub mod link_token_pair_row;
pub mod pause_row;
pub mod remove_local_token_row;
pub mod remove_remote_token_messenger_row;
pub mod set_fee_recipient_row;
pub mod set_max_burn_amount_per_message_row;
pub mod set_min_fee_controller_row;
pub mod set_min_fee_row;
pub mod set_token_controller_row;
pub mod transfer_ownership_row;
pub mod undenylist_account_row;
pub mod unlink_token_pair_row;
pub mod unpause_row;
pub mod update_denylister_row;
pub mod update_pauser_row;

pub use self::{
    accept_ownership_row::*, add_local_token_row::*, add_remote_token_messenger_row::*,
    burn_token_custody_row::*, cpi_event_row::*, denylist_account_row::*, deposit_for_burn_row::*,
    deposit_for_burn_with_hook_row::*, handle_receive_finalized_message_row::*,
    handle_receive_unfinalized_message_row::*, initialize_row::*, link_token_pair_row::*,
    pause_row::*, remove_local_token_row::*, remove_remote_token_messenger_row::*,
    set_fee_recipient_row::*, set_max_burn_amount_per_message_row::*,
    set_min_fee_controller_row::*, set_min_fee_row::*, set_token_controller_row::*,
    transfer_ownership_row::*, undenylist_account_row::*, unlink_token_pair_row::*, unpause_row::*,
    update_denylister_row::*, update_pauser_row::*,
};
use super::CircleTokenMessengerV2Instruction;

pub struct CircleTokenMessengerV2InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for CircleTokenMessengerV2InstructionsMigration {
    fn app(&self) -> &str {
        "circle-token-messenger-v2"
    }

    fn name(&self) -> &str {
        "circle_token_messenger_v2_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptOwnershipMigrationOperation),
            Box::new(AddLocalTokenMigrationOperation),
            Box::new(AddRemoteTokenMessengerMigrationOperation),
            Box::new(BurnTokenCustodyMigrationOperation),
            Box::new(DenylistAccountMigrationOperation),
            Box::new(DepositForBurnMigrationOperation),
            Box::new(DepositForBurnWithHookMigrationOperation),
            Box::new(HandleReceiveFinalizedMessageMigrationOperation),
            Box::new(HandleReceiveUnfinalizedMessageMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(LinkTokenPairMigrationOperation),
            Box::new(PauseMigrationOperation),
            Box::new(RemoveLocalTokenMigrationOperation),
            Box::new(RemoveRemoteTokenMessengerMigrationOperation),
            Box::new(SetFeeRecipientMigrationOperation),
            Box::new(SetMaxBurnAmountPerMessageMigrationOperation),
            Box::new(SetMinFeeMigrationOperation),
            Box::new(SetMinFeeControllerMigrationOperation),
            Box::new(SetTokenControllerMigrationOperation),
            Box::new(TransferOwnershipMigrationOperation),
            Box::new(UndenylistAccountMigrationOperation),
            Box::new(UnlinkTokenPairMigrationOperation),
            Box::new(UnpauseMigrationOperation),
            Box::new(UpdateDenylisterMigrationOperation),
            Box::new(UpdatePauserMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct CircleTokenMessengerV2InstructionWithMetadata(
    pub CircleTokenMessengerV2Instruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        CircleTokenMessengerV2Instruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for CircleTokenMessengerV2InstructionWithMetadata
{
    fn from(
        value: (
            CircleTokenMessengerV2Instruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        CircleTokenMessengerV2InstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CircleTokenMessengerV2InstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleTokenMessengerV2InstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            CircleTokenMessengerV2Instruction::AcceptOwnership { data, .. } => {
                let row = accept_ownership_row::AcceptOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::AddLocalToken { data, .. } => {
                let row = add_local_token_row::AddLocalTokenRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::AddRemoteTokenMessenger { data, .. } => {
                let row = add_remote_token_messenger_row::AddRemoteTokenMessengerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::BurnTokenCustody { data, .. } => {
                let row = burn_token_custody_row::BurnTokenCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DenylistAccount { data, .. } => {
                let row = denylist_account_row::DenylistAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DepositForBurn { data, .. } => {
                let row = deposit_for_burn_row::DepositForBurnRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DepositForBurnWithHook { data, .. } => {
                let row = deposit_for_burn_with_hook_row::DepositForBurnWithHookRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::HandleReceiveFinalizedMessage { data, .. } => {
                let row = handle_receive_finalized_message_row::HandleReceiveFinalizedMessageRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::HandleReceiveUnfinalizedMessage { data, .. } => {
                let row = handle_receive_unfinalized_message_row::HandleReceiveUnfinalizedMessageRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::LinkTokenPair { data, .. } => {
                let row = link_token_pair_row::LinkTokenPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::RemoveLocalToken { data, .. } => {
                let row = remove_local_token_row::RemoveLocalTokenRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::RemoveRemoteTokenMessenger { data, .. } => {
                let row =
                    remove_remote_token_messenger_row::RemoveRemoteTokenMessengerRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetFeeRecipient { data, .. } => {
                let row = set_fee_recipient_row::SetFeeRecipientRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMaxBurnAmountPerMessage { data, .. } => {
                let row =
                    set_max_burn_amount_per_message_row::SetMaxBurnAmountPerMessageRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMinFee { data, .. } => {
                let row = set_min_fee_row::SetMinFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMinFeeController { data, .. } => {
                let row = set_min_fee_controller_row::SetMinFeeControllerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetTokenController { data, .. } => {
                let row = set_token_controller_row::SetTokenControllerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UndenylistAccount { data, .. } => {
                let row = undenylist_account_row::UndenylistAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UnlinkTokenPair { data, .. } => {
                let row = unlink_token_pair_row::UnlinkTokenPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UpdateDenylister { data, .. } => {
                let row = update_denylister_row::UpdateDenylisterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UpdatePauser { data, .. } => {
                let row = update_pauser_row::UpdatePauserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CircleTokenMessengerV2InstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleTokenMessengerV2InstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            CircleTokenMessengerV2Instruction::AcceptOwnership { data, .. } => {
                let row = accept_ownership_row::AcceptOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::AddLocalToken { data, .. } => {
                let row = add_local_token_row::AddLocalTokenRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::AddRemoteTokenMessenger { data, .. } => {
                let row = add_remote_token_messenger_row::AddRemoteTokenMessengerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::BurnTokenCustody { data, .. } => {
                let row = burn_token_custody_row::BurnTokenCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DenylistAccount { data, .. } => {
                let row = denylist_account_row::DenylistAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DepositForBurn { data, .. } => {
                let row = deposit_for_burn_row::DepositForBurnRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::DepositForBurnWithHook { data, .. } => {
                let row = deposit_for_burn_with_hook_row::DepositForBurnWithHookRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::HandleReceiveFinalizedMessage { data, .. } => {
                let row = handle_receive_finalized_message_row::HandleReceiveFinalizedMessageRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::HandleReceiveUnfinalizedMessage { data, .. } => {
                let row = handle_receive_unfinalized_message_row::HandleReceiveUnfinalizedMessageRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::LinkTokenPair { data, .. } => {
                let row = link_token_pair_row::LinkTokenPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::RemoveLocalToken { data, .. } => {
                let row = remove_local_token_row::RemoveLocalTokenRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::RemoveRemoteTokenMessenger { data, .. } => {
                let row =
                    remove_remote_token_messenger_row::RemoveRemoteTokenMessengerRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetFeeRecipient { data, .. } => {
                let row = set_fee_recipient_row::SetFeeRecipientRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMaxBurnAmountPerMessage { data, .. } => {
                let row =
                    set_max_burn_amount_per_message_row::SetMaxBurnAmountPerMessageRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMinFee { data, .. } => {
                let row = set_min_fee_row::SetMinFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetMinFeeController { data, .. } => {
                let row = set_min_fee_controller_row::SetMinFeeControllerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::SetTokenController { data, .. } => {
                let row = set_token_controller_row::SetTokenControllerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UndenylistAccount { data, .. } => {
                let row = undenylist_account_row::UndenylistAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UnlinkTokenPair { data, .. } => {
                let row = unlink_token_pair_row::UnlinkTokenPairRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UpdateDenylister { data, .. } => {
                let row = update_denylister_row::UpdateDenylisterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::UpdatePauser { data, .. } => {
                let row = update_pauser_row::UpdatePauserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Instruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

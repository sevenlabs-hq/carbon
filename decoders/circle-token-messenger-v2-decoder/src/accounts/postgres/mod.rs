//! This code was AUTOGENERATED using the Codama library.
pub mod denylisted_account_row;
pub mod local_token_row;
pub mod message_transmitter_row;
pub mod remote_token_messenger_row;
pub mod token_messenger_row;
pub mod token_minter_row;
pub mod token_pair_row;

pub use self::{
    denylisted_account_row::*, local_token_row::*, message_transmitter_row::*,
    remote_token_messenger_row::*, token_messenger_row::*, token_minter_row::*, token_pair_row::*,
};
use super::CircleTokenMessengerV2Account;

pub struct CircleTokenMessengerV2AccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for CircleTokenMessengerV2AccountsMigration {
    fn app(&self) -> &str {
        "circle-token-messenger-v2"
    }

    fn name(&self) -> &str {
        "circle_token_messenger_v2_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(DenylistedAccountMigrationOperation),
            Box::new(LocalTokenMigrationOperation),
            Box::new(MessageTransmitterMigrationOperation),
            Box::new(RemoteTokenMessengerMigrationOperation),
            Box::new(TokenMessengerMigrationOperation),
            Box::new(TokenMinterMigrationOperation),
            Box::new(TokenPairMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct CircleTokenMessengerV2AccountWithMetadata(
    pub CircleTokenMessengerV2Account,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        CircleTokenMessengerV2Account,
        carbon_core::account::AccountMetadata,
    )> for CircleTokenMessengerV2AccountWithMetadata
{
    fn from(
        value: (
            CircleTokenMessengerV2Account,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        CircleTokenMessengerV2AccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CircleTokenMessengerV2AccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleTokenMessengerV2AccountWithMetadata(account, metadata) = self;

        match account {
            CircleTokenMessengerV2Account::DenylistedAccount(account) => {
                let row = denylisted_account_row::DenylistedAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::LocalToken(account) => {
                let row =
                    local_token_row::LocalTokenRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::MessageTransmitter(account) => {
                let row = message_transmitter_row::MessageTransmitterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::RemoteTokenMessenger(account) => {
                let row = remote_token_messenger_row::RemoteTokenMessengerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenMessenger(account) => {
                let row = token_messenger_row::TokenMessengerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenMinter(account) => {
                let row = token_minter_row::TokenMinterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenPair(account) => {
                let row =
                    token_pair_row::TokenPairRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CircleTokenMessengerV2AccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleTokenMessengerV2AccountWithMetadata(account, metadata) = self;
        match account {
            CircleTokenMessengerV2Account::DenylistedAccount(account) => {
                let row = denylisted_account_row::DenylistedAccountRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::LocalToken(account) => {
                let row =
                    local_token_row::LocalTokenRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::MessageTransmitter(account) => {
                let row = message_transmitter_row::MessageTransmitterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::RemoteTokenMessenger(account) => {
                let row = remote_token_messenger_row::RemoteTokenMessengerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenMessenger(account) => {
                let row = token_messenger_row::TokenMessengerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenMinter(account) => {
                let row = token_minter_row::TokenMinterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleTokenMessengerV2Account::TokenPair(account) => {
                let row =
                    token_pair_row::TokenPairRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

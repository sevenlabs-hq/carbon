//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U32},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct RemoteTokenMessengerRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub domain: U32,
    pub token_messenger: Pubkey,
}

impl RemoteTokenMessengerRow {
    pub fn from_parts(
        source: crate::accounts::remote_token_messenger::RemoteTokenMessenger,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            domain: source.domain.into(),
            token_messenger: source.token_messenger.into(),
        }
    }
}

impl TryFrom<RemoteTokenMessengerRow>
    for crate::accounts::remote_token_messenger::RemoteTokenMessenger
{
    type Error = carbon_core::error::Error;
    fn try_from(source: RemoteTokenMessengerRow) -> Result<Self, Self::Error> {
        Ok(Self {
            domain: source.domain.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            token_messenger: *source.token_messenger,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::remote_token_messenger::RemoteTokenMessenger
{
    fn table() -> &'static str {
        "remote_token_messenger_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "domain", "token_messenger"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RemoteTokenMessengerRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO remote_token_messenger_account (
                "domain",
                "token_messenger",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            )"#,
        )
        .bind(self.domain)
        .bind(self.token_messenger)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RemoteTokenMessengerRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO remote_token_messenger_account (
                "domain",
                "token_messenger",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "domain" = EXCLUDED."domain",
                "token_messenger" = EXCLUDED."token_messenger",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.domain)
        .bind(self.token_messenger)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for RemoteTokenMessengerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM remote_token_messenger_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for RemoteTokenMessengerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM remote_token_messenger_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct RemoteTokenMessengerMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for RemoteTokenMessengerMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS remote_token_messenger_account (
                -- Account data
                "domain" INT8 NOT NULL,
                "token_messenger" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS remote_token_messenger_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

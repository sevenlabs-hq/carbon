//! This code was AUTOGENERATED using the Codama library.
use crate::{CircleTokenMessengerV2Decoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod denylisted_account;
pub mod local_token;
pub mod message_transmitter;
pub mod remote_token_messenger;
pub mod token_messenger;
pub mod token_minter;
pub mod token_pair;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum CircleTokenMessengerV2Account {
    DenylistedAccount(Box<denylisted_account::DenylistedAccount>),
    LocalToken(Box<local_token::LocalToken>),
    MessageTransmitter(Box<message_transmitter::MessageTransmitter>),
    RemoteTokenMessenger(Box<remote_token_messenger::RemoteTokenMessenger>),
    TokenMessenger(Box<token_messenger::TokenMessenger>),
    TokenMinter(Box<token_minter::TokenMinter>),
    TokenPair(Box<token_pair::TokenPair>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for CircleTokenMessengerV2Decoder {
    type AccountType = CircleTokenMessengerV2Account;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = denylisted_account::DenylistedAccount::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::DenylistedAccount(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = local_token::LocalToken::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::LocalToken(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = message_transmitter::MessageTransmitter::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::MessageTransmitter(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = remote_token_messenger::RemoteTokenMessenger::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::RemoteTokenMessenger(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = token_messenger::TokenMessenger::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::TokenMessenger(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = token_minter::TokenMinter::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::TokenMinter(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = token_pair::TokenPair::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleTokenMessengerV2Account::TokenPair(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

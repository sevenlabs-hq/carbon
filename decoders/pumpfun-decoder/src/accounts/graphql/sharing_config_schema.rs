//! This code was AUTOGENERATED using the Codama library.
use carbon_core::graphql::primitives::Pubkey;
use carbon_core::graphql::primitives::U8;
use crate::types::graphql::ConfigStatusGraphQL;
use crate::types::graphql::ShareholderGraphQL;
use juniper::GraphQLObject;

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "SharingConfig")]
pub struct SharingConfigGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub bump: U8,
    pub version: U8,
    pub status: ConfigStatusGraphQL,
    pub mint: Pubkey,
    pub admin: Pubkey,
    pub admin_revoked: bool,
    pub shareholders: Vec<ShareholderGraphQL>,
}

impl TryFrom<crate::accounts::postgres::SharingConfigRow> for SharingConfigGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::SharingConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            bump: carbon_core::graphql::primitives::U8((*row.bump) as u8),
            version: carbon_core::graphql::primitives::U8((*row.version) as u8),
            status: row.status.0.into(),
            mint: carbon_core::graphql::primitives::Pubkey(row.mint.0),
            admin: carbon_core::graphql::primitives::Pubkey(row.admin.0),
            admin_revoked: row.admin_revoked,
            shareholders: row.shareholders.0.into_iter().map(|item| item.into()).collect(),
        })
    }
}

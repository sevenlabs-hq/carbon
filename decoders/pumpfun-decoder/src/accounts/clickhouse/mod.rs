//! This code was AUTOGENERATED using the Codama library.
pub mod bonding_curve_row;
pub mod fee_config_row;
pub mod global_row;
pub mod global_volume_accumulator_row;
pub mod sharing_config_row;
pub mod user_volume_accumulator_row;

pub use self::{
    bonding_curve_row::*, fee_config_row::*, global_row::*, global_volume_accumulator_row::*,
    sharing_config_row::*, user_volume_accumulator_row::*,
};
use super::PumpfunAccount;

pub struct PumpfunAccountsMigration;

impl carbon_core::clickhouse::Migration for PumpfunAccountsMigration {
    fn app(&self) -> &str {
        "pumpfun"
    }

    fn name(&self) -> &str {
        "pumpfun_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BondingCurveRowMigrationOperation),
            Box::new(FeeConfigRowMigrationOperation),
            Box::new(GlobalRowMigrationOperation),
            Box::new(GlobalVolumeAccumulatorRowMigrationOperation),
            Box::new(SharingConfigRowMigrationOperation),
            Box::new(UserVolumeAccumulatorRowMigrationOperation),
        ]
    }
}

pub enum PumpfunAccountRow {
    BondingCurve(BondingCurveRow),
    FeeConfig(FeeConfigRow),
    Global(GlobalRow),
    GlobalVolumeAccumulator(GlobalVolumeAccumulatorRow),
    SharingConfig(SharingConfigRow),
    UserVolumeAccumulator(UserVolumeAccumulatorRow),
}

pub struct PumpfunAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a PumpfunAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for PumpfunAccountMetadata<'a> {
    type Row = PumpfunAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let PumpfunAccount::$variant(account) = account {
                    rows.push(PumpfunAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let PumpfunAccount::$variant(account) = account {
                    rows.push(PumpfunAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(BondingCurve, BondingCurveRow, boxed);
        insert_branch!(FeeConfig, FeeConfigRow, boxed);
        insert_branch!(Global, GlobalRow, boxed);
        insert_branch!(GlobalVolumeAccumulator, GlobalVolumeAccumulatorRow, boxed);
        insert_branch!(SharingConfig, SharingConfigRow, boxed);
        insert_branch!(UserVolumeAccumulator, UserVolumeAccumulatorRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for PumpfunAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(BondingCurve, BondingCurveRow);
        commit_branch!(FeeConfig, FeeConfigRow);
        commit_branch!(Global, GlobalRow);
        commit_branch!(GlobalVolumeAccumulator, GlobalVolumeAccumulatorRow);
        commit_branch!(SharingConfig, SharingConfigRow);
        commit_branch!(UserVolumeAccumulator, UserVolumeAccumulatorRow);

        Ok(())
    }
}

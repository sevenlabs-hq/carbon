//! This code was AUTOGENERATED using the Codama library.
pub mod add_strategy_row;
pub mod collect_dust_row;
pub mod cpi_event_row;
pub mod deposit_row;
pub mod deposit_strategy_row;
pub mod enable_vault_row;
pub mod initialize_row;
pub mod initialize_strategy_row;
pub mod remove_strategy2_row;
pub mod remove_strategy_row;
pub mod set_operator_row;
pub mod withdraw2_row;
pub mod withdraw_directly_from_strategy_row;
pub mod withdraw_row;
pub mod withdraw_strategy_row;

pub use self::{
    add_strategy_row::*, collect_dust_row::*, cpi_event_row::*, deposit_row::*,
    deposit_strategy_row::*, enable_vault_row::*, initialize_row::*, initialize_strategy_row::*,
    remove_strategy2_row::*, remove_strategy_row::*, set_operator_row::*, withdraw2_row::*,
    withdraw_directly_from_strategy_row::*, withdraw_row::*, withdraw_strategy_row::*,
};
use super::MeteoraVaultInstruction;

pub struct MeteoraVaultInstructionsMigration;

impl carbon_core::clickhouse::Migration for MeteoraVaultInstructionsMigration {
    fn app(&self) -> &str {
        "meteora-vault"
    }

    fn name(&self) -> &str {
        "meteora_vault_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AddStrategyRowMigrationOperation),
            Box::new(CollectDustRowMigrationOperation),
            Box::new(DepositRowMigrationOperation),
            Box::new(DepositStrategyRowMigrationOperation),
            Box::new(EnableVaultRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(InitializeStrategyRowMigrationOperation),
            Box::new(RemoveStrategyRowMigrationOperation),
            Box::new(RemoveStrategy2RowMigrationOperation),
            Box::new(SetOperatorRowMigrationOperation),
            Box::new(WithdrawRowMigrationOperation),
            Box::new(Withdraw2RowMigrationOperation),
            Box::new(WithdrawDirectlyFromStrategyRowMigrationOperation),
            Box::new(WithdrawStrategyRowMigrationOperation),
            Box::new(CpiEventRowMigrationOperation),
        ]
    }
}

pub enum MeteoraVaultInstructionRow {
    AddStrategy(AddStrategyRow),
    CollectDust(CollectDustRow),
    Deposit(DepositRow),
    DepositStrategy(DepositStrategyRow),
    EnableVault(EnableVaultRow),
    Initialize(InitializeRow),
    InitializeStrategy(InitializeStrategyRow),
    RemoveStrategy(RemoveStrategyRow),
    RemoveStrategy2(RemoveStrategy2Row),
    SetOperator(SetOperatorRow),
    Withdraw(WithdrawRow),
    Withdraw2(Withdraw2Row),
    WithdrawDirectlyFromStrategy(WithdrawDirectlyFromStrategyRow),
    WithdrawStrategy(WithdrawStrategyRow),
    CpiEvent(CpiEventRow),
}

pub struct MeteoraVaultInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a MeteoraVaultInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for MeteoraVaultInstructionMetadata<'a> {
    type Row = MeteoraVaultInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let MeteoraVaultInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(MeteoraVaultInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(AddStrategy, AddStrategyRow);
        insert_branch!(CollectDust, CollectDustRow);
        insert_branch!(Deposit, DepositRow);
        insert_branch!(DepositStrategy, DepositStrategyRow);
        insert_branch!(EnableVault, EnableVaultRow);
        insert_branch!(Initialize, InitializeRow);
        insert_branch!(InitializeStrategy, InitializeStrategyRow);
        insert_branch!(RemoveStrategy, RemoveStrategyRow);
        insert_branch!(RemoveStrategy2, RemoveStrategy2Row);
        insert_branch!(SetOperator, SetOperatorRow);
        insert_branch!(Withdraw, WithdrawRow);
        insert_branch!(Withdraw2, Withdraw2Row);
        insert_branch!(
            WithdrawDirectlyFromStrategy,
            WithdrawDirectlyFromStrategyRow
        );
        insert_branch!(WithdrawStrategy, WithdrawStrategyRow);
        insert_branch!(CpiEvent, CpiEventRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MeteoraVaultInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(AddStrategy, AddStrategyRow);
        commit_branch!(CollectDust, CollectDustRow);
        commit_branch!(Deposit, DepositRow);
        commit_branch!(DepositStrategy, DepositStrategyRow);
        commit_branch!(EnableVault, EnableVaultRow);
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(InitializeStrategy, InitializeStrategyRow);
        commit_branch!(RemoveStrategy, RemoveStrategyRow);
        commit_branch!(RemoveStrategy2, RemoveStrategy2Row);
        commit_branch!(SetOperator, SetOperatorRow);
        commit_branch!(Withdraw, WithdrawRow);
        commit_branch!(Withdraw2, Withdraw2Row);
        commit_branch!(
            WithdrawDirectlyFromStrategy,
            WithdrawDirectlyFromStrategyRow
        );
        commit_branch!(WithdrawStrategy, WithdrawStrategyRow);
        commit_branch!(CpiEvent, CpiEventRow);

        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DeactivateDelinquent {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DeactivateDelinquentInstructionAccounts {
    pub stake: solana_pubkey::Pubkey,
    pub delinquent_vote: solana_pubkey::Pubkey,
    pub reference_vote: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DeactivateDelinquent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [14] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for DeactivateDelinquent {
    type ArrangedAccounts = DeactivateDelinquentInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let stake = next_account(&mut iter)?;
        let delinquent_vote = next_account(&mut iter)?;
        let reference_vote = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DeactivateDelinquentInstructionAccounts {
            stake,
            delinquent_vote,
            reference_vote,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod authorize_checked_row;
pub mod authorize_checked_with_seed_row;
pub mod authorize_row;
pub mod authorize_with_seed_row;
pub mod deactivate_delinquent_row;
pub mod deactivate_row;
pub mod delegate_stake_row;
pub mod get_minimum_delegation_row;
pub mod initialize_checked_row;
pub mod initialize_row;
pub mod merge_row;
pub mod move_lamports_row;
pub mod move_stake_row;
pub mod redelegate_row;
pub mod set_lockup_checked_row;
pub mod set_lockup_row;
pub mod split_row;
pub mod withdraw_row;

pub use self::{
    authorize_checked_row::*, authorize_checked_with_seed_row::*, authorize_row::*,
    authorize_with_seed_row::*, deactivate_delinquent_row::*, deactivate_row::*,
    delegate_stake_row::*, get_minimum_delegation_row::*, initialize_checked_row::*,
    initialize_row::*, merge_row::*, move_lamports_row::*, move_stake_row::*, redelegate_row::*,
    set_lockup_checked_row::*, set_lockup_row::*, split_row::*, withdraw_row::*,
};
use super::StakeProgramInstruction;

pub struct StakeProgramInstructionsMigration;

impl carbon_core::clickhouse::Migration for StakeProgramInstructionsMigration {
    fn app(&self) -> &str {
        "stake-program"
    }

    fn name(&self) -> &str {
        "stake_program_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AuthorizeRowMigrationOperation),
            Box::new(AuthorizeCheckedRowMigrationOperation),
            Box::new(AuthorizeCheckedWithSeedRowMigrationOperation),
            Box::new(AuthorizeWithSeedRowMigrationOperation),
            Box::new(DeactivateRowMigrationOperation),
            Box::new(DeactivateDelinquentRowMigrationOperation),
            Box::new(DelegateStakeRowMigrationOperation),
            Box::new(GetMinimumDelegationRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(InitializeCheckedRowMigrationOperation),
            Box::new(MergeRowMigrationOperation),
            Box::new(MoveLamportsRowMigrationOperation),
            Box::new(MoveStakeRowMigrationOperation),
            Box::new(RedelegateRowMigrationOperation),
            Box::new(SetLockupRowMigrationOperation),
            Box::new(SetLockupCheckedRowMigrationOperation),
            Box::new(SplitRowMigrationOperation),
            Box::new(WithdrawRowMigrationOperation),
        ]
    }
}

pub enum StakeProgramInstructionRow {
    Authorize(AuthorizeRow),
    AuthorizeChecked(AuthorizeCheckedRow),
    AuthorizeCheckedWithSeed(AuthorizeCheckedWithSeedRow),
    AuthorizeWithSeed(AuthorizeWithSeedRow),
    Deactivate(DeactivateRow),
    DeactivateDelinquent(DeactivateDelinquentRow),
    DelegateStake(DelegateStakeRow),
    GetMinimumDelegation(GetMinimumDelegationRow),
    Initialize(InitializeRow),
    InitializeChecked(InitializeCheckedRow),
    Merge(MergeRow),
    MoveLamports(MoveLamportsRow),
    MoveStake(MoveStakeRow),
    Redelegate(RedelegateRow),
    SetLockup(SetLockupRow),
    SetLockupChecked(SetLockupCheckedRow),
    Split(SplitRow),
    Withdraw(WithdrawRow),
}

pub struct StakeProgramInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a StakeProgramInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for StakeProgramInstructionMetadata<'a> {
    type Row = StakeProgramInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let StakeProgramInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(StakeProgramInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Authorize, AuthorizeRow);
        insert_branch!(AuthorizeChecked, AuthorizeCheckedRow);
        insert_branch!(AuthorizeCheckedWithSeed, AuthorizeCheckedWithSeedRow);
        insert_branch!(AuthorizeWithSeed, AuthorizeWithSeedRow);
        insert_branch!(Deactivate, DeactivateRow);
        insert_branch!(DeactivateDelinquent, DeactivateDelinquentRow);
        insert_branch!(DelegateStake, DelegateStakeRow);
        insert_branch!(GetMinimumDelegation, GetMinimumDelegationRow);
        insert_branch!(Initialize, InitializeRow);
        insert_branch!(InitializeChecked, InitializeCheckedRow);
        insert_branch!(Merge, MergeRow);
        insert_branch!(MoveLamports, MoveLamportsRow);
        insert_branch!(MoveStake, MoveStakeRow);
        insert_branch!(Redelegate, RedelegateRow);
        insert_branch!(SetLockup, SetLockupRow);
        insert_branch!(SetLockupChecked, SetLockupCheckedRow);
        insert_branch!(Split, SplitRow);
        insert_branch!(Withdraw, WithdrawRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for StakeProgramInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Authorize, AuthorizeRow);
        commit_branch!(AuthorizeChecked, AuthorizeCheckedRow);
        commit_branch!(AuthorizeCheckedWithSeed, AuthorizeCheckedWithSeedRow);
        commit_branch!(AuthorizeWithSeed, AuthorizeWithSeedRow);
        commit_branch!(Deactivate, DeactivateRow);
        commit_branch!(DeactivateDelinquent, DeactivateDelinquentRow);
        commit_branch!(DelegateStake, DelegateStakeRow);
        commit_branch!(GetMinimumDelegation, GetMinimumDelegationRow);
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(InitializeChecked, InitializeCheckedRow);
        commit_branch!(Merge, MergeRow);
        commit_branch!(MoveLamports, MoveLamportsRow);
        commit_branch!(MoveStake, MoveStakeRow);
        commit_branch!(Redelegate, RedelegateRow);
        commit_branch!(SetLockup, SetLockupRow);
        commit_branch!(SetLockupChecked, SetLockupCheckedRow);
        commit_branch!(Split, SplitRow);
        commit_branch!(Withdraw, WithdrawRow);

        Ok(())
    }
}

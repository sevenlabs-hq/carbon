//! This code was AUTOGENERATED using the Codama library.
pub mod add_custody_row;
pub mod add_liquidity2_row;
pub mod add_pool_row;
pub mod borrow_from_custody_row;
pub mod close_borrow_position_row;
pub mod close_position_request2_row;
pub mod close_position_request3_row;
pub mod cpi_event_row;
pub mod create_and_delegate_stake_account_row;
pub mod create_decrease_position_market_request_row;
pub mod create_decrease_position_request2_row;
pub mod create_increase_position_market_request_row;
pub mod create_token_ledger_row;
pub mod create_token_metadata_row;
pub mod decrease_position4_row;
pub mod decrease_position_with_internal_swap_row;
pub mod decrease_position_with_tpsl_and_internal_swap_row;
pub mod decrease_position_with_tpsl_row;
pub mod deposit_collateral_for_borrows_row;
pub mod get_add_liquidity_amount_and_fee2_row;
pub mod get_assets_under_management2_row;
pub mod get_remove_liquidity_amount_and_fee2_row;
pub mod increase_position4_row;
pub mod increase_position_pre_swap_row;
pub mod increase_position_with_internal_swap_row;
pub mod init_row;
pub mod instant_create_limit_order_row;
pub mod instant_create_tpsl_row;
pub mod instant_decrease_position2_row;
pub mod instant_decrease_position_row;
pub mod instant_increase_position_pre_swap_row;
pub mod instant_increase_position_row;
pub mod instant_update_limit_order_row;
pub mod instant_update_tpsl_row;
pub mod liquidate_borrow_position_row;
pub mod liquidate_full_position4_row;
pub mod operator_set_custody_config_row;
pub mod operator_set_pool_config_row;
pub mod partial_liquidate_borrow_position_row;
pub mod realloc_custody_row;
pub mod realloc_pool_row;
pub mod redeem_stake_row;
pub mod refresh_assets_under_management_row;
pub mod remove_liquidity2_row;
pub mod repay_to_custody_row;
pub mod set_custody_config_row;
pub mod set_max_global_sizes_row;
pub mod set_perpetuals_config_row;
pub mod set_pool_config_row;
pub mod set_test_time_row;
pub mod set_token_ledger_row;
pub mod swap2_row;
pub mod swap_with_token_ledger_row;
pub mod test_init_row;
pub mod transfer_admin_row;
pub mod unstake_row;
pub mod update_decrease_position_request2_row;
pub mod withdraw_collateral_for_borrows_row;
pub mod withdraw_fees2_row;
pub mod withdraw_stake_row;

pub use self::{
    add_custody_row::*, add_liquidity2_row::*, add_pool_row::*, borrow_from_custody_row::*,
    close_borrow_position_row::*, close_position_request2_row::*, close_position_request3_row::*,
    cpi_event_row::*, create_and_delegate_stake_account_row::*,
    create_decrease_position_market_request_row::*, create_decrease_position_request2_row::*,
    create_increase_position_market_request_row::*, create_token_ledger_row::*,
    create_token_metadata_row::*, decrease_position4_row::*,
    decrease_position_with_internal_swap_row::*,
    decrease_position_with_tpsl_and_internal_swap_row::*, decrease_position_with_tpsl_row::*,
    deposit_collateral_for_borrows_row::*, get_add_liquidity_amount_and_fee2_row::*,
    get_assets_under_management2_row::*, get_remove_liquidity_amount_and_fee2_row::*,
    increase_position4_row::*, increase_position_pre_swap_row::*,
    increase_position_with_internal_swap_row::*, init_row::*, instant_create_limit_order_row::*,
    instant_create_tpsl_row::*, instant_decrease_position2_row::*,
    instant_decrease_position_row::*, instant_increase_position_pre_swap_row::*,
    instant_increase_position_row::*, instant_update_limit_order_row::*,
    instant_update_tpsl_row::*, liquidate_borrow_position_row::*, liquidate_full_position4_row::*,
    operator_set_custody_config_row::*, operator_set_pool_config_row::*,
    partial_liquidate_borrow_position_row::*, realloc_custody_row::*, realloc_pool_row::*,
    redeem_stake_row::*, refresh_assets_under_management_row::*, remove_liquidity2_row::*,
    repay_to_custody_row::*, set_custody_config_row::*, set_max_global_sizes_row::*,
    set_perpetuals_config_row::*, set_pool_config_row::*, set_test_time_row::*,
    set_token_ledger_row::*, swap2_row::*, swap_with_token_ledger_row::*, test_init_row::*,
    transfer_admin_row::*, unstake_row::*, update_decrease_position_request2_row::*,
    withdraw_collateral_for_borrows_row::*, withdraw_fees2_row::*, withdraw_stake_row::*,
};
use super::JupiterPerpetualsInstruction;

pub struct JupiterPerpetualsInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterPerpetualsInstructionsMigration {
    fn app(&self) -> &str {
        "jupiter-perpetuals"
    }

    fn name(&self) -> &str {
        "jupiter_perpetuals_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AddCustodyMigrationOperation),
            Box::new(AddLiquidity2MigrationOperation),
            Box::new(AddPoolMigrationOperation),
            Box::new(BorrowFromCustodyMigrationOperation),
            Box::new(CloseBorrowPositionMigrationOperation),
            Box::new(ClosePositionRequest2MigrationOperation),
            Box::new(ClosePositionRequest3MigrationOperation),
            Box::new(CreateAndDelegateStakeAccountMigrationOperation),
            Box::new(CreateDecreasePositionMarketRequestMigrationOperation),
            Box::new(CreateDecreasePositionRequest2MigrationOperation),
            Box::new(CreateIncreasePositionMarketRequestMigrationOperation),
            Box::new(CreateTokenLedgerMigrationOperation),
            Box::new(CreateTokenMetadataMigrationOperation),
            Box::new(DecreasePosition4MigrationOperation),
            Box::new(DecreasePositionWithInternalSwapMigrationOperation),
            Box::new(DecreasePositionWithTpslMigrationOperation),
            Box::new(DecreasePositionWithTpslAndInternalSwapMigrationOperation),
            Box::new(DepositCollateralForBorrowsMigrationOperation),
            Box::new(GetAddLiquidityAmountAndFee2MigrationOperation),
            Box::new(GetAssetsUnderManagement2MigrationOperation),
            Box::new(GetRemoveLiquidityAmountAndFee2MigrationOperation),
            Box::new(IncreasePosition4MigrationOperation),
            Box::new(IncreasePositionPreSwapMigrationOperation),
            Box::new(IncreasePositionWithInternalSwapMigrationOperation),
            Box::new(InitMigrationOperation),
            Box::new(InstantCreateLimitOrderMigrationOperation),
            Box::new(InstantCreateTpslMigrationOperation),
            Box::new(InstantDecreasePositionMigrationOperation),
            Box::new(InstantDecreasePosition2MigrationOperation),
            Box::new(InstantIncreasePositionMigrationOperation),
            Box::new(InstantIncreasePositionPreSwapMigrationOperation),
            Box::new(InstantUpdateLimitOrderMigrationOperation),
            Box::new(InstantUpdateTpslMigrationOperation),
            Box::new(LiquidateBorrowPositionMigrationOperation),
            Box::new(LiquidateFullPosition4MigrationOperation),
            Box::new(OperatorSetCustodyConfigMigrationOperation),
            Box::new(OperatorSetPoolConfigMigrationOperation),
            Box::new(PartialLiquidateBorrowPositionMigrationOperation),
            Box::new(ReallocCustodyMigrationOperation),
            Box::new(ReallocPoolMigrationOperation),
            Box::new(RedeemStakeMigrationOperation),
            Box::new(RefreshAssetsUnderManagementMigrationOperation),
            Box::new(RemoveLiquidity2MigrationOperation),
            Box::new(RepayToCustodyMigrationOperation),
            Box::new(SetCustodyConfigMigrationOperation),
            Box::new(SetMaxGlobalSizesMigrationOperation),
            Box::new(SetPerpetualsConfigMigrationOperation),
            Box::new(SetPoolConfigMigrationOperation),
            Box::new(SetTestTimeMigrationOperation),
            Box::new(SetTokenLedgerMigrationOperation),
            Box::new(Swap2MigrationOperation),
            Box::new(SwapWithTokenLedgerMigrationOperation),
            Box::new(TestInitMigrationOperation),
            Box::new(TransferAdminMigrationOperation),
            Box::new(UnstakeMigrationOperation),
            Box::new(UpdateDecreasePositionRequest2MigrationOperation),
            Box::new(WithdrawCollateralForBorrowsMigrationOperation),
            Box::new(WithdrawFees2MigrationOperation),
            Box::new(WithdrawStakeMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterPerpetualsInstructionWithMetadata(
    pub JupiterPerpetualsInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        JupiterPerpetualsInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for JupiterPerpetualsInstructionWithMetadata
{
    fn from(
        value: (
            JupiterPerpetualsInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        JupiterPerpetualsInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterPerpetualsInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterPerpetualsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterPerpetualsInstruction::Init { data, .. } => {
                let row = init_row::InitRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddPool { data, .. } => {
                let row = add_pool_row::AddPoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddCustody { data, .. } => {
                let row = add_custody_row::AddCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetCustodyConfig { data, .. } => {
                let row = set_custody_config_row::SetCustodyConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetPoolConfig { data, .. } => {
                let row = set_pool_config_row::SetPoolConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetPerpetualsConfig { data, .. } => {
                let row = set_perpetuals_config_row::SetPerpetualsConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::TransferAdmin { data, .. } => {
                let row = transfer_admin_row::TransferAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawFees2 { data, .. } => {
                let row = withdraw_fees2_row::WithdrawFees2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateTokenMetadata { data, .. } => {
                let row = create_token_metadata_row::CreateTokenMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateTokenLedger { data, .. } => {
                let row = create_token_ledger_row::CreateTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ReallocCustody { data, .. } => {
                let row = realloc_custody_row::ReallocCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ReallocPool { data, .. } => {
                let row = realloc_pool_row::ReallocPoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateAndDelegateStakeAccount { data, .. } => {
                let row = create_and_delegate_stake_account_row::CreateAndDelegateStakeAccountRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::Unstake { data, .. } => {
                let row = unstake_row::UnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawStake { data, .. } => {
                let row = withdraw_stake_row::WithdrawStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RedeemStake { data, .. } => {
                let row = redeem_stake_row::RedeemStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::OperatorSetCustodyConfig { data, .. } => {
                let row = operator_set_custody_config_row::OperatorSetCustodyConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::OperatorSetPoolConfig { data, .. } => {
                let row = operator_set_pool_config_row::OperatorSetPoolConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::TestInit { data, .. } => {
                let row = test_init_row::TestInitRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetTestTime { data, .. } => {
                let row = set_test_time_row::SetTestTimeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetTokenLedger { data, .. } => {
                let row = set_token_ledger_row::SetTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::Swap2 { data, .. } => {
                let row = swap2_row::Swap2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SwapWithTokenLedger { data, .. } => {
                let row = swap_with_token_ledger_row::SwapWithTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantIncreasePositionPreSwap { data, .. } => {
                let row = instant_increase_position_pre_swap_row::InstantIncreasePositionPreSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddLiquidity2 { data, .. } => {
                let row = add_liquidity2_row::AddLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RemoveLiquidity2 { data, .. } => {
                let row = remove_liquidity2_row::RemoveLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateIncreasePositionMarketRequest { data, .. } => {
                let row = create_increase_position_market_request_row::CreateIncreasePositionMarketRequestRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateDecreasePositionRequest2 { data, .. } => {
                let row = create_decrease_position_request2_row::CreateDecreasePositionRequest2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateDecreasePositionMarketRequest { data, .. } => {
                let row = create_decrease_position_market_request_row::CreateDecreasePositionMarketRequestRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::UpdateDecreasePositionRequest2 { data, .. } => {
                let row = update_decrease_position_request2_row::UpdateDecreasePositionRequest2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ClosePositionRequest2 { data, .. } => {
                let row = close_position_request2_row::ClosePositionRequest2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ClosePositionRequest3 { data, .. } => {
                let row = close_position_request3_row::ClosePositionRequest3Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePosition4 { data, .. } => {
                let row = increase_position4_row::IncreasePosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePositionPreSwap { data, .. } => {
                let row = increase_position_pre_swap_row::IncreasePositionPreSwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePositionWithInternalSwap { data, .. } => {
                let row = increase_position_with_internal_swap_row::IncreasePositionWithInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePosition4 { data, .. } => {
                let row = decrease_position4_row::DecreasePosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithInternalSwap { data, .. } => {
                let row = decrease_position_with_internal_swap_row::DecreasePositionWithInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithTpsl { data, .. } => {
                let row = decrease_position_with_tpsl_row::DecreasePositionWithTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithTpslAndInternalSwap {
                data, ..
            } => {
                let row = decrease_position_with_tpsl_and_internal_swap_row::DecreasePositionWithTpslAndInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::LiquidateFullPosition4 { data, .. } => {
                let row = liquidate_full_position4_row::LiquidateFullPosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RefreshAssetsUnderManagement { data, .. } => {
                let row = refresh_assets_under_management_row::RefreshAssetsUnderManagementRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetMaxGlobalSizes { data, .. } => {
                let row = set_max_global_sizes_row::SetMaxGlobalSizesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantCreateTpsl { data, .. } => {
                let row = instant_create_tpsl_row::InstantCreateTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantCreateLimitOrder { data, .. } => {
                let row = instant_create_limit_order_row::InstantCreateLimitOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantIncreasePosition { data, .. } => {
                let row = instant_increase_position_row::InstantIncreasePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantDecreasePosition { data, .. } => {
                let row = instant_decrease_position_row::InstantDecreasePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantDecreasePosition2 { data, .. } => {
                let row = instant_decrease_position2_row::InstantDecreasePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantUpdateLimitOrder { data, .. } => {
                let row = instant_update_limit_order_row::InstantUpdateLimitOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantUpdateTpsl { data, .. } => {
                let row = instant_update_tpsl_row::InstantUpdateTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetAddLiquidityAmountAndFee2 { data, .. } => {
                let row = get_add_liquidity_amount_and_fee2_row::GetAddLiquidityAmountAndFee2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetRemoveLiquidityAmountAndFee2 { data, .. } => {
                let row = get_remove_liquidity_amount_and_fee2_row::GetRemoveLiquidityAmountAndFee2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetAssetsUnderManagement2 { data, .. } => {
                let row =
                    get_assets_under_management2_row::GetAssetsUnderManagement2Row::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::BorrowFromCustody { data, .. } => {
                let row = borrow_from_custody_row::BorrowFromCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RepayToCustody { data, .. } => {
                let row = repay_to_custody_row::RepayToCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DepositCollateralForBorrows { data, .. } => {
                let row =
                    deposit_collateral_for_borrows_row::DepositCollateralForBorrowsRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawCollateralForBorrows { data, .. } => {
                let row = withdraw_collateral_for_borrows_row::WithdrawCollateralForBorrowsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::LiquidateBorrowPosition { data, .. } => {
                let row = liquidate_borrow_position_row::LiquidateBorrowPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::PartialLiquidateBorrowPosition { data, .. } => {
                let row = partial_liquidate_borrow_position_row::PartialLiquidateBorrowPositionRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CloseBorrowPosition { data, .. } => {
                let row = close_borrow_position_row::CloseBorrowPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterPerpetualsInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterPerpetualsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterPerpetualsInstruction::Init { data, .. } => {
                let row = init_row::InitRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddPool { data, .. } => {
                let row = add_pool_row::AddPoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddCustody { data, .. } => {
                let row = add_custody_row::AddCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetCustodyConfig { data, .. } => {
                let row = set_custody_config_row::SetCustodyConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetPoolConfig { data, .. } => {
                let row = set_pool_config_row::SetPoolConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetPerpetualsConfig { data, .. } => {
                let row = set_perpetuals_config_row::SetPerpetualsConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::TransferAdmin { data, .. } => {
                let row = transfer_admin_row::TransferAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawFees2 { data, .. } => {
                let row = withdraw_fees2_row::WithdrawFees2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateTokenMetadata { data, .. } => {
                let row = create_token_metadata_row::CreateTokenMetadataRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateTokenLedger { data, .. } => {
                let row = create_token_ledger_row::CreateTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ReallocCustody { data, .. } => {
                let row = realloc_custody_row::ReallocCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ReallocPool { data, .. } => {
                let row = realloc_pool_row::ReallocPoolRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateAndDelegateStakeAccount { data, .. } => {
                let row = create_and_delegate_stake_account_row::CreateAndDelegateStakeAccountRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::Unstake { data, .. } => {
                let row = unstake_row::UnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawStake { data, .. } => {
                let row = withdraw_stake_row::WithdrawStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RedeemStake { data, .. } => {
                let row = redeem_stake_row::RedeemStakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::OperatorSetCustodyConfig { data, .. } => {
                let row = operator_set_custody_config_row::OperatorSetCustodyConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::OperatorSetPoolConfig { data, .. } => {
                let row = operator_set_pool_config_row::OperatorSetPoolConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::TestInit { data, .. } => {
                let row = test_init_row::TestInitRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetTestTime { data, .. } => {
                let row = set_test_time_row::SetTestTimeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetTokenLedger { data, .. } => {
                let row = set_token_ledger_row::SetTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::Swap2 { data, .. } => {
                let row = swap2_row::Swap2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SwapWithTokenLedger { data, .. } => {
                let row = swap_with_token_ledger_row::SwapWithTokenLedgerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantIncreasePositionPreSwap { data, .. } => {
                let row = instant_increase_position_pre_swap_row::InstantIncreasePositionPreSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::AddLiquidity2 { data, .. } => {
                let row = add_liquidity2_row::AddLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RemoveLiquidity2 { data, .. } => {
                let row = remove_liquidity2_row::RemoveLiquidity2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateIncreasePositionMarketRequest { data, .. } => {
                let row = create_increase_position_market_request_row::CreateIncreasePositionMarketRequestRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateDecreasePositionRequest2 { data, .. } => {
                let row = create_decrease_position_request2_row::CreateDecreasePositionRequest2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CreateDecreasePositionMarketRequest { data, .. } => {
                let row = create_decrease_position_market_request_row::CreateDecreasePositionMarketRequestRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::UpdateDecreasePositionRequest2 { data, .. } => {
                let row = update_decrease_position_request2_row::UpdateDecreasePositionRequest2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ClosePositionRequest2 { data, .. } => {
                let row = close_position_request2_row::ClosePositionRequest2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::ClosePositionRequest3 { data, .. } => {
                let row = close_position_request3_row::ClosePositionRequest3Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePosition4 { data, .. } => {
                let row = increase_position4_row::IncreasePosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePositionPreSwap { data, .. } => {
                let row = increase_position_pre_swap_row::IncreasePositionPreSwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::IncreasePositionWithInternalSwap { data, .. } => {
                let row = increase_position_with_internal_swap_row::IncreasePositionWithInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePosition4 { data, .. } => {
                let row = decrease_position4_row::DecreasePosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithInternalSwap { data, .. } => {
                let row = decrease_position_with_internal_swap_row::DecreasePositionWithInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithTpsl { data, .. } => {
                let row = decrease_position_with_tpsl_row::DecreasePositionWithTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DecreasePositionWithTpslAndInternalSwap {
                data, ..
            } => {
                let row = decrease_position_with_tpsl_and_internal_swap_row::DecreasePositionWithTpslAndInternalSwapRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::LiquidateFullPosition4 { data, .. } => {
                let row = liquidate_full_position4_row::LiquidateFullPosition4Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RefreshAssetsUnderManagement { data, .. } => {
                let row = refresh_assets_under_management_row::RefreshAssetsUnderManagementRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::SetMaxGlobalSizes { data, .. } => {
                let row = set_max_global_sizes_row::SetMaxGlobalSizesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantCreateTpsl { data, .. } => {
                let row = instant_create_tpsl_row::InstantCreateTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantCreateLimitOrder { data, .. } => {
                let row = instant_create_limit_order_row::InstantCreateLimitOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantIncreasePosition { data, .. } => {
                let row = instant_increase_position_row::InstantIncreasePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantDecreasePosition { data, .. } => {
                let row = instant_decrease_position_row::InstantDecreasePositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantDecreasePosition2 { data, .. } => {
                let row = instant_decrease_position2_row::InstantDecreasePosition2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantUpdateLimitOrder { data, .. } => {
                let row = instant_update_limit_order_row::InstantUpdateLimitOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::InstantUpdateTpsl { data, .. } => {
                let row = instant_update_tpsl_row::InstantUpdateTpslRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetAddLiquidityAmountAndFee2 { data, .. } => {
                let row = get_add_liquidity_amount_and_fee2_row::GetAddLiquidityAmountAndFee2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetRemoveLiquidityAmountAndFee2 { data, .. } => {
                let row = get_remove_liquidity_amount_and_fee2_row::GetRemoveLiquidityAmountAndFee2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::GetAssetsUnderManagement2 { data, .. } => {
                let row =
                    get_assets_under_management2_row::GetAssetsUnderManagement2Row::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::BorrowFromCustody { data, .. } => {
                let row = borrow_from_custody_row::BorrowFromCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::RepayToCustody { data, .. } => {
                let row = repay_to_custody_row::RepayToCustodyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::DepositCollateralForBorrows { data, .. } => {
                let row =
                    deposit_collateral_for_borrows_row::DepositCollateralForBorrowsRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::WithdrawCollateralForBorrows { data, .. } => {
                let row = withdraw_collateral_for_borrows_row::WithdrawCollateralForBorrowsRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::LiquidateBorrowPosition { data, .. } => {
                let row = liquidate_borrow_position_row::LiquidateBorrowPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::PartialLiquidateBorrowPosition { data, .. } => {
                let row = partial_liquidate_borrow_position_row::PartialLiquidateBorrowPositionRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CloseBorrowPosition { data, .. } => {
                let row = close_borrow_position_row::CloseBorrowPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

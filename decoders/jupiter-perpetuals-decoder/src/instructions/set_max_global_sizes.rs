//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::SetMaxGlobalSizesParams,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SetMaxGlobalSizes {
    pub params: SetMaxGlobalSizesParams,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetMaxGlobalSizesInstructionAccounts {
    pub keeper: solana_pubkey::Pubkey,
    pub custody: solana_pubkey::Pubkey,
    pub pool: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl SetMaxGlobalSizes {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [89, 2, 210, 24, 167, 227, 13, 214] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for SetMaxGlobalSizes {
    type ArrangedAccounts = SetMaxGlobalSizesInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let keeper = next_account(&mut iter)?;
        let custody = next_account(&mut iter)?;
        let pool = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(SetMaxGlobalSizesInstructionAccounts {
            keeper,
            custody,
            pool,
            remaining: remaining.to_vec(),
        })
    }
}

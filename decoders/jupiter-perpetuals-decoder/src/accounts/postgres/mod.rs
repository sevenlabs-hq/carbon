//! This code was AUTOGENERATED using the Codama library.
pub mod borrow_position_row;
pub mod custody_row;
pub mod perpetuals_row;
pub mod pool_row;
pub mod position_request_row;
pub mod position_row;
pub mod stake_info_row;
pub mod token_ledger_row;

pub use self::{
    borrow_position_row::*, custody_row::*, perpetuals_row::*, pool_row::*,
    position_request_row::*, position_row::*, stake_info_row::*, token_ledger_row::*,
};
use super::JupiterPerpetualsAccount;

pub struct JupiterPerpetualsAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterPerpetualsAccountsMigration {
    fn app(&self) -> &str {
        "jupiter-perpetuals"
    }

    fn name(&self) -> &str {
        "jupiter_perpetuals_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(BorrowPositionMigrationOperation),
            Box::new(CustodyMigrationOperation),
            Box::new(PerpetualsMigrationOperation),
            Box::new(PoolMigrationOperation),
            Box::new(PositionMigrationOperation),
            Box::new(PositionRequestMigrationOperation),
            Box::new(StakeInfoMigrationOperation),
            Box::new(TokenLedgerMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterPerpetualsAccountWithMetadata(
    pub JupiterPerpetualsAccount,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        JupiterPerpetualsAccount,
        carbon_core::account::AccountMetadata,
    )> for JupiterPerpetualsAccountWithMetadata
{
    fn from(
        value: (
            JupiterPerpetualsAccount,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        JupiterPerpetualsAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterPerpetualsAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterPerpetualsAccountWithMetadata(account, metadata) = self;

        match account {
            JupiterPerpetualsAccount::BorrowPosition(account) => {
                let row = borrow_position_row::BorrowPositionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Custody(account) => {
                let row = custody_row::CustodyRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Perpetuals(account) => {
                let row =
                    perpetuals_row::PerpetualsRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::PositionRequest(account) => {
                let row = position_request_row::PositionRequestRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Position(account) => {
                let row = position_row::PositionRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::StakeInfo(account) => {
                let row =
                    stake_info_row::StakeInfoRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::TokenLedger(account) => {
                let row = token_ledger_row::TokenLedgerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterPerpetualsAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterPerpetualsAccountWithMetadata(account, metadata) = self;
        match account {
            JupiterPerpetualsAccount::BorrowPosition(account) => {
                let row = borrow_position_row::BorrowPositionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Custody(account) => {
                let row = custody_row::CustodyRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Perpetuals(account) => {
                let row =
                    perpetuals_row::PerpetualsRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::PositionRequest(account) => {
                let row = position_request_row::PositionRequestRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::Position(account) => {
                let row = position_row::PositionRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::StakeInfo(account) => {
                let row =
                    stake_info_row::StakeInfoRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterPerpetualsAccount::TokenLedger(account) => {
                let row = token_ledger_row::TokenLedgerRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

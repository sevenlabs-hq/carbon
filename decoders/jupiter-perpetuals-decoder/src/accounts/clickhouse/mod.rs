//! This code was AUTOGENERATED using the Codama library.
pub mod borrow_position_row;
pub mod custody_row;
pub mod perpetuals_row;
pub mod pool_row;
pub mod position_request_row;
pub mod position_row;
pub mod stake_info_row;
pub mod token_ledger_row;

pub use self::{
    borrow_position_row::*, custody_row::*, perpetuals_row::*, pool_row::*,
    position_request_row::*, position_row::*, stake_info_row::*, token_ledger_row::*,
};
use super::JupiterPerpetualsAccount;

pub struct JupiterPerpetualsAccountsMigration;

impl carbon_core::clickhouse::Migration for JupiterPerpetualsAccountsMigration {
    fn app(&self) -> &str {
        "jupiter-perpetuals"
    }

    fn name(&self) -> &str {
        "jupiter_perpetuals_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BorrowPositionRowMigrationOperation),
            Box::new(CustodyRowMigrationOperation),
            Box::new(PerpetualsRowMigrationOperation),
            Box::new(PoolRowMigrationOperation),
            Box::new(PositionRowMigrationOperation),
            Box::new(PositionRequestRowMigrationOperation),
            Box::new(StakeInfoRowMigrationOperation),
            Box::new(TokenLedgerRowMigrationOperation),
        ]
    }
}

pub enum JupiterPerpetualsAccountRow {
    BorrowPosition(BorrowPositionRow),
    Custody(CustodyRow),
    Perpetuals(PerpetualsRow),
    Pool(PoolRow),
    Position(PositionRow),
    PositionRequest(PositionRequestRow),
    StakeInfo(StakeInfoRow),
    TokenLedger(TokenLedgerRow),
}

pub struct JupiterPerpetualsAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a JupiterPerpetualsAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for JupiterPerpetualsAccountMetadata<'a> {
    type Row = JupiterPerpetualsAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let JupiterPerpetualsAccount::$variant(account) = account {
                    rows.push(JupiterPerpetualsAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let JupiterPerpetualsAccount::$variant(account) = account {
                    rows.push(JupiterPerpetualsAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(BorrowPosition, BorrowPositionRow, boxed);
        insert_branch!(Custody, CustodyRow, boxed);
        insert_branch!(Perpetuals, PerpetualsRow, boxed);
        insert_branch!(Pool, PoolRow, boxed);
        insert_branch!(Position, PositionRow, boxed);
        insert_branch!(PositionRequest, PositionRequestRow, boxed);
        insert_branch!(StakeInfo, StakeInfoRow, boxed);
        insert_branch!(TokenLedger, TokenLedgerRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for JupiterPerpetualsAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(BorrowPosition, BorrowPositionRow);
        commit_branch!(Custody, CustodyRow);
        commit_branch!(Perpetuals, PerpetualsRow);
        commit_branch!(Pool, PoolRow);
        commit_branch!(Position, PositionRow);
        commit_branch!(PositionRequest, PositionRequestRow);
        commit_branch!(StakeInfo, StakeInfoRow);
        commit_branch!(TokenLedger, TokenLedgerRow);

        Ok(())
    }
}

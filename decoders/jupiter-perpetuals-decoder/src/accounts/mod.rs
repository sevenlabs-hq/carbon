//! This code was AUTOGENERATED using the Codama library.
use crate::{JupiterPerpetualsDecoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod borrow_position;
pub mod custody;
pub mod perpetuals;
pub mod pool;
pub mod position;
pub mod position_request;
pub mod stake_info;
pub mod token_ledger;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum JupiterPerpetualsAccount {
    BorrowPosition(Box<borrow_position::BorrowPosition>),
    Custody(Box<custody::Custody>),
    Perpetuals(Box<perpetuals::Perpetuals>),
    Pool(Box<pool::Pool>),
    Position(Box<position::Position>),
    PositionRequest(Box<position_request::PositionRequest>),
    StakeInfo(Box<stake_info::StakeInfo>),
    TokenLedger(Box<token_ledger::TokenLedger>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for JupiterPerpetualsDecoder {
    type AccountType = JupiterPerpetualsAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = borrow_position::BorrowPosition::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::BorrowPosition(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = custody::Custody::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::Custody(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = perpetuals::Perpetuals::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::Perpetuals(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = pool::Pool::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::Pool(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = position_request::PositionRequest::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::PositionRequest(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = position::Position::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::Position(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = stake_info::StakeInfo::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::StakeInfo(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = token_ledger::TokenLedger::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: JupiterPerpetualsAccount::TokenLedger(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

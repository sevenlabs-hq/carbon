//! This code was AUTOGENERATED using the Codama library.
pub mod collect_clmm_fees_and_rewards_row;
pub mod collect_cp_fees_row;
pub mod cpi_event_row;
pub mod lock_clmm_position_row;
pub mod lock_cp_liquidity_row;

pub use self::{
    collect_clmm_fees_and_rewards_row::*, collect_cp_fees_row::*, cpi_event_row::*,
    lock_clmm_position_row::*, lock_cp_liquidity_row::*,
};
use super::RaydiumLiquidityLockingInstruction;

pub struct RaydiumLiquidityLockingInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaydiumLiquidityLockingInstructionsMigration {
    fn app(&self) -> &str {
        "raydium-liquidity-locking"
    }

    fn name(&self) -> &str {
        "raydium_liquidity_locking_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CollectClmmFeesAndRewardsMigrationOperation),
            Box::new(CollectCpFeesMigrationOperation),
            Box::new(LockClmmPositionMigrationOperation),
            Box::new(LockCpLiquidityMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaydiumLiquidityLockingInstructionWithMetadata(
    pub RaydiumLiquidityLockingInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        RaydiumLiquidityLockingInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for RaydiumLiquidityLockingInstructionWithMetadata
{
    fn from(
        value: (
            RaydiumLiquidityLockingInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        RaydiumLiquidityLockingInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaydiumLiquidityLockingInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumLiquidityLockingInstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            RaydiumLiquidityLockingInstruction::LockClmmPosition { data, .. } => {
                let row = lock_clmm_position_row::LockClmmPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CollectClmmFeesAndRewards { data, .. } => {
                let row =
                    collect_clmm_fees_and_rewards_row::CollectClmmFeesAndRewardsRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::LockCpLiquidity { data, .. } => {
                let row = lock_cp_liquidity_row::LockCpLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CollectCpFees { data, .. } => {
                let row = collect_cp_fees_row::CollectCpFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaydiumLiquidityLockingInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumLiquidityLockingInstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            RaydiumLiquidityLockingInstruction::LockClmmPosition { data, .. } => {
                let row = lock_clmm_position_row::LockClmmPositionRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CollectClmmFeesAndRewards { data, .. } => {
                let row =
                    collect_clmm_fees_and_rewards_row::CollectClmmFeesAndRewardsRow::from_parts(
                        data.clone(),
                        metadata.clone(),
                        raw_accounts.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::LockCpLiquidity { data, .. } => {
                let row = lock_cp_liquidity_row::LockCpLiquidityRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CollectCpFees { data, .. } => {
                let row = collect_cp_fees_row::CollectCpFeesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumLiquidityLockingInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

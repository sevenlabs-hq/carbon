//! This code was AUTOGENERATED using the Codama library.
use crate::{RaydiumLiquidityLockingDecoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod locked_clmm_position_state;
pub mod locked_cp_liquidity_state;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum RaydiumLiquidityLockingAccount {
    LockedClmmPositionState(Box<locked_clmm_position_state::LockedClmmPositionState>),
    LockedCpLiquidityState(Box<locked_cp_liquidity_state::LockedCpLiquidityState>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for RaydiumLiquidityLockingDecoder {
    type AccountType = RaydiumLiquidityLockingAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = locked_cp_liquidity_state::LockedCpLiquidityState::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: RaydiumLiquidityLockingAccount::LockedCpLiquidityState(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = locked_clmm_position_state::LockedClmmPositionState::decode(data)
            {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: RaydiumLiquidityLockingAccount::LockedClmmPositionState(Box::new(
                        decoded,
                    )),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

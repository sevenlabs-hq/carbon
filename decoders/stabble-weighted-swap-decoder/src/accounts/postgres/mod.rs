//! This code was AUTOGENERATED using the Codama library.
pub mod pool_row;
pub mod vault_row;

pub use self::{pool_row::*, vault_row::*};
use super::StabbleWeightedSwapAccount;

pub struct StabbleWeightedSwapAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for StabbleWeightedSwapAccountsMigration {
    fn app(&self) -> &str {
        "stabble-weighted-swap"
    }

    fn name(&self) -> &str {
        "stabble_weighted_swap_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(PoolMigrationOperation),
            Box::new(VaultMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct StabbleWeightedSwapAccountWithMetadata(
    pub StabbleWeightedSwapAccount,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        StabbleWeightedSwapAccount,
        carbon_core::account::AccountMetadata,
    )> for StabbleWeightedSwapAccountWithMetadata
{
    fn from(
        value: (
            StabbleWeightedSwapAccount,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        StabbleWeightedSwapAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for StabbleWeightedSwapAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleWeightedSwapAccountWithMetadata(account, metadata) = self;

        match account {
            StabbleWeightedSwapAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapAccount::Vault(account) => {
                let row = vault_row::VaultRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for StabbleWeightedSwapAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleWeightedSwapAccountWithMetadata(account, metadata) = self;
        match account {
            StabbleWeightedSwapAccount::Pool(account) => {
                let row = pool_row::PoolRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapAccount::Vault(account) => {
                let row = vault_row::VaultRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

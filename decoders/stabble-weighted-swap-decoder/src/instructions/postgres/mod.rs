//! This code was AUTOGENERATED using the Codama library.
pub mod accept_owner_row;
pub mod change_max_supply_row;
pub mod change_swap_fee_row;
pub mod cpi_event_row;
pub mod deposit_row;
pub mod initialize_row;
pub mod pause_row;
pub mod reject_owner_row;
pub mod shutdown_row;
pub mod swap_row;
pub mod swap_v2_row;
pub mod transfer_owner_row;
pub mod unpause_row;
pub mod withdraw_row;

pub use self::{
    accept_owner_row::*, change_max_supply_row::*, change_swap_fee_row::*, cpi_event_row::*,
    deposit_row::*, initialize_row::*, pause_row::*, reject_owner_row::*, shutdown_row::*,
    swap_row::*, swap_v2_row::*, transfer_owner_row::*, unpause_row::*, withdraw_row::*,
};
use super::StabbleWeightedSwapInstruction;

pub struct StabbleWeightedSwapInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for StabbleWeightedSwapInstructionsMigration {
    fn app(&self) -> &str {
        "stabble-weighted-swap"
    }

    fn name(&self) -> &str {
        "stabble_weighted_swap_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptOwnerMigrationOperation),
            Box::new(ChangeMaxSupplyMigrationOperation),
            Box::new(ChangeSwapFeeMigrationOperation),
            Box::new(DepositMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(PauseMigrationOperation),
            Box::new(RejectOwnerMigrationOperation),
            Box::new(ShutdownMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(SwapV2MigrationOperation),
            Box::new(TransferOwnerMigrationOperation),
            Box::new(UnpauseMigrationOperation),
            Box::new(WithdrawMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct StabbleWeightedSwapInstructionWithMetadata(
    pub StabbleWeightedSwapInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        StabbleWeightedSwapInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for StabbleWeightedSwapInstructionWithMetadata
{
    fn from(
        value: (
            StabbleWeightedSwapInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        StabbleWeightedSwapInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for StabbleWeightedSwapInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleWeightedSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            StabbleWeightedSwapInstruction::AcceptOwner { data, .. } => {
                let row = accept_owner_row::AcceptOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::ChangeMaxSupply { data, .. } => {
                let row = change_max_supply_row::ChangeMaxSupplyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::ChangeSwapFee { data, .. } => {
                let row = change_swap_fee_row::ChangeSwapFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::RejectOwner { data, .. } => {
                let row = reject_owner_row::RejectOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Shutdown { data, .. } => {
                let row = shutdown_row::ShutdownRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::SwapV2 { data, .. } => {
                let row = swap_v2_row::SwapV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::TransferOwner { data, .. } => {
                let row = transfer_owner_row::TransferOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .insert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for StabbleWeightedSwapInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let StabbleWeightedSwapInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            StabbleWeightedSwapInstruction::AcceptOwner { data, .. } => {
                let row = accept_owner_row::AcceptOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::ChangeMaxSupply { data, .. } => {
                let row = change_max_supply_row::ChangeMaxSupplyRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::ChangeSwapFee { data, .. } => {
                let row = change_swap_fee_row::ChangeSwapFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::RejectOwner { data, .. } => {
                let row = reject_owner_row::RejectOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Shutdown { data, .. } => {
                let row = shutdown_row::ShutdownRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Swap { data, .. } => {
                let row = swap_row::SwapRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::SwapV2 { data, .. } => {
                let row = swap_v2_row::SwapV2Row::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::TransferOwner { data, .. } => {
                let row = transfer_owner_row::TransferOwnerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            StabbleWeightedSwapInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    if accounts.is_some() {
                        raw_accounts.clone()
                    } else {
                        Vec::new()
                    },
                )
                .upsert(pool)
                .await?;
                Ok(())
            }
        }
    }
}

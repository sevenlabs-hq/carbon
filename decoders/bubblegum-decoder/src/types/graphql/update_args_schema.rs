//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::CreatorGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "UpdateArgs")]
pub struct UpdateArgsGraphQL {
    pub name: Option<String>,
    pub symbol: Option<String>,
    pub uri: Option<String>,
    pub creators: Option<Vec<CreatorGraphQL>>,
    pub seller_fee_basis_points: Option<i32>,
    pub primary_sale_happened: Option<bool>,
    pub is_mutable: Option<bool>,
}

impl From<crate::types::UpdateArgs> for UpdateArgsGraphQL {
    fn from(original: crate::types::UpdateArgs) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            creators: original
                .creators
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
            seller_fee_basis_points: original.seller_fee_basis_points.map(|v| v as i32),
            primary_sale_happened: original.primary_sale_happened,
            is_mutable: original.is_mutable,
        }
    }
}

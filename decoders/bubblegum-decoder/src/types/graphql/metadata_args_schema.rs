//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        CollectionGraphQL, CreatorGraphQL, TokenProgramVersionGraphQL, TokenStandardGraphQL,
        UsesGraphQL,
    },
    carbon_core::graphql::primitives::U8,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "MetadataArgs")]
pub struct MetadataArgsGraphQL {
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub seller_fee_basis_points: i32,
    pub primary_sale_happened: bool,
    pub is_mutable: bool,
    pub edition_nonce: Option<U8>,
    pub token_standard: Option<TokenStandardGraphQL>,
    pub collection: Option<CollectionGraphQL>,
    pub uses: Option<UsesGraphQL>,
    pub token_program_version: TokenProgramVersionGraphQL,
    pub creators: Vec<CreatorGraphQL>,
}

impl From<crate::types::MetadataArgs> for MetadataArgsGraphQL {
    fn from(original: crate::types::MetadataArgs) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            seller_fee_basis_points: original.seller_fee_basis_points as i32,
            primary_sale_happened: original.primary_sale_happened,
            is_mutable: original.is_mutable,
            edition_nonce: original
                .edition_nonce
                .map(carbon_core::graphql::primitives::U8),
            token_standard: original.token_standard.map(|v| v.into()),
            collection: original.collection.map(|v| v.into()),
            uses: original.uses.map(|v| v.into()),
            token_program_version: original.token_program_version.into(),
            creators: original
                .creators
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

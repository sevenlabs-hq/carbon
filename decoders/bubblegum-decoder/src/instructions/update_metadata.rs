//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{MetadataArgs, UpdateArgs},
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateMetadata {
    pub root: [u8; 32],
    pub nonce: u64,
    pub index: u32,
    pub current_metadata: MetadataArgs,
    pub update_args: UpdateArgs,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateMetadataInstructionAccounts {
    pub tree_authority: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub collection_mint: Option<solana_pubkey::Pubkey>,
    pub collection_metadata: Option<solana_pubkey::Pubkey>,
    pub collection_authority_record_pda: Option<solana_pubkey::Pubkey>,
    pub leaf_owner: solana_pubkey::Pubkey,
    pub leaf_delegate: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub merkle_tree: solana_pubkey::Pubkey,
    pub log_wrapper: solana_pubkey::Pubkey,
    pub compression_program: solana_pubkey::Pubkey,
    pub token_metadata_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateMetadata {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [170, 182, 43, 239, 97, 78, 225, 186] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateMetadata {
    type ArrangedAccounts = UpdateMetadataInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let tree_authority = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let collection_mint = next_account(&mut iter);
        let collection_metadata = next_account(&mut iter);
        let collection_authority_record_pda = next_account(&mut iter);
        let leaf_owner = next_account(&mut iter)?;
        let leaf_delegate = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let merkle_tree = next_account(&mut iter)?;
        let log_wrapper = next_account(&mut iter)?;
        let compression_program = next_account(&mut iter)?;
        let token_metadata_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateMetadataInstructionAccounts {
            tree_authority,
            authority,
            collection_mint,
            collection_metadata,
            collection_authority_record_pda,
            leaf_owner,
            leaf_delegate,
            payer,
            merkle_tree,
            log_wrapper,
            compression_program,
            token_metadata_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

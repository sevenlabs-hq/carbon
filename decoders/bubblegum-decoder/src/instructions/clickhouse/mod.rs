//! This code was AUTOGENERATED using the Codama library.
pub mod burn_row;
pub mod cancel_redeem_row;
pub mod create_tree_row;
pub mod decompress_v1_row;
pub mod delegate_row;
pub mod mint_to_collection_v1_row;
pub mod mint_v1_row;
pub mod redeem_row;
pub mod set_and_verify_collection_row;
pub mod set_decompressible_state_row;
pub mod set_tree_delegate_row;
pub mod transfer_row;
pub mod unverify_collection_row;
pub mod unverify_creator_row;
pub mod update_metadata_row;
pub mod verify_collection_row;
pub mod verify_creator_row;

pub use self::{
    burn_row::*, cancel_redeem_row::*, create_tree_row::*, decompress_v1_row::*, delegate_row::*,
    mint_to_collection_v1_row::*, mint_v1_row::*, redeem_row::*, set_and_verify_collection_row::*,
    set_decompressible_state_row::*, set_tree_delegate_row::*, transfer_row::*,
    unverify_collection_row::*, unverify_creator_row::*, update_metadata_row::*,
    verify_collection_row::*, verify_creator_row::*,
};
use super::BubblegumInstruction;

pub struct BubblegumInstructionsMigration;

impl carbon_core::clickhouse::Migration for BubblegumInstructionsMigration {
    fn app(&self) -> &str {
        "bubblegum"
    }

    fn name(&self) -> &str {
        "bubblegum_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BurnRowMigrationOperation),
            Box::new(CancelRedeemRowMigrationOperation),
            Box::new(CreateTreeRowMigrationOperation),
            Box::new(DecompressV1RowMigrationOperation),
            Box::new(DelegateRowMigrationOperation),
            Box::new(MintToCollectionV1RowMigrationOperation),
            Box::new(MintV1RowMigrationOperation),
            Box::new(RedeemRowMigrationOperation),
            Box::new(SetAndVerifyCollectionRowMigrationOperation),
            Box::new(SetDecompressibleStateRowMigrationOperation),
            Box::new(SetTreeDelegateRowMigrationOperation),
            Box::new(TransferRowMigrationOperation),
            Box::new(UnverifyCollectionRowMigrationOperation),
            Box::new(UnverifyCreatorRowMigrationOperation),
            Box::new(UpdateMetadataRowMigrationOperation),
            Box::new(VerifyCollectionRowMigrationOperation),
            Box::new(VerifyCreatorRowMigrationOperation),
        ]
    }
}

pub enum BubblegumInstructionRow {
    Burn(BurnRow),
    CancelRedeem(CancelRedeemRow),
    CreateTree(CreateTreeRow),
    DecompressV1(DecompressV1Row),
    Delegate(DelegateRow),
    MintToCollectionV1(MintToCollectionV1Row),
    MintV1(MintV1Row),
    Redeem(RedeemRow),
    SetAndVerifyCollection(SetAndVerifyCollectionRow),
    SetDecompressibleState(SetDecompressibleStateRow),
    SetTreeDelegate(SetTreeDelegateRow),
    Transfer(TransferRow),
    UnverifyCollection(UnverifyCollectionRow),
    UnverifyCreator(UnverifyCreatorRow),
    UpdateMetadata(UpdateMetadataRow),
    VerifyCollection(VerifyCollectionRow),
    VerifyCreator(VerifyCreatorRow),
}

pub struct BubblegumInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a BubblegumInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for BubblegumInstructionMetadata<'a> {
    type Row = BubblegumInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let BubblegumInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(BubblegumInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Burn, BurnRow);
        insert_branch!(CancelRedeem, CancelRedeemRow);
        insert_branch!(CreateTree, CreateTreeRow);
        insert_branch!(DecompressV1, DecompressV1Row);
        insert_branch!(Delegate, DelegateRow);
        insert_branch!(MintToCollectionV1, MintToCollectionV1Row);
        insert_branch!(MintV1, MintV1Row);
        insert_branch!(Redeem, RedeemRow);
        insert_branch!(SetAndVerifyCollection, SetAndVerifyCollectionRow);
        insert_branch!(SetDecompressibleState, SetDecompressibleStateRow);
        insert_branch!(SetTreeDelegate, SetTreeDelegateRow);
        insert_branch!(Transfer, TransferRow);
        insert_branch!(UnverifyCollection, UnverifyCollectionRow);
        insert_branch!(UnverifyCreator, UnverifyCreatorRow);
        insert_branch!(UpdateMetadata, UpdateMetadataRow);
        insert_branch!(VerifyCollection, VerifyCollectionRow);
        insert_branch!(VerifyCreator, VerifyCreatorRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for BubblegumInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Burn, BurnRow);
        commit_branch!(CancelRedeem, CancelRedeemRow);
        commit_branch!(CreateTree, CreateTreeRow);
        commit_branch!(DecompressV1, DecompressV1Row);
        commit_branch!(Delegate, DelegateRow);
        commit_branch!(MintToCollectionV1, MintToCollectionV1Row);
        commit_branch!(MintV1, MintV1Row);
        commit_branch!(Redeem, RedeemRow);
        commit_branch!(SetAndVerifyCollection, SetAndVerifyCollectionRow);
        commit_branch!(SetDecompressibleState, SetDecompressibleStateRow);
        commit_branch!(SetTreeDelegate, SetTreeDelegateRow);
        commit_branch!(Transfer, TransferRow);
        commit_branch!(UnverifyCollection, UnverifyCollectionRow);
        commit_branch!(UnverifyCreator, UnverifyCreatorRow);
        commit_branch!(UpdateMetadata, UpdateMetadataRow);
        commit_branch!(VerifyCollection, VerifyCollectionRow);
        commit_branch!(VerifyCreator, VerifyCreatorRow);

        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod advance_nonce_account_row;
pub mod allocate_row;
pub mod allocate_with_seed_row;
pub mod assign_row;
pub mod assign_with_seed_row;
pub mod authorize_nonce_account_row;
pub mod create_account_row;
pub mod create_account_with_seed_row;
pub mod initialize_nonce_account_row;
pub mod transfer_sol_row;
pub mod transfer_sol_with_seed_row;
pub mod upgrade_nonce_account_row;
pub mod withdraw_nonce_account_row;

pub use self::{
    advance_nonce_account_row::*, allocate_row::*, allocate_with_seed_row::*, assign_row::*,
    assign_with_seed_row::*, authorize_nonce_account_row::*, create_account_row::*,
    create_account_with_seed_row::*, initialize_nonce_account_row::*, transfer_sol_row::*,
    transfer_sol_with_seed_row::*, upgrade_nonce_account_row::*, withdraw_nonce_account_row::*,
};
use super::SystemProgramInstruction;

pub struct SystemProgramInstructionsMigration;

impl carbon_core::clickhouse::Migration for SystemProgramInstructionsMigration {
    fn app(&self) -> &str {
        "system-program"
    }

    fn name(&self) -> &str {
        "system_program_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AdvanceNonceAccountRowMigrationOperation),
            Box::new(AllocateRowMigrationOperation),
            Box::new(AllocateWithSeedRowMigrationOperation),
            Box::new(AssignRowMigrationOperation),
            Box::new(AssignWithSeedRowMigrationOperation),
            Box::new(AuthorizeNonceAccountRowMigrationOperation),
            Box::new(CreateAccountRowMigrationOperation),
            Box::new(CreateAccountWithSeedRowMigrationOperation),
            Box::new(InitializeNonceAccountRowMigrationOperation),
            Box::new(TransferSolRowMigrationOperation),
            Box::new(TransferSolWithSeedRowMigrationOperation),
            Box::new(UpgradeNonceAccountRowMigrationOperation),
            Box::new(WithdrawNonceAccountRowMigrationOperation),
        ]
    }
}

pub enum SystemProgramInstructionRow {
    AdvanceNonceAccount(AdvanceNonceAccountRow),
    Allocate(AllocateRow),
    AllocateWithSeed(AllocateWithSeedRow),
    Assign(AssignRow),
    AssignWithSeed(AssignWithSeedRow),
    AuthorizeNonceAccount(AuthorizeNonceAccountRow),
    CreateAccount(CreateAccountRow),
    CreateAccountWithSeed(CreateAccountWithSeedRow),
    InitializeNonceAccount(InitializeNonceAccountRow),
    TransferSol(TransferSolRow),
    TransferSolWithSeed(TransferSolWithSeedRow),
    UpgradeNonceAccount(UpgradeNonceAccountRow),
    WithdrawNonceAccount(WithdrawNonceAccountRow),
}

pub struct SystemProgramInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a SystemProgramInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for SystemProgramInstructionMetadata<'a> {
    type Row = SystemProgramInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let SystemProgramInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(SystemProgramInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(AdvanceNonceAccount, AdvanceNonceAccountRow);
        insert_branch!(Allocate, AllocateRow);
        insert_branch!(AllocateWithSeed, AllocateWithSeedRow);
        insert_branch!(Assign, AssignRow);
        insert_branch!(AssignWithSeed, AssignWithSeedRow);
        insert_branch!(AuthorizeNonceAccount, AuthorizeNonceAccountRow);
        insert_branch!(CreateAccount, CreateAccountRow);
        insert_branch!(CreateAccountWithSeed, CreateAccountWithSeedRow);
        insert_branch!(InitializeNonceAccount, InitializeNonceAccountRow);
        insert_branch!(TransferSol, TransferSolRow);
        insert_branch!(TransferSolWithSeed, TransferSolWithSeedRow);
        insert_branch!(UpgradeNonceAccount, UpgradeNonceAccountRow);
        insert_branch!(WithdrawNonceAccount, WithdrawNonceAccountRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for SystemProgramInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(AdvanceNonceAccount, AdvanceNonceAccountRow);
        commit_branch!(Allocate, AllocateRow);
        commit_branch!(AllocateWithSeed, AllocateWithSeedRow);
        commit_branch!(Assign, AssignRow);
        commit_branch!(AssignWithSeed, AssignWithSeedRow);
        commit_branch!(AuthorizeNonceAccount, AuthorizeNonceAccountRow);
        commit_branch!(CreateAccount, CreateAccountRow);
        commit_branch!(CreateAccountWithSeed, CreateAccountWithSeedRow);
        commit_branch!(InitializeNonceAccount, InitializeNonceAccountRow);
        commit_branch!(TransferSol, TransferSolRow);
        commit_branch!(TransferSolWithSeed, TransferSolWithSeedRow);
        commit_branch!(UpgradeNonceAccount, UpgradeNonceAccountRow);
        commit_branch!(WithdrawNonceAccount, WithdrawNonceAccountRow);

        Ok(())
    }
}

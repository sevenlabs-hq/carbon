//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawNonceAccount {
    pub withdraw_amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawNonceAccountInstructionAccounts {
    pub nonce_account: solana_pubkey::Pubkey,
    pub recipient_account: solana_pubkey::Pubkey,
    pub recent_blockhashes_sysvar: solana_pubkey::Pubkey,
    pub rent_sysvar: solana_pubkey::Pubkey,
    pub nonce_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawNonceAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 4 {
            return None;
        }
        let discriminator = &data[0..4];
        if discriminator != [5, 0, 0, 0] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[4..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawNonceAccount {
    type ArrangedAccounts = WithdrawNonceAccountInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let nonce_account = next_account(&mut iter)?;
        let recipient_account = next_account(&mut iter)?;
        let recent_blockhashes_sysvar = next_account(&mut iter)?;
        let rent_sysvar = next_account(&mut iter)?;
        let nonce_authority = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawNonceAccountInstructionAccounts {
            nonce_account,
            recipient_account,
            recent_blockhashes_sysvar,
            rent_sysvar,
            nonce_authority,
            remaining: remaining.to_vec(),
        })
    }
}

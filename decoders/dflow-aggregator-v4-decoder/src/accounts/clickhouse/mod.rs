//! This code was AUTOGENERATED using the Codama library.
pub mod order_row;

pub use self::order_row::*;
use super::DflowAggregatorV4Account;

pub struct DflowAggregatorV4AccountsMigration;

impl carbon_core::clickhouse::Migration for DflowAggregatorV4AccountsMigration {
    fn app(&self) -> &str {
        "dflow-aggregator-v4"
    }

    fn name(&self) -> &str {
        "dflow_aggregator_v4_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![Box::new(OrderRowMigrationOperation)]
    }
}

pub enum DflowAggregatorV4AccountRow {
    Order(OrderRow),
}

pub struct DflowAggregatorV4AccountMetadata(
    pub carbon_core::account::AccountMetadata,
    pub DflowAggregatorV4Account,
);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for DflowAggregatorV4AccountMetadata {
    type Row = DflowAggregatorV4AccountRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, account) = self;

        match account {
            DflowAggregatorV4Account::Order(account) => {
                rows.push(DflowAggregatorV4AccountRow::Order(OrderRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for DflowAggregatorV4AccountRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(Order, OrderRow);
        Ok(())
    }
}

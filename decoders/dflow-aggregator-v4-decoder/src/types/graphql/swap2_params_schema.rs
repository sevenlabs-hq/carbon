//! This code was AUTOGENERATED using the Codama library.
use crate::types::graphql::ActionGraphQL;
use carbon_core::graphql::primitives::U64;
use carbon_core::graphql::primitives::U8;
use juniper::GraphQLObject;
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "Swap2Params")]
pub struct Swap2ParamsGraphQL {
    pub actions: Vec<ActionGraphQL>,
    pub quoted_out_amount: U64,
    pub slippage_bps: i32,
    pub platform_fee_bps: i32,
    pub positive_slippage_fee_limit_pct: U8,
}

impl From<crate::types::Swap2Params> for Swap2ParamsGraphQL {
    fn from(original: crate::types::Swap2Params) -> Self {
        Self {
            actions: original
                .actions
                .into_iter()
                .map(|item| item.into())
                .collect(),
            quoted_out_amount: carbon_core::graphql::primitives::U64(original.quoted_out_amount),
            slippage_bps: original.slippage_bps as i32,
            platform_fee_bps: original.platform_fee_bps as i32,
            positive_slippage_fee_limit_pct: carbon_core::graphql::primitives::U8(
                original.positive_slippage_fee_limit_pct,
            ),
        }
    }
}

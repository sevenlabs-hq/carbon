//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
/// Initialize a market ledger if it doesn't already exist
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitMarketLedgerIdempotent {
    pub market_id: [u8; 32],
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitMarketLedgerIdempotentInstructionAccounts {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub predictions_program: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_ledger: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_ledger_redemption_vault: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_ledger_order_vault: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_ledger_reduce_yes_vault: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_ledger_reduce_no_vault: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub settlement_escrow: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub escrow_mint: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_yes_outcome_mint: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub market_no_outcome_mint: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub payer: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub outcome_token_program: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub escrow_token_program: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for InitMarketLedgerIdempotent {
    const DISCRIMINATOR: &'static [u8] = &[183, 43, 139, 79, 125, 225, 35, 159];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitMarketLedgerIdempotent {
    type ArrangedAccounts = InitMarketLedgerIdempotentInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let predictions_program = next_account(&mut iter)?;
        let market_ledger = next_account(&mut iter)?;
        let market_ledger_redemption_vault = next_account(&mut iter)?;
        let market_ledger_order_vault = next_account(&mut iter)?;
        let market_ledger_reduce_yes_vault = next_account(&mut iter)?;
        let market_ledger_reduce_no_vault = next_account(&mut iter)?;
        let settlement_escrow = next_account(&mut iter)?;
        let escrow_mint = next_account(&mut iter)?;
        let market_yes_outcome_mint = next_account(&mut iter)?;
        let market_no_outcome_mint = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let outcome_token_program = next_account(&mut iter)?;
        let escrow_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitMarketLedgerIdempotentInstructionAccounts {
            predictions_program,
            market_ledger,
            market_ledger_redemption_vault,
            market_ledger_order_vault,
            market_ledger_reduce_yes_vault,
            market_ledger_reduce_no_vault,
            settlement_escrow,
            escrow_mint,
            market_yes_outcome_mint,
            market_no_outcome_mint,
            payer,
            outcome_token_program,
            escrow_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

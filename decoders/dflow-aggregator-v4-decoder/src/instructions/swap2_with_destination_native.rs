//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::Swap2Params,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
/// Executes a token swap with a slippage fee. Outputs native SOL to the
/// specified destination account by unwrapping WSOL.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Swap2WithDestinationNative {
    pub params: Swap2Params,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct Swap2WithDestinationNativeInstructionAccounts {
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub user_token_authority: solana_pubkey::Pubkey,
    pub destination_account: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Swap2WithDestinationNative {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [222, 100, 184, 146, 186, 196, 105, 165] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Swap2WithDestinationNative {
    type ArrangedAccounts = Swap2WithDestinationNativeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let user_token_authority = next_account(&mut iter)?;
        let destination_account = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(Swap2WithDestinationNativeInstructionAccounts {
            token_program,
            associated_token_program,
            system_program,
            user_token_authority,
            destination_account,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

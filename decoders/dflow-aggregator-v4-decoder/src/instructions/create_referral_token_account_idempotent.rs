//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

/// Create a referral token account if it doesn't already exist

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CreateReferralTokenAccountIdempotent {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateReferralTokenAccountIdempotentInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub project: solana_pubkey::Pubkey,
    pub referral_account: solana_pubkey::Pubkey,
    pub referral_token_account: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub referral_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreateReferralTokenAccountIdempotent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [46, 232, 41, 144, 85, 37, 170, 175] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for CreateReferralTokenAccountIdempotent {
    type ArrangedAccounts = CreateReferralTokenAccountIdempotentInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let project = next_account(&mut iter)?;
        let referral_account = next_account(&mut iter)?;
        let referral_token_account = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let referral_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateReferralTokenAccountIdempotentInstructionAccounts {
            payer,
            project,
            referral_account,
            referral_token_account,
            mint,
            system_program,
            token_program,
            referral_program,
            remaining: remaining.to_vec(),
        })
    }
}

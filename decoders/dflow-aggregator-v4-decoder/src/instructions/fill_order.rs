//! This code was AUTOGENERATED using the Codama library.
use crate::types::FillOrderParams;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

/// Fills an order, closing the order after filling it

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct FillOrder {
    pub params: FillOrderParams,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct FillOrderInstructionAccounts {
    pub order: solana_pubkey::Pubkey,
    pub order_vault: solana_pubkey::Pubkey,
    pub output_token_account: solana_pubkey::Pubkey,
    pub return_input_token_account: solana_pubkey::Pubkey,
    pub return_rent_to: solana_pubkey::Pubkey,
    pub filler_input_token_account: solana_pubkey::Pubkey,
    pub input_mint: Option<solana_pubkey::Pubkey>,
    pub filler_output_token_account: solana_pubkey::Pubkey,
    pub output_mint: Option<solana_pubkey::Pubkey>,
    pub filler: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl FillOrder {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [232, 122, 115, 25, 199, 143, 136, 162] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for FillOrder {
    type ArrangedAccounts = FillOrderInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let order = next_account(&mut iter)?;
        let order_vault = next_account(&mut iter)?;
        let output_token_account = next_account(&mut iter)?;
        let return_input_token_account = next_account(&mut iter)?;
        let return_rent_to = next_account(&mut iter)?;
        let filler_input_token_account = next_account(&mut iter)?;
        let input_mint = next_account(&mut iter);
        let filler_output_token_account = next_account(&mut iter)?;
        let output_mint = next_account(&mut iter);
        let filler = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(FillOrderInstructionAccounts {
            order,
            order_vault,
            output_token_account,
            return_input_token_account,
            return_rent_to,
            filler_input_token_account,
            input_mint,
            filler_output_token_account,
            output_mint,
            filler,
            token_program,
            associated_token_program,
            system_program,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

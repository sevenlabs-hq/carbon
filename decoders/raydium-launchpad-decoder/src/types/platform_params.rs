//! This code was AUTOGENERATED using the Codama library.
use crate::types::MigrateNftInfo;
/// Represents the parameters for initializing a platform config account
/// # Fields
/// * `migrate_nft_info` - The platform configures liquidity info during
///   migration(Only support MigrateType::CPSWAP)
/// * `fee_rate` - Fee rate of the platform
/// * `name` - Name of the platform
/// * `web` - Website of the platform
/// * `img` - Image link of the platform
///
/// /// * `creator_fee_rate` - The fee rate charged by the creator for each
/// transaction.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct PlatformParams {
    pub migrate_nft_info: MigrateNftInfo,
    pub fee_rate: u64,
    pub name: String,
    pub web: String,
    pub img: String,
    pub creator_fee_rate: u64,
    pub platform_vesting_scale: u64,
}

//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLEnum;

/// Specifies the direction of a trade in the bonding curve
/// This is important because curves can treat tokens differently through
/// weights or offsets
#[derive(Debug, Clone, GraphQLEnum)]
#[graphql(name = "TradeDirection")]
pub enum TradeDirectionGraphQL {
    Buy,
    Sell,
}

impl From<crate::types::TradeDirection> for TradeDirectionGraphQL {
    fn from(original: crate::types::TradeDirection) -> Self {
        match original {
            crate::types::TradeDirection::Buy => Self::Buy,
            crate::types::TradeDirection::Sell => Self::Sell,
        }
    }
}

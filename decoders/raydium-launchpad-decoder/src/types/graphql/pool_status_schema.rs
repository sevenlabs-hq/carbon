//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLEnum;

/// Represents the different states a pool can be in
/// * Fund - Initial state where pool is accepting funds
/// * Migrate - Pool funding has ended and waiting for migration
/// * Trade - Pool migration is complete and amm trading is enabled
#[derive(Debug, Clone, GraphQLEnum)]
#[graphql(name = "PoolStatus")]
pub enum PoolStatusGraphQL {
    Fund,
    Migrate,
    Trade,
}

impl From<crate::types::PoolStatus> for PoolStatusGraphQL {
    fn from(original: crate::types::PoolStatus) -> Self {
        match original {
            crate::types::PoolStatus::Fund => Self::Fund,
            crate::types::PoolStatus::Migrate => Self::Migrate,
            crate::types::PoolStatus::Trade => Self::Trade,
        }
    }
}

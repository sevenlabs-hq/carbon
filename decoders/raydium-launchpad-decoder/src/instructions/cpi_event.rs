//! This code was AUTOGENERATED using the Codama library.

pub use super::super::events;
use carbon_core::{borsh, deserialize::ArrangeAccounts};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(
    Debug,
    Clone,
    carbon_core::borsh::BorshSerialize,
    carbon_core::borsh::BorshDeserialize,
    PartialEq,
)]
pub enum CpiEvent {
    ClaimVestedEvent(events::claim_vested_event::ClaimVestedEventEvent),
    CreateVestingEvent(events::create_vesting_event::CreateVestingEventEvent),
    PoolCreateEvent(events::pool_create_event::PoolCreateEventEvent),
    TradeEvent(events::trade_event::TradeEventEvent),
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CpiEventInstructionAccounts {
    pub program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CpiEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let event_data = if data[..8] == [228, 69, 165, 46, 81, 203, 154, 29] {
            &data[8..]
        } else {
            data
        };

        if let Some(decoded) = events::claim_vested_event::ClaimVestedEventEvent::decode(event_data)
        {
            return Some(CpiEvent::ClaimVestedEvent(decoded));
        }
        if let Some(decoded) =
            events::create_vesting_event::CreateVestingEventEvent::decode(event_data)
        {
            return Some(CpiEvent::CreateVestingEvent(decoded));
        }
        if let Some(decoded) = events::pool_create_event::PoolCreateEventEvent::decode(event_data) {
            return Some(CpiEvent::PoolCreateEvent(decoded));
        }
        if let Some(decoded) = events::trade_event::TradeEventEvent::decode(event_data) {
            return Some(CpiEvent::TradeEvent(decoded));
        }
        None
    }
}

impl ArrangeAccounts for CpiEvent {
    type ArrangedAccounts = CpiEventInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        match accounts {
            [program, event_authority, remaining @ ..] => Some(CpiEventInstructionAccounts {
                program: program.pubkey,
                event_authority: event_authority.pubkey,
                remaining: remaining.to_vec(),
            }),
            [event_authority] => Some(CpiEventInstructionAccounts {
                program: crate::PROGRAM_ID,
                event_authority: event_authority.pubkey,
                remaining: Vec::new(),
            }),
            _ => None,
        }
    }
}

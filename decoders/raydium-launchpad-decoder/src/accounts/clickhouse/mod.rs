//! This code was AUTOGENERATED using the Codama library.
pub mod global_config_row;
pub mod platform_config_row;
pub mod platform_global_access_row;
pub mod pool_state_row;
pub mod vesting_record_row;

pub use self::{
    global_config_row::*, platform_config_row::*, platform_global_access_row::*, pool_state_row::*,
    vesting_record_row::*,
};
use super::RaydiumLaunchpadAccount;

pub struct RaydiumLaunchpadAccountsMigration;

impl carbon_core::clickhouse::Migration for RaydiumLaunchpadAccountsMigration {
    fn app(&self) -> &str {
        "raydium-launchpad"
    }

    fn name(&self) -> &str {
        "raydium_launchpad_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(GlobalConfigRowMigrationOperation),
            Box::new(PlatformConfigRowMigrationOperation),
            Box::new(PlatformGlobalAccessRowMigrationOperation),
            Box::new(PoolStateRowMigrationOperation),
            Box::new(VestingRecordRowMigrationOperation),
        ]
    }
}

pub enum RaydiumLaunchpadAccountRow {
    GlobalConfig(GlobalConfigRow),
    PlatformConfig(PlatformConfigRow),
    PlatformGlobalAccess(PlatformGlobalAccessRow),
    PoolState(PoolStateRow),
    VestingRecord(VestingRecordRow),
}

pub struct RaydiumLaunchpadAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a RaydiumLaunchpadAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for RaydiumLaunchpadAccountMetadata<'a> {
    type Row = RaydiumLaunchpadAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let RaydiumLaunchpadAccount::$variant(account) = account {
                    rows.push(RaydiumLaunchpadAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let RaydiumLaunchpadAccount::$variant(account) = account {
                    rows.push(RaydiumLaunchpadAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(GlobalConfig, GlobalConfigRow, boxed);
        insert_branch!(PlatformConfig, PlatformConfigRow, boxed);
        insert_branch!(PlatformGlobalAccess, PlatformGlobalAccessRow, boxed);
        insert_branch!(PoolState, PoolStateRow, boxed);
        insert_branch!(VestingRecord, VestingRecordRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for RaydiumLaunchpadAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(GlobalConfig, GlobalConfigRow);
        commit_branch!(PlatformConfig, PlatformConfigRow);
        commit_branch!(PlatformGlobalAccess, PlatformGlobalAccessRow);
        commit_branch!(PoolState, PoolStateRow);
        commit_branch!(VestingRecord, VestingRecordRow);

        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (permissionless) Initialize a liquidation record PDA for a marginfi account.
/// The fee_payer pays rent; the record is required for receivership
/// liquidation.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct MarginfiAccountInitLiqRecord {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct MarginfiAccountInitLiqRecordInstructionAccounts {
    pub marginfi_account: solana_pubkey::Pubkey,
    pub fee_payer: solana_pubkey::Pubkey,
    pub liquidation_record: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl MarginfiAccountInitLiqRecord {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [236, 213, 238, 126, 147, 251, 164, 8] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for MarginfiAccountInitLiqRecord {
    type ArrangedAccounts = MarginfiAccountInitLiqRecordInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let fee_payer = next_account(&mut iter)?;
        let liquidation_record = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(MarginfiAccountInitLiqRecordInstructionAccounts {
            marginfi_account,
            fee_payer,
            liquidation_record,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

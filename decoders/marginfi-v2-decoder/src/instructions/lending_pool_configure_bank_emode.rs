//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::EmodeEntry,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
/// (emode_admin only)
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingPoolConfigureBankEmode {
    pub emode_tag: u16,
    pub entries: [EmodeEntry; 10],
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingPoolConfigureBankEmodeInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub emode_admin: solana_pubkey::Pubkey,
    pub bank: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LendingPoolConfigureBankEmode {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [17, 175, 91, 57, 239, 86, 49, 71] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingPoolConfigureBankEmode {
    type ArrangedAccounts = LendingPoolConfigureBankEmodeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let emode_admin = next_account(&mut iter)?;
        let bank = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingPoolConfigureBankEmodeInstructionAccounts {
            group,
            emode_admin,
            bank,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (liquidation_receiver, set in start_liquidation) End receivership
/// liquidation. Validates health improved and seized assets are within fee
/// limits. Charges a flat SOL fee.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct EndLiquidation {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct EndLiquidationInstructionAccounts {
    pub marginfi_account: solana_pubkey::Pubkey,
    pub liquidation_record: solana_pubkey::Pubkey,
    pub liquidation_receiver: solana_pubkey::Pubkey,
    pub fee_state: solana_pubkey::Pubkey,
    pub global_fee_wallet: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl EndLiquidation {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [110, 11, 244, 54, 229, 181, 22, 184] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for EndLiquidation {
    type ArrangedAccounts = EndLiquidationInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let liquidation_record = next_account(&mut iter)?;
        let liquidation_receiver = next_account(&mut iter)?;
        let fee_state = next_account(&mut iter)?;
        let global_fee_wallet = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(EndLiquidationInstructionAccounts {
            marginfi_account,
            liquidation_record,
            liquidation_receiver,
            fee_state,
            global_fee_wallet,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

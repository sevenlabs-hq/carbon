//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (delegate_emissions_admin only)
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingPoolUpdateEmissionsParameters {
    pub emissions_flags: Option<u64>,
    pub emissions_rate: Option<u64>,
    pub additional_emissions: Option<u64>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingPoolUpdateEmissionsParametersInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub delegate_emissions_admin: solana_pubkey::Pubkey,
    pub bank: solana_pubkey::Pubkey,
    pub emissions_mint: solana_pubkey::Pubkey,
    pub emissions_token_account: solana_pubkey::Pubkey,
    pub emissions_funding_account: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LendingPoolUpdateEmissionsParameters {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [55, 213, 224, 168, 153, 53, 197, 40] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingPoolUpdateEmissionsParameters {
    type ArrangedAccounts = LendingPoolUpdateEmissionsParametersInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let delegate_emissions_admin = next_account(&mut iter)?;
        let bank = next_account(&mut iter)?;
        let emissions_mint = next_account(&mut iter)?;
        let emissions_token_account = next_account(&mut iter)?;
        let emissions_funding_account = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingPoolUpdateEmissionsParametersInstructionAccounts {
            group,
            delegate_emissions_admin,
            bank,
            emissions_mint,
            emissions_token_account,
            emissions_funding_account,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

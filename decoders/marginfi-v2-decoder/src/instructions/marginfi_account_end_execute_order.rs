//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (permissionless keeper) End Order execution
/// * Closes the Order (keeper keeps the rent)
/// * Closes the borrow position involved in the Order, the lending position
///   remains open
/// * User health must be "unchanged" (within Order requirements i.e. minus
///   slippage). Keeper
///
/// may keep any slippage in excess of what was needed to complete the Order as
/// profit.
/// * `StartExecuteOrder` must appear earlier in the tx
/// * Must appear last in the tx
/// * CPI is forbidden
/// * Returns rent for ephemeral accounts created during `StartExecuteOrder`
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct MarginfiAccountEndExecuteOrder {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct MarginfiAccountEndExecuteOrderInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub marginfi_account: solana_pubkey::Pubkey,
    pub executor: solana_pubkey::Pubkey,
    pub fee_recipient: solana_pubkey::Pubkey,
    pub order: solana_pubkey::Pubkey,
    pub execute_record: solana_pubkey::Pubkey,
    pub fee_state: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl MarginfiAccountEndExecuteOrder {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [115, 42, 20, 93, 121, 84, 178, 83] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for MarginfiAccountEndExecuteOrder {
    type ArrangedAccounts = MarginfiAccountEndExecuteOrderInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let marginfi_account = next_account(&mut iter)?;
        let executor = next_account(&mut iter)?;
        let fee_recipient = next_account(&mut iter)?;
        let order = next_account(&mut iter)?;
        let execute_record = next_account(&mut iter)?;
        let fee_state = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(MarginfiAccountEndExecuteOrderInstructionAccounts {
            group,
            marginfi_account,
            executor,
            fee_recipient,
            order,
            execute_record,
            fee_state,
            remaining: remaining.to_vec(),
        })
    }
}

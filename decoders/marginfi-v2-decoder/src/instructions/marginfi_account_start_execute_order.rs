//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (permissionless keeper) Begin Order execution
/// * Enables the Keeper to withdraw/repay associated positions until the end of
///   the tx
/// * Only one `StartExecuteOrder` is allowed per tx
/// * Must appear before `EndExecuteOrder` in the tx, and before any
///   instructions except certain
///
/// allowed ones (compute budget, kamino refresh, etc)
/// * `EndExecuteOrder` must also appear in the tx
/// * CPI is forbidden
/// * Costs a small amount of rent, which is returned at the end of the tx, make
///   sure you have
///
/// enough SOL to start the tx.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct MarginfiAccountStartExecuteOrder {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct MarginfiAccountStartExecuteOrderInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub marginfi_account: solana_pubkey::Pubkey,
    pub fee_payer: solana_pubkey::Pubkey,
    pub executor: solana_pubkey::Pubkey,
    pub order: solana_pubkey::Pubkey,
    pub execute_record: solana_pubkey::Pubkey,
    pub instruction_sysvar: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl MarginfiAccountStartExecuteOrder {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [1, 70, 140, 134, 183, 29, 208, 224] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for MarginfiAccountStartExecuteOrder {
    type ArrangedAccounts = MarginfiAccountStartExecuteOrderInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let marginfi_account = next_account(&mut iter)?;
        let fee_payer = next_account(&mut iter)?;
        let executor = next_account(&mut iter)?;
        let order = next_account(&mut iter)?;
        let execute_record = next_account(&mut iter)?;
        let instruction_sysvar = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(MarginfiAccountStartExecuteOrderInstructionAccounts {
            group,
            marginfi_account,
            fee_payer,
            executor,
            order,
            execute_record,
            instruction_sysvar,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

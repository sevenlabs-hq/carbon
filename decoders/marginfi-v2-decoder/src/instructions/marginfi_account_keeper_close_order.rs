//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
/// (permissionless keeper) Close an existing Order after the user account was
/// closed, or it no longer has the associated positions, or the user has
/// executed `marginfi_account_set_keeper_close_flags`. Keeper keeps the rent.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct MarginfiAccountKeeperCloseOrder {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct MarginfiAccountKeeperCloseOrderInstructionAccounts {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub marginfi_account: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub fee_recipient: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub order: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for MarginfiAccountKeeperCloseOrder {
    const DISCRIMINATOR: &'static [u8] = &[128, 114, 71, 46, 194, 71, 186, 106];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for MarginfiAccountKeeperCloseOrder {
    type ArrangedAccounts = MarginfiAccountKeeperCloseOrderInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let fee_recipient = next_account(&mut iter)?;
        let order = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(MarginfiAccountKeeperCloseOrderInstructionAccounts {
            marginfi_account,
            fee_recipient,
            order,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (account authority) Set the wallet whose canonical ATA will receive
/// off-chain emissions.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct MarginfiAccountUpdateEmissionsDestinationAccount {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct MarginfiAccountUpdateEmissionsDestinationAccountInstructionAccounts {
    pub marginfi_account: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub destination_account: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl MarginfiAccountUpdateEmissionsDestinationAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [73, 185, 162, 201, 111, 24, 116, 185] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for MarginfiAccountUpdateEmissionsDestinationAccount {
    type ArrangedAccounts = MarginfiAccountUpdateEmissionsDestinationAccountInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let destination_account = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(
            MarginfiAccountUpdateEmissionsDestinationAccountInstructionAccounts {
                marginfi_account,
                authority,
                destination_account,
                remaining: remaining.to_vec(),
            },
        )
    }
}

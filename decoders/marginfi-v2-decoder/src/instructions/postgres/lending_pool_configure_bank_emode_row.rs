//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::EmodeEntry,
    carbon_core::{
        instruction::InstructionMetadata,
        postgres::{metadata::InstructionRowMetadata, primitives::U16},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct LendingPoolConfigureBankEmodeRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub emode_tag: U16,
    pub entries: sqlx::types::Json<Vec<EmodeEntry>>,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl LendingPoolConfigureBankEmodeRow {
    pub fn from_parts(
        source: crate::instructions::lending_pool_configure_bank_emode::LendingPoolConfigureBankEmode,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            emode_tag: source.emode_tag.into(),
            entries: sqlx::types::Json(source.entries.to_vec()),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<LendingPoolConfigureBankEmodeRow>
    for crate::instructions::lending_pool_configure_bank_emode::LendingPoolConfigureBankEmode
{
    type Error = carbon_core::error::Error;
    fn try_from(source: LendingPoolConfigureBankEmodeRow) -> Result<Self, Self::Error> {
        Ok(Self {
            emode_tag: source.emode_tag.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            entries: source
                .entries
                .0
                .into_iter()
                .collect::<Vec<_>>()
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert value from postgres primitive".to_string(),
                    )
                })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::lending_pool_configure_bank_emode::LendingPoolConfigureBankEmode
{
    fn table() -> &'static str {
        "lending_pool_configure_bank_emode_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "emode_tag",
            "entries",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for LendingPoolConfigureBankEmodeRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO lending_pool_configure_bank_emode_instruction (
                "emode_tag",
                "entries",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(self.emode_tag)
        .bind(&self.entries)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for LendingPoolConfigureBankEmodeRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO lending_pool_configure_bank_emode_instruction (
                "emode_tag",
                "entries",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "emode_tag" = EXCLUDED."emode_tag",
                "entries" = EXCLUDED."entries",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(self.emode_tag)
        .bind(&self.entries)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for LendingPoolConfigureBankEmodeRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM lending_pool_configure_bank_emode_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for LendingPoolConfigureBankEmodeRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM lending_pool_configure_bank_emode_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct LendingPoolConfigureBankEmodeMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for LendingPoolConfigureBankEmodeMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS lending_pool_configure_bank_emode_instruction (
                -- Instruction data
                "emode_tag" INT4 NOT NULL,
                "entries" JSONB NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS lending_pool_configure_bank_emode_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

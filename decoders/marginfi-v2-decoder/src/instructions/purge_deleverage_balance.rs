//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (risk admin only) Purge a user's lending balance without withdrawing
/// anything. Only usable after all the debt has been settled on a bank in
/// deleveraging mode, e.g. when `TOKENLESS_REPAYMENTS_ALLOWED` and
/// `TOKENLESS_REPAYMENTS_COMPLETE`. used to purge remaining lending assets in a
/// now-worthless bank before it is fully sunset.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct PurgeDeleverageBalance {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct PurgeDeleverageBalanceInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub marginfi_account: solana_pubkey::Pubkey,
    pub risk_admin: solana_pubkey::Pubkey,
    pub bank: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl PurgeDeleverageBalance {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [132, 187, 25, 149, 181, 59, 253, 136] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for PurgeDeleverageBalance {
    type ArrangedAccounts = PurgeDeleverageBalanceInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let marginfi_account = next_account(&mut iter)?;
        let risk_admin = next_account(&mut iter)?;
        let bank = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(PurgeDeleverageBalanceInstructionAccounts {
            group,
            marginfi_account,
            risk_admin,
            bank,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (risk_admin only) - Signals all of a bank's liability have been deleveraged.
/// Used if a bank still has liability dust after the risk admin has completed
/// deleveraging all debts. The risk admin is trusted not to execute this until
/// all non-dust debts have been deleveraged.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingPoolForceTokenlessRepayComplete {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingPoolForceTokenlessRepayCompleteInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub risk_admin: solana_pubkey::Pubkey,
    pub bank: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LendingPoolForceTokenlessRepayComplete {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [15, 203, 147, 232, 199, 14, 231, 37] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingPoolForceTokenlessRepayComplete {
    type ArrangedAccounts = LendingPoolForceTokenlessRepayCompleteInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let risk_admin = next_account(&mut iter)?;
        let bank = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingPoolForceTokenlessRepayCompleteInstructionAccounts {
            group,
            risk_admin,
            bank,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (Permissionless) Refresh the internal risk engine health cache. Useful for
/// liquidators and other consumers that want to see the internal risk state of
/// a user account. This cache is read-only and serves no purpose except being
/// populated by this ix.
/// * remaining accounts expected in the same order as borrow, etc. I.e., for
///   each balance the
///
/// user has, pass bank and oracle: <bank1, oracle1, bank2, oracle2>
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingAccountPulseHealth {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingAccountPulseHealthInstructionAccounts {
    pub marginfi_account: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LendingAccountPulseHealth {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [186, 52, 117, 97, 34, 74, 39, 253] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingAccountPulseHealth {
    type ArrangedAccounts = LendingAccountPulseHealthInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingAccountPulseHealthInstructionAccounts {
            marginfi_account,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Liquidate a lending account balance of an unhealthy marginfi account
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingAccountLiquidate {
    pub asset_amount: u64,
    pub liquidatee_accounts: u8,
    pub liquidator_accounts: u8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingAccountLiquidateInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub asset_bank: solana_pubkey::Pubkey,
    pub liab_bank: solana_pubkey::Pubkey,
    pub liquidator_marginfi_account: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub liquidatee_marginfi_account: solana_pubkey::Pubkey,
    pub bank_liquidity_vault_authority: solana_pubkey::Pubkey,
    pub bank_liquidity_vault: solana_pubkey::Pubkey,
    pub bank_insurance_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LendingAccountLiquidate {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [214, 169, 151, 213, 251, 167, 86, 219] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingAccountLiquidate {
    type ArrangedAccounts = LendingAccountLiquidateInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let asset_bank = next_account(&mut iter)?;
        let liab_bank = next_account(&mut iter)?;
        let liquidator_marginfi_account = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let liquidatee_marginfi_account = next_account(&mut iter)?;
        let bank_liquidity_vault_authority = next_account(&mut iter)?;
        let bank_liquidity_vault = next_account(&mut iter)?;
        let bank_insurance_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingAccountLiquidateInstructionAccounts {
            group,
            asset_bank,
            liab_bank,
            liquidator_marginfi_account,
            authority,
            liquidatee_marginfi_account,
            bank_liquidity_vault_authority,
            bank_liquidity_vault,
            bank_insurance_vault,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

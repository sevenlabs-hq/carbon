//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (risk_admin only) Begin forced deleverage on an account. Similar to
/// start_liquidation but does not require the account to be unhealthy.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct StartDeleverage {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct StartDeleverageInstructionAccounts {
    pub marginfi_account: solana_pubkey::Pubkey,
    pub liquidation_record: solana_pubkey::Pubkey,
    pub group: solana_pubkey::Pubkey,
    pub risk_admin: solana_pubkey::Pubkey,
    pub instruction_sysvar: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl StartDeleverage {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [10, 138, 10, 57, 40, 232, 182, 193] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for StartDeleverage {
    type ArrangedAccounts = StartDeleverageInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let liquidation_record = next_account(&mut iter)?;
        let group = next_account(&mut iter)?;
        let risk_admin = next_account(&mut iter)?;
        let instruction_sysvar = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(StartDeleverageInstructionAccounts {
            marginfi_account,
            liquidation_record,
            group,
            risk_admin,
            instruction_sysvar,
            remaining: remaining.to_vec(),
        })
    }
}

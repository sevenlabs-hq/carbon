//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
/// (account authority) Start a flash loan. Must have a corresponding
/// `end_flashloan` ix in the same tx. Health checks are skipped until the flash
/// loan ends.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LendingAccountStartFlashloan {
    pub end_index: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LendingAccountStartFlashloanInstructionAccounts {
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub marginfi_account: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub authority: solana_pubkey::Pubkey,
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub ixs_sysvar: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for LendingAccountStartFlashloan {
    const DISCRIMINATOR: &'static [u8] = &[14, 131, 33, 220, 81, 186, 180, 107];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LendingAccountStartFlashloan {
    type ArrangedAccounts = LendingAccountStartFlashloanInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let marginfi_account = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let ixs_sysvar = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LendingAccountStartFlashloanInstructionAccounts {
            marginfi_account,
            authority,
            ixs_sysvar,
            remaining: remaining.to_vec(),
        })
    }
}

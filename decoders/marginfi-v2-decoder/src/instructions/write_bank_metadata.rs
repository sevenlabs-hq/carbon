//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// (metadata admin only) Write ticker/description information for a bank
/// on-chain. Optional, not all Banks are guaranteed to have metadata.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WriteBankMetadata {
    pub ticker: Option<Vec<u8>>,
    pub description: Option<Vec<u8>>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WriteBankMetadataInstructionAccounts {
    pub group: solana_pubkey::Pubkey,
    pub bank: solana_pubkey::Pubkey,
    pub metadata_admin: solana_pubkey::Pubkey,
    pub metadata: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WriteBankMetadata {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [147, 78, 81, 133, 129, 138, 233, 59] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WriteBankMetadata {
    type ArrangedAccounts = WriteBankMetadataInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let group = next_account(&mut iter)?;
        let bank = next_account(&mut iter)?;
        let metadata_admin = next_account(&mut iter)?;
        let metadata = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WriteBankMetadataInstructionAccounts {
            group,
            bank,
            metadata_admin,
            metadata,
            remaining: remaining.to_vec(),
        })
    }
}

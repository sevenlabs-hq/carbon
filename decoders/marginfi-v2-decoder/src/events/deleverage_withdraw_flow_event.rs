//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::deserialize::CarbonDeserialize, solana_pubkey::Pubkey};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DeleverageWithdrawFlowEventEvent {
    pub group: Pubkey,
    pub bank: Pubkey,
    pub mint: Pubkey,
    /// Equity-denominated outflow value in USD, rounded to integer.
    pub outflow_usd: u32,
    /// Unix timestamp when the flow was recorded
    pub current_timestamp: i64,
}

impl CarbonDeserialize for DeleverageWithdrawFlowEventEvent {
    const DISCRIMINATOR: &'static [u8] = &[109, 90, 139, 200, 10, 204, 84, 176];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

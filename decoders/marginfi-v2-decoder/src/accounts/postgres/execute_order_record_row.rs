//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{ExecuteOrderBalanceRecord, WrappedI80F48},
    carbon_core::{
        account::AccountMetadata,
        postgres::{
            metadata::AccountRowMetadata,
            primitives::{Pubkey, U8},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ExecuteOrderRecordRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub order: Pubkey,
    pub executor: Pubkey,
    pub balance_states: sqlx::types::Json<Vec<ExecuteOrderBalanceRecord>>,
    pub active_balance_count: U8,
    pub inactive_balance_count: U8,
    pub reserved0: Vec<u8>,
    pub order_start_health: sqlx::types::Json<WrappedI80F48>,
}

impl ExecuteOrderRecordRow {
    pub fn from_parts(
        source: crate::accounts::execute_order_record::ExecuteOrderRecord,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            order: source.order.into(),
            executor: source.executor.into(),
            balance_states: sqlx::types::Json(source.balance_states.to_vec()),
            active_balance_count: source.active_balance_count.into(),
            inactive_balance_count: source.inactive_balance_count.into(),
            reserved0: source.reserved0.to_vec(),
            order_start_health: sqlx::types::Json(source.order_start_health),
        }
    }
}

impl TryFrom<ExecuteOrderRecordRow> for crate::accounts::execute_order_record::ExecuteOrderRecord {
    type Error = carbon_core::error::Error;
    fn try_from(source: ExecuteOrderRecordRow) -> Result<Self, Self::Error> {
        Ok(Self {
            order: *source.order,
            executor: *source.executor,
            balance_states: source
                .balance_states
                .0
                .into_iter()
                .collect::<Vec<_>>()
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert value from postgres primitive".to_string(),
                    )
                })?,
            active_balance_count: source.active_balance_count.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            inactive_balance_count: source.inactive_balance_count.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            reserved0: source.reserved0.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 6 bytes"
                        .to_string(),
                )
            })?,
            order_start_health: source.order_start_health.0,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::execute_order_record::ExecuteOrderRecord
{
    fn table() -> &'static str {
        "execute_order_record_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "order",
            "executor",
            "balance_states",
            "active_balance_count",
            "inactive_balance_count",
            "reserved0",
            "order_start_health",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ExecuteOrderRecordRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO execute_order_record_account (
                "order",
                "executor",
                "balance_states",
                "active_balance_count",
                "inactive_balance_count",
                "reserved0",
                "order_start_health",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(self.order)
        .bind(self.executor)
        .bind(&self.balance_states)
        .bind(self.active_balance_count)
        .bind(self.inactive_balance_count)
        .bind(&self.reserved0)
        .bind(&self.order_start_health)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ExecuteOrderRecordRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO execute_order_record_account (
                "order",
                "executor",
                "balance_states",
                "active_balance_count",
                "inactive_balance_count",
                "reserved0",
                "order_start_health",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "order" = EXCLUDED."order",
                "executor" = EXCLUDED."executor",
                "balance_states" = EXCLUDED."balance_states",
                "active_balance_count" = EXCLUDED."active_balance_count",
                "inactive_balance_count" = EXCLUDED."inactive_balance_count",
                "reserved0" = EXCLUDED."reserved0",
                "order_start_health" = EXCLUDED."order_start_health",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.order)
        .bind(self.executor)
        .bind(&self.balance_states)
        .bind(self.active_balance_count)
        .bind(self.inactive_balance_count)
        .bind(&self.reserved0)
        .bind(&self.order_start_health)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ExecuteOrderRecordRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM execute_order_record_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ExecuteOrderRecordRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM execute_order_record_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ExecuteOrderRecordMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for ExecuteOrderRecordMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS execute_order_record_account (
                -- Account data
                "order" BYTEA NOT NULL,
                "executor" BYTEA NOT NULL,
                "balance_states" JSONB NOT NULL,
                "active_balance_count" INT2 NOT NULL,
                "inactive_balance_count" INT2 NOT NULL,
                "reserved0" BYTEA NOT NULL,
                "order_start_health" JSONB NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS execute_order_record_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

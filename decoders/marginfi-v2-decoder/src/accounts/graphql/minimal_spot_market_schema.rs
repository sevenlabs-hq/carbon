//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U32, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "MinimalSpotMarket")]
pub struct MinimalSpotMarketGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub pubkey: Pubkey,
    pub oracle: Pubkey,
    pub mint: Pubkey,
    pub vault: Pubkey,
    pub padding1: Vec<Vec<U64>>,
    pub padding2: Vec<U8>,
    pub deposit_balance: Vec<U8>,
    pub borrow_balance: Vec<U8>,
    pub cumulative_deposit_interest: Vec<U8>,
    pub cumulative_borrow_interest: Vec<U8>,
    pub padding3: Vec<U64>,
    pub last_interest_ts: U64,
    pub padding4: Vec<U64>,
    pub decimals: U32,
    pub market_index: i32,
    pub padding5: Vec<i32>,
    pub padding6: Vec<U8>,
    pub pool_id: U8,
    pub padding7: Vec<U64>,
}

impl TryFrom<crate::accounts::postgres::MinimalSpotMarketRow> for MinimalSpotMarketGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::MinimalSpotMarketRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            pubkey: carbon_core::graphql::primitives::Pubkey(row.pubkey.0),
            oracle: carbon_core::graphql::primitives::Pubkey(row.oracle.0),
            mint: carbon_core::graphql::primitives::Pubkey(row.mint.0),
            vault: carbon_core::graphql::primitives::Pubkey(row.vault.0),
            padding1: row
                .padding1
                .0
                .into_iter()
                .map(|item| {
                    item.into_iter()
                        .map(|item| carbon_core::graphql::primitives::U64(*item))
                        .collect()
                })
                .collect(),
            padding2: row
                .padding2
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            deposit_balance: row
                .deposit_balance
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            borrow_balance: row
                .borrow_balance
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            cumulative_deposit_interest: row
                .cumulative_deposit_interest
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            cumulative_borrow_interest: row
                .cumulative_borrow_interest
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding3: row
                .padding3
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(*item))
                .collect(),
            last_interest_ts: carbon_core::graphql::primitives::U64(*row.last_interest_ts),
            padding4: row
                .padding4
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(*item))
                .collect(),
            decimals: carbon_core::graphql::primitives::U32((*row.decimals) as u32),
            market_index: *row.market_index,
            padding5: row.padding5.into_iter().map(|item| *item).collect(),
            padding6: row
                .padding6
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            pool_id: carbon_core::graphql::primitives::U8((*row.pool_id) as u8),
            padding7: row
                .padding7
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(*item))
                .collect(),
        })
    }
}

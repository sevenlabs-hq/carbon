//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{ExecuteOrderBalanceRecordGraphQL, WrappedI80F48GraphQL},
    carbon_core::graphql::primitives::{Pubkey, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ExecuteOrderRecord")]
pub struct ExecuteOrderRecordGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub order: Pubkey,
    pub executor: Pubkey,
    pub balance_states: Vec<ExecuteOrderBalanceRecordGraphQL>,
    pub active_balance_count: U8,
    pub inactive_balance_count: U8,
    pub reserved0: Vec<U8>,
    pub order_start_health: WrappedI80F48GraphQL,
}

impl TryFrom<crate::accounts::postgres::ExecuteOrderRecordRow> for ExecuteOrderRecordGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(
        row: crate::accounts::postgres::ExecuteOrderRecordRow,
    ) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            order: carbon_core::graphql::primitives::Pubkey(row.order.0),
            executor: carbon_core::graphql::primitives::Pubkey(row.executor.0),
            balance_states: row
                .balance_states
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            active_balance_count: carbon_core::graphql::primitives::U8(
                (*row.active_balance_count) as u8,
            ),
            inactive_balance_count: carbon_core::graphql::primitives::U8(
                (*row.inactive_balance_count) as u8,
            ),
            reserved0: row
                .reserved0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            order_start_health: row.order_start_health.0.into(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod bank_metadata_row;
pub mod bank_row;
pub mod execute_order_record_row;
pub mod fee_state_row;
pub mod lending_row;
pub mod liquidation_record_row;
pub mod marginfi_account_row;
pub mod marginfi_group_row;
pub mod minimal_obligation_row;
pub mod minimal_reserve_row;
pub mod minimal_spot_market_row;
pub mod minimal_user_row;
pub mod order_row;
pub mod solend_minimal_reserve_row;
pub mod staked_settings_row;

pub use self::{
    bank_metadata_row::*, bank_row::*, execute_order_record_row::*, fee_state_row::*,
    lending_row::*, liquidation_record_row::*, marginfi_account_row::*, marginfi_group_row::*,
    minimal_obligation_row::*, minimal_reserve_row::*, minimal_spot_market_row::*,
    minimal_user_row::*, order_row::*, solend_minimal_reserve_row::*, staked_settings_row::*,
};
use super::MarginfiV2Account;

pub struct MarginfiV2AccountsMigration;

impl carbon_core::clickhouse::Migration for MarginfiV2AccountsMigration {
    fn app(&self) -> &str {
        "marginfi-v2"
    }

    fn name(&self) -> &str {
        "marginfi_v2_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BankRowMigrationOperation),
            Box::new(BankMetadataRowMigrationOperation),
            Box::new(ExecuteOrderRecordRowMigrationOperation),
            Box::new(FeeStateRowMigrationOperation),
            Box::new(LendingRowMigrationOperation),
            Box::new(LiquidationRecordRowMigrationOperation),
            Box::new(MarginfiAccountRowMigrationOperation),
            Box::new(MarginfiGroupRowMigrationOperation),
            Box::new(MinimalObligationRowMigrationOperation),
            Box::new(MinimalReserveRowMigrationOperation),
            Box::new(MinimalSpotMarketRowMigrationOperation),
            Box::new(MinimalUserRowMigrationOperation),
            Box::new(OrderRowMigrationOperation),
            Box::new(SolendMinimalReserveRowMigrationOperation),
            Box::new(StakedSettingsRowMigrationOperation),
        ]
    }
}

pub enum MarginfiV2AccountRow {
    Bank(BankRow),
    BankMetadata(BankMetadataRow),
    ExecuteOrderRecord(ExecuteOrderRecordRow),
    FeeState(FeeStateRow),
    Lending(LendingRow),
    LiquidationRecord(LiquidationRecordRow),
    MarginfiAccount(MarginfiAccountRow),
    MarginfiGroup(MarginfiGroupRow),
    MinimalObligation(MinimalObligationRow),
    MinimalReserve(MinimalReserveRow),
    MinimalSpotMarket(MinimalSpotMarketRow),
    MinimalUser(MinimalUserRow),
    Order(OrderRow),
    SolendMinimalReserve(SolendMinimalReserveRow),
    StakedSettings(StakedSettingsRow),
}

pub struct MarginfiV2AccountMetadata(
    pub carbon_core::account::AccountMetadata,
    pub MarginfiV2Account,
);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for MarginfiV2AccountMetadata {
    type Row = MarginfiV2AccountRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, account) = self;

        match account {
            MarginfiV2Account::Bank(account) => {
                rows.push(MarginfiV2AccountRow::Bank(BankRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MarginfiV2Account::BankMetadata(account) => {
                rows.push(MarginfiV2AccountRow::BankMetadata(
                    BankMetadataRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::ExecuteOrderRecord(account) => {
                rows.push(MarginfiV2AccountRow::ExecuteOrderRecord(
                    ExecuteOrderRecordRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::FeeState(account) => {
                rows.push(MarginfiV2AccountRow::FeeState(FeeStateRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MarginfiV2Account::Lending(account) => {
                rows.push(MarginfiV2AccountRow::Lending(LendingRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MarginfiV2Account::LiquidationRecord(account) => {
                rows.push(MarginfiV2AccountRow::LiquidationRecord(
                    LiquidationRecordRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MarginfiAccount(account) => {
                rows.push(MarginfiV2AccountRow::MarginfiAccount(
                    MarginfiAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MarginfiGroup(account) => {
                rows.push(MarginfiV2AccountRow::MarginfiGroup(
                    MarginfiGroupRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MinimalObligation(account) => {
                rows.push(MarginfiV2AccountRow::MinimalObligation(
                    MinimalObligationRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MinimalReserve(account) => {
                rows.push(MarginfiV2AccountRow::MinimalReserve(
                    MinimalReserveRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MinimalSpotMarket(account) => {
                rows.push(MarginfiV2AccountRow::MinimalSpotMarket(
                    MinimalSpotMarketRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::MinimalUser(account) => {
                rows.push(MarginfiV2AccountRow::MinimalUser(MinimalUserRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            MarginfiV2Account::Order(account) => {
                rows.push(MarginfiV2AccountRow::Order(OrderRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            MarginfiV2Account::SolendMinimalReserve(account) => {
                rows.push(MarginfiV2AccountRow::SolendMinimalReserve(
                    SolendMinimalReserveRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            MarginfiV2Account::StakedSettings(account) => {
                rows.push(MarginfiV2AccountRow::StakedSettings(
                    StakedSettingsRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MarginfiV2AccountRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(Bank, BankRow);
        commit_branch!(BankMetadata, BankMetadataRow);
        commit_branch!(ExecuteOrderRecord, ExecuteOrderRecordRow);
        commit_branch!(FeeState, FeeStateRow);
        commit_branch!(Lending, LendingRow);
        commit_branch!(LiquidationRecord, LiquidationRecordRow);
        commit_branch!(MarginfiAccount, MarginfiAccountRow);
        commit_branch!(MarginfiGroup, MarginfiGroupRow);
        commit_branch!(MinimalObligation, MinimalObligationRow);
        commit_branch!(MinimalReserve, MinimalReserveRow);
        commit_branch!(MinimalSpotMarket, MinimalSpotMarketRow);
        commit_branch!(MinimalUser, MinimalUserRow);
        commit_branch!(Order, OrderRow);
        commit_branch!(SolendMinimalReserve, SolendMinimalReserveRow);
        commit_branch!(StakedSettings, StakedSettingsRow);
        Ok(())
    }
}

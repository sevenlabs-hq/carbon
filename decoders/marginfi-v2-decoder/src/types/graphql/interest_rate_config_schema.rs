//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{RatePointGraphQL, WrappedI80F48GraphQL},
    carbon_core::graphql::primitives::{U32, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "InterestRateConfig")]
pub struct InterestRateConfigGraphQL {
    pub optimal_utilization_rate: WrappedI80F48GraphQL,
    pub plateau_interest_rate: WrappedI80F48GraphQL,
    pub max_interest_rate: WrappedI80F48GraphQL,
    pub insurance_fee_fixed_apr: WrappedI80F48GraphQL,
    pub insurance_ir_fee: WrappedI80F48GraphQL,
    pub protocol_fixed_fee_apr: WrappedI80F48GraphQL,
    pub protocol_ir_fee: WrappedI80F48GraphQL,
    pub protocol_origination_fee: WrappedI80F48GraphQL,
    pub zero_util_rate: U32,
    pub hundred_util_rate: U32,
    pub points: Vec<RatePointGraphQL>,
    pub curve_type: U8,
    pub pad0: Vec<U8>,
    pub padding1: Vec<U8>,
    pub padding2: Vec<U8>,
    pub padding3: Vec<U8>,
}

impl From<crate::types::InterestRateConfig> for InterestRateConfigGraphQL {
    fn from(original: crate::types::InterestRateConfig) -> Self {
        Self {
            optimal_utilization_rate: original.optimal_utilization_rate.into(),
            plateau_interest_rate: original.plateau_interest_rate.into(),
            max_interest_rate: original.max_interest_rate.into(),
            insurance_fee_fixed_apr: original.insurance_fee_fixed_apr.into(),
            insurance_ir_fee: original.insurance_ir_fee.into(),
            protocol_fixed_fee_apr: original.protocol_fixed_fee_apr.into(),
            protocol_ir_fee: original.protocol_ir_fee.into(),
            protocol_origination_fee: original.protocol_origination_fee.into(),
            zero_util_rate: carbon_core::graphql::primitives::U32(original.zero_util_rate),
            hundred_util_rate: carbon_core::graphql::primitives::U32(original.hundred_util_rate),
            points: original
                .points
                .into_iter()
                .map(|item| item.into())
                .collect(),
            curve_type: carbon_core::graphql::primitives::U8(original.curve_type),
            pad0: original
                .pad0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding1: original
                .padding1
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding2: original
                .padding2
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding3: original
                .padding3
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        }
    }
}

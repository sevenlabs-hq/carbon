//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{I64, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "LiquidationEntry")]
pub struct LiquidationEntryGraphQL {
    pub asset_amount_seized: Vec<U8>,
    pub liab_amount_repaid: Vec<U8>,
    pub placeholder0: U64,
    pub timestamp: I64,
    pub reserved0: Vec<U8>,
}

impl From<crate::types::LiquidationEntry> for LiquidationEntryGraphQL {
    fn from(original: crate::types::LiquidationEntry) -> Self {
        Self {
            asset_amount_seized: original
                .asset_amount_seized
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            liab_amount_repaid: original
                .liab_amount_repaid
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            placeholder0: carbon_core::graphql::primitives::U64(original.placeholder0),
            timestamp: carbon_core::graphql::primitives::I64(original.timestamp),
            reserved0: original
                .reserved0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        }
    }
}

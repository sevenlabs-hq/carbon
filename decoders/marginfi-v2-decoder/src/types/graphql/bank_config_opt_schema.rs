//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        BankOperationalStateGraphQL, InterestRateConfigOptGraphQL, RiskTierGraphQL,
        WrappedI80F48GraphQL,
    },
    carbon_core::graphql::primitives::{U32, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "BankConfigOpt")]
pub struct BankConfigOptGraphQL {
    pub asset_weight_init: Option<WrappedI80F48GraphQL>,
    pub asset_weight_maint: Option<WrappedI80F48GraphQL>,
    pub liability_weight_init: Option<WrappedI80F48GraphQL>,
    pub liability_weight_maint: Option<WrappedI80F48GraphQL>,
    pub deposit_limit: Option<U64>,
    pub borrow_limit: Option<U64>,
    pub operational_state: Option<BankOperationalStateGraphQL>,
    pub interest_rate_config: Option<InterestRateConfigOptGraphQL>,
    pub risk_tier: Option<RiskTierGraphQL>,
    pub asset_tag: Option<U8>,
    pub total_asset_value_init_limit: Option<U64>,
    pub oracle_max_confidence: Option<U32>,
    pub oracle_max_age: Option<i32>,
    pub permissionless_bad_debt_settlement: Option<bool>,
    pub freeze_settings: Option<bool>,
    pub tokenless_repayments_allowed: Option<bool>,
}

impl From<crate::types::BankConfigOpt> for BankConfigOptGraphQL {
    fn from(original: crate::types::BankConfigOpt) -> Self {
        Self {
            asset_weight_init: original.asset_weight_init.map(|v| v.into()),
            asset_weight_maint: original.asset_weight_maint.map(|v| v.into()),
            liability_weight_init: original.liability_weight_init.map(|v| v.into()),
            liability_weight_maint: original.liability_weight_maint.map(|v| v.into()),
            deposit_limit: original
                .deposit_limit
                .map(carbon_core::graphql::primitives::U64),
            borrow_limit: original
                .borrow_limit
                .map(carbon_core::graphql::primitives::U64),
            operational_state: original.operational_state.map(|v| v.into()),
            interest_rate_config: original.interest_rate_config.map(|v| v.into()),
            risk_tier: original.risk_tier.map(|v| v.into()),
            asset_tag: original.asset_tag.map(carbon_core::graphql::primitives::U8),
            total_asset_value_init_limit: original
                .total_asset_value_init_limit
                .map(carbon_core::graphql::primitives::U64),
            oracle_max_confidence: original
                .oracle_max_confidence
                .map(carbon_core::graphql::primitives::U32),
            oracle_max_age: original.oracle_max_age.map(|v| v as i32),
            permissionless_bad_debt_settlement: original.permissionless_bad_debt_settlement,
            freeze_settings: original.freeze_settings,
            tokenless_repayments_allowed: original.tokenless_repayments_allowed,
        }
    }
}

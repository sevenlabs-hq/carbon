//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{
        BankOperationalStateGraphQL, InterestRateConfigGraphQL, OracleSetupGraphQL,
        RiskTierGraphQL, WrappedI80F48GraphQL,
    },
    carbon_core::graphql::primitives::{Pubkey, U32, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "BankConfig")]
pub struct BankConfigGraphQL {
    pub asset_weight_init: WrappedI80F48GraphQL,
    pub asset_weight_maint: WrappedI80F48GraphQL,
    pub liability_weight_init: WrappedI80F48GraphQL,
    pub liability_weight_maint: WrappedI80F48GraphQL,
    pub deposit_limit: U64,
    pub interest_rate_config: InterestRateConfigGraphQL,
    pub operational_state: BankOperationalStateGraphQL,
    pub oracle_setup: OracleSetupGraphQL,
    pub oracle_keys: Vec<Pubkey>,
    pub pad0: Vec<U8>,
    pub borrow_limit: U64,
    pub risk_tier: RiskTierGraphQL,
    pub asset_tag: U8,
    pub config_flags: U8,
    pub pad1: Vec<U8>,
    pub total_asset_value_init_limit: U64,
    pub oracle_max_age: i32,
    pub padding0: Vec<U8>,
    pub oracle_max_confidence: U32,
    pub fixed_price: WrappedI80F48GraphQL,
    pub padding1: Vec<U8>,
}

impl From<crate::types::BankConfig> for BankConfigGraphQL {
    fn from(original: crate::types::BankConfig) -> Self {
        Self {
            asset_weight_init: original.asset_weight_init.into(),
            asset_weight_maint: original.asset_weight_maint.into(),
            liability_weight_init: original.liability_weight_init.into(),
            liability_weight_maint: original.liability_weight_maint.into(),
            deposit_limit: carbon_core::graphql::primitives::U64(original.deposit_limit),
            interest_rate_config: original.interest_rate_config.into(),
            operational_state: original.operational_state.into(),
            oracle_setup: original.oracle_setup.into(),
            oracle_keys: original
                .oracle_keys
                .into_iter()
                .map(carbon_core::graphql::primitives::Pubkey)
                .collect(),
            pad0: original
                .pad0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            borrow_limit: carbon_core::graphql::primitives::U64(original.borrow_limit),
            risk_tier: original.risk_tier.into(),
            asset_tag: carbon_core::graphql::primitives::U8(original.asset_tag),
            config_flags: carbon_core::graphql::primitives::U8(original.config_flags),
            pad1: original
                .pad1
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            total_asset_value_init_limit: carbon_core::graphql::primitives::U64(
                original.total_asset_value_init_limit,
            ),
            oracle_max_age: original.oracle_max_age as i32,
            padding0: original
                .padding0
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            oracle_max_confidence: carbon_core::graphql::primitives::U32(
                original.oracle_max_confidence,
            ),
            fixed_price: original.fixed_price.into(),
            padding1: original
                .padding1
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        }
    }
}

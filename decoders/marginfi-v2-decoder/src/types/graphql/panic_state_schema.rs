//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{I64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "PanicState")]
pub struct PanicStateGraphQL {
    pub pause_flags: U8,
    pub daily_pause_count: U8,
    pub consecutive_pause_count: U8,
    pub reserved: Vec<U8>,
    pub pause_start_timestamp: I64,
    pub last_daily_reset_timestamp: I64,
    pub reserved_space: Vec<U8>,
}

impl From<crate::types::PanicState> for PanicStateGraphQL {
    fn from(original: crate::types::PanicState) -> Self {
        Self {
            pause_flags: carbon_core::graphql::primitives::U8(original.pause_flags),
            daily_pause_count: carbon_core::graphql::primitives::U8(original.daily_pause_count),
            consecutive_pause_count: carbon_core::graphql::primitives::U8(
                original.consecutive_pause_count,
            ),
            reserved: original
                .reserved
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            pause_start_timestamp: carbon_core::graphql::primitives::I64(
                original.pause_start_timestamp,
            ),
            last_daily_reset_timestamp: carbon_core::graphql::primitives::I64(
                original.last_daily_reset_timestamp,
            ),
            reserved_space: original
                .reserved_space
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{RatePointGraphQL, WrappedI80F48GraphQL},
    carbon_core::graphql::primitives::U32,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "InterestRateConfigOpt")]
pub struct InterestRateConfigOptGraphQL {
    pub insurance_fee_fixed_apr: Option<WrappedI80F48GraphQL>,
    pub insurance_ir_fee: Option<WrappedI80F48GraphQL>,
    pub protocol_fixed_fee_apr: Option<WrappedI80F48GraphQL>,
    pub protocol_ir_fee: Option<WrappedI80F48GraphQL>,
    pub protocol_origination_fee: Option<WrappedI80F48GraphQL>,
    pub zero_util_rate: Option<U32>,
    pub hundred_util_rate: Option<U32>,
    pub points: Option<Vec<RatePointGraphQL>>,
}

impl From<crate::types::InterestRateConfigOpt> for InterestRateConfigOptGraphQL {
    fn from(original: crate::types::InterestRateConfigOpt) -> Self {
        Self {
            insurance_fee_fixed_apr: original.insurance_fee_fixed_apr.map(|v| v.into()),
            insurance_ir_fee: original.insurance_ir_fee.map(|v| v.into()),
            protocol_fixed_fee_apr: original.protocol_fixed_fee_apr.map(|v| v.into()),
            protocol_ir_fee: original.protocol_ir_fee.map(|v| v.into()),
            protocol_origination_fee: original.protocol_origination_fee.map(|v| v.into()),
            zero_util_rate: original
                .zero_util_rate
                .map(carbon_core::graphql::primitives::U32),
            hundred_util_rate: original
                .hundred_util_rate
                .map(carbon_core::graphql::primitives::U32),
            points: original
                .points
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
        }
    }
}

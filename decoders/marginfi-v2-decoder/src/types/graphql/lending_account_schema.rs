//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::BalanceGraphQL,
    carbon_core::graphql::primitives::{U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "LendingAccount")]
pub struct LendingAccountGraphQL {
    pub balances: Vec<BalanceGraphQL>,
    pub last_tag_used: i32,
    pub pad1: Vec<U8>,
    pub padding: Vec<U64>,
}

impl From<crate::types::LendingAccount> for LendingAccountGraphQL {
    fn from(original: crate::types::LendingAccount) -> Self {
        Self {
            balances: original
                .balances
                .into_iter()
                .map(|item| item.into())
                .collect(),
            last_tag_used: original.last_tag_used as i32,
            pad1: original
                .pad1
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
            padding: original
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
        }
    }
}

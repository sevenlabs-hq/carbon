//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::EmodeEntryGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "EmodeConfig")]
pub struct EmodeConfigGraphQL {
    pub entries: Vec<EmodeEntryGraphQL>,
}

impl From<crate::types::EmodeConfig> for EmodeConfigGraphQL {
    fn from(original: crate::types::EmodeConfig) -> Self {
        Self {
            entries: original
                .entries
                .into_iter()
                .map(|item| item.into())
                .collect(),
        }
    }
}

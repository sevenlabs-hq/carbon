//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{OracleSetup, RiskTier, WrappedI80F48},
    solana_pubkey::Pubkey,
};
/// Used to configure JupLend banks. A simplified version of `BankConfigCompact`
/// which omits most values related to interest since JupLend banks cannot earn
/// interest or be borrowed against. Note: JupLend banks do not take an
/// Operational State, they always start in `Paused` state and are set to
/// `Operational` via `juplend_init_position` (seed deposit + protocol fToken
/// vault).
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct JuplendConfigCompact {
    pub oracle: Pubkey,
    pub asset_weight_init: WrappedI80F48,
    pub asset_weight_maint: WrappedI80F48,
    pub deposit_limit: u64,
    /// Either `JuplendPythPull` or `JuplendSwitchboardPull`
    pub oracle_setup: OracleSetup,
    /// Isolated or Collateral
    pub risk_tier: RiskTier,
    /// Config flags for future-proofing, currently ignored
    pub config_flags: u8,
    /// In $
    pub total_asset_value_init_limit: u64,
    /// In seconds
    pub oracle_max_age: u16,
    /// Oracle confidence threshold (0 = use default 10%)
    pub oracle_max_confidence: u32,
}

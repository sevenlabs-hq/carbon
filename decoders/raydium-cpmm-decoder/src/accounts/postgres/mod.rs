//! This code was AUTOGENERATED using the Codama library.
pub mod amm_config_row;
pub mod observation_state_row;
pub mod pool_state_row;

pub use self::{amm_config_row::*, observation_state_row::*, pool_state_row::*};
use super::RaydiumCpmmAccount;

pub struct RaydiumCpmmAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaydiumCpmmAccountsMigration {
    fn app(&self) -> &str {
        "raydium-cpmm"
    }

    fn name(&self) -> &str {
        "raydium_cpmm_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AmmConfigMigrationOperation),
            Box::new(ObservationStateMigrationOperation),
            Box::new(PoolStateMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaydiumCpmmAccountWithMetadata(
    pub RaydiumCpmmAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(RaydiumCpmmAccount, carbon_core::account::AccountMetadata)>
    for RaydiumCpmmAccountWithMetadata
{
    fn from(value: (RaydiumCpmmAccount, carbon_core::account::AccountMetadata)) -> Self {
        RaydiumCpmmAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaydiumCpmmAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumCpmmAccountWithMetadata(account, metadata) = self;

        match account {
            RaydiumCpmmAccount::AmmConfig(account) => {
                let row =
                    amm_config_row::AmmConfigRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmAccount::ObservationState(account) => {
                let row = observation_state_row::ObservationStateRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmAccount::PoolState(account) => {
                let row =
                    pool_state_row::PoolStateRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaydiumCpmmAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumCpmmAccountWithMetadata(account, metadata) = self;
        match account {
            RaydiumCpmmAccount::AmmConfig(account) => {
                let row =
                    amm_config_row::AmmConfigRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmAccount::ObservationState(account) => {
                let row = observation_state_row::ObservationStateRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmAccount::PoolState(account) => {
                let row =
                    pool_state_row::PoolStateRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod collect_fund_fee_row;
pub mod collect_protocol_fee_row;
pub mod cpi_event_row;
pub mod create_amm_config_row;
pub mod deposit_row;
pub mod initialize_row;
pub mod swap_base_input_row;
pub mod swap_base_output_row;
pub mod update_amm_config_row;
pub mod update_pool_status_row;
pub mod withdraw_row;

pub use self::{
    collect_fund_fee_row::*, collect_protocol_fee_row::*, cpi_event_row::*,
    create_amm_config_row::*, deposit_row::*, initialize_row::*, swap_base_input_row::*,
    swap_base_output_row::*, update_amm_config_row::*, update_pool_status_row::*, withdraw_row::*,
};
use super::RaydiumCpmmInstruction;

pub struct RaydiumCpmmInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaydiumCpmmInstructionsMigration {
    fn app(&self) -> &str {
        "raydium-cpmm"
    }

    fn name(&self) -> &str {
        "raydium_cpmm_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CollectFundFeeMigrationOperation),
            Box::new(CollectProtocolFeeMigrationOperation),
            Box::new(CreateAmmConfigMigrationOperation),
            Box::new(DepositMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(SwapBaseInputMigrationOperation),
            Box::new(SwapBaseOutputMigrationOperation),
            Box::new(UpdateAmmConfigMigrationOperation),
            Box::new(UpdatePoolStatusMigrationOperation),
            Box::new(WithdrawMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaydiumCpmmInstructionWithMetadata(
    pub RaydiumCpmmInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        RaydiumCpmmInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for RaydiumCpmmInstructionWithMetadata
{
    fn from(
        value: (
            RaydiumCpmmInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        RaydiumCpmmInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaydiumCpmmInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumCpmmInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            RaydiumCpmmInstruction::CollectFundFee { data, .. } => {
                let row = collect_fund_fee_row::CollectFundFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CollectProtocolFee { data, .. } => {
                let row = collect_protocol_fee_row::CollectProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CreateAmmConfig { data, .. } => {
                let row = create_amm_config_row::CreateAmmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::SwapBaseInput { data, .. } => {
                let row = swap_base_input_row::SwapBaseInputRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::SwapBaseOutput { data, .. } => {
                let row = swap_base_output_row::SwapBaseOutputRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::UpdateAmmConfig { data, .. } => {
                let row = update_amm_config_row::UpdateAmmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::UpdatePoolStatus { data, .. } => {
                let row = update_pool_status_row::UpdatePoolStatusRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaydiumCpmmInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaydiumCpmmInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            RaydiumCpmmInstruction::CollectFundFee { data, .. } => {
                let row = collect_fund_fee_row::CollectFundFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CollectProtocolFee { data, .. } => {
                let row = collect_protocol_fee_row::CollectProtocolFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CreateAmmConfig { data, .. } => {
                let row = create_amm_config_row::CreateAmmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Deposit { data, .. } => {
                let row = deposit_row::DepositRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::SwapBaseInput { data, .. } => {
                let row = swap_base_input_row::SwapBaseInputRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::SwapBaseOutput { data, .. } => {
                let row = swap_base_output_row::SwapBaseOutputRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::UpdateAmmConfig { data, .. } => {
                let row = update_amm_config_row::UpdateAmmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::UpdatePoolStatus { data, .. } => {
                let row = update_pool_status_row::UpdatePoolStatusRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::Withdraw { data, .. } => {
                let row = withdraw_row::WithdrawRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaydiumCpmmInstruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

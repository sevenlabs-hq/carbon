//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Withdraw lp for token0 and token1
/// # Arguments
/// * `ctx`- The context of accounts
/// * `lp_token_amount` - Amount of pool tokens to burn. User receives an output
///   of token a and b based on the percentage of the pool tokens that are
///   returned.
/// * `minimum_token_0_amount` -  Minimum amount of token 0 to receive, prevents
///   excessive slippage
/// * `minimum_token_1_amount` -  Minimum amount of token 1 to receive, prevents
///   excessive slippage
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Withdraw {
    pub lp_token_amount: u64,
    pub minimum_token0_amount: u64,
    pub minimum_token1_amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawInstructionAccounts {
    pub owner: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub pool_state: solana_pubkey::Pubkey,
    pub owner_lp_token: solana_pubkey::Pubkey,
    pub token0_account: solana_pubkey::Pubkey,
    pub token1_account: solana_pubkey::Pubkey,
    pub token0_vault: solana_pubkey::Pubkey,
    pub token1_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub token_program2022: solana_pubkey::Pubkey,
    pub vault0_mint: solana_pubkey::Pubkey,
    pub vault1_mint: solana_pubkey::Pubkey,
    pub lp_mint: solana_pubkey::Pubkey,
    pub memo_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Withdraw {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [183, 18, 70, 156, 148, 109, 161, 34] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Withdraw {
    type ArrangedAccounts = WithdrawInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let owner = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let pool_state = next_account(&mut iter)?;
        let owner_lp_token = next_account(&mut iter)?;
        let token0_account = next_account(&mut iter)?;
        let token1_account = next_account(&mut iter)?;
        let token0_vault = next_account(&mut iter)?;
        let token1_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let token_program2022 = next_account(&mut iter)?;
        let vault0_mint = next_account(&mut iter)?;
        let vault1_mint = next_account(&mut iter)?;
        let lp_mint = next_account(&mut iter)?;
        let memo_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawInstructionAccounts {
            owner,
            authority,
            pool_state,
            owner_lp_token,
            token0_account,
            token1_account,
            token0_vault,
            token1_vault,
            token_program,
            token_program2022,
            vault0_mint,
            vault1_mint,
            lp_mint,
            memo_program,
            remaining: remaining.to_vec(),
        })
    }
}

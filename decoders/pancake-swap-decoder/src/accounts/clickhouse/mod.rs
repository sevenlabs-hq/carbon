//! This code was AUTOGENERATED using the Codama library.
pub mod amm_config_row;
pub mod observation_state_row;
pub mod operation_state_row;
pub mod permissionless_farm_switch_row;
pub mod personal_position_state_row;
pub mod pool_state_row;
pub mod protocol_position_state_row;
pub mod support_mint_associated_row;
pub mod tick_array_bitmap_extension_row;
pub mod tick_array_state_row;

pub use self::{
    amm_config_row::*, observation_state_row::*, operation_state_row::*,
    permissionless_farm_switch_row::*, personal_position_state_row::*, pool_state_row::*,
    protocol_position_state_row::*, support_mint_associated_row::*,
    tick_array_bitmap_extension_row::*, tick_array_state_row::*,
};
use super::PancakeSwapAccount;

pub struct PancakeSwapAccountsMigration;

impl carbon_core::clickhouse::Migration for PancakeSwapAccountsMigration {
    fn app(&self) -> &str {
        "pancake-swap"
    }

    fn name(&self) -> &str {
        "pancake_swap_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(AmmConfigRowMigrationOperation),
            Box::new(ObservationStateRowMigrationOperation),
            Box::new(OperationStateRowMigrationOperation),
            Box::new(PermissionlessFarmSwitchRowMigrationOperation),
            Box::new(PersonalPositionStateRowMigrationOperation),
            Box::new(PoolStateRowMigrationOperation),
            Box::new(ProtocolPositionStateRowMigrationOperation),
            Box::new(SupportMintAssociatedRowMigrationOperation),
            Box::new(TickArrayBitmapExtensionRowMigrationOperation),
            Box::new(TickArrayStateRowMigrationOperation),
        ]
    }
}

pub enum PancakeSwapAccountRow {
    AmmConfig(AmmConfigRow),
    ObservationState(ObservationStateRow),
    OperationState(OperationStateRow),
    PermissionlessFarmSwitch(PermissionlessFarmSwitchRow),
    PersonalPositionState(PersonalPositionStateRow),
    PoolState(PoolStateRow),
    ProtocolPositionState(ProtocolPositionStateRow),
    SupportMintAssociated(SupportMintAssociatedRow),
    TickArrayBitmapExtension(TickArrayBitmapExtensionRow),
    TickArrayState(TickArrayStateRow),
}

pub struct PancakeSwapAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a PancakeSwapAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for PancakeSwapAccountMetadata<'a> {
    type Row = PancakeSwapAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let PancakeSwapAccount::$variant(account) = account {
                    rows.push(PancakeSwapAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let PancakeSwapAccount::$variant(account) = account {
                    rows.push(PancakeSwapAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(AmmConfig, AmmConfigRow, boxed);
        insert_branch!(ObservationState, ObservationStateRow, boxed);
        insert_branch!(OperationState, OperationStateRow, boxed);
        insert_branch!(PermissionlessFarmSwitch, PermissionlessFarmSwitchRow, boxed);
        insert_branch!(PersonalPositionState, PersonalPositionStateRow, boxed);
        insert_branch!(PoolState, PoolStateRow, boxed);
        insert_branch!(ProtocolPositionState, ProtocolPositionStateRow, boxed);
        insert_branch!(SupportMintAssociated, SupportMintAssociatedRow, boxed);
        insert_branch!(TickArrayBitmapExtension, TickArrayBitmapExtensionRow, boxed);
        insert_branch!(TickArrayState, TickArrayStateRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for PancakeSwapAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(AmmConfig, AmmConfigRow);
        commit_branch!(ObservationState, ObservationStateRow);
        commit_branch!(OperationState, OperationStateRow);
        commit_branch!(PermissionlessFarmSwitch, PermissionlessFarmSwitchRow);
        commit_branch!(PersonalPositionState, PersonalPositionStateRow);
        commit_branch!(PoolState, PoolStateRow);
        commit_branch!(ProtocolPositionState, ProtocolPositionStateRow);
        commit_branch!(SupportMintAssociated, SupportMintAssociatedRow);
        commit_branch!(TickArrayBitmapExtension, TickArrayBitmapExtensionRow);
        commit_branch!(TickArrayState, TickArrayStateRow);

        Ok(())
    }
}

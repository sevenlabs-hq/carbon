//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{metadata::AccountRowMetadata, primitives::U8},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct PermissionlessFarmSwitchRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub bump: U8,
    pub is_on: bool,
}

impl PermissionlessFarmSwitchRow {
    pub fn from_parts(
        source: crate::accounts::permissionless_farm_switch::PermissionlessFarmSwitch,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            bump: source.bump.into(),
            is_on: source.is_on,
        }
    }
}

impl TryFrom<PermissionlessFarmSwitchRow>
    for crate::accounts::permissionless_farm_switch::PermissionlessFarmSwitch
{
    type Error = carbon_core::error::Error;
    fn try_from(source: PermissionlessFarmSwitchRow) -> Result<Self, Self::Error> {
        Ok(Self {
            bump: source.bump.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            is_on: source.is_on,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::permissionless_farm_switch::PermissionlessFarmSwitch
{
    fn table() -> &'static str {
        "permissionless_farm_switch_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "bump", "is_on"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PermissionlessFarmSwitchRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO permissionless_farm_switch_account (
                "bump",
                "is_on",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            )"#,
        )
        .bind(self.bump)
        .bind(self.is_on)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PermissionlessFarmSwitchRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO permissionless_farm_switch_account (
                "bump",
                "is_on",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "bump" = EXCLUDED."bump",
                "is_on" = EXCLUDED."is_on",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.bump)
        .bind(self.is_on)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for PermissionlessFarmSwitchRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM permissionless_farm_switch_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for PermissionlessFarmSwitchRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM permissionless_farm_switch_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct PermissionlessFarmSwitchMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for PermissionlessFarmSwitchMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS permissionless_farm_switch_account (
                -- Account data
                "bump" INT2 NOT NULL,
                "is_on" BOOLEAN NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS permissionless_farm_switch_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U16, U64, U8},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct AddressLookupTableRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub deactivation_slot: U64,
    pub last_extended_slot: U64,
    pub last_extended_slot_start_index: U8,
    pub authority: Option<Pubkey>,
    pub padding: U16,
    pub addresses: Vec<Pubkey>,
}

impl AddressLookupTableRow {
    pub fn from_parts(
        source: crate::accounts::address_lookup_table::AddressLookupTable,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            deactivation_slot: source.deactivation_slot.into(),
            last_extended_slot: source.last_extended_slot.into(),
            last_extended_slot_start_index: source.last_extended_slot_start_index.into(),
            authority: source.authority.map(|value| value.into()),
            padding: source.padding.into(),
            addresses: source
                .addresses
                .into_iter()
                .map(|element| element.into())
                .collect(),
        }
    }
}

impl TryFrom<AddressLookupTableRow> for crate::accounts::address_lookup_table::AddressLookupTable {
    type Error = carbon_core::error::Error;
    fn try_from(source: AddressLookupTableRow) -> Result<Self, Self::Error> {
        Ok(Self {
            deactivation_slot: *source.deactivation_slot,
            last_extended_slot: *source.last_extended_slot,
            last_extended_slot_start_index: source
                .last_extended_slot_start_index
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert value from postgres primitive".to_string(),
                    )
                })?,
            authority: source.authority.map(|value| *value),
            padding: source.padding.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            addresses: source
                .addresses
                .into_iter()
                .map(|element| *element)
                .collect(),
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::address_lookup_table::AddressLookupTable
{
    fn table() -> &'static str {
        "address_lookup_table_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "deactivation_slot",
            "last_extended_slot",
            "last_extended_slot_start_index",
            "authority",
            "padding",
            "addresses",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddressLookupTableRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO address_lookup_table_account (
                "deactivation_slot",
                "last_extended_slot",
                "last_extended_slot_start_index",
                "authority",
                "padding",
                "addresses",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(&self.deactivation_slot)
        .bind(&self.last_extended_slot)
        .bind(self.last_extended_slot_start_index)
        .bind(self.authority)
        .bind(self.padding)
        .bind(&self.addresses)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddressLookupTableRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO address_lookup_table_account (
                "deactivation_slot",
                "last_extended_slot",
                "last_extended_slot_start_index",
                "authority",
                "padding",
                "addresses",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "deactivation_slot" = EXCLUDED."deactivation_slot",
                "last_extended_slot" = EXCLUDED."last_extended_slot",
                "last_extended_slot_start_index" = EXCLUDED."last_extended_slot_start_index",
                "authority" = EXCLUDED."authority",
                "padding" = EXCLUDED."padding",
                "addresses" = EXCLUDED."addresses",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.deactivation_slot)
        .bind(&self.last_extended_slot)
        .bind(self.last_extended_slot_start_index)
        .bind(self.authority)
        .bind(self.padding)
        .bind(&self.addresses)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for AddressLookupTableRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM address_lookup_table_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for AddressLookupTableRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM address_lookup_table_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct AddressLookupTableMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for AddressLookupTableMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS address_lookup_table_account (
                -- Account data
                "deactivation_slot" NUMERIC(20) NOT NULL,
                "last_extended_slot" NUMERIC(20) NOT NULL,
                "last_extended_slot_start_index" INT2 NOT NULL,
                "authority" BYTEA,
                "padding" INT4 NOT NULL,
                "addresses" BYTEA[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS address_lookup_table_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

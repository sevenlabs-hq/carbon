//! This code was AUTOGENERATED using the Codama library.
pub mod address_lookup_table_row;

pub use self::address_lookup_table_row::*;
use super::AddressLookupTableAccount;

pub struct AddressLookupTableAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for AddressLookupTableAccountsMigration {
    fn app(&self) -> &str {
        "address-lookup-table"
    }

    fn name(&self) -> &str {
        "address_lookup_table_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![Box::new(AddressLookupTableMigrationOperation)]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct AddressLookupTableAccountWithMetadata(
    pub AddressLookupTableAccount,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        AddressLookupTableAccount,
        carbon_core::account::AccountMetadata,
    )> for AddressLookupTableAccountWithMetadata
{
    fn from(
        value: (
            AddressLookupTableAccount,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        AddressLookupTableAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddressLookupTableAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AddressLookupTableAccountWithMetadata(account, metadata) = self;

        match account {
            AddressLookupTableAccount::AddressLookupTable(account) => {
                let row = address_lookup_table_row::AddressLookupTableRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddressLookupTableAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AddressLookupTableAccountWithMetadata(account, metadata) = self;
        match account {
            AddressLookupTableAccount::AddressLookupTable(account) => {
                let row = address_lookup_table_row::AddressLookupTableRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod close_lookup_table_row;
pub mod create_lookup_table_row;
pub mod deactivate_lookup_table_row;
pub mod extend_lookup_table_row;
pub mod freeze_lookup_table_row;

pub use self::{
    close_lookup_table_row::*, create_lookup_table_row::*, deactivate_lookup_table_row::*,
    extend_lookup_table_row::*, freeze_lookup_table_row::*,
};
use super::AddressLookupTableInstruction;

pub struct AddressLookupTableInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for AddressLookupTableInstructionsMigration {
    fn app(&self) -> &str {
        "address-lookup-table"
    }

    fn name(&self) -> &str {
        "address_lookup_table_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CloseLookupTableMigrationOperation),
            Box::new(CreateLookupTableMigrationOperation),
            Box::new(DeactivateLookupTableMigrationOperation),
            Box::new(ExtendLookupTableMigrationOperation),
            Box::new(FreezeLookupTableMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct AddressLookupTableInstructionWithMetadata(
    pub AddressLookupTableInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        AddressLookupTableInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for AddressLookupTableInstructionWithMetadata
{
    fn from(
        value: (
            AddressLookupTableInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        AddressLookupTableInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for AddressLookupTableInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AddressLookupTableInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            AddressLookupTableInstruction::CreateLookupTable { data, .. } => {
                let row = create_lookup_table_row::CreateLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::FreezeLookupTable { data, .. } => {
                let row = freeze_lookup_table_row::FreezeLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::ExtendLookupTable { data, .. } => {
                let row = extend_lookup_table_row::ExtendLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::DeactivateLookupTable { data, .. } => {
                let row = deactivate_lookup_table_row::DeactivateLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::CloseLookupTable { data, .. } => {
                let row = close_lookup_table_row::CloseLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for AddressLookupTableInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let AddressLookupTableInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            AddressLookupTableInstruction::CreateLookupTable { data, .. } => {
                let row = create_lookup_table_row::CreateLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::FreezeLookupTable { data, .. } => {
                let row = freeze_lookup_table_row::FreezeLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::ExtendLookupTable { data, .. } => {
                let row = extend_lookup_table_row::ExtendLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::DeactivateLookupTable { data, .. } => {
                let row = deactivate_lookup_table_row::DeactivateLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            AddressLookupTableInstruction::CloseLookupTable { data, .. } => {
                let row = close_lookup_table_row::CloseLookupTableRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

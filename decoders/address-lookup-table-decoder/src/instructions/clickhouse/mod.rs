//! This code was AUTOGENERATED using the Codama library.
pub mod close_lookup_table_row;
pub mod create_lookup_table_row;
pub mod deactivate_lookup_table_row;
pub mod extend_lookup_table_row;
pub mod freeze_lookup_table_row;

pub use self::{
    close_lookup_table_row::*, create_lookup_table_row::*, deactivate_lookup_table_row::*,
    extend_lookup_table_row::*, freeze_lookup_table_row::*,
};
use super::AddressLookupTableInstruction;

pub struct AddressLookupTableInstructionsMigration;

impl carbon_core::clickhouse::Migration for AddressLookupTableInstructionsMigration {
    fn app(&self) -> &str {
        "address-lookup-table"
    }

    fn name(&self) -> &str {
        "address_lookup_table_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(CloseLookupTableRowMigrationOperation),
            Box::new(CreateLookupTableRowMigrationOperation),
            Box::new(DeactivateLookupTableRowMigrationOperation),
            Box::new(ExtendLookupTableRowMigrationOperation),
            Box::new(FreezeLookupTableRowMigrationOperation),
        ]
    }
}

pub enum AddressLookupTableInstructionRow {
    CloseLookupTable(CloseLookupTableRow),
    CreateLookupTable(CreateLookupTableRow),
    DeactivateLookupTable(DeactivateLookupTableRow),
    ExtendLookupTable(ExtendLookupTableRow),
    FreezeLookupTable(FreezeLookupTableRow),
}

pub struct AddressLookupTableInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a AddressLookupTableInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for AddressLookupTableInstructionMetadata<'a> {
    type Row = AddressLookupTableInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let AddressLookupTableInstruction::$variant { data, accounts, .. } = instruction
                {
                    rows.push(AddressLookupTableInstructionRow::$variant(
                        <$row>::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                    ));
                    return Ok(());
                }
            };
        }

        insert_branch!(CloseLookupTable, CloseLookupTableRow);
        insert_branch!(CreateLookupTable, CreateLookupTableRow);
        insert_branch!(DeactivateLookupTable, DeactivateLookupTableRow);
        insert_branch!(ExtendLookupTable, ExtendLookupTableRow);
        insert_branch!(FreezeLookupTable, FreezeLookupTableRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for AddressLookupTableInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(CloseLookupTable, CloseLookupTableRow);
        commit_branch!(CreateLookupTable, CreateLookupTableRow);
        commit_branch!(DeactivateLookupTable, DeactivateLookupTableRow);
        commit_branch!(ExtendLookupTable, ExtendLookupTableRow);
        commit_branch!(FreezeLookupTable, FreezeLookupTableRow);

        Ok(())
    }
}

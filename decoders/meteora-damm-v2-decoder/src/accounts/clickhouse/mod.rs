//! This code was AUTOGENERATED using the Codama library.
pub mod config_row;
pub mod operator_row;
pub mod pod_aligned_fee_market_cap_scheduler_row;
pub mod pod_aligned_fee_rate_limiter_row;
pub mod pod_aligned_fee_time_scheduler_row;
pub mod pool_row;
pub mod position_row;
pub mod token_badge_row;
pub mod vesting_row;

pub use self::{
    config_row::*, operator_row::*, pod_aligned_fee_market_cap_scheduler_row::*,
    pod_aligned_fee_rate_limiter_row::*, pod_aligned_fee_time_scheduler_row::*, pool_row::*,
    position_row::*, token_badge_row::*, vesting_row::*,
};
use super::MeteoraDammV2Account;

pub struct MeteoraDammV2AccountsMigration;

impl carbon_core::clickhouse::Migration for MeteoraDammV2AccountsMigration {
    fn app(&self) -> &str {
        "meteora-damm-v2"
    }

    fn name(&self) -> &str {
        "meteora_damm_v2_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(ConfigRowMigrationOperation),
            Box::new(OperatorRowMigrationOperation),
            Box::new(PodAlignedFeeMarketCapSchedulerRowMigrationOperation),
            Box::new(PodAlignedFeeRateLimiterRowMigrationOperation),
            Box::new(PodAlignedFeeTimeSchedulerRowMigrationOperation),
            Box::new(PoolRowMigrationOperation),
            Box::new(PositionRowMigrationOperation),
            Box::new(TokenBadgeRowMigrationOperation),
            Box::new(VestingRowMigrationOperation),
        ]
    }
}

pub enum MeteoraDammV2AccountRow {
    Config(ConfigRow),
    Operator(OperatorRow),
    PodAlignedFeeMarketCapScheduler(PodAlignedFeeMarketCapSchedulerRow),
    PodAlignedFeeRateLimiter(PodAlignedFeeRateLimiterRow),
    PodAlignedFeeTimeScheduler(PodAlignedFeeTimeSchedulerRow),
    Pool(PoolRow),
    Position(PositionRow),
    TokenBadge(TokenBadgeRow),
    Vesting(VestingRow),
}

pub struct MeteoraDammV2AccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a MeteoraDammV2Account,
);

impl<'a> carbon_core::clickhouse::BatchInsert for MeteoraDammV2AccountMetadata<'a> {
    type Row = MeteoraDammV2AccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let MeteoraDammV2Account::$variant(account) = account {
                    rows.push(MeteoraDammV2AccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let MeteoraDammV2Account::$variant(account) = account {
                    rows.push(MeteoraDammV2AccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Config, ConfigRow, boxed);
        insert_branch!(Operator, OperatorRow, boxed);
        insert_branch!(
            PodAlignedFeeMarketCapScheduler,
            PodAlignedFeeMarketCapSchedulerRow,
            boxed
        );
        insert_branch!(PodAlignedFeeRateLimiter, PodAlignedFeeRateLimiterRow, boxed);
        insert_branch!(
            PodAlignedFeeTimeScheduler,
            PodAlignedFeeTimeSchedulerRow,
            boxed
        );
        insert_branch!(Pool, PoolRow, boxed);
        insert_branch!(Position, PositionRow, boxed);
        insert_branch!(TokenBadge, TokenBadgeRow, boxed);
        insert_branch!(Vesting, VestingRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for MeteoraDammV2AccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Config, ConfigRow);
        commit_branch!(Operator, OperatorRow);
        commit_branch!(
            PodAlignedFeeMarketCapScheduler,
            PodAlignedFeeMarketCapSchedulerRow
        );
        commit_branch!(PodAlignedFeeRateLimiter, PodAlignedFeeRateLimiterRow);
        commit_branch!(PodAlignedFeeTimeScheduler, PodAlignedFeeTimeSchedulerRow);
        commit_branch!(Pool, PoolRow);
        commit_branch!(Position, PositionRow);
        commit_branch!(TokenBadge, TokenBadgeRow);
        commit_branch!(Vesting, VestingRow);

        Ok(())
    }
}

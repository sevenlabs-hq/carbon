//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{U16, U64, U8},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct PodAlignedFeeTimeSchedulerRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub cliff_fee_numerator: U64,
    pub base_fee_mode: U8,
    pub padding: Vec<u8>,
    pub number_of_period: U16,
    pub period_frequency: U64,
    pub reduction_factor: U64,
}

impl PodAlignedFeeTimeSchedulerRow {
    pub fn from_parts(
        source: crate::accounts::pod_aligned_fee_time_scheduler::PodAlignedFeeTimeScheduler,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            cliff_fee_numerator: source.cliff_fee_numerator.into(),
            base_fee_mode: source.base_fee_mode.into(),
            padding: source.padding.to_vec(),
            number_of_period: source.number_of_period.into(),
            period_frequency: source.period_frequency.into(),
            reduction_factor: source.reduction_factor.into(),
        }
    }
}

impl TryFrom<PodAlignedFeeTimeSchedulerRow>
    for crate::accounts::pod_aligned_fee_time_scheduler::PodAlignedFeeTimeScheduler
{
    type Error = carbon_core::error::Error;
    fn try_from(source: PodAlignedFeeTimeSchedulerRow) -> Result<Self, Self::Error> {
        Ok(Self {
            cliff_fee_numerator: *source.cliff_fee_numerator,
            base_fee_mode: source.base_fee_mode.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            padding: source.padding.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 5 bytes"
                        .to_string(),
                )
            })?,
            number_of_period: source.number_of_period.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            period_frequency: *source.period_frequency,
            reduction_factor: *source.reduction_factor,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::pod_aligned_fee_time_scheduler::PodAlignedFeeTimeScheduler
{
    fn table() -> &'static str {
        "pod_aligned_fee_time_scheduler_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "cliff_fee_numerator",
            "base_fee_mode",
            "padding",
            "number_of_period",
            "period_frequency",
            "reduction_factor",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PodAlignedFeeTimeSchedulerRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO pod_aligned_fee_time_scheduler_account (
                "cliff_fee_numerator",
                "base_fee_mode",
                "padding",
                "number_of_period",
                "period_frequency",
                "reduction_factor",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(&self.cliff_fee_numerator)
        .bind(self.base_fee_mode)
        .bind(&self.padding)
        .bind(self.number_of_period)
        .bind(&self.period_frequency)
        .bind(&self.reduction_factor)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PodAlignedFeeTimeSchedulerRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO pod_aligned_fee_time_scheduler_account (
                "cliff_fee_numerator",
                "base_fee_mode",
                "padding",
                "number_of_period",
                "period_frequency",
                "reduction_factor",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "cliff_fee_numerator" = EXCLUDED."cliff_fee_numerator",
                "base_fee_mode" = EXCLUDED."base_fee_mode",
                "padding" = EXCLUDED."padding",
                "number_of_period" = EXCLUDED."number_of_period",
                "period_frequency" = EXCLUDED."period_frequency",
                "reduction_factor" = EXCLUDED."reduction_factor",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.cliff_fee_numerator)
        .bind(self.base_fee_mode)
        .bind(&self.padding)
        .bind(self.number_of_period)
        .bind(&self.period_frequency)
        .bind(&self.reduction_factor)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for PodAlignedFeeTimeSchedulerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM pod_aligned_fee_time_scheduler_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for PodAlignedFeeTimeSchedulerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM pod_aligned_fee_time_scheduler_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct PodAlignedFeeTimeSchedulerMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for PodAlignedFeeTimeSchedulerMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS pod_aligned_fee_time_scheduler_account (
                -- Account data
                "cliff_fee_numerator" NUMERIC(20) NOT NULL,
                "base_fee_mode" INT2 NOT NULL,
                "padding" BYTEA NOT NULL,
                "number_of_period" INT4 NOT NULL,
                "period_frequency" NUMERIC(20) NOT NULL,
                "reduction_factor" NUMERIC(20) NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS pod_aligned_fee_time_scheduler_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

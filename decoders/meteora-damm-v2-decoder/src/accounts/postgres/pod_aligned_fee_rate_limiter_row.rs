//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{U16, U32, U64, U8},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct PodAlignedFeeRateLimiterRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub cliff_fee_numerator: U64,
    pub base_fee_mode: U8,
    pub padding: Vec<u8>,
    pub fee_increment_bps: U16,
    pub max_limiter_duration: U32,
    pub max_fee_bps: U32,
    pub reference_amount: U64,
}

impl PodAlignedFeeRateLimiterRow {
    pub fn from_parts(
        source: crate::accounts::pod_aligned_fee_rate_limiter::PodAlignedFeeRateLimiter,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            cliff_fee_numerator: source.cliff_fee_numerator.into(),
            base_fee_mode: source.base_fee_mode.into(),
            padding: source.padding.to_vec(),
            fee_increment_bps: source.fee_increment_bps.into(),
            max_limiter_duration: source.max_limiter_duration.into(),
            max_fee_bps: source.max_fee_bps.into(),
            reference_amount: source.reference_amount.into(),
        }
    }
}

impl TryFrom<PodAlignedFeeRateLimiterRow>
    for crate::accounts::pod_aligned_fee_rate_limiter::PodAlignedFeeRateLimiter
{
    type Error = carbon_core::error::Error;
    fn try_from(source: PodAlignedFeeRateLimiterRow) -> Result<Self, Self::Error> {
        Ok(Self {
            cliff_fee_numerator: *source.cliff_fee_numerator,
            base_fee_mode: source.base_fee_mode.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            padding: source.padding.as_slice().try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert padding from postgres primitive: expected 5 bytes"
                        .to_string(),
                )
            })?,
            fee_increment_bps: source.fee_increment_bps.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            max_limiter_duration: source.max_limiter_duration.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            max_fee_bps: source.max_fee_bps.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            reference_amount: *source.reference_amount,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::pod_aligned_fee_rate_limiter::PodAlignedFeeRateLimiter
{
    fn table() -> &'static str {
        "pod_aligned_fee_rate_limiter_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "cliff_fee_numerator",
            "base_fee_mode",
            "padding",
            "fee_increment_bps",
            "max_limiter_duration",
            "max_fee_bps",
            "reference_amount",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for PodAlignedFeeRateLimiterRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO pod_aligned_fee_rate_limiter_account (
                "cliff_fee_numerator",
                "base_fee_mode",
                "padding",
                "fee_increment_bps",
                "max_limiter_duration",
                "max_fee_bps",
                "reference_amount",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#,
        )
        .bind(&self.cliff_fee_numerator)
        .bind(self.base_fee_mode)
        .bind(&self.padding)
        .bind(self.fee_increment_bps)
        .bind(self.max_limiter_duration)
        .bind(self.max_fee_bps)
        .bind(&self.reference_amount)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for PodAlignedFeeRateLimiterRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO pod_aligned_fee_rate_limiter_account (
                "cliff_fee_numerator",
                "base_fee_mode",
                "padding",
                "fee_increment_bps",
                "max_limiter_duration",
                "max_fee_bps",
                "reference_amount",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "cliff_fee_numerator" = EXCLUDED."cliff_fee_numerator",
                "base_fee_mode" = EXCLUDED."base_fee_mode",
                "padding" = EXCLUDED."padding",
                "fee_increment_bps" = EXCLUDED."fee_increment_bps",
                "max_limiter_duration" = EXCLUDED."max_limiter_duration",
                "max_fee_bps" = EXCLUDED."max_fee_bps",
                "reference_amount" = EXCLUDED."reference_amount",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.cliff_fee_numerator)
        .bind(self.base_fee_mode)
        .bind(&self.padding)
        .bind(self.fee_increment_bps)
        .bind(self.max_limiter_duration)
        .bind(self.max_fee_bps)
        .bind(&self.reference_amount)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for PodAlignedFeeRateLimiterRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM pod_aligned_fee_rate_limiter_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for PodAlignedFeeRateLimiterRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM pod_aligned_fee_rate_limiter_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct PodAlignedFeeRateLimiterMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for PodAlignedFeeRateLimiterMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS pod_aligned_fee_rate_limiter_account (
                -- Account data
                "cliff_fee_numerator" NUMERIC(20) NOT NULL,
                "base_fee_mode" INT2 NOT NULL,
                "padding" BYTEA NOT NULL,
                "fee_increment_bps" INT4 NOT NULL,
                "max_limiter_duration" INT8 NOT NULL,
                "max_fee_bps" INT8 NOT NULL,
                "reference_amount" NUMERIC(20) NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS pod_aligned_fee_rate_limiter_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

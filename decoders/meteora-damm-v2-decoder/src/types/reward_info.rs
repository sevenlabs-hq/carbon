//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;
/// Stores the state relevant for tracking liquidity mining rewards
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RewardInfo {
    /// Indicates if the reward has been initialized
    pub initialized: u8,
    /// reward token flag
    pub reward_token_flag: u8,
    /// padding
    pub padding0: [u8; 6],
    /// Padding to ensure `reward_rate: u128` is 16-byte aligned
    pub padding1: [u8; 8],
    /// Reward token mint.
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub mint: Pubkey,
    /// Reward vault token account.
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub vault: Pubkey,
    /// Authority account that allows to fund rewards
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub funder: Pubkey,
    /// reward duration
    pub reward_duration: u64,
    /// reward duration end
    pub reward_duration_end: u64,
    /// reward rate
    pub reward_rate: u128,
    /// Reward per token stored
    pub reward_per_token_stored: [u8; 32],
    /// The last time reward states were updated.
    pub last_update_time: u64,
    /// Accumulated seconds when the farm distributed rewards but the bin was
    /// empty. These rewards will be carried over to the next reward time
    /// window.
    pub cumulative_seconds_with_empty_liquidity_reward: u64,
}

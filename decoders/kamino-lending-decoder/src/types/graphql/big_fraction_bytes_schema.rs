//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::graphql::primitives::U64, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "BigFractionBytes")]
pub struct BigFractionBytesGraphQL {
    pub value: Vec<U64>,
    pub padding: Vec<U64>,
}

impl From<crate::types::BigFractionBytes> for BigFractionBytesGraphQL {
    fn from(original: crate::types::BigFractionBytes) -> Self {
        Self {
            value: original
                .value
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
            padding: original
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::graphql::primitives::Pubkey, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ScopeConfiguration")]
pub struct ScopeConfigurationGraphQL {
    pub price_feed: Pubkey,
    pub price_chain: Vec<i32>,
    pub twap_chain: Vec<i32>,
}

impl From<crate::types::ScopeConfiguration> for ScopeConfigurationGraphQL {
    fn from(original: crate::types::ScopeConfiguration) -> Self {
        Self {
            price_feed: carbon_core::graphql::primitives::Pubkey(original.price_feed),
            price_chain: original
                .price_chain
                .into_iter()
                .map(|item| item as i32)
                .collect(),
            twap_chain: original
                .twap_chain
                .into_iter()
                .map(|item| item as i32)
                .collect(),
        }
    }
}

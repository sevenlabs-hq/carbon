//! This code was AUTOGENERATED using the Codama library.
/// A definition of optional customizations that should be applied after cloning
/// the config.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ReserveConfigCustomizationArgs {
    /// A gate for [Self::fixed_borrow_rate_bps].
    pub override_fixed_rate_bps: u8,
    /// If [Self::override_fixed_rate_bps] is non-zero, this borrow rate will be
    /// used to override the [ReserveConfig::borrow_rate_curve] with a fixed
    /// one.
    pub fixed_borrow_rate_bps: u32,
    /// A gate for [Self::debt_term_seconds].
    pub override_debt_term_seconds: u8,
    /// If [Self::override_debt_term_seconds] is non-zero, this value will be
    /// used to override the [ReserveConfig::debt_term_seconds].
    pub debt_term_seconds: u64,
    /// Whether the target reserve should have zeroed
    /// [ReserveConfig::elevation_groups] (i.e. not cloned from source).
    /// This customization is mandatory when cloning a reserve (with some
    /// elevation groups) into a different market (where those elevation
    /// group indices would have different meaning).
    pub clear_elevation_groups: u8,
}

//! This code was AUTOGENERATED using the Codama library.
/// A callback to be notified when the ticket is being processed.
/// ## Why an enum?
/// Only reliable programs may be used for callbacks (since any error or panic
/// returned from a CPI aborts an entire transaction, which would stall the
/// queue progress). Hence, we need a whitelist, and the simplest initial
/// implementation is a hardcoded enum. If we want to be able to add new
/// whitelist items without SC updates, we can implement such support using a
/// special enum value (e.g. `SPECIFIED_BY_PDA = 255`).
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub enum ProgressCallbackType {
    None,
    KlendQueueAccountingHandlerOnKvault,
}

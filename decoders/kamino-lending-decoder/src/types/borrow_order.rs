//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;
/// A borrow order.
/// When the [Obligation::borrow_order] is populated (i.e. non-zeroed) on an
/// Obligation, then the permissionless "fill" operations may borrow liquidity
/// to the owner according to this specification.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct BorrowOrder {
    /// The asset to be borrowed.
    /// The reserves used for [Obligation::borrows] *must* all provide exactly
    /// this asset.
    pub debt_liquidity_mint: Pubkey,
    /// The amount of debt that still needs to be filled, in lamports.
    pub remaining_debt_amount: u64,
    /// The token account owned by the [Obligation::owner] and holding
    /// [Self::debt_liquidity_mint], where the filled funds should be
    /// transferred to.
    pub filled_debt_destination: Pubkey,
    /// The minimum allowed debt term that the obligation owner agrees to.
    /// The reserves used to fill this order *cannot* define their debt term
    /// *lower* than this. If zeroed, then only indefinite-term reserves may
    /// be used.
    pub min_debt_term_seconds: u64,
    /// The time until which the borrow order can still be filled.
    pub fillable_until_timestamp: u64,
    /// The time at which this order was placed.
    /// Currently, this is only a piece of metadata.
    pub placed_at_timestamp: u64,
    /// The time at which this order was most-recently updated (including:
    /// created). Currently, this is only a piece of metadata.
    pub last_updated_at_timestamp: u64,
    /// The amount of debt that was originally requested when this order was
    /// most-recently updated. In other words: this field holds a value of
    /// [Self::remaining_debt_amount] captured at
    /// [Self::last_updated_at_timestamp].
    /// Currently, this is only a piece of metadata.
    pub requested_debt_amount: u64,
    /// The maximum borrow rate that the obligation owner agrees to.
    /// The reserves used for [Obligation::borrows] *cannot* define their
    /// maximum borrow rate *higher* than this.
    pub max_borrow_rate_bps: u32,
    /// Whether the [Self::remaining_debt_amount] is non-zero.
    /// This field is *not* used by smart contract logic (which prefers to treat
    /// the above [Self::remaining_debt_amount]-based definition as the
    /// single source of truth). However, it is useful for off-chain bots
    /// (order-searchers) to efficiently list (i.e. `memcmp` filter)
    /// just the obligations that have active borrow orders.
    pub active: u8,
    /// Alignment padding.
    pub padding1: [u8; 3],
    /// End padding.
    pub end_padding: [u64; 5],
}

//! This code was AUTOGENERATED using the Codama library.
use {crate::types::BigFractionBytes, solana_pubkey::Pubkey};
/// Reserve liquidity
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ReserveLiquidity {
    /// Reserve liquidity mint address
    pub mint_pubkey: Pubkey,
    /// Reserve liquidity supply address
    pub supply_vault: Pubkey,
    /// Reserve liquidity fee collection address
    pub fee_vault: Pubkey,
    /// Total reserve liquidity available.
    /// Note: not all of this liquidity can be freely used for any purpose.
    /// Production code should use the specialized getters - see e.g.
    /// [Reserve::total_available_liquidity_amount()],
    /// [Reserve::freely_available_liquidity_amount()].
    pub total_available_amount: u64,
    /// Reserve liquidity borrowed (scaled fraction)
    pub borrowed_amount_sf: u128,
    /// Reserve liquidity market price in quote currency (scaled fraction)
    pub market_price_sf: u128,
    /// Unix timestamp of the market price (from the oracle)
    pub market_price_last_updated_ts: u64,
    /// Reserve liquidity mint decimals
    pub mint_decimals: u64,
    /// Timestamp when the last refresh reserve detected that the liquidity
    /// amount is above the deposit cap. When this threshold is crossed, then
    /// redemptions (auto-deleverage) are enabled. If the threshold is not
    /// crossed, then the timestamp is set to 0
    pub deposit_limit_crossed_timestamp: u64,
    /// Timestamp when the last refresh reserve detected that the borrowed
    /// amount is above the borrow cap. When this threshold is crossed, then
    /// redemptions (auto-deleverage) are enabled. If the threshold is not
    /// crossed, then the timestamp is set to 0
    pub borrow_limit_crossed_timestamp: u64,
    /// Reserve liquidity cumulative borrow rate (scaled fraction)
    pub cumulative_borrow_rate_bsf: BigFractionBytes,
    /// Reserve cumulative protocol fees (scaled fraction)
    pub accumulated_protocol_fees_sf: u128,
    /// Reserve cumulative referrer fees (scaled fraction)
    pub accumulated_referrer_fees_sf: u128,
    /// Reserve pending referrer fees, to be claimed in refresh_obligation by
    /// referrer or protocol (scaled fraction)
    pub pending_referrer_fees_sf: u128,
    /// Reserve referrer fee absolute rate calculated at each refresh_reserve
    /// operation (scaled fraction)
    pub absolute_referral_rate_sf: u128,
    /// Token program of the liquidity mint
    pub token_program: Pubkey,
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding2: [u64; 51],
    pub padding3: [u128; 32],
}

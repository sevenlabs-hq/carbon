//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::InitObligationArgs,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitObligation {
    pub args: InitObligationArgs,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitObligationInstructionAccounts {
    pub obligation_owner: solana_pubkey::Pubkey,
    pub fee_payer: solana_pubkey::Pubkey,
    pub obligation: solana_pubkey::Pubkey,
    pub lending_market: solana_pubkey::Pubkey,
    pub seed1_account: solana_pubkey::Pubkey,
    pub seed2_account: solana_pubkey::Pubkey,
    pub owner_user_metadata: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitObligation {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [251, 10, 231, 76, 27, 11, 159, 96] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitObligation {
    type ArrangedAccounts = InitObligationInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let obligation_owner = next_account(&mut iter)?;
        let fee_payer = next_account(&mut iter)?;
        let obligation = next_account(&mut iter)?;
        let lending_market = next_account(&mut iter)?;
        let seed1_account = next_account(&mut iter)?;
        let seed2_account = next_account(&mut iter)?;
        let owner_user_metadata = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitObligationInstructionAccounts {
            obligation_owner,
            fee_payer,
            obligation,
            lending_market,
            seed1_account,
            seed2_account,
            owner_user_metadata,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

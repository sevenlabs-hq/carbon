//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::UpdateObligationConfigMode,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateObligationConfig {
    pub mode: UpdateObligationConfigMode,
    pub value: Vec<u8>,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateObligationConfigInstructionAccounts {
    pub owner: solana_pubkey::Pubkey,
    pub obligation: solana_pubkey::Pubkey,
    pub borrow_reserve: Option<solana_pubkey::Pubkey>,
    pub deposit_reserve: Option<solana_pubkey::Pubkey>,
    pub lending_market: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateObligationConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [82, 152, 213, 69, 250, 0, 157, 188] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateObligationConfig {
    type ArrangedAccounts = UpdateObligationConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let owner = next_account(&mut iter)?;
        let obligation = next_account(&mut iter)?;
        let borrow_reserve = next_account(&mut iter);
        let deposit_reserve = next_account(&mut iter);
        let lending_market = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateObligationConfigInstructionAccounts {
            owner,
            obligation,
            borrow_reserve,
            deposit_reserve,
            lending_market,
            remaining: remaining.to_vec(),
        })
    }
}

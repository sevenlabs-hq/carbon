//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DeleteReferrerStateAndShortUrl {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DeleteReferrerStateAndShortUrlInstructionAccounts {
    pub referrer: solana_pubkey::Pubkey,
    pub referrer_state: solana_pubkey::Pubkey,
    pub short_url: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DeleteReferrerStateAndShortUrl {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [153, 185, 99, 28, 228, 179, 187, 150] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for DeleteReferrerStateAndShortUrl {
    type ArrangedAccounts = DeleteReferrerStateAndShortUrlInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let referrer = next_account(&mut iter)?;
        let referrer_state = next_account(&mut iter)?;
        let short_url = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DeleteReferrerStateAndShortUrlInstructionAccounts {
            referrer,
            referrer_state,
            short_url,
            rent,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

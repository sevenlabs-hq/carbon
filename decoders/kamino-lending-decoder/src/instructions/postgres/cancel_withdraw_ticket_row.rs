//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    instruction::InstructionMetadata,
    postgres::{metadata::InstructionRowMetadata, primitives::U64},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CancelWithdrawTicketRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub ticket_sequence_number: U64,
    pub collateral_amount_to_cancel: U64,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl CancelWithdrawTicketRow {
    pub fn from_parts(
        source: crate::instructions::cancel_withdraw_ticket::CancelWithdrawTicket,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            ticket_sequence_number: source.ticket_sequence_number.into(),
            collateral_amount_to_cancel: source.collateral_amount_to_cancel.into(),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<CancelWithdrawTicketRow>
    for crate::instructions::cancel_withdraw_ticket::CancelWithdrawTicket
{
    type Error = carbon_core::error::Error;
    fn try_from(source: CancelWithdrawTicketRow) -> Result<Self, Self::Error> {
        Ok(Self {
            ticket_sequence_number: *source.ticket_sequence_number,
            collateral_amount_to_cancel: *source.collateral_amount_to_cancel,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::cancel_withdraw_ticket::CancelWithdrawTicket
{
    fn table() -> &'static str {
        "cancel_withdraw_ticket_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "ticket_sequence_number",
            "collateral_amount_to_cancel",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CancelWithdrawTicketRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO cancel_withdraw_ticket_instruction (
                "ticket_sequence_number",
                "collateral_amount_to_cancel",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(&self.ticket_sequence_number)
        .bind(&self.collateral_amount_to_cancel)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CancelWithdrawTicketRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO cancel_withdraw_ticket_instruction (
                "ticket_sequence_number",
                "collateral_amount_to_cancel",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "ticket_sequence_number" = EXCLUDED."ticket_sequence_number",
                "collateral_amount_to_cancel" = EXCLUDED."collateral_amount_to_cancel",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(&self.ticket_sequence_number)
        .bind(&self.collateral_amount_to_cancel)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CancelWithdrawTicketRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM cancel_withdraw_ticket_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Lookup for CancelWithdrawTicketRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM cancel_withdraw_ticket_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CancelWithdrawTicketMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CancelWithdrawTicketMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS cancel_withdraw_ticket_instruction (
                -- Instruction data
                "ticket_sequence_number" NUMERIC(20) NOT NULL,
                "collateral_amount_to_cancel" NUMERIC(20) NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS cancel_withdraw_ticket_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

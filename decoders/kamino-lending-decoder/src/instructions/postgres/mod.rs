//! This code was AUTOGENERATED using the Codama library.
pub mod borrow_obligation_liquidity_row;
pub mod borrow_obligation_liquidity_v2_row;
pub mod cpi_event_row;
pub mod delete_referrer_state_and_short_url_row;
pub mod deposit_and_withdraw_row;
pub mod deposit_obligation_collateral_row;
pub mod deposit_obligation_collateral_v2_row;
pub mod deposit_reserve_liquidity_and_obligation_collateral_row;
pub mod deposit_reserve_liquidity_and_obligation_collateral_v2_row;
pub mod deposit_reserve_liquidity_row;
pub mod enqueue_to_withdraw_row;
pub mod fill_borrow_order_row;
pub mod flash_borrow_reserve_liquidity_row;
pub mod flash_repay_reserve_liquidity_row;
pub mod idl_missing_types_row;
pub mod init_farms_for_reserve_row;
pub mod init_global_config_row;
pub mod init_lending_market_row;
pub mod init_obligation_farms_for_reserve_row;
pub mod init_obligation_row;
pub mod init_referrer_state_and_short_url_row;
pub mod init_referrer_token_state_row;
pub mod init_reserve_row;
pub mod init_user_metadata_row;
pub mod liquidate_obligation_and_redeem_reserve_collateral_row;
pub mod liquidate_obligation_and_redeem_reserve_collateral_v2_row;
pub mod mark_obligation_for_deleveraging_row;
pub mod recover_invalid_ticket_collateral_row;
pub mod redeem_fees_row;
pub mod redeem_reserve_collateral_row;
pub mod refresh_obligation_farms_for_reserve_row;
pub mod refresh_obligation_row;
pub mod refresh_reserve_row;
pub mod refresh_reserves_batch_row;
pub mod repay_and_withdraw_and_redeem_row;
pub mod repay_obligation_liquidity_row;
pub mod repay_obligation_liquidity_v2_row;
pub mod request_elevation_group_row;
pub mod rollover_fixed_term_borrow_row;
pub mod seed_deposit_on_init_reserve_row;
pub mod set_borrow_order_row;
pub mod set_obligation_order_row;
pub mod socialize_loss_row;
pub mod socialize_loss_v2_row;
pub mod update_global_config_admin_row;
pub mod update_global_config_row;
pub mod update_lending_market_owner_row;
pub mod update_lending_market_row;
pub mod update_obligation_config_row;
pub mod update_reserve_config_row;
pub mod withdraw_obligation_collateral_and_redeem_reserve_collateral_row;
pub mod withdraw_obligation_collateral_and_redeem_reserve_collateral_v2_row;
pub mod withdraw_obligation_collateral_row;
pub mod withdraw_obligation_collateral_v2_row;
pub mod withdraw_protocol_fee_row;
pub mod withdraw_queued_liquidity_row;
pub mod withdraw_referrer_fees_row;

pub use self::{
    borrow_obligation_liquidity_row::*, borrow_obligation_liquidity_v2_row::*, cpi_event_row::*,
    delete_referrer_state_and_short_url_row::*, deposit_and_withdraw_row::*,
    deposit_obligation_collateral_row::*, deposit_obligation_collateral_v2_row::*,
    deposit_reserve_liquidity_and_obligation_collateral_row::*,
    deposit_reserve_liquidity_and_obligation_collateral_v2_row::*,
    deposit_reserve_liquidity_row::*, enqueue_to_withdraw_row::*, fill_borrow_order_row::*,
    flash_borrow_reserve_liquidity_row::*, flash_repay_reserve_liquidity_row::*,
    idl_missing_types_row::*, init_farms_for_reserve_row::*, init_global_config_row::*,
    init_lending_market_row::*, init_obligation_farms_for_reserve_row::*, init_obligation_row::*,
    init_referrer_state_and_short_url_row::*, init_referrer_token_state_row::*,
    init_reserve_row::*, init_user_metadata_row::*,
    liquidate_obligation_and_redeem_reserve_collateral_row::*,
    liquidate_obligation_and_redeem_reserve_collateral_v2_row::*,
    mark_obligation_for_deleveraging_row::*, recover_invalid_ticket_collateral_row::*,
    redeem_fees_row::*, redeem_reserve_collateral_row::*,
    refresh_obligation_farms_for_reserve_row::*, refresh_obligation_row::*, refresh_reserve_row::*,
    refresh_reserves_batch_row::*, repay_and_withdraw_and_redeem_row::*,
    repay_obligation_liquidity_row::*, repay_obligation_liquidity_v2_row::*,
    request_elevation_group_row::*, rollover_fixed_term_borrow_row::*,
    seed_deposit_on_init_reserve_row::*, set_borrow_order_row::*, set_obligation_order_row::*,
    socialize_loss_row::*, socialize_loss_v2_row::*, update_global_config_admin_row::*,
    update_global_config_row::*, update_lending_market_owner_row::*, update_lending_market_row::*,
    update_obligation_config_row::*, update_reserve_config_row::*,
    withdraw_obligation_collateral_and_redeem_reserve_collateral_row::*,
    withdraw_obligation_collateral_and_redeem_reserve_collateral_v2_row::*,
    withdraw_obligation_collateral_row::*, withdraw_obligation_collateral_v2_row::*,
    withdraw_protocol_fee_row::*, withdraw_queued_liquidity_row::*, withdraw_referrer_fees_row::*,
};
use super::KaminoLendingInstruction;

pub struct KaminoLendingInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for KaminoLendingInstructionsMigration {
    fn app(&self) -> &str {
        "kamino-lending"
    }

    fn name(&self) -> &str {
        "kamino_lending_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(BorrowObligationLiquidityMigrationOperation),
            Box::new(BorrowObligationLiquidityV2MigrationOperation),
            Box::new(DeleteReferrerStateAndShortUrlMigrationOperation),
            Box::new(DepositAndWithdrawMigrationOperation),
            Box::new(DepositObligationCollateralMigrationOperation),
            Box::new(DepositObligationCollateralV2MigrationOperation),
            Box::new(DepositReserveLiquidityMigrationOperation),
            Box::new(DepositReserveLiquidityAndObligationCollateralMigrationOperation),
            Box::new(DepositReserveLiquidityAndObligationCollateralV2MigrationOperation),
            Box::new(EnqueueToWithdrawMigrationOperation),
            Box::new(FillBorrowOrderMigrationOperation),
            Box::new(FlashBorrowReserveLiquidityMigrationOperation),
            Box::new(FlashRepayReserveLiquidityMigrationOperation),
            Box::new(IdlMissingTypesMigrationOperation),
            Box::new(InitFarmsForReserveMigrationOperation),
            Box::new(InitGlobalConfigMigrationOperation),
            Box::new(InitLendingMarketMigrationOperation),
            Box::new(InitObligationMigrationOperation),
            Box::new(InitObligationFarmsForReserveMigrationOperation),
            Box::new(InitReferrerStateAndShortUrlMigrationOperation),
            Box::new(InitReferrerTokenStateMigrationOperation),
            Box::new(InitReserveMigrationOperation),
            Box::new(InitUserMetadataMigrationOperation),
            Box::new(LiquidateObligationAndRedeemReserveCollateralMigrationOperation),
            Box::new(LiquidateObligationAndRedeemReserveCollateralV2MigrationOperation),
            Box::new(MarkObligationForDeleveragingMigrationOperation),
            Box::new(RecoverInvalidTicketCollateralMigrationOperation),
            Box::new(RedeemFeesMigrationOperation),
            Box::new(RedeemReserveCollateralMigrationOperation),
            Box::new(RefreshObligationMigrationOperation),
            Box::new(RefreshObligationFarmsForReserveMigrationOperation),
            Box::new(RefreshReserveMigrationOperation),
            Box::new(RefreshReservesBatchMigrationOperation),
            Box::new(RepayAndWithdrawAndRedeemMigrationOperation),
            Box::new(RepayObligationLiquidityMigrationOperation),
            Box::new(RepayObligationLiquidityV2MigrationOperation),
            Box::new(RequestElevationGroupMigrationOperation),
            Box::new(RolloverFixedTermBorrowMigrationOperation),
            Box::new(SeedDepositOnInitReserveMigrationOperation),
            Box::new(SetBorrowOrderMigrationOperation),
            Box::new(SetObligationOrderMigrationOperation),
            Box::new(SocializeLossMigrationOperation),
            Box::new(SocializeLossV2MigrationOperation),
            Box::new(UpdateGlobalConfigMigrationOperation),
            Box::new(UpdateGlobalConfigAdminMigrationOperation),
            Box::new(UpdateLendingMarketMigrationOperation),
            Box::new(UpdateLendingMarketOwnerMigrationOperation),
            Box::new(UpdateObligationConfigMigrationOperation),
            Box::new(UpdateReserveConfigMigrationOperation),
            Box::new(WithdrawObligationCollateralMigrationOperation),
            Box::new(WithdrawObligationCollateralAndRedeemReserveCollateralMigrationOperation),
            Box::new(WithdrawObligationCollateralAndRedeemReserveCollateralV2MigrationOperation),
            Box::new(WithdrawObligationCollateralV2MigrationOperation),
            Box::new(WithdrawProtocolFeeMigrationOperation),
            Box::new(WithdrawQueuedLiquidityMigrationOperation),
            Box::new(WithdrawReferrerFeesMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct KaminoLendingInstructionWithMetadata(
    pub KaminoLendingInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        KaminoLendingInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for KaminoLendingInstructionWithMetadata
{
    fn from(
        value: (
            KaminoLendingInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        KaminoLendingInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for KaminoLendingInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoLendingInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            KaminoLendingInstruction::InitLendingMarket { data, .. } => {
                let row = init_lending_market_row::InitLendingMarketRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateLendingMarket { data, .. } => {
                let row = update_lending_market_row::UpdateLendingMarketRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateLendingMarketOwner { data, .. } => {
                let row = update_lending_market_owner_row::UpdateLendingMarketOwnerRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReserve { data, .. } => {
                let row = init_reserve_row::InitReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitFarmsForReserve { data, .. } => {
                let row = init_farms_for_reserve_row::InitFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateReserveConfig { data, .. } => {
                let row = update_reserve_config_row::UpdateReserveConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RedeemFees { data, .. } => {
                let row = redeem_fees_row::RedeemFeesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawProtocolFee { data, .. } => {
                let row = withdraw_protocol_fee_row::WithdrawProtocolFeeRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SeedDepositOnInitReserve { data, .. } => {
                let row = seed_deposit_on_init_reserve_row::SeedDepositOnInitReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SocializeLoss { data, .. } => {
                let row = socialize_loss_row::SocializeLossRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SocializeLossV2 { data, .. } => {
                let row = socialize_loss_v2_row::SocializeLossV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::MarkObligationForDeleveraging { data, .. } => {
                let row = mark_obligation_for_deleveraging_row::MarkObligationForDeleveragingRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshReserve { data, .. } => {
                let row = refresh_reserve_row::RefreshReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshReservesBatch { data, .. } => {
                let row = refresh_reserves_batch_row::RefreshReservesBatchRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidity { data, .. } => {
                let row = deposit_reserve_liquidity_row::DepositReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RedeemReserveCollateral { data, .. } => {
                let row = redeem_reserve_collateral_row::RedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitObligation { data, .. } => {
                let row = init_obligation_row::InitObligationRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitObligationFarmsForReserve { data, .. } => {
                let row = init_obligation_farms_for_reserve_row::InitObligationFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshObligationFarmsForReserve { data, .. } => {
                let row = refresh_obligation_farms_for_reserve_row::RefreshObligationFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshObligation { data, .. } => {
                let row = refresh_obligation_row::RefreshObligationRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositObligationCollateral { data, .. } => {
                let row = deposit_obligation_collateral_row::DepositObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositObligationCollateralV2 { data, .. } => {
                let row = deposit_obligation_collateral_v2_row::DepositObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateral { data, .. } => {
                let row = withdraw_obligation_collateral_row::WithdrawObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralV2 { data, .. } => {
                let row = withdraw_obligation_collateral_v2_row::WithdrawObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::BorrowObligationLiquidity { data, .. } => {
                let row = borrow_obligation_liquidity_row::BorrowObligationLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::BorrowObligationLiquidityV2 { data, .. } => {
                let row = borrow_obligation_liquidity_v2_row::BorrowObligationLiquidityV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayObligationLiquidity { data, .. } => {
                let row = repay_obligation_liquidity_row::RepayObligationLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayObligationLiquidityV2 { data, .. } => {
                let row = repay_obligation_liquidity_v2_row::RepayObligationLiquidityV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayAndWithdrawAndRedeem { data, .. } => {
                let row = repay_and_withdraw_and_redeem_row::RepayAndWithdrawAndRedeemRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositAndWithdraw { data, .. } => {
                let row = deposit_and_withdraw_row::DepositAndWithdrawRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidityAndObligationCollateral { data, .. } => {
                let row = deposit_reserve_liquidity_and_obligation_collateral_row::DepositReserveLiquidityAndObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidityAndObligationCollateralV2 { data, .. } => {
                let row = deposit_reserve_liquidity_and_obligation_collateral_v2_row::DepositReserveLiquidityAndObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralAndRedeemReserveCollateral { data, .. } => {
                let row = withdraw_obligation_collateral_and_redeem_reserve_collateral_row::WithdrawObligationCollateralAndRedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralAndRedeemReserveCollateralV2 { data, .. } => {
                let row = withdraw_obligation_collateral_and_redeem_reserve_collateral_v2_row::WithdrawObligationCollateralAndRedeemReserveCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::LiquidateObligationAndRedeemReserveCollateral { data, .. } => {
                let row = liquidate_obligation_and_redeem_reserve_collateral_row::LiquidateObligationAndRedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::LiquidateObligationAndRedeemReserveCollateralV2 { data, .. } => {
                let row = liquidate_obligation_and_redeem_reserve_collateral_v2_row::LiquidateObligationAndRedeemReserveCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FlashRepayReserveLiquidity { data, .. } => {
                let row = flash_repay_reserve_liquidity_row::FlashRepayReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FlashBorrowReserveLiquidity { data, .. } => {
                let row = flash_borrow_reserve_liquidity_row::FlashBorrowReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RequestElevationGroup { data, .. } => {
                let row = request_elevation_group_row::RequestElevationGroupRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReferrerTokenState { data, .. } => {
                let row = init_referrer_token_state_row::InitReferrerTokenStateRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitUserMetadata { data, .. } => {
                let row = init_user_metadata_row::InitUserMetadataRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawReferrerFees { data, .. } => {
                let row = withdraw_referrer_fees_row::WithdrawReferrerFeesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReferrerStateAndShortUrl { data, .. } => {
                let row = init_referrer_state_and_short_url_row::InitReferrerStateAndShortUrlRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DeleteReferrerStateAndShortUrl { data, .. } => {
                let row = delete_referrer_state_and_short_url_row::DeleteReferrerStateAndShortUrlRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SetObligationOrder { data, .. } => {
                let row = set_obligation_order_row::SetObligationOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SetBorrowOrder { data, .. } => {
                let row = set_borrow_order_row::SetBorrowOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateObligationConfig { data, .. } => {
                let row = update_obligation_config_row::UpdateObligationConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RolloverFixedTermBorrow { data, .. } => {
                let row = rollover_fixed_term_borrow_row::RolloverFixedTermBorrowRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FillBorrowOrder { data, .. } => {
                let row = fill_borrow_order_row::FillBorrowOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::EnqueueToWithdraw { data, .. } => {
                let row = enqueue_to_withdraw_row::EnqueueToWithdrawRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawQueuedLiquidity { data, .. } => {
                let row = withdraw_queued_liquidity_row::WithdrawQueuedLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RecoverInvalidTicketCollateral { data, .. } => {
                let row = recover_invalid_ticket_collateral_row::RecoverInvalidTicketCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitGlobalConfig { data, .. } => {
                let row = init_global_config_row::InitGlobalConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::IdlMissingTypes { data, .. } => {
                let row = idl_missing_types_row::IdlMissingTypesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(data.clone(), metadata.clone(), if accounts.is_some() { raw_accounts.clone() } else { Vec::new() }).insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for KaminoLendingInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoLendingInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            KaminoLendingInstruction::InitLendingMarket { data, .. } => {
                let row = init_lending_market_row::InitLendingMarketRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateLendingMarket { data, .. } => {
                let row = update_lending_market_row::UpdateLendingMarketRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateLendingMarketOwner { data, .. } => {
                let row = update_lending_market_owner_row::UpdateLendingMarketOwnerRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReserve { data, .. } => {
                let row = init_reserve_row::InitReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitFarmsForReserve { data, .. } => {
                let row = init_farms_for_reserve_row::InitFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateReserveConfig { data, .. } => {
                let row = update_reserve_config_row::UpdateReserveConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RedeemFees { data, .. } => {
                let row = redeem_fees_row::RedeemFeesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawProtocolFee { data, .. } => {
                let row = withdraw_protocol_fee_row::WithdrawProtocolFeeRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SeedDepositOnInitReserve { data, .. } => {
                let row = seed_deposit_on_init_reserve_row::SeedDepositOnInitReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SocializeLoss { data, .. } => {
                let row = socialize_loss_row::SocializeLossRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SocializeLossV2 { data, .. } => {
                let row = socialize_loss_v2_row::SocializeLossV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::MarkObligationForDeleveraging { data, .. } => {
                let row = mark_obligation_for_deleveraging_row::MarkObligationForDeleveragingRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshReserve { data, .. } => {
                let row = refresh_reserve_row::RefreshReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshReservesBatch { data, .. } => {
                let row = refresh_reserves_batch_row::RefreshReservesBatchRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidity { data, .. } => {
                let row = deposit_reserve_liquidity_row::DepositReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RedeemReserveCollateral { data, .. } => {
                let row = redeem_reserve_collateral_row::RedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitObligation { data, .. } => {
                let row = init_obligation_row::InitObligationRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitObligationFarmsForReserve { data, .. } => {
                let row = init_obligation_farms_for_reserve_row::InitObligationFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshObligationFarmsForReserve { data, .. } => {
                let row = refresh_obligation_farms_for_reserve_row::RefreshObligationFarmsForReserveRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RefreshObligation { data, .. } => {
                let row = refresh_obligation_row::RefreshObligationRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositObligationCollateral { data, .. } => {
                let row = deposit_obligation_collateral_row::DepositObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositObligationCollateralV2 { data, .. } => {
                let row = deposit_obligation_collateral_v2_row::DepositObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateral { data, .. } => {
                let row = withdraw_obligation_collateral_row::WithdrawObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralV2 { data, .. } => {
                let row = withdraw_obligation_collateral_v2_row::WithdrawObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::BorrowObligationLiquidity { data, .. } => {
                let row = borrow_obligation_liquidity_row::BorrowObligationLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::BorrowObligationLiquidityV2 { data, .. } => {
                let row = borrow_obligation_liquidity_v2_row::BorrowObligationLiquidityV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayObligationLiquidity { data, .. } => {
                let row = repay_obligation_liquidity_row::RepayObligationLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayObligationLiquidityV2 { data, .. } => {
                let row = repay_obligation_liquidity_v2_row::RepayObligationLiquidityV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RepayAndWithdrawAndRedeem { data, .. } => {
                let row = repay_and_withdraw_and_redeem_row::RepayAndWithdrawAndRedeemRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositAndWithdraw { data, .. } => {
                let row = deposit_and_withdraw_row::DepositAndWithdrawRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidityAndObligationCollateral { data, .. } => {
                let row = deposit_reserve_liquidity_and_obligation_collateral_row::DepositReserveLiquidityAndObligationCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DepositReserveLiquidityAndObligationCollateralV2 { data, .. } => {
                let row = deposit_reserve_liquidity_and_obligation_collateral_v2_row::DepositReserveLiquidityAndObligationCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralAndRedeemReserveCollateral { data, .. } => {
                let row = withdraw_obligation_collateral_and_redeem_reserve_collateral_row::WithdrawObligationCollateralAndRedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawObligationCollateralAndRedeemReserveCollateralV2 { data, .. } => {
                let row = withdraw_obligation_collateral_and_redeem_reserve_collateral_v2_row::WithdrawObligationCollateralAndRedeemReserveCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::LiquidateObligationAndRedeemReserveCollateral { data, .. } => {
                let row = liquidate_obligation_and_redeem_reserve_collateral_row::LiquidateObligationAndRedeemReserveCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::LiquidateObligationAndRedeemReserveCollateralV2 { data, .. } => {
                let row = liquidate_obligation_and_redeem_reserve_collateral_v2_row::LiquidateObligationAndRedeemReserveCollateralV2Row::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FlashRepayReserveLiquidity { data, .. } => {
                let row = flash_repay_reserve_liquidity_row::FlashRepayReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FlashBorrowReserveLiquidity { data, .. } => {
                let row = flash_borrow_reserve_liquidity_row::FlashBorrowReserveLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RequestElevationGroup { data, .. } => {
                let row = request_elevation_group_row::RequestElevationGroupRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReferrerTokenState { data, .. } => {
                let row = init_referrer_token_state_row::InitReferrerTokenStateRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitUserMetadata { data, .. } => {
                let row = init_user_metadata_row::InitUserMetadataRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawReferrerFees { data, .. } => {
                let row = withdraw_referrer_fees_row::WithdrawReferrerFeesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitReferrerStateAndShortUrl { data, .. } => {
                let row = init_referrer_state_and_short_url_row::InitReferrerStateAndShortUrlRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::DeleteReferrerStateAndShortUrl { data, .. } => {
                let row = delete_referrer_state_and_short_url_row::DeleteReferrerStateAndShortUrlRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SetObligationOrder { data, .. } => {
                let row = set_obligation_order_row::SetObligationOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::SetBorrowOrder { data, .. } => {
                let row = set_borrow_order_row::SetBorrowOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateObligationConfig { data, .. } => {
                let row = update_obligation_config_row::UpdateObligationConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RolloverFixedTermBorrow { data, .. } => {
                let row = rollover_fixed_term_borrow_row::RolloverFixedTermBorrowRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::FillBorrowOrder { data, .. } => {
                let row = fill_borrow_order_row::FillBorrowOrderRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::EnqueueToWithdraw { data, .. } => {
                let row = enqueue_to_withdraw_row::EnqueueToWithdrawRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::WithdrawQueuedLiquidity { data, .. } => {
                let row = withdraw_queued_liquidity_row::WithdrawQueuedLiquidityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::RecoverInvalidTicketCollateral { data, .. } => {
                let row = recover_invalid_ticket_collateral_row::RecoverInvalidTicketCollateralRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::InitGlobalConfig { data, .. } => {
                let row = init_global_config_row::InitGlobalConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::IdlMissingTypes { data, .. } => {
                let row = idl_missing_types_row::IdlMissingTypesRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoLendingInstruction::CpiEvent { data, accounts, .. } => {
                cpi_event_row::CpiEventRow::from_parts(data.clone(), metadata.clone(), if accounts.is_some() { raw_accounts.clone() } else { Vec::new() }).upsert(pool).await?;
                Ok(())
            }
        }
    }
}

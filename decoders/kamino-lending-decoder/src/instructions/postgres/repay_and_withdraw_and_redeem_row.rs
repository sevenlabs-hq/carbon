//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    instruction::InstructionMetadata,
    postgres::{metadata::InstructionRowMetadata, primitives::U64},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct RepayAndWithdrawAndRedeemRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub repay_amount: U64,
    pub withdraw_collateral_amount: U64,
    #[sqlx(rename = "__accounts")]
    pub accounts: sqlx::types::Json<Vec<solana_instruction::AccountMeta>>,
}

impl RepayAndWithdrawAndRedeemRow {
    pub fn from_parts(
        source: crate::instructions::repay_and_withdraw_and_redeem::RepayAndWithdrawAndRedeem,
        metadata: InstructionMetadata,
        accounts: Vec<solana_instruction::AccountMeta>,
    ) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            repay_amount: source.repay_amount.into(),
            withdraw_collateral_amount: source.withdraw_collateral_amount.into(),
            accounts: sqlx::types::Json(accounts),
        }
    }
}

impl TryFrom<RepayAndWithdrawAndRedeemRow>
    for crate::instructions::repay_and_withdraw_and_redeem::RepayAndWithdrawAndRedeem
{
    type Error = carbon_core::error::Error;
    fn try_from(source: RepayAndWithdrawAndRedeemRow) -> Result<Self, Self::Error> {
        Ok(Self {
            repay_amount: *source.repay_amount,
            withdraw_collateral_amount: *source.withdraw_collateral_amount,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::instructions::repay_and_withdraw_and_redeem::RepayAndWithdrawAndRedeem
{
    fn table() -> &'static str {
        "repay_and_withdraw_and_redeem_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "repay_amount",
            "withdraw_collateral_amount",
            "__accounts",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RepayAndWithdrawAndRedeemRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO repay_and_withdraw_and_redeem_instruction (
                "repay_amount",
                "withdraw_collateral_amount",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            )"#,
        )
        .bind(&self.repay_amount)
        .bind(&self.withdraw_collateral_amount)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RepayAndWithdrawAndRedeemRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO repay_and_withdraw_and_redeem_instruction (
                "repay_amount",
                "withdraw_collateral_amount",
                __signature, __instruction_index, __stack_height, __slot, __accounts
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "repay_amount" = EXCLUDED."repay_amount",
                "withdraw_collateral_amount" = EXCLUDED."withdraw_collateral_amount",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot,
                __accounts = EXCLUDED.__accounts
            "#,
        )
        .bind(&self.repay_amount)
        .bind(&self.withdraw_collateral_amount)
        .bind(&self.instruction_metadata.signature)
        .bind(self.instruction_metadata.instruction_index)
        .bind(self.instruction_metadata.stack_height)
        .bind(&self.instruction_metadata.slot)
        .bind(&self.accounts)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for RepayAndWithdrawAndRedeemRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM repay_and_withdraw_and_redeem_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for RepayAndWithdrawAndRedeemRow {
    type Key = (
        String,
        carbon_core::postgres::primitives::U32,
        carbon_core::postgres::primitives::U32,
    );

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM repay_and_withdraw_and_redeem_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#,
        )
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct RepayAndWithdrawAndRedeemMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for RepayAndWithdrawAndRedeemMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS repay_and_withdraw_and_redeem_instruction (
                -- Instruction data
                "repay_amount" NUMERIC(20) NOT NULL,
                "withdraw_collateral_amount" NUMERIC(20) NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                __accounts JSONB NOT NULL,
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS repay_and_withdraw_and_redeem_instruction"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

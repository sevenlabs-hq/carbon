//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{
        BorrowOrder, LastUpdate, ObligationCollateral, ObligationLiquidity, ObligationOrder,
    },
    solana_pubkey::Pubkey,
};

/// Lending market obligation state
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Obligation {
    /// Version of the struct
    pub tag: u64,
    /// Last update to collateral, liquidity, or their market values
    pub last_update: LastUpdate,
    /// Lending market address
    pub lending_market: Pubkey,
    /// Owner authority which can borrow liquidity
    pub owner: Pubkey,
    /// Deposited collateral for the obligation, unique by deposit reserve
    /// address
    pub deposits: [ObligationCollateral; 8],
    /// Worst LTV for the collaterals backing the loan, represented as a
    /// percentage
    pub lowest_reserve_deposit_liquidation_ltv: u64,
    /// Market value of deposits (scaled fraction)
    pub deposited_value_sf: u128,
    /// Borrowed liquidity for the obligation, unique by borrow reserve address
    pub borrows: [ObligationLiquidity; 5],
    /// Risk adjusted market value of borrows/debt (sum of price *
    /// borrowed_amount * borrow_factor) (scaled fraction)
    pub borrow_factor_adjusted_debt_value_sf: u128,
    /// Market value of borrows - used for max_liquidatable_borrowed_amount
    /// (scaled fraction)
    pub borrowed_assets_market_value_sf: u128,
    /// The maximum borrow value at the weighted average loan to value ratio
    /// (scaled fraction)
    pub allowed_borrow_value_sf: u128,
    /// The dangerous borrow value at the weighted average liquidation threshold
    /// (scaled fraction)
    pub unhealthy_borrow_value_sf: u128,
    /// The asset tier of the deposits
    pub padding_deprecated_asset_tiers: [u8; 13],
    /// The elevation group id the obligation opted into.
    pub elevation_group: u8,
    /// The number of obsolete reserves the obligation has a deposit in
    pub num_of_obsolete_deposit_reserves: u8,
    /// Marked = 1 if borrows array is not empty, 0 = borrows empty
    pub has_debt: u8,
    /// Wallet address of the referrer
    pub referrer: Pubkey,
    /// Marked = 1 if borrowing disabled, 0 = borrowing enabled
    pub borrowing_disabled: u8,
    /// A target LTV set by the market owner when marking this obligation for
    /// deleveraging. Only effective when
    /// `deleveraging_margin_call_started_slot != 0`.
    pub autodeleverage_target_ltv_pct: u8,
    /// The lowest max LTV found amongst the collateral deposits
    pub lowest_reserve_deposit_max_ltv_pct: u8,
    /// The number of obsolete reserves the obligation has a borrow in
    pub num_of_obsolete_borrow_reserves: u8,
    pub reserved: [u8; 4],
    pub highest_borrow_factor_pct: u64,
    /// A timestamp at which the market owner most-recently marked this
    /// obligation for deleveraging. Zero if not currently subject to
    /// deleveraging.
    pub autodeleverage_margin_call_started_timestamp: u64,
    /// Owner-defined, permissionlessly-executed repay orders.
    /// Typical use-cases would be a stop-loss and a take-profit (possibly
    /// co-existing).
    pub obligation_orders: [ObligationOrder; 2],
    /// Owner-defined, permissionlessly-executed borrow order applicable to this
    /// obligation. Non-zeroed only on a newly-initialized fixed-rate,
    /// fixed-term obligation.
    pub borrow_order: BorrowOrder,
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding3: [u64; 73],
}

impl Obligation {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [168, 206, 141, 106, 88, 76, 172, 167] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

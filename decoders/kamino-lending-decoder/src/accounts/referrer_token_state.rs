//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;

/// Referrer account -> each owner can have multiple accounts for specific
/// reserves
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ReferrerTokenState {
    /// Pubkey of the referrer/owner
    pub referrer: Pubkey,
    /// Token mint for the account
    pub mint: Pubkey,
    /// Amount that has been accumulated and not claimed yet -> available to
    /// claim (scaled fraction)
    pub amount_unclaimed_sf: u128,
    /// Amount that has been accumulated in total -> both already claimed and
    /// unclaimed (scaled fraction)
    pub amount_cumulative_sf: u128,
    /// Referrer token state bump, used for address validation
    pub bump: u64,
    pub padding: [u64; 31],
}

impl ReferrerTokenState {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [39, 15, 208, 77, 32, 195, 105, 56] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

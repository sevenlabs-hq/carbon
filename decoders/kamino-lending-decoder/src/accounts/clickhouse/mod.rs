//! This code was AUTOGENERATED using the Codama library.
pub mod global_config_row;
pub mod lending_market_row;
pub mod obligation_row;
pub mod referrer_state_row;
pub mod referrer_token_state_row;
pub mod reserve_row;
pub mod short_url_row;
pub mod user_metadata_row;
pub mod user_state_row;
pub mod withdraw_ticket_row;

pub use self::{
    global_config_row::*, lending_market_row::*, obligation_row::*, referrer_state_row::*,
    referrer_token_state_row::*, reserve_row::*, short_url_row::*, user_metadata_row::*,
    user_state_row::*, withdraw_ticket_row::*,
};
use super::KaminoLendingAccount;

pub struct KaminoLendingAccountsMigration;

impl carbon_core::clickhouse::Migration for KaminoLendingAccountsMigration {
    fn app(&self) -> &str {
        "kamino-lending"
    }

    fn name(&self) -> &str {
        "kamino_lending_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(GlobalConfigRowMigrationOperation),
            Box::new(LendingMarketRowMigrationOperation),
            Box::new(ObligationRowMigrationOperation),
            Box::new(ReferrerStateRowMigrationOperation),
            Box::new(ReferrerTokenStateRowMigrationOperation),
            Box::new(ReserveRowMigrationOperation),
            Box::new(ShortUrlRowMigrationOperation),
            Box::new(UserMetadataRowMigrationOperation),
            Box::new(UserStateRowMigrationOperation),
            Box::new(WithdrawTicketRowMigrationOperation),
        ]
    }
}

pub enum KaminoLendingAccountRow {
    GlobalConfig(GlobalConfigRow),
    LendingMarket(LendingMarketRow),
    Obligation(ObligationRow),
    ReferrerState(ReferrerStateRow),
    ReferrerTokenState(ReferrerTokenStateRow),
    Reserve(ReserveRow),
    ShortUrl(ShortUrlRow),
    UserMetadata(UserMetadataRow),
    UserState(UserStateRow),
    WithdrawTicket(WithdrawTicketRow),
}

pub struct KaminoLendingAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a KaminoLendingAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for KaminoLendingAccountMetadata<'a> {
    type Row = KaminoLendingAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let KaminoLendingAccount::$variant(account) = account {
                    rows.push(KaminoLendingAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let KaminoLendingAccount::$variant(account) = account {
                    rows.push(KaminoLendingAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(GlobalConfig, GlobalConfigRow, boxed);
        insert_branch!(LendingMarket, LendingMarketRow, boxed);
        insert_branch!(Obligation, ObligationRow, boxed);
        insert_branch!(ReferrerState, ReferrerStateRow, boxed);
        insert_branch!(ReferrerTokenState, ReferrerTokenStateRow, boxed);
        insert_branch!(Reserve, ReserveRow, boxed);
        insert_branch!(ShortUrl, ShortUrlRow, boxed);
        insert_branch!(UserMetadata, UserMetadataRow, boxed);
        insert_branch!(UserState, UserStateRow, boxed);
        insert_branch!(WithdrawTicket, WithdrawTicketRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for KaminoLendingAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(GlobalConfig, GlobalConfigRow);
        commit_branch!(LendingMarket, LendingMarketRow);
        commit_branch!(Obligation, ObligationRow);
        commit_branch!(ReferrerState, ReferrerStateRow);
        commit_branch!(ReferrerTokenState, ReferrerTokenStateRow);
        commit_branch!(Reserve, ReserveRow);
        commit_branch!(ShortUrl, ShortUrlRow);
        commit_branch!(UserMetadata, UserMetadataRow);
        commit_branch!(UserState, UserStateRow);
        commit_branch!(WithdrawTicket, WithdrawTicketRow);

        Ok(())
    }
}

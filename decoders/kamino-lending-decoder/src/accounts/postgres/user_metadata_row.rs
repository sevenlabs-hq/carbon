//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U64},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct UserMetadataRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub referrer: Pubkey,
    pub bump: U64,
    pub user_lookup_table: Pubkey,
    pub owner: Pubkey,
    pub padding1: Vec<U64>,
    pub padding2: Vec<U64>,
}

impl UserMetadataRow {
    pub fn from_parts(
        source: crate::accounts::user_metadata::UserMetadata,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            referrer: source.referrer.into(),
            bump: source.bump.into(),
            user_lookup_table: source.user_lookup_table.into(),
            owner: source.owner.into(),
            padding1: source
                .padding1
                .into_iter()
                .map(|element| element.into())
                .collect(),
            padding2: source
                .padding2
                .into_iter()
                .map(|element| element.into())
                .collect(),
        }
    }
}

impl TryFrom<UserMetadataRow> for crate::accounts::user_metadata::UserMetadata {
    type Error = carbon_core::error::Error;
    fn try_from(source: UserMetadataRow) -> Result<Self, Self::Error> {
        Ok(Self {
            referrer: *source.referrer,
            bump: *source.bump,
            user_lookup_table: *source.user_lookup_table,
            owner: *source.owner,
            padding1: source
                .padding1
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
            padding2: source
                .padding2
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::user_metadata::UserMetadata {
    fn table() -> &'static str {
        "user_metadata_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "referrer",
            "bump",
            "user_lookup_table",
            "owner",
            "padding1",
            "padding2",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for UserMetadataRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO user_metadata_account (
                "referrer",
                "bump",
                "user_lookup_table",
                "owner",
                "padding1",
                "padding2",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.referrer)
        .bind(&self.bump)
        .bind(self.user_lookup_table)
        .bind(self.owner)
        .bind(&self.padding1)
        .bind(&self.padding2)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for UserMetadataRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO user_metadata_account (
                "referrer",
                "bump",
                "user_lookup_table",
                "owner",
                "padding1",
                "padding2",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "referrer" = EXCLUDED."referrer",
                "bump" = EXCLUDED."bump",
                "user_lookup_table" = EXCLUDED."user_lookup_table",
                "owner" = EXCLUDED."owner",
                "padding1" = EXCLUDED."padding1",
                "padding2" = EXCLUDED."padding2",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.referrer)
        .bind(&self.bump)
        .bind(self.user_lookup_table)
        .bind(self.owner)
        .bind(&self.padding1)
        .bind(&self.padding2)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for UserMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM user_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for UserMetadataRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM user_metadata_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct UserMetadataMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for UserMetadataMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS user_metadata_account (
                -- Account data
                "referrer" BYTEA NOT NULL,
                "bump" NUMERIC(20) NOT NULL,
                "user_lookup_table" BYTEA NOT NULL,
                "owner" BYTEA NOT NULL,
                "padding1" NUMERIC(20)[] NOT NULL,
                "padding2" NUMERIC(20)[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS user_metadata_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

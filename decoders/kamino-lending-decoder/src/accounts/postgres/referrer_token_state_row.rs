//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U128, U64},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ReferrerTokenStateRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub referrer: Pubkey,
    pub mint: Pubkey,
    pub amount_unclaimed_sf: U128,
    pub amount_cumulative_sf: U128,
    pub bump: U64,
    pub padding: Vec<U64>,
}

impl ReferrerTokenStateRow {
    pub fn from_parts(
        source: crate::accounts::referrer_token_state::ReferrerTokenState,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            referrer: source.referrer.into(),
            mint: source.mint.into(),
            amount_unclaimed_sf: source.amount_unclaimed_sf.into(),
            amount_cumulative_sf: source.amount_cumulative_sf.into(),
            bump: source.bump.into(),
            padding: source
                .padding
                .into_iter()
                .map(|element| element.into())
                .collect(),
        }
    }
}

impl TryFrom<ReferrerTokenStateRow> for crate::accounts::referrer_token_state::ReferrerTokenState {
    type Error = carbon_core::error::Error;
    fn try_from(source: ReferrerTokenStateRow) -> Result<Self, Self::Error> {
        Ok(Self {
            referrer: *source.referrer,
            mint: *source.mint,
            amount_unclaimed_sf: *source.amount_unclaimed_sf,
            amount_cumulative_sf: *source.amount_cumulative_sf,
            bump: *source.bump,
            padding: source
                .padding
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::referrer_token_state::ReferrerTokenState
{
    fn table() -> &'static str {
        "referrer_token_state_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "referrer",
            "mint",
            "amount_unclaimed_sf",
            "amount_cumulative_sf",
            "bump",
            "padding",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ReferrerTokenStateRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO referrer_token_state_account (
                "referrer",
                "mint",
                "amount_unclaimed_sf",
                "amount_cumulative_sf",
                "bump",
                "padding",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.referrer)
        .bind(self.mint)
        .bind(&self.amount_unclaimed_sf)
        .bind(&self.amount_cumulative_sf)
        .bind(&self.bump)
        .bind(&self.padding)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ReferrerTokenStateRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO referrer_token_state_account (
                "referrer",
                "mint",
                "amount_unclaimed_sf",
                "amount_cumulative_sf",
                "bump",
                "padding",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "referrer" = EXCLUDED."referrer",
                "mint" = EXCLUDED."mint",
                "amount_unclaimed_sf" = EXCLUDED."amount_unclaimed_sf",
                "amount_cumulative_sf" = EXCLUDED."amount_cumulative_sf",
                "bump" = EXCLUDED."bump",
                "padding" = EXCLUDED."padding",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.referrer)
        .bind(self.mint)
        .bind(&self.amount_unclaimed_sf)
        .bind(&self.amount_cumulative_sf)
        .bind(&self.bump)
        .bind(&self.padding)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ReferrerTokenStateRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM referrer_token_state_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ReferrerTokenStateRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM referrer_token_state_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ReferrerTokenStateMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for ReferrerTokenStateMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS referrer_token_state_account (
                -- Account data
                "referrer" BYTEA NOT NULL,
                "mint" BYTEA NOT NULL,
                "amount_unclaimed_sf" NUMERIC(39) NOT NULL,
                "amount_cumulative_sf" NUMERIC(39) NOT NULL,
                "bump" NUMERIC(20) NOT NULL,
                "padding" NUMERIC(20)[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS referrer_token_state_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

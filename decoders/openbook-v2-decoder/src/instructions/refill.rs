//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Refill a certain amount of `base` and `quote` lamports. The amount being
/// passed is the total lamports that the [`Position`](crate::state::Position)
/// will have. Makers might wish to `refill`, rather than have actual tokens
/// moved for each trade, in order to reduce CUs.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Refill {
    pub base_amount: u64,
    pub quote_amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RefillInstructionAccounts {
    pub owner: solana_pubkey::Pubkey,
    pub user_base_account: solana_pubkey::Pubkey,
    pub user_quote_account: solana_pubkey::Pubkey,
    pub open_orders_account: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub market_base_vault: solana_pubkey::Pubkey,
    pub market_quote_vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Refill {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [128, 207, 142, 11, 54, 232, 38, 201] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Refill {
    type ArrangedAccounts = RefillInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let owner = next_account(&mut iter)?;
        let user_base_account = next_account(&mut iter)?;
        let user_quote_account = next_account(&mut iter)?;
        let open_orders_account = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let market_base_vault = next_account(&mut iter)?;
        let market_quote_vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RefillInstructionAccounts {
            owner,
            user_base_account,
            user_quote_account,
            open_orders_account,
            market,
            market_base_vault,
            market_quote_vault,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct BumpExecuted {
    pub bumper: Pubkey,
    pub victim: Pubkey,
    pub launch: Pubkey,
    pub victim_slot_index: u32,
    pub victim_token_amount: u64,
    pub total_fee: u64,
    pub community_amt: u64,
    pub timestamp: i64,
    pub slot_index: u32,
    pub tokens_transferred: u64,
    /// 0 = lower (entry < 1/3 GRAD), 1 = middle, 2 = upper.
    pub fee_bracket: u8,
    pub refund_amount: u64,
    /// 2026-05 multi-spin unlock — instruction's bump_index arg, lets
    /// indexer / UI distinguish "spin history" entries when the same
    /// spin yields N>1 victims (each consumed via a separate
    /// confirm_bump tx).
    pub bump_index: u8,
    /// Total gacha extension after this bump (0 if no extension triggered).
    pub gacha_extension_secs: u32,
}


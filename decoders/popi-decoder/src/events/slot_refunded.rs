//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SlotRefundedEvent {
    pub user: Pubkey,
    pub launch: Pubkey,
    pub slot_index: u32,
    pub deposit: u64,
    /// Total refund fee charged to the user (platform_fee + dev_share).
    pub fee: u64,
    pub net_refund: u64,
    pub fee_bps: u16,
    pub refund_log_size_after: u32,
    pub real_sol_deposited: u64,
    pub timestamp: i64,
    /// V8.4.16: dev's slice of `fee` on fair launches (0 on dev_bundle
    /// or if dev_fee_bps=0). Accumulates into launch.dev_pending_fees.
    pub dev_share: u64,
}

impl SlotRefundedEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[234, 109, 187, 73, 174, 175, 18, 9] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

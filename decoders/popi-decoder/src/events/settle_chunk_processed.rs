//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SettleChunkProcessedEvent {
    pub launch: Pubkey,
    pub caller: Pubkey,
    pub processed: u32,
    pub settled_count_after: u32,
    pub timestamp: i64,
    /// First refund_log index appended this chunk; valid only when
    /// `refund_log_count > 0`.
    pub refund_log_first_idx: u32,
    pub refund_log_count: u32,
}

impl SettleChunkProcessedEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[152, 208, 3, 147, 189, 241, 131, 231] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

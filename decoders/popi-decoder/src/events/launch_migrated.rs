//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LaunchMigratedEvent {
    pub launch: Pubkey,
    pub creator: Pubkey,
    pub mint: Pubkey,
    pub pool: Pubkey,
    pub caller: Pubkey,
    pub claimable_sol: u64,
    pub migrate_fee: u64,
    pub amm_sol: u64,
    pub pool_tokens: u64,
    pub migrated_at_slot: u64,
    pub timestamp: i64,
    /// Wire-stable identifier; always "migrated" from this handler
    /// (distinct from on-chain Phase::Graduated).
    pub phase: String,
}

impl LaunchMigratedEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[135, 27, 61, 35, 85, 170, 159, 33] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

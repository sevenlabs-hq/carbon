//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct GlobalConfig {
    pub admin: Pubkey,
    pub bump: u8,
    pub buy_fee_recipients: [Pubkey; 5],
    pub bump_fee_recipients: [Pubkey; 5],
    pub spin_fee_recipients: [Pubkey; 5],
    pub migration_fee_recipient: Pubkey,
    pub misc_fee_recipient: Pubkey,
    pub lottery_authority: Pubkey,
    pub graduation_threshold_lamports: u64,
    pub bump_fee_lower_bps: u16,
    pub bump_fee_middle_bps: u16,
    pub bump_fee_upper_bps: u16,
    pub buy_fee_bps: u16,
    pub refund_fee_bps: [u16; 4],
    pub spin_fee_lamports: u64,
    pub create_launch_fee_lamports: u64,
    pub dev_bundle_fee_bps: u16,
    pub migrate_fee_bps: u16,
    pub dev_fee_bps: u16,
    pub min_buy_fee_bps: u16,
    pub refund_fee_recipients: [Pubkey; 5],
    /// V8.4.8 H-2: seal-after-migrate flag. `migrate_global_config` flips
    /// this true on completion; subsequent calls revert with
    /// `GlobalConfigAlreadyMigrated`. Cleared by `reset_migration_completed`
    /// when admin wants to bless a future schema migration.
    pub migration_completed: bool,
}

impl GlobalConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[149, 8, 156, 202, 160, 252, 176, 217] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ParticipantSlot {
    pub launch: Pubkey,
    pub owner: Pubkey,
    pub slot_index: u32,
    pub deposited_sol: u64,
    pub token_amount: u64,
    pub sol_at_entry: u64,
    /// launch.real_sol_deposited at moment of buy. Curve position; settle ordering key.
    pub start_cumulative_sol_at_buy: u64,
    /// launch.refund_log_size at moment of buy. Re-buy bug fix: distinguishes
    /// refunds applied before vs. after this slot exists.
    pub refund_log_index_at_buy: u32,
    /// Lifetime SOL drained via claim_excess + bump auto-flush. Net-basis input
    /// for confirm_bump fee calc.
    pub cumulative_sol_refunded: u64,
    /// Unclaimed settle / cap-clamp excess. Vault cannot close while sum is non-zero.
    pub pending_excess: u64,
    pub settled: bool,
    pub bump: u8,
}

impl ParticipantSlot {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[112, 32, 121, 212, 19, 156, 224, 127] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

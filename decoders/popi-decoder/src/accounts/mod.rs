//! This code was AUTOGENERATED using the Codama library.
use crate::PROGRAM_ID;
use crate::PopiDecoder;

pub mod global_config;
pub mod keeper;
pub mod launch;
pub mod lottery_snapshot;
pub mod membership;
pub mod multi_spin_result;
pub mod participant_slot;
pub mod refund_log_account;
pub mod split_binding;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum PopiAccount {
    GlobalConfig(Box<global_config::GlobalConfig>),
    Keeper(Box<keeper::Keeper>),
    Launch(Box<launch::Launch>),
    LotterySnapshot(Box<lottery_snapshot::LotterySnapshot>),
    Membership(Box<membership::Membership>),
    MultiSpinResult(Box<multi_spin_result::MultiSpinResult>),
    ParticipantSlot(Box<participant_slot::ParticipantSlot>),
    RefundLogAccount(Box<refund_log_account::RefundLogAccount>),
    SplitBinding(Box<split_binding::SplitBinding>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for PopiDecoder {
    type AccountType = PopiAccount;
    
    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }
        
        let data = account.data.as_slice();
        
        {
            if let Some(decoded) = global_config::GlobalConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::GlobalConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = keeper::Keeper::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::Keeper(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = launch::Launch::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::Launch(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = lottery_snapshot::LotterySnapshot::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::LotterySnapshot(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = membership::Membership::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::Membership(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = multi_spin_result::MultiSpinResult::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::MultiSpinResult(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = participant_slot::ParticipantSlot::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::ParticipantSlot(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = refund_log_account::RefundLogAccount::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::RefundLogAccount(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = split_binding::SplitBinding::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: PopiAccount::SplitBinding(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        
        None
    }
}


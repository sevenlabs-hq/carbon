//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
use crate::types::Phase;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Launch {
    pub creator: Pubkey,
    pub created_at: i64,
    pub created_at_slot: u64,
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub mint: Pubkey,
    pub phase: Phase,
    pub participants_count: u32,
    pub total_participants_tokens: u64,
    pub virtual_sol_reserves: u64,
    pub virtual_token_reserves: u64,
    pub real_sol_deposited: u64,
    pub dev_bundle: bool,
    pub dev_deposited_sol: u64,
    pub dev_token_amount: u64,
    pub bonding_curve_duration_secs: u32,
    pub gacha_duration_secs: u32,
    /// Set when buy_slot graduates the launch; gacha measured from here, not created_at.
    pub gacha_started_at: Option<i64>,
    /// Accumulated liveness extension; +GACHA_BUMP_EXTENSION_SECS per
    /// confirm_bump landing in the last 30s of gacha. Uncapped (checked_add
    /// guards overflow at u32::MAX).
    pub gacha_extension_secs: u32,
    pub accumulated_community_lamports: u64,
    pub migration_completed_at: Option<i64>,
    /// Monotonic counter for slot PDA derivation; never decremented.
    pub next_slot_index: u32,
    /// start_cum of last settled slot (composite key with last_settled_slot_index).
    pub last_settled_position: u64,
    pub last_settled_slot_index: u32,
    pub settled_count: u32,
    /// Vault-close gate: sum of unclaimed claim_excess balances.
    pub total_pending_excess: u64,
    pub refund_log_size: u32,
    pub refund_log_account: Pubkey,
    pub settled: bool,
    pub migrated: bool,
    /// Stamped when settle_slots flips `settled` to true; drives the
    /// keeper-priority window in open_gacha.
    pub settle_completed_at: i64,
    /// Counter of lamports earmarked for the creator dev fee (0.5% on
    /// every buy_slot + claim_refund). The lamports physically sit in the
    /// launch vault; this counter is the off-by-one ledger between vault
    /// balance and "creator's share." Paid out via claim_dev_fees in the
    /// Graduated phase, or routed to platform fee_account on terminal
    /// failed-launch close.
    pub dev_pending_fees: u64,
    pub bump: u8,
}

impl Launch {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[144, 51, 51, 163, 206, 85, 213, 38] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

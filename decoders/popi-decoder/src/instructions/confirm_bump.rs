//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct ConfirmBump {
    pub victim_slot_index: u32,
    pub bump_index: u8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ConfirmBumpInstructionAccounts {
    pub bumper: solana_pubkey::Pubkey,
    pub bumper_member: solana_pubkey::Pubkey,
    pub launch: solana_pubkey::Pubkey,
    pub spin_result: solana_pubkey::Pubkey,
    pub victim: solana_pubkey::Pubkey,
    pub victim_slot: solana_pubkey::Pubkey,
    pub victim_member: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub creator: solana_pubkey::Pubkey,
    pub fee_account: solana_pubkey::Pubkey,
    pub global_config: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl ConfirmBump {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[249, 147, 75, 98, 108, 50, 127, 176] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for ConfirmBump {
    type ArrangedAccounts = ConfirmBumpInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let bumper = next_account(&mut iter)?;
        let bumper_member = next_account(&mut iter)?;
        let launch = next_account(&mut iter)?;
        let spin_result = next_account(&mut iter)?;
        let victim = next_account(&mut iter)?;
        let victim_slot = next_account(&mut iter)?;
        let victim_member = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let creator = next_account(&mut iter)?;
        let fee_account = next_account(&mut iter)?;
        let global_config = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ConfirmBumpInstructionAccounts {
            bumper,
            bumper_member,
            launch,
            spin_result,
            victim,
            victim_slot,
            victim_member,
            vault,
            creator,
            fee_account,
            global_config,
            system_program,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CommitLottery {
    pub holders_commitment: [u8; 32],
    pub total_weight: u64,
    pub holder_count: u32,
    pub snapshot_slot: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CommitLotteryInstructionAccounts {
    pub authority: solana_pubkey::Pubkey,
    pub launch: solana_pubkey::Pubkey,
    pub snapshot: solana_pubkey::Pubkey,
    pub orao_vrf: solana_pubkey::Pubkey,
    pub network_state: solana_pubkey::Pubkey,
    pub treasury: solana_pubkey::Pubkey,
    pub random_request: solana_pubkey::Pubkey,
    pub global_config: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CommitLottery {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[84, 218, 60, 61, 228, 13, 104, 82] {
            return None;
        }

        let mut data_slice = data;
        data_slice = &data_slice[8..];
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CommitLottery {
    type ArrangedAccounts = CommitLotteryInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let authority = next_account(&mut iter)?;
        let launch = next_account(&mut iter)?;
        let snapshot = next_account(&mut iter)?;
        let orao_vrf = next_account(&mut iter)?;
        let network_state = next_account(&mut iter)?;
        let treasury = next_account(&mut iter)?;
        let random_request = next_account(&mut iter)?;
        let global_config = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CommitLotteryInstructionAccounts {
            authority,
            launch,
            snapshot,
            orao_vrf,
            network_state,
            treasury,
            random_request,
            global_config,
            system_program,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

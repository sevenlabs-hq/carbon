//! This code was AUTOGENERATED using the Codama library.
        pub mod buy_row;
pub mod claim_row;
pub mod create_row;
pub mod quote_buy_row;
pub mod quote_sell_row;
pub mod sell_row;
pub mod cpi_event_row;

        pub use self::buy_row::*;
pub use self::claim_row::*;
pub use self::create_row::*;
pub use self::quote_buy_row::*;
pub use self::quote_sell_row::*;
pub use self::sell_row::*;
pub use self::cpi_event_row::*;
        use super::VertigoInstruction;

        pub struct VertigoInstructionsMigration;

        impl carbon_core::clickhouse::Migration for VertigoInstructionsMigration {
            fn app(&self) -> &str {
                "vertigo"
            }

            fn name(&self) -> &str {
                "vertigo_instructions"
            }

            fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
                vec![
                    Box::new(BuyRowMigrationOperation),
            Box::new(ClaimRowMigrationOperation),
            Box::new(CreateRowMigrationOperation),
            Box::new(QuoteBuyRowMigrationOperation),
            Box::new(QuoteSellRowMigrationOperation),
            Box::new(SellRowMigrationOperation),
            Box::new(CpiEventRowMigrationOperation)
                ]
            }
        }

        pub enum VertigoInstructionRow {
    Buy(BuyRow),
    Claim(ClaimRow),
    Create(CreateRow),
    QuoteBuy(QuoteBuyRow),
    QuoteSell(QuoteSellRow),
    Sell(SellRow),
    CpiEvent(CpiEventRow),
        }

        pub struct VertigoInstructionMetadata(
            pub carbon_core::instruction::InstructionMetadata,
            pub VertigoInstruction,
        );

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchInsert for VertigoInstructionMetadata {
            type Row = VertigoInstructionRow;

            async fn batch_insert(
                &self,
                rows: &mut Vec<Self::Row>,
            ) -> carbon_core::error::CarbonResult<()> {
                let Self(metadata, instruction) = self;

                match instruction {
            VertigoInstruction::Buy { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::Buy(
                    BuyRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::Claim { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::Claim(
                    ClaimRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::Create { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::Create(
                    CreateRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::QuoteBuy { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::QuoteBuy(
                    QuoteBuyRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::QuoteSell { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::QuoteSell(
                    QuoteSellRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::Sell { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::Sell(
                    SellRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VertigoInstruction::CpiEvent { data, accounts, .. } => {
                rows.push(VertigoInstructionRow::CpiEvent(
                    CpiEventRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
                }

                Ok(())
            }
        }

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchCommit for VertigoInstructionRow {
            async fn batch_commit(
                &self,
                client: &clickhouse::Client,
                rows: &[Self],
            ) -> carbon_core::error::CarbonResult<()> {
                macro_rules! commit_branch {
                    ($variant:ident, $row:ty) => {
                        if let Self::$variant(source) = self {
                            let branch_rows: Vec<$row> = rows
                                .iter()
                                .filter_map(|row| match row {
                                    Self::$variant(row) => Some(row.clone()),
                                    _ => None,
                                })
                                .collect();
                            return <$row as carbon_core::clickhouse::Insert>::insert(
                                source,
                                client,
                                &branch_rows,
                            )
                            .await;
                        }
                    };
                }

        commit_branch!(Buy, BuyRow);
        commit_branch!(Claim, ClaimRow);
        commit_branch!(Create, CreateRow);
        commit_branch!(QuoteBuy, QuoteBuyRow);
        commit_branch!(QuoteSell, QuoteSellRow);
        commit_branch!(Sell, SellRow);
        commit_branch!(CpiEvent, CpiEventRow);
                Ok(())
            }
        }

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LpTransfer {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LpTransferInstructionAccounts {
    pub lp_authority: solana_pubkey::Pubkey,
    pub new_lp_authority: solana_pubkey::Pubkey,
    pub lp_escrow: solana_pubkey::Pubkey,
    pub new_lp_escrow: solana_pubkey::Pubkey,
    pub position_mint: solana_pubkey::Pubkey,
    pub position: solana_pubkey::Pubkey,
    pub position_token_account: solana_pubkey::Pubkey,
    pub new_position_token_account: solana_pubkey::Pubkey,
    pub lock_config: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token22_program: solana_pubkey::Pubkey,
    pub ata_program: solana_pubkey::Pubkey,
    pub whirlpool_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LpTransfer {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [57] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LpTransfer {
    type ArrangedAccounts = LpTransferInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let lp_authority = next_account(&mut iter)?;
        let new_lp_authority = next_account(&mut iter)?;
        let lp_escrow = next_account(&mut iter)?;
        let new_lp_escrow = next_account(&mut iter)?;
        let position_mint = next_account(&mut iter)?;
        let position = next_account(&mut iter)?;
        let position_token_account = next_account(&mut iter)?;
        let new_position_token_account = next_account(&mut iter)?;
        let lock_config = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token22_program = next_account(&mut iter)?;
        let ata_program = next_account(&mut iter)?;
        let whirlpool_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LpTransferInstructionAccounts {
            lp_authority,
            new_lp_authority,
            lp_escrow,
            new_lp_escrow,
            position_mint,
            position,
            position_token_account,
            new_position_token_account,
            lock_config,
            system_program,
            token22_program,
            ata_program,
            whirlpool_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {crate::types::graphql::MetaplexCreatorGraphQL, juniper::GraphQLObject};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "MetaplexData")]
pub struct MetaplexDataGraphQL {
    pub name: String,
    pub symbol: String,
    pub uri: String,
    pub seller_fee_basis_points: i32,
    pub creators: Option<Vec<MetaplexCreatorGraphQL>>,
}

impl From<crate::types::MetaplexData> for MetaplexDataGraphQL {
    fn from(original: crate::types::MetaplexData) -> Self {
        Self {
            name: original.name,
            symbol: original.symbol,
            uri: original.uri,
            seller_fee_basis_points: original.seller_fee_basis_points as i32,
            creators: original
                .creators
                .map(|v| v.into_iter().map(|item| item.into()).collect()),
        }
    }
}

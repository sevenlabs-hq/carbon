//! This code was AUTOGENERATED using the Codama library.
pub mod authority_config_row;
pub mod bonding_curve_row;
pub mod consumed_permission_row;
pub mod mint_config_row;
pub mod permission_config_row;

pub use self::{
    authority_config_row::*, bonding_curve_row::*, consumed_permission_row::*, mint_config_row::*,
    permission_config_row::*,
};
use super::WavebreakAccount;

pub struct WavebreakAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for WavebreakAccountsMigration {
    fn app(&self) -> &str {
        "wavebreak"
    }

    fn name(&self) -> &str {
        "wavebreak_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AuthorityConfigMigrationOperation),
            Box::new(BondingCurveMigrationOperation),
            Box::new(ConsumedPermissionMigrationOperation),
            Box::new(MintConfigMigrationOperation),
            Box::new(PermissionConfigMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct WavebreakAccountWithMetadata(
    pub WavebreakAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(WavebreakAccount, carbon_core::account::AccountMetadata)>
    for WavebreakAccountWithMetadata
{
    fn from(value: (WavebreakAccount, carbon_core::account::AccountMetadata)) -> Self {
        WavebreakAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for WavebreakAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let WavebreakAccountWithMetadata(account, metadata) = self;

        match account {
            WavebreakAccount::AuthorityConfig(account) => {
                let row = authority_config_row::AuthorityConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            WavebreakAccount::BondingCurve(account) => {
                let row = bonding_curve_row::BondingCurveRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            WavebreakAccount::MintConfig(account) => {
                let row =
                    mint_config_row::MintConfigRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            WavebreakAccount::PermissionConfig(account) => {
                let row = permission_config_row::PermissionConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            WavebreakAccount::ConsumedPermission(account) => {
                let row = consumed_permission_row::ConsumedPermissionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for WavebreakAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let WavebreakAccountWithMetadata(account, metadata) = self;
        match account {
            WavebreakAccount::AuthorityConfig(account) => {
                let row = authority_config_row::AuthorityConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            WavebreakAccount::BondingCurve(account) => {
                let row = bonding_curve_row::BondingCurveRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            WavebreakAccount::MintConfig(account) => {
                let row =
                    mint_config_row::MintConfigRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            WavebreakAccount::PermissionConfig(account) => {
                let row = permission_config_row::PermissionConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            WavebreakAccount::ConsumedPermission(account) => {
                let row = consumed_permission_row::ConsumedPermissionRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::PermissionSignerGraphQL,
    carbon_core::graphql::primitives::{Pubkey, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "PermissionConfig")]
pub struct PermissionConfigGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub consumer_program: Pubkey,
    pub allowed_signers: Vec<PermissionSignerGraphQL>,
    pub padding: Vec<U8>,
}

impl TryFrom<crate::accounts::postgres::PermissionConfigRow> for PermissionConfigGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::PermissionConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            consumer_program: carbon_core::graphql::primitives::Pubkey(row.consumer_program.0),
            allowed_signers: row
                .allowed_signers
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            padding: row
                .padding
                .into_iter()
                .map(|x| carbon_core::graphql::primitives::U8((*x) as u8))
                .collect(),
        })
    }
}

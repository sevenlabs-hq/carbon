//! This code was AUTOGENERATED using the Codama library.
pub mod farm_state_row;
pub mod global_config_row;
pub mod oracle_prices_row;
pub mod user_state_row;

pub use self::{farm_state_row::*, global_config_row::*, oracle_prices_row::*, user_state_row::*};
use super::KaminoFarmsAccount;

pub struct KaminoFarmsAccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for KaminoFarmsAccountsMigration {
    fn app(&self) -> &str {
        "kamino-farms"
    }

    fn name(&self) -> &str {
        "kamino_farms_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(FarmStateMigrationOperation),
            Box::new(GlobalConfigMigrationOperation),
            Box::new(OraclePricesMigrationOperation),
            Box::new(UserStateMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct KaminoFarmsAccountWithMetadata(
    pub KaminoFarmsAccount,
    pub carbon_core::account::AccountMetadata,
);

impl From<(KaminoFarmsAccount, carbon_core::account::AccountMetadata)>
    for KaminoFarmsAccountWithMetadata
{
    fn from(value: (KaminoFarmsAccount, carbon_core::account::AccountMetadata)) -> Self {
        KaminoFarmsAccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for KaminoFarmsAccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoFarmsAccountWithMetadata(account, metadata) = self;

        match account {
            KaminoFarmsAccount::FarmState(account) => {
                let row =
                    farm_state_row::FarmStateRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::GlobalConfig(account) => {
                let row = global_config_row::GlobalConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::UserState(account) => {
                let row =
                    user_state_row::UserStateRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::OraclePrices(account) => {
                let row = oracle_prices_row::OraclePricesRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for KaminoFarmsAccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoFarmsAccountWithMetadata(account, metadata) = self;
        match account {
            KaminoFarmsAccount::FarmState(account) => {
                let row =
                    farm_state_row::FarmStateRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::GlobalConfig(account) => {
                let row = global_config_row::GlobalConfigRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::UserState(account) => {
                let row =
                    user_state_row::UserStateRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsAccount::OraclePrices(account) => {
                let row = oracle_prices_row::OraclePricesRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{
        metadata::AccountRowMetadata,
        primitives::{Pubkey, U128, U64},
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct GlobalConfigRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub global_admin: Pubkey,
    pub treasury_fee_bps: U64,
    pub treasury_vaults_authority: Pubkey,
    pub treasury_vaults_authority_bump: U64,
    pub pending_global_admin: Pubkey,
    pub padding1: Vec<U128>,
}

impl GlobalConfigRow {
    pub fn from_parts(
        source: crate::accounts::global_config::GlobalConfig,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            global_admin: source.global_admin.into(),
            treasury_fee_bps: source.treasury_fee_bps.into(),
            treasury_vaults_authority: source.treasury_vaults_authority.into(),
            treasury_vaults_authority_bump: source.treasury_vaults_authority_bump.into(),
            pending_global_admin: source.pending_global_admin.into(),
            padding1: source
                .padding1
                .into_iter()
                .map(|element| element.into())
                .collect(),
        }
    }
}

impl TryFrom<GlobalConfigRow> for crate::accounts::global_config::GlobalConfig {
    type Error = carbon_core::error::Error;
    fn try_from(source: GlobalConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            global_admin: *source.global_admin,
            treasury_fee_bps: *source.treasury_fee_bps,
            treasury_vaults_authority: *source.treasury_vaults_authority,
            treasury_vaults_authority_bump: *source.treasury_vaults_authority_bump,
            pending_global_admin: *source.pending_global_admin,
            padding1: source
                .padding1
                .into_iter()
                .map(|element| Ok(*element))
                .collect::<Result<Vec<_>, carbon_core::error::Error>>()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to collect array elements".to_string(),
                    )
                })?
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert array element to primitive".to_string(),
                    )
                })?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::global_config::GlobalConfig {
    fn table() -> &'static str {
        "global_config_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "global_admin",
            "treasury_fee_bps",
            "treasury_vaults_authority",
            "treasury_vaults_authority_bump",
            "pending_global_admin",
            "padding1",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for GlobalConfigRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO global_config_account (
                "global_admin",
                "treasury_fee_bps",
                "treasury_vaults_authority",
                "treasury_vaults_authority_bump",
                "pending_global_admin",
                "padding1",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            )"#,
        )
        .bind(self.global_admin)
        .bind(&self.treasury_fee_bps)
        .bind(self.treasury_vaults_authority)
        .bind(&self.treasury_vaults_authority_bump)
        .bind(self.pending_global_admin)
        .bind(&self.padding1)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for GlobalConfigRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO global_config_account (
                "global_admin",
                "treasury_fee_bps",
                "treasury_vaults_authority",
                "treasury_vaults_authority_bump",
                "pending_global_admin",
                "padding1",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "global_admin" = EXCLUDED."global_admin",
                "treasury_fee_bps" = EXCLUDED."treasury_fee_bps",
                "treasury_vaults_authority" = EXCLUDED."treasury_vaults_authority",
                "treasury_vaults_authority_bump" = EXCLUDED."treasury_vaults_authority_bump",
                "pending_global_admin" = EXCLUDED."pending_global_admin",
                "padding1" = EXCLUDED."padding1",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.global_admin)
        .bind(&self.treasury_fee_bps)
        .bind(self.treasury_vaults_authority)
        .bind(&self.treasury_vaults_authority_bump)
        .bind(self.pending_global_admin)
        .bind(&self.padding1)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for GlobalConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM global_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for GlobalConfigRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM global_config_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct GlobalConfigMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for GlobalConfigMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS global_config_account (
                -- Account data
                "global_admin" BYTEA NOT NULL,
                "treasury_fee_bps" NUMERIC(20) NOT NULL,
                "treasury_vaults_authority" BYTEA NOT NULL,
                "treasury_vaults_authority_bump" NUMERIC(20) NOT NULL,
                "pending_global_admin" BYTEA NOT NULL,
                "padding1" NUMERIC(39)[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS global_config_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

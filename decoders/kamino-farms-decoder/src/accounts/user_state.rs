//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UserState {
    pub user_id: u64,
    pub farm_state: Pubkey,
    pub owner: Pubkey,
    /// Indicate if this user state is part of a delegated farm
    pub is_farm_delegated: u8,
    pub padding0: [u8; 7],
    /// Rewards tally used for computation of gained rewards
    /// (scaled from `Decimal` representation).
    pub rewards_tally_scaled: [u128; 10],
    /// Number of reward tokens ready for claim
    pub rewards_issued_unclaimed: [u64; 10],
    pub last_claim_ts: [u64; 10],
    /// User stake deposited and usable, generating rewards and fees.
    /// (scaled from `Decimal` representation).
    pub active_stake_scaled: u128,
    /// User stake deposited but not usable and not generating rewards yet.
    /// (scaled from `Decimal` representation).
    pub pending_deposit_stake_scaled: u128,
    /// After this timestamp, pending user stake can be moved to user stake
    /// Initialized to now() + delayed user stake period
    pub pending_deposit_stake_ts: u64,
    /// User deposits unstaked, pending for withdrawal, not usable and not
    /// generating rewards. (scaled from `Decimal` representation).
    pub pending_withdrawal_unstake_scaled: u128,
    /// After this timestamp, user can withdraw their deposit.
    pub pending_withdrawal_unstake_ts: u64,
    /// User bump used for account address validation
    pub bump: u64,
    /// Delegatee used for initialisation - useful to check against
    pub delegatee: Pubkey,
    pub last_stake_ts: u64,
    /// Cumulative rewards issued to the user - ONLY used for stats/analytics
    /// DO NOT USE IN ANY CALCULATIONS
    /// Old userStates will have this field populated only from the point of
    /// release not reflecting any historical data before this was released
    pub rewards_issued_cumulative: [u64; 10],
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding1: [u64; 40],
}

impl UserState {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [72, 177, 85, 249, 76, 167, 186, 126] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

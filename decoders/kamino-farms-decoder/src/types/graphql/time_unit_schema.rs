//! This code was AUTOGENERATED using the Codama library.
use juniper::GraphQLEnum;

#[derive(Debug, Clone, GraphQLEnum)]
#[graphql(name = "TimeUnit")]
pub enum TimeUnitGraphQL {
    Seconds,
    Slots,
}

impl From<crate::types::TimeUnit> for TimeUnitGraphQL {
    fn from(original: crate::types::TimeUnit) -> Self {
        match original {
            crate::types::TimeUnit::Seconds => Self::Seconds,
            crate::types::TimeUnit::Slots => Self::Slots,
        }
    }
}

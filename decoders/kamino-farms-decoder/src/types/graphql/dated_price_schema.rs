//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::PriceGraphQL, carbon_core::graphql::primitives::U64,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "DatedPrice")]
pub struct DatedPriceGraphQL {
    pub price: PriceGraphQL,
    pub last_updated_slot: U64,
    pub unix_timestamp: U64,
    pub reserved: Vec<U64>,
    pub reserved2: Vec<i32>,
    pub index: i32,
}

impl From<crate::types::DatedPrice> for DatedPriceGraphQL {
    fn from(original: crate::types::DatedPrice) -> Self {
        Self {
            price: original.price.into(),
            last_updated_slot: carbon_core::graphql::primitives::U64(original.last_updated_slot),
            unix_timestamp: carbon_core::graphql::primitives::U64(original.unix_timestamp),
            reserved: original
                .reserved
                .into_iter()
                .map(carbon_core::graphql::primitives::U64)
                .collect(),
            reserved2: original
                .reserved2
                .into_iter()
                .map(|item| item as i32)
                .collect(),
            index: original.index as i32,
        }
    }
}

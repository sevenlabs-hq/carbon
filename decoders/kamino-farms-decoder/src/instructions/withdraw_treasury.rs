//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct WithdrawTreasury {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawTreasuryInstructionAccounts {
    pub global_admin: solana_pubkey::Pubkey,
    pub global_config: solana_pubkey::Pubkey,
    pub reward_mint: solana_pubkey::Pubkey,
    pub reward_treasury_vault: solana_pubkey::Pubkey,
    pub treasury_vault_authority: solana_pubkey::Pubkey,
    pub withdraw_destination_token_account: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl WithdrawTreasury {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [40, 63, 122, 158, 144, 216, 83, 96] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for WithdrawTreasury {
    type ArrangedAccounts = WithdrawTreasuryInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let global_admin = next_account(&mut iter)?;
        let global_config = next_account(&mut iter)?;
        let reward_mint = next_account(&mut iter)?;
        let reward_treasury_vault = next_account(&mut iter)?;
        let treasury_vault_authority = next_account(&mut iter)?;
        let withdraw_destination_token_account = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(WithdrawTreasuryInstructionAccounts {
            global_admin,
            global_config,
            reward_mint,
            reward_treasury_vault,
            treasury_vault_authority,
            withdraw_destination_token_account,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

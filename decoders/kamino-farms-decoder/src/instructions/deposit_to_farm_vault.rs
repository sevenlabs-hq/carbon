//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DepositToFarmVault {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DepositToFarmVaultInstructionAccounts {
    pub depositor: solana_pubkey::Pubkey,
    pub farm_state: solana_pubkey::Pubkey,
    pub farm_vault: solana_pubkey::Pubkey,
    pub depositor_ata: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DepositToFarmVault {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [131, 166, 64, 94, 108, 213, 114, 183] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for DepositToFarmVault {
    type ArrangedAccounts = DepositToFarmVaultInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let depositor = next_account(&mut iter)?;
        let farm_state = next_account(&mut iter)?;
        let farm_vault = next_account(&mut iter)?;
        let depositor_ata = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DepositToFarmVaultInstructionAccounts {
            depositor,
            farm_state,
            farm_vault,
            depositor_ata,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

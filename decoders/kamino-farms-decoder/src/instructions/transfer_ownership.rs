//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TransferOwnership {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct TransferOwnershipInstructionAccounts {
    pub old_owner: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub new_owner: solana_pubkey::Pubkey,
    pub old_user_state: solana_pubkey::Pubkey,
    pub new_user_state: solana_pubkey::Pubkey,
    pub farm_state: solana_pubkey::Pubkey,
    pub scope_prices: Option<solana_pubkey::Pubkey>,
    pub system_program: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl TransferOwnership {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [65, 177, 215, 73, 53, 45, 99, 47] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for TransferOwnership {
    type ArrangedAccounts = TransferOwnershipInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let old_owner = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let new_owner = next_account(&mut iter)?;
        let old_user_state = next_account(&mut iter)?;
        let new_user_state = next_account(&mut iter)?;
        let farm_state = next_account(&mut iter)?;
        let scope_prices = next_account(&mut iter);
        let system_program = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(TransferOwnershipInstructionAccounts {
            old_owner,
            payer,
            new_owner,
            old_user_state,
            new_user_state,
            farm_state,
            scope_prices,
            system_program,
            rent,
            remaining: remaining.to_vec(),
        })
    }
}

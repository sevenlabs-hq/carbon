//! This code was AUTOGENERATED using the Codama library.
pub mod add_rewards_row;
pub mod deposit_to_farm_vault_row;
pub mod harvest_reward_row;
pub mod idl_missing_types_row;
pub mod initialize_farm_delegated_row;
pub mod initialize_farm_row;
pub mod initialize_global_config_row;
pub mod initialize_reward_row;
pub mod initialize_user_row;
pub mod refresh_farm_row;
pub mod refresh_user_state_row;
pub mod reward_user_once_row;
pub mod set_stake_delegated_row;
pub mod stake_row;
pub mod transfer_ownership_row;
pub mod unstake_row;
pub mod update_farm_admin_row;
pub mod update_farm_config_row;
pub mod update_global_config_admin_row;
pub mod update_global_config_row;
pub mod update_second_delegated_authority_row;
pub mod withdraw_from_farm_vault_row;
pub mod withdraw_reward_row;
pub mod withdraw_slashed_amount_row;
pub mod withdraw_treasury_row;
pub mod withdraw_unstaked_deposits_row;

pub use self::{
    add_rewards_row::*, deposit_to_farm_vault_row::*, harvest_reward_row::*,
    idl_missing_types_row::*, initialize_farm_delegated_row::*, initialize_farm_row::*,
    initialize_global_config_row::*, initialize_reward_row::*, initialize_user_row::*,
    refresh_farm_row::*, refresh_user_state_row::*, reward_user_once_row::*,
    set_stake_delegated_row::*, stake_row::*, transfer_ownership_row::*, unstake_row::*,
    update_farm_admin_row::*, update_farm_config_row::*, update_global_config_admin_row::*,
    update_global_config_row::*, update_second_delegated_authority_row::*,
    withdraw_from_farm_vault_row::*, withdraw_reward_row::*, withdraw_slashed_amount_row::*,
    withdraw_treasury_row::*, withdraw_unstaked_deposits_row::*,
};
use super::KaminoFarmsInstruction;

pub struct KaminoFarmsInstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for KaminoFarmsInstructionsMigration {
    fn app(&self) -> &str {
        "kamino-farms"
    }

    fn name(&self) -> &str {
        "kamino_farms_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AddRewardsMigrationOperation),
            Box::new(DepositToFarmVaultMigrationOperation),
            Box::new(HarvestRewardMigrationOperation),
            Box::new(IdlMissingTypesMigrationOperation),
            Box::new(InitializeFarmMigrationOperation),
            Box::new(InitializeFarmDelegatedMigrationOperation),
            Box::new(InitializeGlobalConfigMigrationOperation),
            Box::new(InitializeRewardMigrationOperation),
            Box::new(InitializeUserMigrationOperation),
            Box::new(RefreshFarmMigrationOperation),
            Box::new(RefreshUserStateMigrationOperation),
            Box::new(RewardUserOnceMigrationOperation),
            Box::new(SetStakeDelegatedMigrationOperation),
            Box::new(StakeMigrationOperation),
            Box::new(TransferOwnershipMigrationOperation),
            Box::new(UnstakeMigrationOperation),
            Box::new(UpdateFarmAdminMigrationOperation),
            Box::new(UpdateFarmConfigMigrationOperation),
            Box::new(UpdateGlobalConfigMigrationOperation),
            Box::new(UpdateGlobalConfigAdminMigrationOperation),
            Box::new(UpdateSecondDelegatedAuthorityMigrationOperation),
            Box::new(WithdrawFromFarmVaultMigrationOperation),
            Box::new(WithdrawRewardMigrationOperation),
            Box::new(WithdrawSlashedAmountMigrationOperation),
            Box::new(WithdrawTreasuryMigrationOperation),
            Box::new(WithdrawUnstakedDepositsMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct KaminoFarmsInstructionWithMetadata(
    pub KaminoFarmsInstruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        KaminoFarmsInstruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for KaminoFarmsInstructionWithMetadata
{
    fn from(
        value: (
            KaminoFarmsInstruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        KaminoFarmsInstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for KaminoFarmsInstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoFarmsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            KaminoFarmsInstruction::InitializeGlobalConfig { data, .. } => {
                let row = initialize_global_config_row::InitializeGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeFarm { data, .. } => {
                let row = initialize_farm_row::InitializeFarmRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeFarmDelegated { data, .. } => {
                let row = initialize_farm_delegated_row::InitializeFarmDelegatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeReward { data, .. } => {
                let row = initialize_reward_row::InitializeRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::AddRewards { data, .. } => {
                let row = add_rewards_row::AddRewardsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateFarmConfig { data, .. } => {
                let row = update_farm_config_row::UpdateFarmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeUser { data, .. } => {
                let row = initialize_user_row::InitializeUserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RewardUserOnce { data, .. } => {
                let row = reward_user_once_row::RewardUserOnceRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RefreshFarm { data, .. } => {
                let row = refresh_farm_row::RefreshFarmRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::Stake { data, .. } => {
                let row = stake_row::StakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::SetStakeDelegated { data, .. } => {
                let row = set_stake_delegated_row::SetStakeDelegatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::HarvestReward { data, .. } => {
                let row = harvest_reward_row::HarvestRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::Unstake { data, .. } => {
                let row = unstake_row::UnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RefreshUserState { data, .. } => {
                let row = refresh_user_state_row::RefreshUserStateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawUnstakedDeposits { data, .. } => {
                let row = withdraw_unstaked_deposits_row::WithdrawUnstakedDepositsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawTreasury { data, .. } => {
                let row = withdraw_treasury_row::WithdrawTreasuryRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::DepositToFarmVault { data, .. } => {
                let row = deposit_to_farm_vault_row::DepositToFarmVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawFromFarmVault { data, .. } => {
                let row = withdraw_from_farm_vault_row::WithdrawFromFarmVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawSlashedAmount { data, .. } => {
                let row = withdraw_slashed_amount_row::WithdrawSlashedAmountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateFarmAdmin { data, .. } => {
                let row = update_farm_admin_row::UpdateFarmAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawReward { data, .. } => {
                let row = withdraw_reward_row::WithdrawRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateSecondDelegatedAuthority { data, .. } => {
                let row = update_second_delegated_authority_row::UpdateSecondDelegatedAuthorityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.insert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::IdlMissingTypes { data, .. } => {
                let row = idl_missing_types_row::IdlMissingTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for KaminoFarmsInstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let KaminoFarmsInstructionWithMetadata(decoded_instruction, metadata, raw_accounts) = self;
        match decoded_instruction {
            KaminoFarmsInstruction::InitializeGlobalConfig { data, .. } => {
                let row = initialize_global_config_row::InitializeGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateGlobalConfig { data, .. } => {
                let row = update_global_config_row::UpdateGlobalConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeFarm { data, .. } => {
                let row = initialize_farm_row::InitializeFarmRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeFarmDelegated { data, .. } => {
                let row = initialize_farm_delegated_row::InitializeFarmDelegatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeReward { data, .. } => {
                let row = initialize_reward_row::InitializeRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::AddRewards { data, .. } => {
                let row = add_rewards_row::AddRewardsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateFarmConfig { data, .. } => {
                let row = update_farm_config_row::UpdateFarmConfigRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::InitializeUser { data, .. } => {
                let row = initialize_user_row::InitializeUserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RewardUserOnce { data, .. } => {
                let row = reward_user_once_row::RewardUserOnceRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RefreshFarm { data, .. } => {
                let row = refresh_farm_row::RefreshFarmRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::Stake { data, .. } => {
                let row = stake_row::StakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::SetStakeDelegated { data, .. } => {
                let row = set_stake_delegated_row::SetStakeDelegatedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::HarvestReward { data, .. } => {
                let row = harvest_reward_row::HarvestRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::Unstake { data, .. } => {
                let row = unstake_row::UnstakeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::RefreshUserState { data, .. } => {
                let row = refresh_user_state_row::RefreshUserStateRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawUnstakedDeposits { data, .. } => {
                let row = withdraw_unstaked_deposits_row::WithdrawUnstakedDepositsRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawTreasury { data, .. } => {
                let row = withdraw_treasury_row::WithdrawTreasuryRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::DepositToFarmVault { data, .. } => {
                let row = deposit_to_farm_vault_row::DepositToFarmVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawFromFarmVault { data, .. } => {
                let row = withdraw_from_farm_vault_row::WithdrawFromFarmVaultRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawSlashedAmount { data, .. } => {
                let row = withdraw_slashed_amount_row::WithdrawSlashedAmountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateFarmAdmin { data, .. } => {
                let row = update_farm_admin_row::UpdateFarmAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateGlobalConfigAdmin { data, .. } => {
                let row = update_global_config_admin_row::UpdateGlobalConfigAdminRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::WithdrawReward { data, .. } => {
                let row = withdraw_reward_row::WithdrawRewardRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::UpdateSecondDelegatedAuthority { data, .. } => {
                let row = update_second_delegated_authority_row::UpdateSecondDelegatedAuthorityRow::from_parts(data.clone(), metadata.clone(), raw_accounts.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            KaminoFarmsInstruction::IdlMissingTypes { data, .. } => {
                let row = idl_missing_types_row::IdlMissingTypesRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

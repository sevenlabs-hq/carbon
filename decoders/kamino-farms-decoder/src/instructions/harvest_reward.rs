//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct HarvestReward {
    pub reward_index: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct HarvestRewardInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub user_state: solana_pubkey::Pubkey,
    pub farm_state: solana_pubkey::Pubkey,
    pub global_config: solana_pubkey::Pubkey,
    pub reward_mint: solana_pubkey::Pubkey,
    pub user_reward_token_account: solana_pubkey::Pubkey,
    pub rewards_vault: solana_pubkey::Pubkey,
    pub rewards_treasury_vault: solana_pubkey::Pubkey,
    pub farm_vaults_authority: solana_pubkey::Pubkey,
    pub scope_prices: Option<solana_pubkey::Pubkey>,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl HarvestReward {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [68, 200, 228, 233, 184, 32, 226, 188] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for HarvestReward {
    type ArrangedAccounts = HarvestRewardInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let user_state = next_account(&mut iter)?;
        let farm_state = next_account(&mut iter)?;
        let global_config = next_account(&mut iter)?;
        let reward_mint = next_account(&mut iter)?;
        let user_reward_token_account = next_account(&mut iter)?;
        let rewards_vault = next_account(&mut iter)?;
        let rewards_treasury_vault = next_account(&mut iter)?;
        let farm_vaults_authority = next_account(&mut iter)?;
        let scope_prices = next_account(&mut iter);
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(HarvestRewardInstructionAccounts {
            payer,
            user_state,
            farm_state,
            global_config,
            reward_mint,
            user_reward_token_account,
            rewards_vault,
            rewards_treasury_vault,
            farm_vaults_authority,
            scope_prices,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

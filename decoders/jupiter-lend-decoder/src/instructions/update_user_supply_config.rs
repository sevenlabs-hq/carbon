//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::UserSupplyConfig,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateUserSupplyConfig {
    pub user_supply_config: UserSupplyConfig,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateUserSupplyConfigInstructionAccounts {
    pub authority: solana_pubkey::Pubkey,
    pub protocol: solana_pubkey::Pubkey,
    pub auth_list: solana_pubkey::Pubkey,
    pub rate_model: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub token_reserve: solana_pubkey::Pubkey,
    pub user_supply_position: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateUserSupplyConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [217, 239, 225, 218, 33, 49, 234, 183] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateUserSupplyConfig {
    type ArrangedAccounts = UpdateUserSupplyConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let authority = next_account(&mut iter)?;
        let protocol = next_account(&mut iter)?;
        let auth_list = next_account(&mut iter)?;
        let rate_model = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let token_reserve = next_account(&mut iter)?;
        let user_supply_position = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateUserSupplyConfigInstructionAccounts {
            authority,
            protocol,
            auth_list,
            rate_model,
            mint,
            token_reserve,
            user_supply_position,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CollectRevenue {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CollectRevenueInstructionAccounts {
    pub authority: solana_pubkey::Pubkey,
    pub liquidity: solana_pubkey::Pubkey,
    pub auth_list: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub revenue_collector_account: solana_pubkey::Pubkey,
    pub revenue_collector: solana_pubkey::Pubkey,
    pub token_reserve: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CollectRevenue {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [87, 96, 211, 36, 240, 43, 246, 87] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CollectRevenue {
    type ArrangedAccounts = CollectRevenueInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let authority = next_account(&mut iter)?;
        let liquidity = next_account(&mut iter)?;
        let auth_list = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let revenue_collector_account = next_account(&mut iter)?;
        let revenue_collector = next_account(&mut iter)?;
        let token_reserve = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CollectRevenueInstructionAccounts {
            authority,
            liquidity,
            auth_list,
            mint,
            revenue_collector_account,
            revenue_collector,
            token_reserve,
            vault,
            token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

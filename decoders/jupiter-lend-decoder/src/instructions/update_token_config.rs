//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::TokenConfig,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateTokenConfig {
    pub token_config: TokenConfig,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateTokenConfigInstructionAccounts {
    pub authority: solana_pubkey::Pubkey,
    pub auth_list: solana_pubkey::Pubkey,
    pub rate_model: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub token_reserve: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateTokenConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [231, 122, 181, 79, 255, 79, 144, 167] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateTokenConfig {
    type ArrangedAccounts = UpdateTokenConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let authority = next_account(&mut iter)?;
        let auth_list = next_account(&mut iter)?;
        let rate_model = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let token_reserve = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateTokenConfigInstructionAccounts {
            authority,
            auth_list,
            rate_model,
            mint,
            token_reserve,
            remaining: remaining.to_vec(),
        })
    }
}

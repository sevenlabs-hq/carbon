//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
    solana_pubkey::Pubkey,
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Claim {
    pub recipient: Pubkey,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct ClaimInstructionAccounts {
    pub user: solana_pubkey::Pubkey,
    pub liquidity: solana_pubkey::Pubkey,
    pub token_reserve: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub recipient_token_account: solana_pubkey::Pubkey,
    pub vault: solana_pubkey::Pubkey,
    pub claim_account: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Claim {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [62, 198, 214, 193, 213, 159, 108, 210] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Claim {
    type ArrangedAccounts = ClaimInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let user = next_account(&mut iter)?;
        let liquidity = next_account(&mut iter)?;
        let token_reserve = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let recipient_token_account = next_account(&mut iter)?;
        let vault = next_account(&mut iter)?;
        let claim_account = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(ClaimInstructionAccounts {
            user,
            liquidity,
            token_reserve,
            mint,
            recipient_token_account,
            vault,
            claim_account,
            token_program,
            associated_token_program,
            remaining: remaining.to_vec(),
        })
    }
}

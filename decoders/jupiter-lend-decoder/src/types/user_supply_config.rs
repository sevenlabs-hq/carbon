//! This code was AUTOGENERATED using the Codama library.
/// @notice struct to set user supply & withdrawal config
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UserSupplyConfig {
    /// @param mode: 0 = without interest. 1 = with interest
    pub mode: u8,
    /// @param expandPercent withdrawal limit expand percent. in 1e2: 100% =
    /// 10_000; 1% = 100 Also used to calculate rate at which withdrawal
    /// limit should decrease (instant).
    pub expand_percent: u128,
    /// @param expandDuration withdrawal limit expand duration in seconds.
    /// used to calculate rate together with expandPercent
    pub expand_duration: u128,
    /// @param baseWithdrawalLimit base limit, below this, user can withdraw the
    /// entire amount. amount in raw (to be multiplied with exchange price)
    /// or normal depends on configured mode in user config for the token:
    /// with interest -> raw, without interest -> normal
    pub base_withdrawal_limit: u128,
}

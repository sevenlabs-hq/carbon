//! This code was AUTOGENERATED using the Codama library.
use solana_pubkey::Pubkey;
/// @notice struct to set token config
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TokenConfig {
    /// @param token address
    pub token: Pubkey,
    /// @param fee charges on borrower's interest. in 1e2: 100% = 10_000; 1% =
    /// 100
    pub fee: u128,
    /// @param maxUtilization maximum allowed utilization. in 1e2: 100% =
    /// 10_000; 1% = 100 set to 100% to disable and have default limit of
    /// 100% (avoiding SLOAD).
    pub max_utilization: u128,
}

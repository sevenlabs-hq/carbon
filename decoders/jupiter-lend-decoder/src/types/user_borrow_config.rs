//! This code was AUTOGENERATED using the Codama library.
/// @notice struct to set user borrow & payback config
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UserBorrowConfig {
    /// @param mode: 0 = without interest. 1 = with interest
    pub mode: u8,
    /// @param expandPercent debt limit expand percent. in 1e2: 100% = 10_000;
    /// 1% = 100 Also used to calculate rate at which debt limit should
    /// decrease (instant).
    pub expand_percent: u128,
    /// @param expandDuration debt limit expand duration in seconds.
    /// used to calculate rate together with expandPercent
    pub expand_duration: u128,
    /// @param baseDebtCeiling base borrow limit. until here, borrow limit
    /// remains as baseDebtCeiling (user can borrow until this point at once
    /// without stepped expansion). Above this, automated limit comes in place.
    /// amount in raw (to be multiplied with exchange price) or normal depends
    /// on configured mode in user config for the token: with interest ->
    /// raw, without interest -> normal
    pub base_debt_ceiling: u128,
    /// @param maxDebtCeiling max borrow ceiling, maximum amount the user can
    /// borrow. amount in raw (to be multiplied with exchange price) or
    /// normal depends on configured mode in user config for the token: with
    /// interest -> raw, without interest -> normal
    pub max_debt_ceiling: u128,
}

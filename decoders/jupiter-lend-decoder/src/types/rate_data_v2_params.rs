//! This code was AUTOGENERATED using the Codama library.
/// @notice struct to set borrow rate data for version 2
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RateDataV2Params {
    /// @param kink1 first kink in borrow rate. in 1e2: 100% = 10_000; 1% = 100
    /// utilization below kink 1 usually means slow increase in rate, once
    /// utilization is above kink 1 borrow rate increases faster
    pub kink1: u128,
    /// @param kink2 second kink in borrow rate. in 1e2: 100% = 10_000; 1% = 100
    /// utilization below kink 2 usually means slow / medium increase in rate,
    /// once utilization is above kink 2 borrow rate increases fast
    pub kink2: u128,
    /// @param rateAtUtilizationZero desired borrow rate when utilization is
    /// zero. in 1e2: 100% = 10_000; 1% = 100 i.e. constant minimum borrow
    /// rate e.g. at utilization = 0.01% rate could still be at least 4%
    /// (rateAtUtilizationZero would be 400 then)
    pub rate_at_utilization_zero: u128,
    /// @param rateAtUtilizationKink1 desired borrow rate when utilization is at
    /// first kink. in 1e2: 100% = 10_000; 1% = 100 e.g. when rate should be
    /// 7% at first kink then rateAtUtilizationKink would be 700
    pub rate_at_utilization_kink1: u128,
    /// @param rateAtUtilizationKink2 desired borrow rate when utilization is at
    /// second kink. in 1e2: 100% = 10_000; 1% = 100 e.g. when rate should
    /// be 7% at second kink then rateAtUtilizationKink would be 1_200
    pub rate_at_utilization_kink2: u128,
    /// @param rateAtUtilizationMax desired borrow rate when utilization is
    /// maximum at 100%. in 1e2: 100% = 10_000; 1% = 100 e.g. when rate
    /// should be 125% at 100% then rateAtUtilizationMax would be 12_500
    pub rate_at_utilization_max: u128,
}

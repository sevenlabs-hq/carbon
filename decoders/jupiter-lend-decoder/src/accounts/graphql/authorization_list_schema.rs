//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::UserClassGraphQL, carbon_core::graphql::primitives::Pubkey,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "AuthorizationList")]
pub struct AuthorizationListGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub auth_users: Vec<Pubkey>,
    pub guardians: Vec<Pubkey>,
    pub user_classes: Vec<UserClassGraphQL>,
}

impl TryFrom<crate::accounts::postgres::AuthorizationListRow> for AuthorizationListGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::AuthorizationListRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            auth_users: row
                .auth_users
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::Pubkey(item.0))
                .collect(),
            guardians: row
                .guardians
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::Pubkey(item.0))
                .collect(),
            user_classes: row
                .user_classes
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
        })
    }
}

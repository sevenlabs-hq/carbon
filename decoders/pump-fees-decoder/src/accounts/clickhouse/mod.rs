//! This code was AUTOGENERATED using the Codama library.
pub mod bonding_curve_row;
pub mod buyback_vault_row;
pub mod donation_fee_pda_row;
pub mod fee_config_row;
pub mod fee_program_global_row;
pub mod global_row;
pub mod pool_row;
pub mod sharing_config_row;
pub mod social_fee_pda_row;

pub use self::{
    bonding_curve_row::*, buyback_vault_row::*, donation_fee_pda_row::*, fee_config_row::*,
    fee_program_global_row::*, global_row::*, pool_row::*, sharing_config_row::*,
    social_fee_pda_row::*,
};
use super::PumpFeesAccount;

pub struct PumpFeesAccountsMigration;

impl carbon_core::clickhouse::Migration for PumpFeesAccountsMigration {
    fn app(&self) -> &str {
        "pump-fees"
    }

    fn name(&self) -> &str {
        "pump_fees_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BondingCurveRowMigrationOperation),
            Box::new(BuybackVaultRowMigrationOperation),
            Box::new(DonationFeePdaRowMigrationOperation),
            Box::new(FeeConfigRowMigrationOperation),
            Box::new(FeeProgramGlobalRowMigrationOperation),
            Box::new(GlobalRowMigrationOperation),
            Box::new(PoolRowMigrationOperation),
            Box::new(SharingConfigRowMigrationOperation),
            Box::new(SocialFeePdaRowMigrationOperation),
        ]
    }
}

pub enum PumpFeesAccountRow {
    BondingCurve(BondingCurveRow),
    BuybackVault(BuybackVaultRow),
    DonationFeePda(DonationFeePdaRow),
    FeeConfig(FeeConfigRow),
    FeeProgramGlobal(FeeProgramGlobalRow),
    Global(GlobalRow),
    Pool(PoolRow),
    SharingConfig(SharingConfigRow),
    SocialFeePda(SocialFeePdaRow),
}

pub struct PumpFeesAccountMetadata<'a>(
    pub &'a carbon_core::account::AccountMetadata,
    pub &'a PumpFeesAccount,
);

impl<'a> carbon_core::clickhouse::BatchInsert for PumpFeesAccountMetadata<'a> {
    type Row = PumpFeesAccountRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, account) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty, boxed) => {
                if let PumpFeesAccount::$variant(account) = account {
                    rows.push(PumpFeesAccountRow::$variant(<$row>::try_from((
                        account.as_ref().clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
            ($variant:ident, $row:ty, plain) => {
                if let PumpFeesAccount::$variant(account) = account {
                    rows.push(PumpFeesAccountRow::$variant(<$row>::try_from((
                        account.clone(),
                        metadata.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(BondingCurve, BondingCurveRow, boxed);
        insert_branch!(BuybackVault, BuybackVaultRow, boxed);
        insert_branch!(DonationFeePda, DonationFeePdaRow, boxed);
        insert_branch!(FeeConfig, FeeConfigRow, boxed);
        insert_branch!(FeeProgramGlobal, FeeProgramGlobalRow, boxed);
        insert_branch!(Global, GlobalRow, boxed);
        insert_branch!(Pool, PoolRow, boxed);
        insert_branch!(SharingConfig, SharingConfigRow, boxed);
        insert_branch!(SocialFeePda, SocialFeePdaRow, boxed);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for PumpFeesAccountRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(BondingCurve, BondingCurveRow);
        commit_branch!(BuybackVault, BuybackVaultRow);
        commit_branch!(DonationFeePda, DonationFeePdaRow);
        commit_branch!(FeeConfig, FeeConfigRow);
        commit_branch!(FeeProgramGlobal, FeeProgramGlobalRow);
        commit_branch!(Global, GlobalRow);
        commit_branch!(Pool, PoolRow);
        commit_branch!(SharingConfig, SharingConfigRow);
        commit_branch!(SocialFeePda, SocialFeePdaRow);

        Ok(())
    }
}

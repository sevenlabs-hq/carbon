//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account::AccountMetadata;
use carbon_core::postgres::metadata::AccountRowMetadata;
use carbon_core::postgres::primitives::Pubkey;
use carbon_core::postgres::primitives::U64;
use carbon_core::postgres::primitives::U8;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct BuybackVaultRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub authority: Pubkey,
    pub total_claimed: U64,
    pub total_claimed_token1: U64,
    pub total_claimed_token2: U64,
    pub last_claimed: i64,
    pub claim_rate_limit: i64,
    pub reserved: Vec<U8>,
}

impl BuybackVaultRow {
    pub fn from_parts(source: crate::accounts::buyback_vault::BuybackVault, metadata: AccountMetadata) -> Self {
        Self {
            account_metadata: metadata.into(),
            authority: source.authority.into(),
            total_claimed: source.total_claimed.into(),
            total_claimed_token1: source.total_claimed_token1.into(),
            total_claimed_token2: source.total_claimed_token2.into(),
            last_claimed: source.last_claimed.into(),
            claim_rate_limit: source.claim_rate_limit.into(),
            reserved: source.reserved.into_iter().map(|element| element.into()).collect(),
        }
    }
}

impl TryFrom<BuybackVaultRow> for crate::accounts::buyback_vault::BuybackVault {
    type Error = carbon_core::error::Error;
    fn try_from(source: BuybackVaultRow) -> Result<Self, Self::Error> {
        Ok(Self {
            authority: *source.authority,
            total_claimed: *source.total_claimed,
            total_claimed_token1: *source.total_claimed_token1,
            total_claimed_token2: *source.total_claimed_token2,
            last_claimed: source.last_claimed.into(),
            claim_rate_limit: source.claim_rate_limit.into(),
            reserved: source.reserved.into_iter().map(|element| Ok(element.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?)).collect::<Result<Vec<_>, _>>()?.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert array element to primitive".to_string()))?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::buyback_vault::BuybackVault {
    fn table() -> &'static str {
        "buyback_vault_account"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__pubkey",
            "__slot",
            "authority",
            "total_claimed",
            "total_claimed_token1",
            "total_claimed_token2",
            "last_claimed",
            "claim_rate_limit",
            "reserved",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for BuybackVaultRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO buyback_vault_account (
                "authority",
                "total_claimed",
                "total_claimed_token1",
                "total_claimed_token2",
                "last_claimed",
                "claim_rate_limit",
                "reserved",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            )"#)
        .bind(self.authority.clone())
        .bind(self.total_claimed.clone())
        .bind(self.total_claimed_token1.clone())
        .bind(self.total_claimed_token2.clone())
        .bind(self.last_claimed.clone())
        .bind(self.claim_rate_limit.clone())
        .bind(self.reserved.clone())
        .bind(self.account_metadata.pubkey.clone())
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for BuybackVaultRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO buyback_vault_account (
                "authority",
                "total_claimed",
                "total_claimed_token1",
                "total_claimed_token2",
                "last_claimed",
                "claim_rate_limit",
                "reserved",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6, $7, $8, $9
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "authority" = EXCLUDED."authority",
                "total_claimed" = EXCLUDED."total_claimed",
                "total_claimed_token1" = EXCLUDED."total_claimed_token1",
                "total_claimed_token2" = EXCLUDED."total_claimed_token2",
                "last_claimed" = EXCLUDED."last_claimed",
                "claim_rate_limit" = EXCLUDED."claim_rate_limit",
                "reserved" = EXCLUDED."reserved",
                __slot = EXCLUDED.__slot
            "#)
        .bind(self.authority.clone())
        .bind(self.total_claimed.clone())
        .bind(self.total_claimed_token1.clone())
        .bind(self.total_claimed_token2.clone())
        .bind(self.last_claimed.clone())
        .bind(self.claim_rate_limit.clone())
        .bind(self.reserved.clone())
        .bind(self.account_metadata.pubkey)
        .bind(self.account_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for BuybackVaultRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM buyback_vault_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for BuybackVaultRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM buyback_vault_account WHERE
                __pubkey = $1
            "#)
        .bind(key)
        .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct BuybackVaultMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for BuybackVaultMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS buyback_vault_account (
                -- Account data
                "authority" BYTEA NOT NULL,
                "total_claimed" NUMERIC(20) NOT NULL,
                "total_claimed_token1" NUMERIC(20) NOT NULL,
                "total_claimed_token2" NUMERIC(20) NOT NULL,
                "last_claimed" INT8 NOT NULL,
                "claim_rate_limit" INT8 NOT NULL,
                "reserved" INT2[] NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS buyback_vault_account"#).execute(connection).await?;
        Ok(())
    }
}

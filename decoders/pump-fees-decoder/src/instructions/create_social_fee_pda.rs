//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct CreateSocialFeePda {
    pub user_id: String,
    pub platform: u8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateSocialFeePdaInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub social_fee_pda: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub fee_program_global: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreateSocialFeePda {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[144, 224, 59, 211, 78, 248, 202, 220] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for CreateSocialFeePda {
    type ArrangedAccounts = CreateSocialFeePdaInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let social_fee_pda = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let fee_program_global = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreateSocialFeePdaInstructionAccounts {
            payer,
            social_fee_pda,
            system_program,
            fee_program_global,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

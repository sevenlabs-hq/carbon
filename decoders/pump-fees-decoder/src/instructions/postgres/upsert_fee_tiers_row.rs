//! This code was AUTOGENERATED using the Codama library.
use carbon_core::instruction::InstructionMetadata;
use carbon_core::postgres::metadata::InstructionRowMetadata;
use carbon_core::postgres::primitives::U8;
use crate::types::FeeTier;

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct UpsertFeeTiersRow {
    #[sqlx(flatten)]
    pub instruction_metadata: InstructionRowMetadata,
    pub fee_tiers: sqlx::types::Json<Vec<FeeTier>>,
    pub offset: U8,
}

impl UpsertFeeTiersRow {
    pub fn from_parts(source: crate::instructions::upsert_fee_tiers::UpsertFeeTiers, metadata: InstructionMetadata) -> Self {
        Self {
            instruction_metadata: metadata.into(),
            fee_tiers: sqlx::types::Json(source.fee_tiers.into_iter().map(|element| element.into()).collect()),
            offset: source.offset.into(),
        }
    }
}

impl TryFrom<UpsertFeeTiersRow> for crate::instructions::upsert_fee_tiers::UpsertFeeTiers {
    type Error = carbon_core::error::Error;
    fn try_from(source: UpsertFeeTiersRow) -> Result<Self, Self::Error> {
        Ok(Self {
            fee_tiers: source.fee_tiers.0,
            offset: source.offset.try_into().map_err(|_| carbon_core::error::Error::Custom("Failed to convert value from postgres primitive".to_string()))?,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::instructions::upsert_fee_tiers::UpsertFeeTiers {
    fn table() -> &'static str {
        "upsert_fee_tiers_instruction"
    }

    fn columns() -> Vec<&'static str> {
        vec![
            "__signature",
            "__instruction_index",
            "__stack_height",
            "__slot",
            "fee_tiers",
            "offset",
        ]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for UpsertFeeTiersRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"
            INSERT INTO upsert_fee_tiers_instruction (
                "fee_tiers",
                "offset",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            )"#)
        .bind(self.fee_tiers.clone())
        .bind(self.offset.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for UpsertFeeTiersRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"INSERT INTO upsert_fee_tiers_instruction (
                "fee_tiers",
                "offset",
                __signature, __instruction_index, __stack_height, __slot
            ) VALUES (
                $1, $2, $3, $4, $5, $6
            ) ON CONFLICT (
                __signature, __instruction_index, __stack_height
            ) DO UPDATE SET
                "fee_tiers" = EXCLUDED."fee_tiers",
                "offset" = EXCLUDED."offset",
                __instruction_index = EXCLUDED.__instruction_index,
                __stack_height = EXCLUDED.__stack_height,
                __slot = EXCLUDED.__slot
            "#)
        .bind(self.fee_tiers.clone())
        .bind(self.offset.clone())
        .bind(self.instruction_metadata.signature.clone())
        .bind(self.instruction_metadata.instruction_index.clone())
        .bind(self.instruction_metadata.stack_height.clone())
        .bind(self.instruction_metadata.slot.clone())
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for UpsertFeeTiersRow {
    type Key = (String, carbon_core::postgres::primitives::U32, carbon_core::postgres::primitives::U32);

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(r#"DELETE FROM upsert_fee_tiers_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#)
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .execute(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for UpsertFeeTiersRow {
    type Key = (String, carbon_core::postgres::primitives::U32, carbon_core::postgres::primitives::U32);

    async fn lookup(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(r#"SELECT * FROM upsert_fee_tiers_instruction WHERE
                __signature = $1 AND __instruction_index = $2 AND __stack_height = $3
            "#)
        .bind(key.0)
        .bind(key.1)
        .bind(key.2)
        .fetch_optional(pool).await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct UpsertFeeTiersMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for UpsertFeeTiersMigrationOperation {
    async fn up(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"CREATE TABLE IF NOT EXISTS upsert_fee_tiers_instruction (
                -- Instruction data
                "fee_tiers" JSONB NOT NULL,
                "offset" INT2 NOT NULL,
                -- Instruction metadata
                __signature TEXT NOT NULL,
                __instruction_index BIGINT NOT NULL,
                __stack_height BIGINT NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__signature, __instruction_index, __stack_height)
            )"#).execute(connection).await?;
        Ok(())
    }

    async fn down(&self, connection: &mut sqlx::PgConnection) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS upsert_fee_tiers_instruction"#).execute(connection).await?;
        Ok(())
    }
}

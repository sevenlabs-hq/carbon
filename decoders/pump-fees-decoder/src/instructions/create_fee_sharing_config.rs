//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Create Fee Sharing Config
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CreateFeeSharingConfig {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreateFeeSharingConfigInstructionAccounts {
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub global: solana_pubkey::Pubkey,
    pub mint: solana_pubkey::Pubkey,
    pub sharing_config: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub bonding_curve: solana_pubkey::Pubkey,
    pub pump_program: solana_pubkey::Pubkey,
    pub pump_event_authority: solana_pubkey::Pubkey,
    pub pool: Option<solana_pubkey::Pubkey>,
    pub pump_amm_program: Option<solana_pubkey::Pubkey>,
    pub pump_amm_event_authority: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreateFeeSharingConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [195, 78, 86, 76, 111, 52, 251, 213] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CreateFeeSharingConfig {
    type ArrangedAccounts = CreateFeeSharingConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let global = next_account(&mut iter)?;
        let mint = next_account(&mut iter)?;
        let sharing_config = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let bonding_curve = next_account(&mut iter)?;
        let pump_program = next_account(&mut iter)?;
        let pump_event_authority = next_account(&mut iter)?;
        let pool = next_account(&mut iter);
        let pump_amm_program = next_account(&mut iter);
        let pump_amm_event_authority = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(CreateFeeSharingConfigInstructionAccounts {
            event_authority,
            program,
            payer,
            global,
            mint,
            sharing_config,
            system_program,
            bonding_curve,
            pump_program,
            pump_event_authority,
            pool,
            pump_amm_program,
            pump_amm_event_authority,
            remaining: remaining.to_vec(),
        })
    }
}

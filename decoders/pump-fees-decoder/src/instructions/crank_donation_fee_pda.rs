//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account_utils::next_account,
    deserialize::{ArrangeAccounts, CarbonDeserialize},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CrankDonationFeePda {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CrankDonationFeePdaInstructionAccounts {
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub fee_program_global: solana_pubkey::Pubkey,
    pub donation_fee_pda: solana_pubkey::Pubkey,
    pub quote_mint: solana_pubkey::Pubkey,
    pub donation_fee_pda_ata: solana_pubkey::Pubkey,
    pub donation_relay_program: solana_pubkey::Pubkey,
    pub donation_relay_event_authority: solana_pubkey::Pubkey,
    pub mint_whitelist: solana_pubkey::Pubkey,
    pub epoch_tracker: solana_pubkey::Pubkey,
    pub debouncer: solana_pubkey::Pubkey,
    pub debouncer_ata: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for CrankDonationFeePda {
    const DISCRIMINATOR: &'static [u8] = &[220, 10, 189, 167, 169, 17, 25, 69];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CrankDonationFeePda {
    type ArrangedAccounts = CrankDonationFeePdaInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;
        let fee_program_global = next_account(&mut iter)?;
        let donation_fee_pda = next_account(&mut iter)?;
        let quote_mint = next_account(&mut iter)?;
        let donation_fee_pda_ata = next_account(&mut iter)?;
        let donation_relay_program = next_account(&mut iter)?;
        let donation_relay_event_authority = next_account(&mut iter)?;
        let mint_whitelist = next_account(&mut iter)?;
        let epoch_tracker = next_account(&mut iter)?;
        let debouncer = next_account(&mut iter)?;
        let debouncer_ata = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CrankDonationFeePdaInstructionAccounts {
            event_authority,
            program,
            payer,
            system_program,
            token_program,
            associated_token_program,
            rent,
            fee_program_global,
            donation_fee_pda,
            quote_mint,
            donation_fee_pda_ata,
            donation_relay_program,
            donation_relay_event_authority,
            mint_whitelist,
            epoch_tracker,
            debouncer,
            debouncer_ata,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::FeeTier,
    carbon_core::{
        account_utils::next_account,
        deserialize::{ArrangeAccounts, CarbonDeserialize},
    },
};
/// Update or expand fee tiers (only callable by admin)
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpsertStableFeeTiers {
    pub fee_tiers: Vec<FeeTier>,
    pub offset: u8,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpsertStableFeeTiersInstructionAccounts {
    pub fee_config: solana_pubkey::Pubkey,
    pub admin: solana_pubkey::Pubkey,
    pub config_program_id: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CarbonDeserialize for UpsertStableFeeTiers {
    const DISCRIMINATOR: &'static [u8] = &[181, 160, 162, 252, 74, 76, 224, 221];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpsertStableFeeTiers {
    type ArrangedAccounts = UpsertStableFeeTiersInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let fee_config = next_account(&mut iter)?;
        let admin = next_account(&mut iter)?;
        let config_program_id = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpsertStableFeeTiersInstructionAccounts {
            fee_config,
            admin,
            config_program_id,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

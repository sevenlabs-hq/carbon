//! This code was AUTOGENERATED using the Codama library.
use carbon_core::CarbonDeserialize;
use carbon_core::borsh;
use carbon_core::deserialize::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct SocialFeePdaClaimedEvent {
    pub timestamp: i64,
    pub user_id: String,
    pub platform: u8,
    pub social_fee_pda: Pubkey,
    pub recipient: Pubkey,
    pub social_claim_authority: Pubkey,
    pub amount_claimed: u64,
    pub claimable_before: u64,
    pub lifetime_claimed: u64,
    pub recipient_balance_before: u64,
    pub recipient_balance_after: u64,
}

impl SocialFeePdaClaimedEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[50, 18, 193, 65, 237, 210, 234, 236] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

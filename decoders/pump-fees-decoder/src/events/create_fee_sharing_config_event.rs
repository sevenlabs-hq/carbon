//! This code was AUTOGENERATED using the Codama library.
use crate::types::ConfigStatus;
use crate::types::Shareholder;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CreateFeeSharingConfigEventEvent {
    pub timestamp: i64,
    pub mint: Pubkey,
    pub bonding_curve: Pubkey,
    pub pool: Option<Pubkey>,
    pub sharing_config: Pubkey,
    pub admin: Pubkey,
    pub initial_shareholders: Vec<Shareholder>,
    pub status: ConfigStatus,
}

impl CreateFeeSharingConfigEventEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [133, 105, 170, 200, 184, 116, 251, 88] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

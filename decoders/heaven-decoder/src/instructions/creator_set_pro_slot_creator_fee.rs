//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::SlotFeeBrackets,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CreatorSetProSlotCreatorFee {
    pub fees: SlotFeeBrackets,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CreatorSetProSlotCreatorFeeInstructionAccounts {
    pub liquidity_pool_state: solana_pubkey::Pubkey,
    pub creator: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CreatorSetProSlotCreatorFee {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [21, 70, 77, 228, 4, 225, 3, 83] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for CreatorSetProSlotCreatorFee {
    type ArrangedAccounts = CreatorSetProSlotCreatorFeeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let liquidity_pool_state = next_account(&mut iter)?;
        let creator = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(CreatorSetProSlotCreatorFeeInstructionAccounts {
            liquidity_pool_state,
            creator,
            remaining: remaining.to_vec(),
        })
    }
}

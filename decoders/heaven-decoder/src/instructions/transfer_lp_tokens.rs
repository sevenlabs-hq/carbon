//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct TransferLpTokens {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct TransferLpTokensInstructionAccounts {
    pub system_program: solana_pubkey::Pubkey,
    pub liquidity_pool_state: solana_pubkey::Pubkey,
    pub sender: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub receiver: solana_pubkey::Pubkey,
    pub sender_lp_position: solana_pubkey::Pubkey,
    pub receiver_lp_position: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl TransferLpTokens {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [238, 121, 14, 82, 103, 109, 70, 180] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for TransferLpTokens {
    type ArrangedAccounts = TransferLpTokensInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let system_program = next_account(&mut iter)?;
        let liquidity_pool_state = next_account(&mut iter)?;
        let sender = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let receiver = next_account(&mut iter)?;
        let sender_lp_position = next_account(&mut iter)?;
        let receiver_lp_position = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(TransferLpTokensInstructionAccounts {
            system_program,
            liquidity_pool_state,
            sender,
            payer,
            receiver,
            sender_lp_position,
            receiver_lp_position,
            remaining: remaining.to_vec(),
        })
    }
}

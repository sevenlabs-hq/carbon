//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{metadata::AccountRowMetadata, primitives::U64},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct MsolTicketSolSpentRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub cost_basis: U64,
    pub msol_unstaked: U64,
}

impl MsolTicketSolSpentRow {
    pub fn from_parts(
        source: crate::accounts::msol_ticket_sol_spent::MsolTicketSolSpent,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            cost_basis: source.cost_basis.into(),
            msol_unstaked: source.msol_unstaked.into(),
        }
    }
}

impl TryFrom<MsolTicketSolSpentRow> for crate::accounts::msol_ticket_sol_spent::MsolTicketSolSpent {
    type Error = carbon_core::error::Error;
    fn try_from(source: MsolTicketSolSpentRow) -> Result<Self, Self::Error> {
        Ok(Self {
            cost_basis: *source.cost_basis,
            msol_unstaked: *source.msol_unstaked,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::msol_ticket_sol_spent::MsolTicketSolSpent
{
    fn table() -> &'static str {
        "msol_ticket_sol_spent_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "cost_basis", "msol_unstaked"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for MsolTicketSolSpentRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO msol_ticket_sol_spent_account (
                "cost_basis",
                "msol_unstaked",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            )"#,
        )
        .bind(&self.cost_basis)
        .bind(&self.msol_unstaked)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for MsolTicketSolSpentRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO msol_ticket_sol_spent_account (
                "cost_basis",
                "msol_unstaked",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "cost_basis" = EXCLUDED."cost_basis",
                "msol_unstaked" = EXCLUDED."msol_unstaked",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(&self.cost_basis)
        .bind(&self.msol_unstaked)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for MsolTicketSolSpentRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM msol_ticket_sol_spent_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for MsolTicketSolSpentRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM msol_ticket_sol_spent_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct MsolTicketSolSpentMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for MsolTicketSolSpentMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS msol_ticket_sol_spent_account (
                -- Account data
                "cost_basis" NUMERIC(20) NOT NULL,
                "msol_unstaked" NUMERIC(20) NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS msol_ticket_sol_spent_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

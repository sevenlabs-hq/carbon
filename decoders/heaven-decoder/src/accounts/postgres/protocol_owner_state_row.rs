//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{
    account::AccountMetadata,
    postgres::{metadata::AccountRowMetadata, primitives::Pubkey},
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct ProtocolOwnerStateRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub current_protocol_owner: Pubkey,
}

impl ProtocolOwnerStateRow {
    pub fn from_parts(
        source: crate::accounts::protocol_owner_state::ProtocolOwnerState,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            current_protocol_owner: source.current_protocol_owner.into(),
        }
    }
}

impl TryFrom<ProtocolOwnerStateRow> for crate::accounts::protocol_owner_state::ProtocolOwnerState {
    type Error = carbon_core::error::Error;
    fn try_from(source: ProtocolOwnerStateRow) -> Result<Self, Self::Error> {
        Ok(Self {
            current_protocol_owner: *source.current_protocol_owner,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::protocol_owner_state::ProtocolOwnerState
{
    fn table() -> &'static str {
        "protocol_owner_state_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "current_protocol_owner"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for ProtocolOwnerStateRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO protocol_owner_state_account (
                "current_protocol_owner",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            )"#,
        )
        .bind(self.current_protocol_owner)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for ProtocolOwnerStateRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO protocol_owner_state_account (
                "current_protocol_owner",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "current_protocol_owner" = EXCLUDED."current_protocol_owner",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.current_protocol_owner)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for ProtocolOwnerStateRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM protocol_owner_state_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for ProtocolOwnerStateRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM protocol_owner_state_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct ProtocolOwnerStateMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for ProtocolOwnerStateMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS protocol_owner_state_account (
                -- Account data
                "current_protocol_owner" BYTEA NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS protocol_owner_state_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::SlotFeeBracketGraphQL, carbon_core::graphql::primitives::U8,
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "SlotFeeBrackets")]
pub struct SlotFeeBracketsGraphQL {
    pub brackets: Vec<SlotFeeBracketGraphQL>,
    pub max_slot_offset: i32,
    pub max_fee_bps: i32,
    pub count: U8,
    pub enabled: U8,
    pub padding: Vec<U8>,
}

impl From<crate::types::SlotFeeBrackets> for SlotFeeBracketsGraphQL {
    fn from(original: crate::types::SlotFeeBrackets) -> Self {
        Self {
            brackets: original
                .brackets
                .into_iter()
                .map(|item| item.into())
                .collect(),
            max_slot_offset: original.max_slot_offset as i32,
            max_fee_bps: original.max_fee_bps as i32,
            count: carbon_core::graphql::primitives::U8(original.count),
            enabled: carbon_core::graphql::primitives::U8(original.enabled),
            padding: original
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        }
    }
}

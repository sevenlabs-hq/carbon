//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Updates the dynamic fee configuration constants.
/// This function allows updating the parameters that control dynamic fee
/// calculation, such as filter period, decay period, reduction factor, fee
/// control factor, etc. Only the admin or the amm config owner can update these
/// settings. # Arguments
/// * `ctx` - The context of accounts
/// * `filter_period` - Period that determines the high frequency trading time
///   window (in seconds)
/// * `decay_period` - Period that determines when the dynamic fee starts to
///   decrease (in seconds)
/// * `reduction_factor` - Dynamic fee rate decrement rate, used for volatility
///   reference decay
/// * `dynamic_fee_control` - Factor used to scale the dynamic fee component
/// * `max_volatility_accumulator` - Maximum value for the volatility
///   accumulator
/// * `create_pool_authority` - The new authority pubkey that can use this
///   dynamic fee config to create pools
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct UpdateDynamicFeeConfig {
    pub filter_period: u16,
    pub decay_period: u16,
    pub reduction_factor: u16,
    pub dynamic_fee_control: u32,
    pub max_volatility_accumulator: u32,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateDynamicFeeConfigInstructionAccounts {
    pub owner: solana_pubkey::Pubkey,
    pub dynamic_fee_config: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl UpdateDynamicFeeConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [7, 7, 80, 8, 2, 199, 132, 240] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for UpdateDynamicFeeConfig {
    type ArrangedAccounts = UpdateDynamicFeeConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let owner = next_account(&mut iter)?;
        let dynamic_fee_config = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(UpdateDynamicFeeConfigInstructionAccounts {
            owner,
            dynamic_fee_config,
            remaining: remaining.to_vec(),
        })
    }
}

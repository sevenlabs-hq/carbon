//! This code was AUTOGENERATED using the Codama library.
use {carbon_core::deserialize::CarbonDeserialize, solana_pubkey::Pubkey};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct IncreaseLimitOrderEventEvent {
    /// The pool whose limit order was increased
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub pool_id: Pubkey,
    /// The limit order account
    #[cfg_attr(
        feature = "base58",
        serde(serialize_with = "carbon_core::convert::base58::serialize")
    )]
    pub limit_order: Pubkey,
    /// Direction of the limit order (true if zero_for_one)
    pub zero_for_one: bool,
    /// Tick index of the limit order
    pub tick_index: i32,
    /// Total amount of the limit order
    pub total_amount: u64,
    /// Increased amount of the limit order
    pub increased_amount: u64,
    /// Transfer fee of the limit order
    pub transfer_fee: u64,
}

impl CarbonDeserialize for IncreaseLimitOrderEventEvent {
    const DISCRIMINATOR: &'static [u8] = &[11, 120, 13, 204, 199, 87, 19, 200];

    fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != Self::DISCRIMINATOR {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

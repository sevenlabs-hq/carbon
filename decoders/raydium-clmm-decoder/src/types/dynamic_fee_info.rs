//! This code was AUTOGENERATED using the Codama library.
/// Dynamic fee information for pool configuration
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DynamicFeeInfo {
    /// Period that determines the high frequency trading time window (in
    /// seconds).
    pub filter_period: u16,
    /// Period that determines when the dynamic fee starts to decrease (in
    /// seconds).
    pub decay_period: u16,
    /// Dynamic fee rate decrement rate, used for volatility reference decay.
    pub reduction_factor: u16,
    /// Factor used to scale the dynamic fee component in the fee rate
    /// calculation.
    pub dynamic_fee_control: u32,
    /// Maximum value for the volatility accumulator, used to cap the dynamic
    /// fee rate.
    pub max_volatility_accumulator: u32,
    /// Active tick spacing index at the last reference update.
    pub tick_spacing_index_reference: i32,
    /// Volatility reference value, stores the decayed volatility accumulator.
    pub volatility_reference: u32,
    /// Volatility accumulator, used to calculate the dynamic fee rate.
    pub volatility_accumulator: u32,
    /// Last timestamp (block time) when `volatility_reference` and
    /// `tick_spacing_index_reference` were updated.
    pub last_update_timestamp: u64,
    /// Reserved for future upgrades.
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding: [u8; 46],
}

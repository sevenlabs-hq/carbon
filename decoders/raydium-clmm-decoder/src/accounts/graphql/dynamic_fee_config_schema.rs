//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{U32, U64},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "DynamicFeeConfig")]
pub struct DynamicFeeConfigGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub index: i32,
    pub filter_period: i32,
    pub decay_period: i32,
    pub reduction_factor: i32,
    pub dynamic_fee_control: U32,
    pub max_volatility_accumulator: U32,
    pub padding: Vec<U64>,
}

impl TryFrom<crate::accounts::postgres::DynamicFeeConfigRow> for DynamicFeeConfigGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::DynamicFeeConfigRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            index: *row.index,
            filter_period: *row.filter_period,
            decay_period: *row.decay_period,
            reduction_factor: *row.reduction_factor,
            dynamic_fee_control: carbon_core::graphql::primitives::U32(
                (*row.dynamic_fee_control) as u32,
            ),
            max_volatility_accumulator: carbon_core::graphql::primitives::U32(
                (*row.max_volatility_accumulator) as u32,
            ),
            padding: row
                .padding
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(*item))
                .collect(),
        })
    }
}

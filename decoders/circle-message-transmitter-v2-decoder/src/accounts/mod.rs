//! This code was AUTOGENERATED using the Codama library.
use crate::{CircleMessageTransmitterV2Decoder, PROGRAM_ID};

#[cfg(feature = "postgres")]
pub mod postgres;

#[cfg(feature = "graphql")]
pub mod graphql;

pub mod message_sent;
pub mod message_transmitter;
pub mod used_nonce;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum CircleMessageTransmitterV2Account {
    MessageSent(Box<message_sent::MessageSent>),
    MessageTransmitter(Box<message_transmitter::MessageTransmitter>),
    UsedNonce(Box<used_nonce::UsedNonce>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for CircleMessageTransmitterV2Decoder {
    type AccountType = CircleMessageTransmitterV2Account;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = message_sent::MessageSent::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleMessageTransmitterV2Account::MessageSent(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = message_transmitter::MessageTransmitter::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleMessageTransmitterV2Account::MessageTransmitter(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = used_nonce::UsedNonce::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: CircleMessageTransmitterV2Account::UsedNonce(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}

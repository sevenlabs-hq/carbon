//! This code was AUTOGENERATED using the Codama library.
pub mod message_sent_row;
pub mod message_transmitter_row;
pub mod used_nonce_row;

pub use self::{message_sent_row::*, message_transmitter_row::*, used_nonce_row::*};
use super::CircleMessageTransmitterV2Account;

pub struct CircleMessageTransmitterV2AccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for CircleMessageTransmitterV2AccountsMigration {
    fn app(&self) -> &str {
        "circle-message-transmitter-v2"
    }

    fn name(&self) -> &str {
        "circle_message_transmitter_v2_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(MessageSentMigrationOperation),
            Box::new(MessageTransmitterMigrationOperation),
            Box::new(UsedNonceMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct CircleMessageTransmitterV2AccountWithMetadata(
    pub CircleMessageTransmitterV2Account,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        CircleMessageTransmitterV2Account,
        carbon_core::account::AccountMetadata,
    )> for CircleMessageTransmitterV2AccountWithMetadata
{
    fn from(
        value: (
            CircleMessageTransmitterV2Account,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        CircleMessageTransmitterV2AccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CircleMessageTransmitterV2AccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleMessageTransmitterV2AccountWithMetadata(account, metadata) = self;

        match account {
            CircleMessageTransmitterV2Account::MessageSent(account) => {
                let row = message_sent_row::MessageSentRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Account::MessageTransmitter(account) => {
                let row = message_transmitter_row::MessageTransmitterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Account::UsedNonce(account) => {
                let row =
                    used_nonce_row::UsedNonceRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CircleMessageTransmitterV2AccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleMessageTransmitterV2AccountWithMetadata(account, metadata) = self;
        match account {
            CircleMessageTransmitterV2Account::MessageSent(account) => {
                let row = message_sent_row::MessageSentRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Account::MessageTransmitter(account) => {
                let row = message_transmitter_row::MessageTransmitterRow::from_parts(
                    *account.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Account::UsedNonce(account) => {
                let row =
                    used_nonce_row::UsedNonceRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

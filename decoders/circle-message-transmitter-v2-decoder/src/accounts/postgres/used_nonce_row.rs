//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account::AccountMetadata, postgres::metadata::AccountRowMetadata};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct UsedNonceRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub is_used: bool,
}

impl UsedNonceRow {
    pub fn from_parts(
        source: crate::accounts::used_nonce::UsedNonce,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            is_used: source.is_used,
        }
    }
}

impl TryFrom<UsedNonceRow> for crate::accounts::used_nonce::UsedNonce {
    type Error = carbon_core::error::Error;
    fn try_from(source: UsedNonceRow) -> Result<Self, Self::Error> {
        Ok(Self {
            is_used: source.is_used,
        })
    }
}

impl carbon_core::postgres::operations::Table for crate::accounts::used_nonce::UsedNonce {
    fn table() -> &'static str {
        "used_nonce_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "is_used"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for UsedNonceRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO used_nonce_account (
                "is_used",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            )"#,
        )
        .bind(self.is_used)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for UsedNonceRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO used_nonce_account (
                "is_used",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "is_used" = EXCLUDED."is_used",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.is_used)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for UsedNonceRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM used_nonce_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for UsedNonceRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM used_nonce_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct UsedNonceMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for UsedNonceMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS used_nonce_account (
                -- Account data
                "is_used" BOOLEAN NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS used_nonce_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

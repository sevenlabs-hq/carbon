//! This code was AUTOGENERATED using the Codama library.
pub mod accept_ownership_row;
pub mod cpi_event_row;
pub mod disable_attester_row;
pub mod enable_attester_row;
pub mod initialize_row;
pub mod is_nonce_used_row;
pub mod pause_row;
pub mod receive_message_row;
pub mod reclaim_event_account_row;
pub mod send_message_row;
pub mod set_max_message_body_size_row;
pub mod set_signature_threshold_row;
pub mod transfer_ownership_row;
pub mod unpause_row;
pub mod update_attester_manager_row;
pub mod update_pauser_row;

pub use self::{
    accept_ownership_row::*, cpi_event_row::*, disable_attester_row::*, enable_attester_row::*,
    initialize_row::*, is_nonce_used_row::*, pause_row::*, receive_message_row::*,
    reclaim_event_account_row::*, send_message_row::*, set_max_message_body_size_row::*,
    set_signature_threshold_row::*, transfer_ownership_row::*, unpause_row::*,
    update_attester_manager_row::*, update_pauser_row::*,
};
use super::CircleMessageTransmitterV2Instruction;

pub struct CircleMessageTransmitterV2InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for CircleMessageTransmitterV2InstructionsMigration {
    fn app(&self) -> &str {
        "circle-message-transmitter-v2"
    }

    fn name(&self) -> &str {
        "circle_message_transmitter_v2_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(AcceptOwnershipMigrationOperation),
            Box::new(DisableAttesterMigrationOperation),
            Box::new(EnableAttesterMigrationOperation),
            Box::new(InitializeMigrationOperation),
            Box::new(IsNonceUsedMigrationOperation),
            Box::new(PauseMigrationOperation),
            Box::new(ReceiveMessageMigrationOperation),
            Box::new(ReclaimEventAccountMigrationOperation),
            Box::new(SendMessageMigrationOperation),
            Box::new(SetMaxMessageBodySizeMigrationOperation),
            Box::new(SetSignatureThresholdMigrationOperation),
            Box::new(TransferOwnershipMigrationOperation),
            Box::new(UnpauseMigrationOperation),
            Box::new(UpdateAttesterManagerMigrationOperation),
            Box::new(UpdatePauserMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct CircleMessageTransmitterV2InstructionWithMetadata(
    pub CircleMessageTransmitterV2Instruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        CircleMessageTransmitterV2Instruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for CircleMessageTransmitterV2InstructionWithMetadata
{
    fn from(
        value: (
            CircleMessageTransmitterV2Instruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        CircleMessageTransmitterV2InstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert
    for CircleMessageTransmitterV2InstructionWithMetadata
{
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleMessageTransmitterV2InstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            CircleMessageTransmitterV2Instruction::AcceptOwnership { data, .. } => {
                let row = accept_ownership_row::AcceptOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::DisableAttester { data, .. } => {
                let row = disable_attester_row::DisableAttesterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::EnableAttester { data, .. } => {
                let row = enable_attester_row::EnableAttesterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::IsNonceUsed { data, .. } => {
                let row = is_nonce_used_row::IsNonceUsedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::ReceiveMessage { data, .. } => {
                let row = receive_message_row::ReceiveMessageRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::ReclaimEventAccount { data, .. } => {
                let row = reclaim_event_account_row::ReclaimEventAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SendMessage { data, .. } => {
                let row = send_message_row::SendMessageRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SetMaxMessageBodySize { data, .. } => {
                let row = set_max_message_body_size_row::SetMaxMessageBodySizeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SetSignatureThreshold { data, .. } => {
                let row = set_signature_threshold_row::SetSignatureThresholdRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::UpdateAttesterManager { data, .. } => {
                let row = update_attester_manager_row::UpdateAttesterManagerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::UpdatePauser { data, .. } => {
                let row = update_pauser_row::UpdatePauserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert
    for CircleMessageTransmitterV2InstructionWithMetadata
{
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let CircleMessageTransmitterV2InstructionWithMetadata(
            decoded_instruction,
            metadata,
            raw_accounts,
        ) = self;
        match decoded_instruction {
            CircleMessageTransmitterV2Instruction::AcceptOwnership { data, .. } => {
                let row = accept_ownership_row::AcceptOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::DisableAttester { data, .. } => {
                let row = disable_attester_row::DisableAttesterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::EnableAttester { data, .. } => {
                let row = enable_attester_row::EnableAttesterRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Initialize { data, .. } => {
                let row = initialize_row::InitializeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::IsNonceUsed { data, .. } => {
                let row = is_nonce_used_row::IsNonceUsedRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Pause { data, .. } => {
                let row = pause_row::PauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::ReceiveMessage { data, .. } => {
                let row = receive_message_row::ReceiveMessageRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::ReclaimEventAccount { data, .. } => {
                let row = reclaim_event_account_row::ReclaimEventAccountRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SendMessage { data, .. } => {
                let row = send_message_row::SendMessageRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SetMaxMessageBodySize { data, .. } => {
                let row = set_max_message_body_size_row::SetMaxMessageBodySizeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::SetSignatureThreshold { data, .. } => {
                let row = set_signature_threshold_row::SetSignatureThresholdRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::TransferOwnership { data, .. } => {
                let row = transfer_ownership_row::TransferOwnershipRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::Unpause { data, .. } => {
                let row = unpause_row::UnpauseRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::UpdateAttesterManager { data, .. } => {
                let row = update_attester_manager_row::UpdateAttesterManagerRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::UpdatePauser { data, .. } => {
                let row = update_pauser_row::UpdatePauserRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            CircleMessageTransmitterV2Instruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

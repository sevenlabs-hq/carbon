//! This code was AUTOGENERATED using the Codama library.
pub mod claim_row;
pub mod cpi_event_row;
pub mod create_token_account_row;
pub mod create_token_account_with_seed_row;
pub mod jit_swap_row;
pub mod swap_row;

pub use self::{
    claim_row::*, cpi_event_row::*, create_token_account_row::*,
    create_token_account_with_seed_row::*, jit_swap_row::*, swap_row::*,
};
use super::RaptorV1Instruction;

pub struct RaptorV1InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for RaptorV1InstructionsMigration {
    fn app(&self) -> &str {
        "raptor-v1"
    }

    fn name(&self) -> &str {
        "raptor_v1_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(ClaimMigrationOperation),
            Box::new(CreateTokenAccountMigrationOperation),
            Box::new(CreateTokenAccountWithSeedMigrationOperation),
            Box::new(JitSwapMigrationOperation),
            Box::new(SwapMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct RaptorV1InstructionWithMetadata(
    pub RaptorV1Instruction,
    pub carbon_core::instruction::InstructionMetadata,
);

impl
    From<(
        RaptorV1Instruction,
        carbon_core::instruction::InstructionMetadata,
    )> for RaptorV1InstructionWithMetadata
{
    fn from(
        value: (
            RaptorV1Instruction,
            carbon_core::instruction::InstructionMetadata,
        ),
    ) -> Self {
        RaptorV1InstructionWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for RaptorV1InstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaptorV1InstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            RaptorV1Instruction::Claim(instruction) => {
                let row = claim_row::ClaimRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CreateTokenAccount(instruction) => {
                let row = create_token_account_row::CreateTokenAccountRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CreateTokenAccountWithSeed(instruction) => {
                let row =
                    create_token_account_with_seed_row::CreateTokenAccountWithSeedRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.insert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::JitSwap(instruction) => {
                let row =
                    jit_swap_row::JitSwapRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::Swap(instruction) => {
                let row = swap_row::SwapRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CpiEvent(instruction) => {
                let row =
                    cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for RaptorV1InstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let RaptorV1InstructionWithMetadata(instruction, metadata) = self;
        match instruction {
            RaptorV1Instruction::Claim(instruction) => {
                let row = claim_row::ClaimRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CreateTokenAccount(instruction) => {
                let row = create_token_account_row::CreateTokenAccountRow::from_parts(
                    instruction.clone(),
                    metadata.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CreateTokenAccountWithSeed(instruction) => {
                let row =
                    create_token_account_with_seed_row::CreateTokenAccountWithSeedRow::from_parts(
                        instruction.clone(),
                        metadata.clone(),
                    );
                row.upsert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::JitSwap(instruction) => {
                let row =
                    jit_swap_row::JitSwapRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::Swap(instruction) => {
                let row = swap_row::SwapRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            RaptorV1Instruction::CpiEvent(instruction) => {
                let row =
                    cpi_event_row::CpiEventRow::from_parts(instruction.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

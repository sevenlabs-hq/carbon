//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::SwapParams,
    carbon_core::{account_utils::next_account, borsh, deserialize::ArrangeAccounts},
};

/// Swap instruction - direct and proxy swaps with configurable fees (Raptor v1
/// built by Solana Tracker)
///
/// # Arguments
/// * `args` - SwapArgs containing:
/// - `amount_in`: Amount of input tokens
/// - `expect_amount_out`: Expected output (for slippage calculation)
/// - `min_return`: Minimum acceptable output
/// - `amounts/routes`: Routing configuration
/// - `fee_config`: Fee configuration with:
/// - `fee_bps`: Fee in basis points (100 = 1%)
/// - `from_input`: true = take from input, false = take from output
/// - `charge_bps`: Extra charge on positive slippage (from output excess)
/// - `unwrap_sol`: Whether to unwrap wSOL to SOL at the end
/// * `order_id`: Unique order identifier for tracking
/// # Accounts
/// - `fee_wallet`: Wallet to receive fees (optional)
/// - For proxy swaps, include: `authority`, `authority_source`,
///   `authority_destination`
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Swap {
    pub args: SwapParams,
    pub order_id: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SwapInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub source_token_account: solana_pubkey::Pubkey,
    pub destination_token_account: solana_pubkey::Pubkey,
    pub source_mint: solana_pubkey::Pubkey,
    pub destination_mint: solana_pubkey::Pubkey,
    pub fee_wallet: Option<solana_pubkey::Pubkey>,
    pub authority: Option<solana_pubkey::Pubkey>,
    pub authority_source: Option<solana_pubkey::Pubkey>,
    pub authority_destination: Option<solana_pubkey::Pubkey>,
    pub source_token_program: Option<solana_pubkey::Pubkey>,
    pub destination_token_program: Option<solana_pubkey::Pubkey>,
    pub associated_token_program: Option<solana_pubkey::Pubkey>,
    pub system_program: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Swap {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[248, 198, 158, 145, 225, 117, 135, 200] {
            return None;
        }
        let data_slice = &data[8..];
        let mut data_slice = data_slice;
        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Swap {
    type ArrangedAccounts = SwapInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let source_token_account = next_account(&mut iter)?;
        let destination_token_account = next_account(&mut iter)?;
        let source_mint = next_account(&mut iter)?;
        let destination_mint = next_account(&mut iter)?;
        let fee_wallet = next_account(&mut iter);
        let authority = next_account(&mut iter);
        let authority_source = next_account(&mut iter);
        let authority_destination = next_account(&mut iter);
        let source_token_program = next_account(&mut iter);
        let destination_token_program = next_account(&mut iter);
        let associated_token_program = next_account(&mut iter);
        let system_program = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(SwapInstructionAccounts {
            payer,
            source_token_account,
            destination_token_account,
            source_mint,
            destination_mint,
            fee_wallet,
            authority,
            authority_source,
            authority_destination,
            source_token_program,
            destination_token_program,
            associated_token_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{FeeConfig, Route},
    carbon_core::borsh,
};
/// Swap parameters - combines amount info with fee configuration

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct SwapParams {
    /// Amount of input tokens to swap
    pub amount_in: u64,
    /// Expected output amount (used for slippage and charge calculation)
    pub expect_amount_out: u64,
    /// Minimum acceptable output amount
    pub min_return: u64,
    /// Split amounts for multi-route swaps
    pub amounts: Vec<u64>,
    /// Routing configuration
    pub routes: Vec<Vec<Route>>,
    /// Fee configuration
    pub fee_config: FeeConfig,
    /// If true, unwrap wSOL to SOL at the end
    pub unwrap_sol: bool,
}

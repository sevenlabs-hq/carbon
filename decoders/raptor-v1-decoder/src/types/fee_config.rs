//! This code was AUTOGENERATED using the Codama library.
use carbon_core::borsh;
/// Fee configuration for unified swaps

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct FeeConfig {
    /// Fee amount in basis points (1 bps = 0.01%)
    pub fee_bps: u16,
    /// If true, fee is taken from input token; if false, from output token
    pub from_input: bool,
    /// Extra charge rate in basis points (for trim/charge from excess output)
    pub charge_bps: u16,
}

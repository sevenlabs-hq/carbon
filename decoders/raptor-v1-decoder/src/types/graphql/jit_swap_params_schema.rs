//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{FeeConfigGraphQL, JitHopPlanGraphQL, RouteGraphQL},
    carbon_core::graphql::primitives::U64,
    juniper::GraphQLObject,
};
#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "JitSwapParams")]
pub struct JitSwapParamsGraphQL {
    pub amount_in: U64,
    pub expect_amount_out: U64,
    pub min_return: U64,
    pub amounts: Vec<U64>,
    pub routes: Vec<Vec<RouteGraphQL>>,
    pub hop_plans: Vec<Vec<JitHopPlanGraphQL>>,
    pub fee_config: FeeConfigGraphQL,
    pub unwrap_sol: bool,
}

impl From<crate::types::JitSwapParams> for JitSwapParamsGraphQL {
    fn from(original: crate::types::JitSwapParams) -> Self {
        Self {
            amount_in: carbon_core::graphql::primitives::U64(original.amount_in),
            expect_amount_out: carbon_core::graphql::primitives::U64(original.expect_amount_out),
            min_return: carbon_core::graphql::primitives::U64(original.min_return),
            amounts: original
                .amounts
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(item))
                .collect(),
            routes: original
                .routes
                .into_iter()
                .map(|item| item.into_iter().map(|item| item.into()).collect())
                .collect(),
            hop_plans: original
                .hop_plans
                .into_iter()
                .map(|item| item.into_iter().map(|item| item.into()).collect())
                .collect(),
            fee_config: original.fee_config.into(),
            unwrap_sol: original.unwrap_sol,
        }
    }
}

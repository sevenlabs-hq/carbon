//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// Transfers from and closes a nested associated token account: an
/// associated token account owned by an associated token account.
/// The tokens are moved from the nested associated token account to the
/// wallet's associated token account, and the nested account lamports are
/// moved to the wallet.
/// Note: Nested token accounts are an anti-pattern, and almost always
/// created unintentionally, so this instruction should only be used to
/// recover from errors.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct RecoverNestedAssociatedToken {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RecoverNestedAssociatedTokenInstructionAccounts {
    pub nested_associated_account_address: solana_pubkey::Pubkey,
    pub nested_token_mint_address: solana_pubkey::Pubkey,
    pub destination_associated_account_address: solana_pubkey::Pubkey,
    pub owner_associated_account_address: solana_pubkey::Pubkey,
    pub owner_token_mint_address: solana_pubkey::Pubkey,
    pub wallet_address: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl RecoverNestedAssociatedToken {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.is_empty() {
            return None;
        }
        let discriminator = &data[0..1];
        if discriminator != [2] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[1..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for RecoverNestedAssociatedToken {
    type ArrangedAccounts = RecoverNestedAssociatedTokenInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let nested_associated_account_address = next_account(&mut iter)?;
        let nested_token_mint_address = next_account(&mut iter)?;
        let destination_associated_account_address = next_account(&mut iter)?;
        let owner_associated_account_address = next_account(&mut iter)?;
        let owner_token_mint_address = next_account(&mut iter)?;
        let wallet_address = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(RecoverNestedAssociatedTokenInstructionAccounts {
            nested_associated_account_address,
            nested_token_mint_address,
            destination_associated_account_address,
            owner_associated_account_address,
            owner_token_mint_address,
            wallet_address,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
pub mod buy_row;
pub mod claim_fees_row;
pub mod cpi_event_row;
pub mod create_meteora_pool_row;
pub mod initialize_meteora_accounts_row;
pub mod initialize_row;
pub mod launch_row;
pub mod sell_row;
pub mod update_pool_creator_row;

pub use self::{
    buy_row::*, claim_fees_row::*, cpi_event_row::*, create_meteora_pool_row::*,
    initialize_meteora_accounts_row::*, initialize_row::*, launch_row::*, sell_row::*,
    update_pool_creator_row::*,
};
use super::VirtualsInstruction;

pub struct VirtualsInstructionsMigration;

impl carbon_core::clickhouse::Migration for VirtualsInstructionsMigration {
    fn app(&self) -> &str {
        "virtuals"
    }

    fn name(&self) -> &str {
        "virtuals_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(BuyRowMigrationOperation),
            Box::new(ClaimFeesRowMigrationOperation),
            Box::new(CreateMeteoraPoolRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(InitializeMeteoraAccountsRowMigrationOperation),
            Box::new(LaunchRowMigrationOperation),
            Box::new(SellRowMigrationOperation),
            Box::new(UpdatePoolCreatorRowMigrationOperation),
            Box::new(CpiEventRowMigrationOperation),
        ]
    }
}

pub enum VirtualsInstructionRow {
    Buy(BuyRow),
    ClaimFees(ClaimFeesRow),
    CreateMeteoraPool(CreateMeteoraPoolRow),
    Initialize(InitializeRow),
    InitializeMeteoraAccounts(InitializeMeteoraAccountsRow),
    Launch(LaunchRow),
    Sell(SellRow),
    UpdatePoolCreator(UpdatePoolCreatorRow),
    CpiEvent(CpiEventRow),
}

pub struct VirtualsInstructionMetadata<'a>(
    pub &'a carbon_core::instruction::InstructionMetadata,
    pub &'a VirtualsInstruction,
);

impl<'a> carbon_core::clickhouse::BatchInsert for VirtualsInstructionMetadata<'a> {
    type Row = VirtualsInstructionRow;

    fn batch_insert(&self, rows: &mut Vec<Self::Row>) -> carbon_core::error::CarbonResult<()> {
        let &Self(metadata, instruction) = self;

        macro_rules! insert_branch {
            ($variant:ident, $row:ty) => {
                if let VirtualsInstruction::$variant { data, accounts, .. } = instruction {
                    rows.push(VirtualsInstructionRow::$variant(<$row>::try_from((
                        data.clone(),
                        metadata.clone(),
                        accounts.clone(),
                    ))?));
                    return Ok(());
                }
            };
        }

        insert_branch!(Buy, BuyRow);
        insert_branch!(ClaimFees, ClaimFeesRow);
        insert_branch!(CreateMeteoraPool, CreateMeteoraPoolRow);
        insert_branch!(Initialize, InitializeRow);
        insert_branch!(InitializeMeteoraAccounts, InitializeMeteoraAccountsRow);
        insert_branch!(Launch, LaunchRow);
        insert_branch!(Sell, SellRow);
        insert_branch!(UpdatePoolCreator, UpdatePoolCreatorRow);
        insert_branch!(CpiEvent, CpiEventRow);

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for VirtualsInstructionRow {
    async fn batch_commit(
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {{
                let branch_rows: Vec<$row> = rows
                    .iter()
                    .filter_map(|row| match row {
                        Self::$variant(row) => Some(row.clone()),
                        _ => None,
                    })
                    .collect();

                if !branch_rows.is_empty() {
                    <$row as carbon_core::clickhouse::Insert>::insert(client, &branch_rows).await?;
                }
            }};
        }

        commit_branch!(Buy, BuyRow);
        commit_branch!(ClaimFees, ClaimFeesRow);
        commit_branch!(CreateMeteoraPool, CreateMeteoraPoolRow);
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(InitializeMeteoraAccounts, InitializeMeteoraAccountsRow);
        commit_branch!(Launch, LaunchRow);
        commit_branch!(Sell, SellRow);
        commit_branch!(UpdatePoolCreator, UpdatePoolCreatorRow);
        commit_branch!(CpiEvent, CpiEventRow);

        Ok(())
    }
}

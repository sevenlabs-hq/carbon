//! This code was AUTOGENERATED using the Codama library.
        pub mod buy_row;
pub mod claim_fees_row;
pub mod create_meteora_pool_row;
pub mod initialize_row;
pub mod initialize_meteora_accounts_row;
pub mod launch_row;
pub mod sell_row;
pub mod update_pool_creator_row;
pub mod cpi_event_row;

        pub use self::buy_row::*;
pub use self::claim_fees_row::*;
pub use self::create_meteora_pool_row::*;
pub use self::initialize_row::*;
pub use self::initialize_meteora_accounts_row::*;
pub use self::launch_row::*;
pub use self::sell_row::*;
pub use self::update_pool_creator_row::*;
pub use self::cpi_event_row::*;
        use super::VirtualsInstruction;

        pub struct VirtualsInstructionsMigration;

        impl carbon_core::clickhouse::Migration for VirtualsInstructionsMigration {
            fn app(&self) -> &str {
                "virtuals"
            }

            fn name(&self) -> &str {
                "virtuals_instructions"
            }

            fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
                vec![
                    Box::new(BuyRowMigrationOperation),
            Box::new(ClaimFeesRowMigrationOperation),
            Box::new(CreateMeteoraPoolRowMigrationOperation),
            Box::new(InitializeRowMigrationOperation),
            Box::new(InitializeMeteoraAccountsRowMigrationOperation),
            Box::new(LaunchRowMigrationOperation),
            Box::new(SellRowMigrationOperation),
            Box::new(UpdatePoolCreatorRowMigrationOperation),
            Box::new(CpiEventRowMigrationOperation)
                ]
            }
        }

        pub enum VirtualsInstructionRow {
    Buy(BuyRow),
    ClaimFees(ClaimFeesRow),
    CreateMeteoraPool(CreateMeteoraPoolRow),
    Initialize(InitializeRow),
    InitializeMeteoraAccounts(InitializeMeteoraAccountsRow),
    Launch(LaunchRow),
    Sell(SellRow),
    UpdatePoolCreator(UpdatePoolCreatorRow),
    CpiEvent(CpiEventRow),
        }

        pub struct VirtualsInstructionMetadata(
            pub carbon_core::instruction::InstructionMetadata,
            pub VirtualsInstruction,
        );

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchInsert for VirtualsInstructionMetadata {
            type Row = VirtualsInstructionRow;

            async fn batch_insert(
                &self,
                rows: &mut Vec<Self::Row>,
            ) -> carbon_core::error::CarbonResult<()> {
                let Self(metadata, instruction) = self;

                match instruction {
            VirtualsInstruction::Buy { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::Buy(
                    BuyRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::ClaimFees { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::ClaimFees(
                    ClaimFeesRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::CreateMeteoraPool { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::CreateMeteoraPool(
                    CreateMeteoraPoolRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::Initialize { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::Initialize(
                    InitializeRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::InitializeMeteoraAccounts { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::InitializeMeteoraAccounts(
                    InitializeMeteoraAccountsRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::Launch { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::Launch(
                    LaunchRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::Sell { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::Sell(
                    SellRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::UpdatePoolCreator { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::UpdatePoolCreator(
                    UpdatePoolCreatorRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            VirtualsInstruction::CpiEvent { data, accounts, .. } => {
                rows.push(VirtualsInstructionRow::CpiEvent(
                    CpiEventRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
                }

                Ok(())
            }
        }

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchCommit for VirtualsInstructionRow {
            async fn batch_commit(
                &self,
                client: &clickhouse::Client,
                rows: &[Self],
            ) -> carbon_core::error::CarbonResult<()> {
                macro_rules! commit_branch {
                    ($variant:ident, $row:ty) => {
                        if let Self::$variant(source) = self {
                            let branch_rows: Vec<$row> = rows
                                .iter()
                                .filter_map(|row| match row {
                                    Self::$variant(row) => Some(row.clone()),
                                    _ => None,
                                })
                                .collect();
                            return <$row as carbon_core::clickhouse::Insert>::insert(
                                source,
                                client,
                                &branch_rows,
                            )
                            .await;
                        }
                    };
                }

        commit_branch!(Buy, BuyRow);
        commit_branch!(ClaimFees, ClaimFeesRow);
        commit_branch!(CreateMeteoraPool, CreateMeteoraPoolRow);
        commit_branch!(Initialize, InitializeRow);
        commit_branch!(InitializeMeteoraAccounts, InitializeMeteoraAccountsRow);
        commit_branch!(Launch, LaunchRow);
        commit_branch!(Sell, SellRow);
        commit_branch!(UpdatePoolCreator, UpdatePoolCreatorRow);
        commit_branch!(CpiEvent, CpiEventRow);
                Ok(())
            }
        }

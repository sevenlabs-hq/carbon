//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
/// This is where a user comes along and purchases a mint address and pool. The
/// user will provide a name, symbol and URI containing offchain metadata. The
/// contract will initialize a new onchain metadata account containing these
/// values, then revoke the mint authority.
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Launch {
    pub symbol: String,
    pub name: String,
    pub uri: String,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LaunchInstructionAccounts {
    pub creator: solana_pubkey::Pubkey,
    pub creator_virtuals_ata: solana_pubkey::Pubkey,
    pub token_mint: solana_pubkey::Pubkey,
    pub platform_prototype: solana_pubkey::Pubkey,
    pub platform_prototype_virtuals_ata: solana_pubkey::Pubkey,
    pub vpool: solana_pubkey::Pubkey,
    pub token_metadata: solana_pubkey::Pubkey,
    pub metadata_program: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub associated_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub rent: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Launch {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [153, 241, 93, 225, 22, 69, 74, 61] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Launch {
    type ArrangedAccounts = LaunchInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let creator = next_account(&mut iter)?;
        let creator_virtuals_ata = next_account(&mut iter)?;
        let token_mint = next_account(&mut iter)?;
        let platform_prototype = next_account(&mut iter)?;
        let platform_prototype_virtuals_ata = next_account(&mut iter)?;
        let vpool = next_account(&mut iter)?;
        let token_metadata = next_account(&mut iter)?;
        let metadata_program = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let associated_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let rent = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LaunchInstructionAccounts {
            creator,
            creator_virtuals_ata,
            token_mint,
            platform_prototype,
            platform_prototype_virtuals_ata,
            vpool,
            token_metadata,
            metadata_program,
            token_program,
            associated_token_program,
            system_program,
            rent,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.

pub use super::super::events;
use carbon_core::{borsh, deserialize::ArrangeAccounts};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(
    Debug,
    Clone,
    carbon_core::borsh::BorshSerialize,
    carbon_core::borsh::BorshDeserialize,
    PartialEq,
)]
pub enum CpiEvent {
    TradeEvent(events::trade_event::TradeEventEvent),
    TradeEventV2(events::trade_event_v2::TradeEventV2Event),
    TradeEventV3(events::trade_event_v3::TradeEventV3Event),
    PositionMovementEvent(events::position_movement_event::PositionMovementEventEvent),
    PlaceOrderEvent(events::place_order_event::PlaceOrderEventEvent),
    LiquidationEvent(events::liquidation_event::LiquidationEventEvent),
    OrderCompleteEvent(events::order_complete_event::OrderCompleteEventEvent),
    ApplyFundingEvent(events::apply_funding_event::ApplyFundingEventEvent),
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct CpiEventInstructionAccounts {
    pub program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl CpiEvent {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let event_data = if data[..8] == [228, 69, 165, 46, 81, 203, 154, 29] {
            &data[8..]
        } else {
            data
        };

        if let Some(decoded) = events::trade_event::TradeEventEvent::decode(event_data) {
            return Some(CpiEvent::TradeEvent(decoded));
        }
        if let Some(decoded) = events::trade_event_v2::TradeEventV2Event::decode(event_data) {
            return Some(CpiEvent::TradeEventV2(decoded));
        }
        if let Some(decoded) = events::trade_event_v3::TradeEventV3Event::decode(event_data) {
            return Some(CpiEvent::TradeEventV3(decoded));
        }
        if let Some(decoded) =
            events::position_movement_event::PositionMovementEventEvent::decode(event_data)
        {
            return Some(CpiEvent::PositionMovementEvent(decoded));
        }
        if let Some(decoded) = events::place_order_event::PlaceOrderEventEvent::decode(event_data) {
            return Some(CpiEvent::PlaceOrderEvent(decoded));
        }
        if let Some(decoded) = events::liquidation_event::LiquidationEventEvent::decode(event_data)
        {
            return Some(CpiEvent::LiquidationEvent(decoded));
        }
        if let Some(decoded) =
            events::order_complete_event::OrderCompleteEventEvent::decode(event_data)
        {
            return Some(CpiEvent::OrderCompleteEvent(decoded));
        }
        if let Some(decoded) =
            events::apply_funding_event::ApplyFundingEventEvent::decode(event_data)
        {
            return Some(CpiEvent::ApplyFundingEvent(decoded));
        }
        None
    }
}

impl ArrangeAccounts for CpiEvent {
    type ArrangedAccounts = CpiEventInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        match accounts {
            [program, event_authority, remaining @ ..] => Some(CpiEventInstructionAccounts {
                program: program.pubkey,
                event_authority: event_authority.pubkey,
                remaining: remaining.to_vec(),
            }),
            [event_authority] => Some(CpiEventInstructionAccounts {
                program: crate::PROGRAM_ID,
                event_authority: event_authority.pubkey,
                remaining: Vec::new(),
            }),
            _ => None,
        }
    }
}

//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct Liquidate {
    pub size: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LiquidateInstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub liquidator: solana_pubkey::Pubkey,
    pub liquidator_margin_account: solana_pubkey::Pubkey,
    pub greeks: solana_pubkey::Pubkey,
    pub oracle: solana_pubkey::Pubkey,
    pub oracle_backup_feed: solana_pubkey::Pubkey,
    pub oracle_backup_program: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub zeta_group: solana_pubkey::Pubkey,
    pub liquidated_margin_account: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl Liquidate {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [223, 179, 226, 125, 48, 46, 39, 74] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for Liquidate {
    type ArrangedAccounts = LiquidateInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let liquidator = next_account(&mut iter)?;
        let liquidator_margin_account = next_account(&mut iter)?;
        let greeks = next_account(&mut iter)?;
        let oracle = next_account(&mut iter)?;
        let oracle_backup_feed = next_account(&mut iter)?;
        let oracle_backup_program = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let zeta_group = next_account(&mut iter)?;
        let liquidated_margin_account = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LiquidateInstructionAccounts {
            state,
            liquidator,
            liquidator_margin_account,
            greeks,
            oracle,
            oracle_backup_feed,
            oracle_backup_program,
            market,
            zeta_group,
            liquidated_margin_account,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::Asset,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct LiquidateV2 {
    pub size: u64,
    pub asset: Asset,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct LiquidateV2InstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub liquidator: solana_pubkey::Pubkey,
    pub liquidator_account: solana_pubkey::Pubkey,
    pub pricing: solana_pubkey::Pubkey,
    pub oracle: solana_pubkey::Pubkey,
    pub oracle_backup_feed: solana_pubkey::Pubkey,
    pub oracle_backup_program: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub liquidated_account: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl LiquidateV2 {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [15, 86, 85, 55, 2, 225, 161, 235] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for LiquidateV2 {
    type ArrangedAccounts = LiquidateV2InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let liquidator = next_account(&mut iter)?;
        let liquidator_account = next_account(&mut iter)?;
        let pricing = next_account(&mut iter)?;
        let oracle = next_account(&mut iter)?;
        let oracle_backup_feed = next_account(&mut iter)?;
        let oracle_backup_program = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let liquidated_account = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(LiquidateV2InstructionAccounts {
            state,
            liquidator,
            liquidator_account,
            pricing,
            oracle,
            oracle_backup_feed,
            oracle_backup_program,
            market,
            liquidated_account,
            remaining: remaining.to_vec(),
        })
    }
}

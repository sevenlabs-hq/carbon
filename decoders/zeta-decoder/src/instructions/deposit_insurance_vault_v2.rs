//! This code was AUTOGENERATED using the Codama library.
use carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct DepositInsuranceVaultV2 {
    pub amount: u64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DepositInsuranceVaultV2InstructionAccounts {
    pub state: solana_pubkey::Pubkey,
    pub pricing: solana_pubkey::Pubkey,
    pub insurance_vault: solana_pubkey::Pubkey,
    pub insurance_deposit_account: solana_pubkey::Pubkey,
    pub user_token_account: solana_pubkey::Pubkey,
    pub zeta_vault: solana_pubkey::Pubkey,
    pub socialized_loss_account: solana_pubkey::Pubkey,
    pub authority: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DepositInsuranceVaultV2 {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [242, 44, 24, 19, 91, 59, 7, 201] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for DepositInsuranceVaultV2 {
    type ArrangedAccounts = DepositInsuranceVaultV2InstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let state = next_account(&mut iter)?;
        let pricing = next_account(&mut iter)?;
        let insurance_vault = next_account(&mut iter)?;
        let insurance_deposit_account = next_account(&mut iter)?;
        let user_token_account = next_account(&mut iter)?;
        let zeta_vault = next_account(&mut iter)?;
        let socialized_loss_account = next_account(&mut iter)?;
        let authority = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DepositInsuranceVaultV2InstructionAccounts {
            state,
            pricing,
            insurance_vault,
            insurance_deposit_account,
            user_token_account,
            zeta_vault,
            socialized_loss_account,
            authority,
            token_program,
            remaining: remaining.to_vec(),
        })
    }
}

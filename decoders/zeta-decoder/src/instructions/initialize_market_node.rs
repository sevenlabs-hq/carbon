//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::InitializeMarketNodeArgs,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct InitializeMarketNode {
    pub args: InitializeMarketNodeArgs,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeMarketNodeInstructionAccounts {
    pub zeta_group: solana_pubkey::Pubkey,
    pub market_node: solana_pubkey::Pubkey,
    pub greeks: solana_pubkey::Pubkey,
    pub payer: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeMarketNode {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [50, 118, 21, 21, 179, 248, 23, 128] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for InitializeMarketNode {
    type ArrangedAccounts = InitializeMarketNodeInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let zeta_group = next_account(&mut iter)?;
        let market_node = next_account(&mut iter)?;
        let greeks = next_account(&mut iter)?;
        let payer = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeMarketNodeInstructionAccounts {
            zeta_group,
            market_node,
            greeks,
            payer,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}

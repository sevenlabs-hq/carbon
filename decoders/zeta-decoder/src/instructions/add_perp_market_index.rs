//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::Asset,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct AddPerpMarketIndex {
    pub asset: Asset,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AddPerpMarketIndexInstructionAccounts {
    pub market_indexes: solana_pubkey::Pubkey,
    pub pricing: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl AddPerpMarketIndex {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [122, 40, 14, 64, 169, 18, 231, 136] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for AddPerpMarketIndex {
    type ArrangedAccounts = AddPerpMarketIndexInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let market_indexes = next_account(&mut iter)?;
        let pricing = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(AddPerpMarketIndexInstructionAccounts {
            market_indexes,
            pricing,
            remaining: remaining.to_vec(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::CrossMarginAccountInfo,
    carbon_core::{
        account::AccountMetadata,
        postgres::{
            metadata::AccountRowMetadata,
            primitives::{Pubkey, U8},
        },
    },
};

#[derive(sqlx::FromRow, Debug, Clone)]
pub struct CrossMarginAccountManagerRow {
    #[sqlx(flatten)]
    pub account_metadata: AccountRowMetadata,
    pub nonce: U8,
    pub authority: Pubkey,
    pub accounts: sqlx::types::Json<Vec<CrossMarginAccountInfo>>,
}

impl CrossMarginAccountManagerRow {
    pub fn from_parts(
        source: crate::accounts::cross_margin_account_manager::CrossMarginAccountManager,
        metadata: AccountMetadata,
    ) -> Self {
        Self {
            account_metadata: metadata.into(),
            nonce: source.nonce.into(),
            authority: source.authority.into(),
            accounts: sqlx::types::Json(source.accounts.to_vec()),
        }
    }
}

impl TryFrom<CrossMarginAccountManagerRow>
    for crate::accounts::cross_margin_account_manager::CrossMarginAccountManager
{
    type Error = carbon_core::error::Error;
    fn try_from(source: CrossMarginAccountManagerRow) -> Result<Self, Self::Error> {
        Ok(Self {
            nonce: source.nonce.try_into().map_err(|_| {
                carbon_core::error::Error::Custom(
                    "Failed to convert value from postgres primitive".to_string(),
                )
            })?,
            authority: *source.authority,
            accounts: source
                .accounts
                .0
                .into_iter()
                .collect::<Vec<_>>()
                .try_into()
                .map_err(|_| {
                    carbon_core::error::Error::Custom(
                        "Failed to convert value from postgres primitive".to_string(),
                    )
                })?,
        })
    }
}

impl carbon_core::postgres::operations::Table
    for crate::accounts::cross_margin_account_manager::CrossMarginAccountManager
{
    fn table() -> &'static str {
        "cross_margin_account_manager_account"
    }

    fn columns() -> Vec<&'static str> {
        vec!["__pubkey", "__slot", "nonce", "authority", "accounts"]
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for CrossMarginAccountManagerRow {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"
            INSERT INTO cross_margin_account_manager_account (
                "nonce",
                "authority",
                "accounts",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5
            )"#,
        )
        .bind(self.nonce)
        .bind(self.authority)
        .bind(&self.accounts)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for CrossMarginAccountManagerRow {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"INSERT INTO cross_margin_account_manager_account (
                "nonce",
                "authority",
                "accounts",
                __pubkey, __slot
            ) VALUES (
                $1, $2, $3, $4, $5
            ) ON CONFLICT (
                __pubkey
            ) DO UPDATE SET
                "nonce" = EXCLUDED."nonce",
                "authority" = EXCLUDED."authority",
                "accounts" = EXCLUDED."accounts",
                __slot = EXCLUDED.__slot
            "#,
        )
        .bind(self.nonce)
        .bind(self.authority)
        .bind(&self.accounts)
        .bind(self.account_metadata.pubkey)
        .bind(&self.account_metadata.slot)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Delete for CrossMarginAccountManagerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn delete(key: Self::Key, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        sqlx::query(
            r#"DELETE FROM cross_margin_account_manager_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .execute(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::LookUp for CrossMarginAccountManagerRow {
    type Key = carbon_core::postgres::primitives::Pubkey;

    async fn lookup(
        key: Self::Key,
        pool: &sqlx::PgPool,
    ) -> carbon_core::error::CarbonResult<Option<Self>> {
        let row = sqlx::query_as(
            r#"SELECT * FROM cross_margin_account_manager_account WHERE
                __pubkey = $1
            "#,
        )
        .bind(key)
        .fetch_optional(pool)
        .await
        .map_err(|e| carbon_core::error::Error::Custom(e.to_string()))?;
        Ok(row)
    }
}

pub struct CrossMarginAccountManagerMigrationOperation;

#[async_trait::async_trait]
impl sqlx_migrator::Operation<sqlx::Postgres> for CrossMarginAccountManagerMigrationOperation {
    async fn up(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(
            r#"CREATE TABLE IF NOT EXISTS cross_margin_account_manager_account (
                -- Account data
                "nonce" INT2 NOT NULL,
                "authority" BYTEA NOT NULL,
                "accounts" JSONB NOT NULL,
                -- Account metadata
                __pubkey BYTEA NOT NULL,
                __slot NUMERIC(20),
                PRIMARY KEY (__pubkey)
            )"#,
        )
        .execute(connection)
        .await?;
        Ok(())
    }

    async fn down(
        &self,
        connection: &mut sqlx::PgConnection,
    ) -> Result<(), sqlx_migrator::error::Error> {
        sqlx::query(r#"DROP TABLE IF EXISTS cross_margin_account_manager_account"#)
            .execute(connection)
            .await?;
        Ok(())
    }
}

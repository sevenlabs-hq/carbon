//! This code was AUTOGENERATED using the Codama library.
pub mod cross_margin_account_manager_row;
pub mod cross_margin_account_row;
pub mod cross_open_orders_map_row;
pub mod greeks_row;
pub mod insurance_deposit_account_row;
pub mod margin_account_row;
pub mod market_indexes_row;
pub mod market_node_row;
pub mod open_orders_map_row;
pub mod perp_sync_queue_row;
pub mod pricing_row;
pub mod referral_account_row;
pub mod referrer_account_row;
pub mod referrer_alias_row;
pub mod settlement_account_row;
pub mod socialized_loss_account_row;
pub mod spread_account_row;
pub mod state_row;
pub mod underlying_row;
pub mod whitelist_deposit_account_row;
pub mod whitelist_insurance_account_row;
pub mod whitelist_trading_fees_account_row;
pub mod zeta_group_row;

pub use self::{
    cross_margin_account_manager_row::*, cross_margin_account_row::*, cross_open_orders_map_row::*,
    greeks_row::*, insurance_deposit_account_row::*, margin_account_row::*, market_indexes_row::*,
    market_node_row::*, open_orders_map_row::*, perp_sync_queue_row::*, pricing_row::*,
    referral_account_row::*, referrer_account_row::*, referrer_alias_row::*,
    settlement_account_row::*, socialized_loss_account_row::*, spread_account_row::*, state_row::*,
    underlying_row::*, whitelist_deposit_account_row::*, whitelist_insurance_account_row::*,
    whitelist_trading_fees_account_row::*, zeta_group_row::*,
};
use super::ZetaAccount;

pub struct ZetaAccountsMigration;

impl carbon_core::clickhouse::Migration for ZetaAccountsMigration {
    fn app(&self) -> &str {
        "zeta"
    }

    fn name(&self) -> &str {
        "zeta_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
        vec![
            Box::new(CrossMarginAccountRowMigrationOperation),
            Box::new(CrossMarginAccountManagerRowMigrationOperation),
            Box::new(CrossOpenOrdersMapRowMigrationOperation),
            Box::new(GreeksRowMigrationOperation),
            Box::new(InsuranceDepositAccountRowMigrationOperation),
            Box::new(MarginAccountRowMigrationOperation),
            Box::new(MarketIndexesRowMigrationOperation),
            Box::new(MarketNodeRowMigrationOperation),
            Box::new(OpenOrdersMapRowMigrationOperation),
            Box::new(PerpSyncQueueRowMigrationOperation),
            Box::new(PricingRowMigrationOperation),
            Box::new(ReferralAccountRowMigrationOperation),
            Box::new(ReferrerAccountRowMigrationOperation),
            Box::new(ReferrerAliasRowMigrationOperation),
            Box::new(SettlementAccountRowMigrationOperation),
            Box::new(SocializedLossAccountRowMigrationOperation),
            Box::new(SpreadAccountRowMigrationOperation),
            Box::new(StateRowMigrationOperation),
            Box::new(UnderlyingRowMigrationOperation),
            Box::new(WhitelistDepositAccountRowMigrationOperation),
            Box::new(WhitelistInsuranceAccountRowMigrationOperation),
            Box::new(WhitelistTradingFeesAccountRowMigrationOperation),
            Box::new(ZetaGroupRowMigrationOperation),
        ]
    }
}

pub enum ZetaAccountRow {
    CrossMarginAccount(CrossMarginAccountRow),
    CrossMarginAccountManager(CrossMarginAccountManagerRow),
    CrossOpenOrdersMap(CrossOpenOrdersMapRow),
    Greeks(GreeksRow),
    InsuranceDepositAccount(InsuranceDepositAccountRow),
    MarginAccount(MarginAccountRow),
    MarketIndexes(MarketIndexesRow),
    MarketNode(MarketNodeRow),
    OpenOrdersMap(OpenOrdersMapRow),
    PerpSyncQueue(PerpSyncQueueRow),
    Pricing(PricingRow),
    ReferralAccount(ReferralAccountRow),
    ReferrerAccount(ReferrerAccountRow),
    ReferrerAlias(ReferrerAliasRow),
    SettlementAccount(SettlementAccountRow),
    SocializedLossAccount(SocializedLossAccountRow),
    SpreadAccount(SpreadAccountRow),
    State(StateRow),
    Underlying(UnderlyingRow),
    WhitelistDepositAccount(WhitelistDepositAccountRow),
    WhitelistInsuranceAccount(WhitelistInsuranceAccountRow),
    WhitelistTradingFeesAccount(WhitelistTradingFeesAccountRow),
    ZetaGroup(ZetaGroupRow),
}

pub struct ZetaAccountMetadata(pub carbon_core::account::AccountMetadata, pub ZetaAccount);

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchInsert for ZetaAccountMetadata {
    type Row = ZetaAccountRow;

    async fn batch_insert(
        &self,
        rows: &mut Vec<Self::Row>,
    ) -> carbon_core::error::CarbonResult<()> {
        let Self(metadata, account) = self;

        match account {
            ZetaAccount::CrossMarginAccount(account) => {
                rows.push(ZetaAccountRow::CrossMarginAccount(
                    CrossMarginAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::CrossMarginAccountManager(account) => {
                rows.push(ZetaAccountRow::CrossMarginAccountManager(
                    CrossMarginAccountManagerRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::CrossOpenOrdersMap(account) => {
                rows.push(ZetaAccountRow::CrossOpenOrdersMap(
                    CrossOpenOrdersMapRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::Greeks(account) => {
                rows.push(ZetaAccountRow::Greeks(GreeksRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            ZetaAccount::InsuranceDepositAccount(account) => {
                rows.push(ZetaAccountRow::InsuranceDepositAccount(
                    InsuranceDepositAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::MarginAccount(account) => {
                rows.push(ZetaAccountRow::MarginAccount(MarginAccountRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::MarketIndexes(account) => {
                rows.push(ZetaAccountRow::MarketIndexes(MarketIndexesRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::MarketNode(account) => {
                rows.push(ZetaAccountRow::MarketNode(MarketNodeRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            ZetaAccount::OpenOrdersMap(account) => {
                rows.push(ZetaAccountRow::OpenOrdersMap(OpenOrdersMapRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::PerpSyncQueue(account) => {
                rows.push(ZetaAccountRow::PerpSyncQueue(PerpSyncQueueRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::Pricing(account) => {
                rows.push(ZetaAccountRow::Pricing(PricingRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            ZetaAccount::ReferralAccount(account) => {
                rows.push(ZetaAccountRow::ReferralAccount(
                    ReferralAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::ReferrerAccount(account) => {
                rows.push(ZetaAccountRow::ReferrerAccount(
                    ReferrerAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::ReferrerAlias(account) => {
                rows.push(ZetaAccountRow::ReferrerAlias(ReferrerAliasRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::SettlementAccount(account) => {
                rows.push(ZetaAccountRow::SettlementAccount(
                    SettlementAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::SocializedLossAccount(account) => {
                rows.push(ZetaAccountRow::SocializedLossAccount(
                    SocializedLossAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::SpreadAccount(account) => {
                rows.push(ZetaAccountRow::SpreadAccount(SpreadAccountRow::try_from(
                    (*account.clone(), metadata.clone()),
                )?));
            }
            ZetaAccount::State(account) => {
                rows.push(ZetaAccountRow::State(StateRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            ZetaAccount::Underlying(account) => {
                rows.push(ZetaAccountRow::Underlying(UnderlyingRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
            ZetaAccount::WhitelistDepositAccount(account) => {
                rows.push(ZetaAccountRow::WhitelistDepositAccount(
                    WhitelistDepositAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::WhitelistInsuranceAccount(account) => {
                rows.push(ZetaAccountRow::WhitelistInsuranceAccount(
                    WhitelistInsuranceAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::WhitelistTradingFeesAccount(account) => {
                rows.push(ZetaAccountRow::WhitelistTradingFeesAccount(
                    WhitelistTradingFeesAccountRow::try_from((*account.clone(), metadata.clone()))?,
                ));
            }
            ZetaAccount::ZetaGroup(account) => {
                rows.push(ZetaAccountRow::ZetaGroup(ZetaGroupRow::try_from((
                    *account.clone(),
                    metadata.clone(),
                ))?));
            }
        }

        Ok(())
    }
}

#[async_trait::async_trait]
impl carbon_core::clickhouse::BatchCommit for ZetaAccountRow {
    async fn batch_commit(
        &self,
        client: &clickhouse::Client,
        rows: &[Self],
    ) -> carbon_core::error::CarbonResult<()> {
        macro_rules! commit_branch {
            ($variant:ident, $row:ty) => {
                if let Self::$variant(source) = self {
                    let branch_rows: Vec<$row> = rows
                        .iter()
                        .filter_map(|row| match row {
                            Self::$variant(row) => Some(row.clone()),
                            _ => None,
                        })
                        .collect();
                    return <$row as carbon_core::clickhouse::Insert>::insert(
                        source,
                        client,
                        &branch_rows,
                    )
                    .await;
                }
            };
        }

        commit_branch!(CrossMarginAccount, CrossMarginAccountRow);
        commit_branch!(CrossMarginAccountManager, CrossMarginAccountManagerRow);
        commit_branch!(CrossOpenOrdersMap, CrossOpenOrdersMapRow);
        commit_branch!(Greeks, GreeksRow);
        commit_branch!(InsuranceDepositAccount, InsuranceDepositAccountRow);
        commit_branch!(MarginAccount, MarginAccountRow);
        commit_branch!(MarketIndexes, MarketIndexesRow);
        commit_branch!(MarketNode, MarketNodeRow);
        commit_branch!(OpenOrdersMap, OpenOrdersMapRow);
        commit_branch!(PerpSyncQueue, PerpSyncQueueRow);
        commit_branch!(Pricing, PricingRow);
        commit_branch!(ReferralAccount, ReferralAccountRow);
        commit_branch!(ReferrerAccount, ReferrerAccountRow);
        commit_branch!(ReferrerAlias, ReferrerAliasRow);
        commit_branch!(SettlementAccount, SettlementAccountRow);
        commit_branch!(SocializedLossAccount, SocializedLossAccountRow);
        commit_branch!(SpreadAccount, SpreadAccountRow);
        commit_branch!(State, StateRow);
        commit_branch!(Underlying, UnderlyingRow);
        commit_branch!(WhitelistDepositAccount, WhitelistDepositAccountRow);
        commit_branch!(WhitelistInsuranceAccount, WhitelistInsuranceAccountRow);
        commit_branch!(WhitelistTradingFeesAccount, WhitelistTradingFeesAccountRow);
        commit_branch!(ZetaGroup, ZetaGroupRow);
        Ok(())
    }
}

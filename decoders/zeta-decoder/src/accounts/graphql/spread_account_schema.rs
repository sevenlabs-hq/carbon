//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::graphql::{AssetGraphQL, PositionGraphQL},
    carbon_core::graphql::primitives::{Pubkey, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "SpreadAccount")]
pub struct SpreadAccountGraphQL {
    pub account_metadata: crate::accounts::graphql::AccountMetadataGraphQL,
    pub authority: Pubkey,
    pub nonce: U8,
    pub balance: U64,
    pub series_expiry: Vec<U64>,
    pub series_expiry_padding: U64,
    pub positions: Vec<PositionGraphQL>,
    pub positions_padding: Vec<PositionGraphQL>,
    pub asset: AssetGraphQL,
    pub padding: Vec<U8>,
}

impl TryFrom<crate::accounts::postgres::SpreadAccountRow> for SpreadAccountGraphQL {
    type Error = carbon_core::error::Error;
    fn try_from(row: crate::accounts::postgres::SpreadAccountRow) -> Result<Self, Self::Error> {
        Ok(Self {
            account_metadata: row.account_metadata.into(),
            authority: carbon_core::graphql::primitives::Pubkey(row.authority.0),
            nonce: carbon_core::graphql::primitives::U8((*row.nonce) as u8),
            balance: carbon_core::graphql::primitives::U64(*row.balance),
            series_expiry: row
                .series_expiry
                .into_iter()
                .map(|item| carbon_core::graphql::primitives::U64(*item))
                .collect(),
            series_expiry_padding: carbon_core::graphql::primitives::U64(
                *row.series_expiry_padding,
            ),
            positions: row
                .positions
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            positions_padding: row
                .positions_padding
                .0
                .into_iter()
                .map(|item| item.into())
                .collect(),
            asset: row.asset.0.into(),
            padding: row
                .padding
                .into_iter()
                .map(carbon_core::graphql::primitives::U8)
                .collect(),
        })
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::{AnchorDecimal, MarginAccountType, ProductLedger},
    solana_pubkey::Pubkey,
};

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct CrossMarginAccount {
    pub authority: Pubkey,
    pub delegated_pubkey: Pubkey,
    pub balance: u64,
    pub subaccount_index: u8,
    pub nonce: u8,
    pub force_cancel_flag: bool,
    pub account_type: MarginAccountType,
    pub open_orders_nonces: [u8; 5],
    pub open_orders_nonces_padding: [u8; 20],
    pub rebalance_amount: i64,
    pub last_funding_deltas: [AnchorDecimal; 5],
    pub last_funding_deltas_padding: [AnchorDecimal; 20],
    pub product_ledgers: [ProductLedger; 5],
    pub product_ledgers_padding: [ProductLedger; 20],
    #[cfg_attr(feature = "serde", serde(with = "serde_big_array::BigArray"))]
    pub padding: [u8; 2000],
}

impl CrossMarginAccount {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [242, 94, 142, 131, 35, 244, 147, 28] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

//! This code was AUTOGENERATED using the Codama library.
use {
    carbon_core::graphql::primitives::{Pubkey, U32, U64, U8},
    juniper::GraphQLObject,
};

#[derive(Debug, Clone, GraphQLObject)]
#[graphql(name = "ConfigParams")]
pub struct ConfigParamsGraphQL {
    pub migration_authority: Option<Pubkey>,
    pub backend_authority: Option<Pubkey>,
    pub config_authority: Option<Pubkey>,
    pub helio_fee: Option<Pubkey>,
    pub dex_fee: Option<Pubkey>,
    pub fee_bps: Option<i32>,
    pub dex_fee_share: Option<U8>,
    pub migration_fee: Option<U64>,
    pub marketcap_threshold: Option<U64>,
    pub marketcap_currency: Option<U8>,
    pub min_supported_decimal_places: Option<U8>,
    pub max_supported_decimal_places: Option<U8>,
    pub min_supported_token_supply: Option<U64>,
    pub max_supported_token_supply: Option<U64>,
    pub coef_b: Option<U32>,
}

impl From<crate::types::ConfigParams> for ConfigParamsGraphQL {
    fn from(original: crate::types::ConfigParams) -> Self {
        Self {
            migration_authority: original
                .migration_authority
                .map(carbon_core::graphql::primitives::Pubkey),
            backend_authority: original
                .backend_authority
                .map(carbon_core::graphql::primitives::Pubkey),
            config_authority: original
                .config_authority
                .map(carbon_core::graphql::primitives::Pubkey),
            helio_fee: original
                .helio_fee
                .map(carbon_core::graphql::primitives::Pubkey),
            dex_fee: original
                .dex_fee
                .map(carbon_core::graphql::primitives::Pubkey),
            fee_bps: original.fee_bps.map(|v| v as i32),
            dex_fee_share: original
                .dex_fee_share
                .map(carbon_core::graphql::primitives::U8),
            migration_fee: original
                .migration_fee
                .map(carbon_core::graphql::primitives::U64),
            marketcap_threshold: original
                .marketcap_threshold
                .map(carbon_core::graphql::primitives::U64),
            marketcap_currency: original
                .marketcap_currency
                .map(carbon_core::graphql::primitives::U8),
            min_supported_decimal_places: original
                .min_supported_decimal_places
                .map(carbon_core::graphql::primitives::U8),
            max_supported_decimal_places: original
                .max_supported_decimal_places
                .map(carbon_core::graphql::primitives::U8),
            min_supported_token_supply: original
                .min_supported_token_supply
                .map(carbon_core::graphql::primitives::U64),
            max_supported_token_supply: original
                .max_supported_token_supply
                .map(carbon_core::graphql::primitives::U64),
            coef_b: original.coef_b.map(carbon_core::graphql::primitives::U32),
        }
    }
}

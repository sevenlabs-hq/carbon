//! This code was AUTOGENERATED using the Codama library.
        pub mod buy_row;
pub mod config_init_row;
pub mod config_update_row;
pub mod migrate_funds_row;
pub mod sell_row;
pub mod token_mint_row;
pub mod cpi_event_row;

        pub use self::buy_row::*;
pub use self::config_init_row::*;
pub use self::config_update_row::*;
pub use self::migrate_funds_row::*;
pub use self::sell_row::*;
pub use self::token_mint_row::*;
pub use self::cpi_event_row::*;
        use super::MoonshotInstruction;

        pub struct MoonshotInstructionsMigration;

        impl carbon_core::clickhouse::Migration for MoonshotInstructionsMigration {
            fn app(&self) -> &str {
                "moonshot"
            }

            fn name(&self) -> &str {
                "moonshot_instructions"
            }

            fn operations(&self) -> Vec<Box<dyn carbon_core::clickhouse::Operation>> {
                vec![
                    Box::new(BuyRowMigrationOperation),
            Box::new(ConfigInitRowMigrationOperation),
            Box::new(ConfigUpdateRowMigrationOperation),
            Box::new(MigrateFundsRowMigrationOperation),
            Box::new(SellRowMigrationOperation),
            Box::new(TokenMintRowMigrationOperation),
            Box::new(CpiEventRowMigrationOperation)
                ]
            }
        }

        pub enum MoonshotInstructionRow {
    Buy(BuyRow),
    ConfigInit(ConfigInitRow),
    ConfigUpdate(ConfigUpdateRow),
    MigrateFunds(MigrateFundsRow),
    Sell(SellRow),
    TokenMint(TokenMintRow),
    CpiEvent(CpiEventRow),
        }

        pub struct MoonshotInstructionMetadata(
            pub carbon_core::instruction::InstructionMetadata,
            pub MoonshotInstruction,
        );

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchInsert for MoonshotInstructionMetadata {
            type Row = MoonshotInstructionRow;

            async fn batch_insert(
                &self,
                rows: &mut Vec<Self::Row>,
            ) -> carbon_core::error::CarbonResult<()> {
                let Self(metadata, instruction) = self;

                match instruction {
            MoonshotInstruction::Buy { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::Buy(
                    BuyRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::ConfigInit { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::ConfigInit(
                    ConfigInitRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::ConfigUpdate { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::ConfigUpdate(
                    ConfigUpdateRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::MigrateFunds { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::MigrateFunds(
                    MigrateFundsRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::Sell { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::Sell(
                    SellRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::TokenMint { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::TokenMint(
                    TokenMintRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
            MoonshotInstruction::CpiEvent { data, accounts, .. } => {
                rows.push(MoonshotInstructionRow::CpiEvent(
                    CpiEventRow::try_from((data.clone(), metadata.clone(), accounts.clone()))?,
                ));
            }
                }

                Ok(())
            }
        }

        #[async_trait::async_trait]
        impl carbon_core::clickhouse::BatchCommit for MoonshotInstructionRow {
            async fn batch_commit(
                &self,
                client: &clickhouse::Client,
                rows: &[Self],
            ) -> carbon_core::error::CarbonResult<()> {
                macro_rules! commit_branch {
                    ($variant:ident, $row:ty) => {
                        if let Self::$variant(source) = self {
                            let branch_rows: Vec<$row> = rows
                                .iter()
                                .filter_map(|row| match row {
                                    Self::$variant(row) => Some(row.clone()),
                                    _ => None,
                                })
                                .collect();
                            return <$row as carbon_core::clickhouse::Insert>::insert(
                                source,
                                client,
                                &branch_rows,
                            )
                            .await;
                        }
                    };
                }

        commit_branch!(Buy, BuyRow);
        commit_branch!(ConfigInit, ConfigInitRow);
        commit_branch!(ConfigUpdate, ConfigUpdateRow);
        commit_branch!(MigrateFunds, MigrateFundsRow);
        commit_branch!(Sell, SellRow);
        commit_branch!(TokenMint, TokenMintRow);
        commit_branch!(CpiEvent, CpiEventRow);
                Ok(())
            }
        }

//! This code was AUTOGENERATED using the Codama library.
pub mod fee_row;
pub mod order_row;

pub use self::{fee_row::*, order_row::*};
use super::JupiterLimitOrder2Account;

pub struct JupiterLimitOrder2AccountsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterLimitOrder2AccountsMigration {
    fn app(&self) -> &str {
        "jupiter-limit-order-2"
    }

    fn name(&self) -> &str {
        "jupiter_limit_order_2_accounts"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(FeeMigrationOperation),
            Box::new(OrderMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterLimitOrder2AccountWithMetadata(
    pub JupiterLimitOrder2Account,
    pub carbon_core::account::AccountMetadata,
);

impl
    From<(
        JupiterLimitOrder2Account,
        carbon_core::account::AccountMetadata,
    )> for JupiterLimitOrder2AccountWithMetadata
{
    fn from(
        value: (
            JupiterLimitOrder2Account,
            carbon_core::account::AccountMetadata,
        ),
    ) -> Self {
        JupiterLimitOrder2AccountWithMetadata(value.0, value.1)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterLimitOrder2AccountWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrder2AccountWithMetadata(account, metadata) = self;

        match account {
            JupiterLimitOrder2Account::Fee(account) => {
                let row = fee_row::FeeRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Account::Order(account) => {
                let row = order_row::OrderRow::from_parts(*account.clone(), metadata.clone());
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterLimitOrder2AccountWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrder2AccountWithMetadata(account, metadata) = self;
        match account {
            JupiterLimitOrder2Account::Fee(account) => {
                let row = fee_row::FeeRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Account::Order(account) => {
                let row = order_row::OrderRow::from_parts(*account.clone(), metadata.clone());
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}

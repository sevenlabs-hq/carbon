//! This code was AUTOGENERATED using the Codama library.
use {
    crate::types::FlashFillOrderParams,
    carbon_core::{account_utils::next_account, deserialize::ArrangeAccounts},
};
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, borsh::BorshDeserialize, PartialEq)]
pub struct FlashFillOrder {
    pub params: FlashFillOrderParams,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct FlashFillOrderInstructionAccounts {
    pub taker: solana_pubkey::Pubkey,
    pub maker: solana_pubkey::Pubkey,
    pub order: solana_pubkey::Pubkey,
    pub input_mint_reserve: solana_pubkey::Pubkey,
    pub maker_output_mint_account: Option<solana_pubkey::Pubkey>,
    pub taker_output_mint_account: Option<solana_pubkey::Pubkey>,
    pub fee_account: solana_pubkey::Pubkey,
    pub input_token_program: solana_pubkey::Pubkey,
    pub output_mint: solana_pubkey::Pubkey,
    pub output_token_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub event_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl FlashFillOrder {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != [252, 104, 18, 134, 164, 78, 18, 140] {
            return None;
        }

        let mut data_slice = data;

        data_slice = &data_slice[8..];

        borsh::BorshDeserialize::deserialize(&mut data_slice).ok()
    }
}

impl ArrangeAccounts for FlashFillOrder {
    type ArrangedAccounts = FlashFillOrderInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let taker = next_account(&mut iter)?;
        let maker = next_account(&mut iter)?;
        let order = next_account(&mut iter)?;
        let input_mint_reserve = next_account(&mut iter)?;
        let maker_output_mint_account = next_account(&mut iter);
        let taker_output_mint_account = next_account(&mut iter);
        let fee_account = next_account(&mut iter)?;
        let input_token_program = next_account(&mut iter)?;
        let output_mint = next_account(&mut iter)?;
        let output_token_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;
        let event_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(FlashFillOrderInstructionAccounts {
            taker,
            maker,
            order,
            input_mint_reserve,
            maker_output_mint_account,
            taker_output_mint_account,
            fee_account,
            input_token_program,
            output_mint,
            output_token_program,
            system_program,
            event_authority,
            program,
            remaining: remaining.to_vec(),
        })
    }
}

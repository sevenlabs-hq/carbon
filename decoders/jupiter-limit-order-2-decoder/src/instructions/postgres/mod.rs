//! This code was AUTOGENERATED using the Codama library.
pub mod cancel_dust_order_row;
pub mod cancel_order_row;
pub mod cpi_event_row;
pub mod fill_order_row;
pub mod flash_fill_order_row;
pub mod initialize_order_row;
pub mod pre_flash_fill_order_row;
pub mod update_fee_row;
pub mod withdraw_fee_row;

pub use self::{
    cancel_dust_order_row::*, cancel_order_row::*, cpi_event_row::*, fill_order_row::*,
    flash_fill_order_row::*, initialize_order_row::*, pre_flash_fill_order_row::*,
    update_fee_row::*, withdraw_fee_row::*,
};
use super::JupiterLimitOrder2Instruction;

pub struct JupiterLimitOrder2InstructionsMigration;

impl sqlx_migrator::Migration<sqlx::Postgres> for JupiterLimitOrder2InstructionsMigration {
    fn app(&self) -> &str {
        "jupiter-limit-order-2"
    }

    fn name(&self) -> &str {
        "jupiter_limit_order_2_instructions"
    }

    fn operations(&self) -> Vec<Box<dyn sqlx_migrator::Operation<sqlx::Postgres>>> {
        vec![
            Box::new(CancelDustOrderMigrationOperation),
            Box::new(CancelOrderMigrationOperation),
            Box::new(FillOrderMigrationOperation),
            Box::new(FlashFillOrderMigrationOperation),
            Box::new(InitializeOrderMigrationOperation),
            Box::new(PreFlashFillOrderMigrationOperation),
            Box::new(UpdateFeeMigrationOperation),
            Box::new(WithdrawFeeMigrationOperation),
            Box::new(CpiEventMigrationOperation),
        ]
    }

    fn parents(&self) -> Vec<Box<dyn sqlx_migrator::Migration<sqlx::Postgres>>> {
        vec![]
    }
}

pub struct JupiterLimitOrder2InstructionWithMetadata(
    pub JupiterLimitOrder2Instruction,
    pub carbon_core::instruction::InstructionMetadata,
    pub Vec<solana_instruction::AccountMeta>,
);

impl
    From<(
        JupiterLimitOrder2Instruction,
        carbon_core::instruction::InstructionMetadata,
        Vec<solana_instruction::AccountMeta>,
    )> for JupiterLimitOrder2InstructionWithMetadata
{
    fn from(
        value: (
            JupiterLimitOrder2Instruction,
            carbon_core::instruction::InstructionMetadata,
            Vec<solana_instruction::AccountMeta>,
        ),
    ) -> Self {
        JupiterLimitOrder2InstructionWithMetadata(value.0, value.1, value.2)
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Insert for JupiterLimitOrder2InstructionWithMetadata {
    async fn insert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrder2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterLimitOrder2Instruction::CancelDustOrder { data, .. } => {
                let row = cancel_dust_order_row::CancelDustOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::CancelOrder { data, .. } => {
                let row = cancel_order_row::CancelOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::FillOrder { data, .. } => {
                let row = fill_order_row::FillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::FlashFillOrder { data, .. } => {
                let row = flash_fill_order_row::FlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::InitializeOrder { data, .. } => {
                let row = initialize_order_row::InitializeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::PreFlashFillOrder { data, .. } => {
                let row = pre_flash_fill_order_row::PreFlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::UpdateFee { data, .. } => {
                let row = update_fee_row::UpdateFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::WithdrawFee { data, .. } => {
                let row = withdraw_fee_row::WithdrawFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.insert(pool).await?;
                Ok(())
            }
        }
    }
}

#[async_trait::async_trait]
impl carbon_core::postgres::operations::Upsert for JupiterLimitOrder2InstructionWithMetadata {
    async fn upsert(&self, pool: &sqlx::PgPool) -> carbon_core::error::CarbonResult<()> {
        let JupiterLimitOrder2InstructionWithMetadata(decoded_instruction, metadata, raw_accounts) =
            self;
        match decoded_instruction {
            JupiterLimitOrder2Instruction::CancelDustOrder { data, .. } => {
                let row = cancel_dust_order_row::CancelDustOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::CancelOrder { data, .. } => {
                let row = cancel_order_row::CancelOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::FillOrder { data, .. } => {
                let row = fill_order_row::FillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::FlashFillOrder { data, .. } => {
                let row = flash_fill_order_row::FlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::InitializeOrder { data, .. } => {
                let row = initialize_order_row::InitializeOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::PreFlashFillOrder { data, .. } => {
                let row = pre_flash_fill_order_row::PreFlashFillOrderRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::UpdateFee { data, .. } => {
                let row = update_fee_row::UpdateFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::WithdrawFee { data, .. } => {
                let row = withdraw_fee_row::WithdrawFeeRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
            JupiterLimitOrder2Instruction::CpiEvent { data, .. } => {
                let row = cpi_event_row::CpiEventRow::from_parts(
                    data.clone(),
                    metadata.clone(),
                    raw_accounts.clone(),
                );
                row.upsert(pool).await?;
                Ok(())
            }
        }
    }
}
